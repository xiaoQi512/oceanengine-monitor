# 巨量引擎监控系统整体架构规划 — 评审意见

> **评审方**: WorkBuddy (AI Assistant)
> **评审日期**: 2026-06-27
> **评审范围**: `架构规划_整体方案.md` v1.0
> **评审目标**: 技术可行性核验、架构缺陷识别、改进建议输出

---

## 一、总体评价

### 优点

1. **问题定位准确** — 竞态 bug（matchedRows=0）根因分析到位，playwright-core auto-waiting 是正确方向
2. **架构分层清晰** — L1-L4 四层职责边界明确，采集/存储/分析/服务解耦合理
3. **渐进式迁移策略** — JSON 双写期、分阶段落地，风险控制意识强
4. **技术选型务实** — SQLite + DuckDB 双引擎匹配单机场景，未过度设计
5. **非目标明确** — 主动排除实时流/分布式/自研前端，聚焦核心路径

### 缺点 / 待修正

1. **关键技术假设未验证** — Chrome CDP + Playwright `newContext()` 不可行（见下文 P0-#1）
2. **SQLite schema 有语法错误** — 外键引用不完整（见 P0-#2）
3. **OVUI 兼容性风险处理不够** — 标记为"风险"但未前置验证
4. **配置管理缺失** — account_id / chat_id 等硬编码，多账户扩展时会成为障碍
5. **运维维度薄弱** — 无日志方案、无备份策略、无数据保留策略

---

## 二、P0 关键问题（必须解决，否则 P1 无法推进）

### P0-#1：Chrome CDP + Playwright `newContext()` 不可行

**问题描述**：
文档 3.1 节示例代码写了 `browser.contexts()[0]` 或 `newContext()` 隔离。
但 **Chrome 通过 `connectOverCDP` 连接时无法创建新 context**——这是 Chrome 的原生限制，只有 Playwright 自己启动的浏览器（`chromium.launch()`）才支持多 context。

文档 3.2 节"多账户 context 策略"说 5 个 AI 区域号用"同Chrome不同tab"，
但 **tab 级别无隔离**——cookies/localStorage 是 context 级别的，同 context 下所有 tab 共享登录态，无法区分账户。

**影响**：若按文档实施，5 个 AI 区域号无法同时采集（会互相覆盖登录态）。

**建议修正（三选一）**：

| 方案 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| A | 每个账户启动独立 Chrome 实例（不同 `--remote-debugging-port`） | 完全隔离，playwright 可分别连接 | 内存占用高（约 500MB/实例） |
| B | 仅将 playwright 用于单账户（真人号）主监控；AI 区域号维持现有 CDP 方案 | 风险最小，渐进迁移 | 两套采集层并存，维护成本增加 |
| C | 用 Playwright 启动浏览器（`chromium.launch()` + `userDataDir` 指向现有 Chrome profile） | 原生支持多 context | 需重新处理登录态；Chrome profile 可能被改动 |

**推荐**：方案 B（渐进）+ 方案 A（长期），P1 先只迁移真人号，AI 区域号 P2 再评估。

---

### P0-#2：SQLite 外键定义错误

**问题描述**：
`campaign_snapshots` 表定义：
```sql
FOREIGN KEY (campaign_id) REFERENCES campaigns(campaign_id)
```
但 `campaigns` 表的主键是 `campaign_id`（单列），而业务上 `campaign_id` 在不同平台/账户可能重复。
正确设计应为复合外键：

```sql
-- campaigns 表需先建立复合唯一约束
CREATE TABLE campaigns (
  campaign_id    TEXT NOT NULL,
  account_id     TEXT NOT NULL,
  platform       TEXT NOT NULL DEFAULT 'oceanengine',
  campaign_name  TEXT NOT NULL,
  status         TEXT,
  daily_budget   REAL,
  created_at     TEXT DEFAULT (datetime('now', 'localtime')),
  PRIMARY KEY (campaign_id, account_id, platform)  -- 改为复合主键
);

-- campaign_snapshots 外键同步修改
CREATE TABLE campaign_snapshots (
  id             INTEGER PRIMARY KEY AUTOINCREMENT,
  campaign_id    TEXT NOT NULL,
  account_id     TEXT NOT NULL,
  platform       TEXT NOT NULL,
  snapshot_time  TEXT NOT NULL,
  consume        REAL DEFAULT 0,
  leads          INTEGER DEFAULT 0,
  conversions    INTEGER DEFAULT 0,
  -- ...其他字段
  FOREIGN KEY (campaign_id, account_id, platform) 
    REFERENCES campaigns(campaign_id, account_id, platform)
);
```

**影响**：若不修正，跨平台/跨账户 campaign_id 重复时会出现数据关联错误。

---

### P0-#3：OVUI 兼容性验证应前置

**问题描述**：
文档将"OVUI组件需真实鼠标事件"标记为风险，但 P1 步骤顺序是：安装 playwright → 封装 pw-client.mjs → 迁移脚本。
正确顺序应为：**先验证，再封装**。

**建议 P1 步骤重排**：

| 原顺序 | 新顺序 | 步骤 |
|---------|---------|------|
| 1 | 1 | 写 OVUI 验证脚本（分页/排序/下拉框） |
| — | 2 | **验证结果决策**：兼容→继续；不兼容→保留 CDP `Input.dispatchMouseEvent`，调整架构 |
| 2 | 3 | 封装 `pw-client.mjs` |
| 3-6 | 4-7 | 迁移脚本 |

---

## 三、P1 重要改进建议

### P1-#4：统一配置管理

**问题**：`account_id`、`chat_id`、飞书 token 等硬编码在各脚本中，多账户扩展时维护成本高。

**建议**：引入 `config/accounts.json` + `config/app.json`，所有脚本从该配置读取。
`ecosystem.config.cjs` 通过 `env` 注入配置文件路径。

### P1-#5：SQLite Schema 迁移机制

**问题**：SQLite 不支持完整的 `ALTER TABLE`，schema 变更时需要"新建表 → 迁移数据 → 删旧表 → 重命名"。

**建议**：引入迁移脚本目录 `migrations/`，命名格式 `001_add_platform.js`，内含 `up()` / `down()`。
`db-writer.mjs` 初始化时自动执行未运行的迁移。

### P1-#8：统一日志方案

**问题**：各脚本 `console.log` 输出到 PM2 日志，无结构化，检索困难。

**建议**：封装 `logger.mjs`，输出 JSON Lines 格式到 `monitor-data/logs/app.log`。
PM2 安装 `pm2-logrotate` 做日志轮转。

---

## 四、P2/P3 建议补充

### P2-#6：DuckDB API 需 PoC 确认

文档写 `duckdb.createConnection()`，但 `@duckdb/node-api` 的实际 API 可能是 `new duckdb.Database()` 或 `duckdb.connect()`。
**建议 P3 前先写最小验证脚本确认 API 用法。**

### P2-#7：Parquet 归档查询路由

文档说"DuckDB直接查询"Parquet，但未说明查询时如何自动路由到正确数据源。
**建议**在 P3 设计一个联合视图：

```sql
CREATE VIEW all_snapshots AS
SELECT * FROM ocean.campaign_snapshots
UNION ALL
SELECT * FROM read_parquet('monitor-data/archive/*.parquet');
```

### P2-#9：数据库备份策略

SQLite WAL 模式下在线备份用 `better-sqlite3` 的 `backup()` API，建议每天 02:00 执行。
备份文件存 `monitor-data/backups/`，保留最近 7 天。

### P2-#10：数据保留策略

建议补充：
- `campaign_snapshots` 热数据 90 天在主库，之后归档 Parquet
- `alerts` / `feedbacks` 保留 180 天
- Parquet 归档永久保留（除非磁盘告警自动清理最老月份）

---

## 五、安全风险

### P3-#12：`/query` 端点 SQL 解析

简单关键字过滤（拦截 DDL/DML）不够安全。建议：
1. 用 SQL 解析器或严格正则确保只能是 `SELECT` 开头
2. 强制附加 `LIMIT 10000`
3. 禁止 `INTO OUTFILE`、`LOAD DATA` 等危险语句

### P3-#13：`/trigger` 端点 token 管理

建议用 `crypto.randomBytes(32).toString('hex')` 生成 token，
存储在环境变量，支持多 token 同时有效（标注过期时间，实现轮换）。

---

## 六、P4 AI 闭环补充

### P4-#14：执行联动设计

`optimization_actions` 表中 `status=approved` 的动作，需要有消费者执行。
建议设计轮询 REST API：

```
GET /poll-actions  → 返回待执行动作列表
POST /confirm-action  → 更新执行结果和 status
```

### P4-#15：飞书审批流程

"需用户确认"的自动调预算，建议用飞书卡片的 **审批按钮**，
或接入飞书审批 API，避免简单的是/否按钮被误触。

---

## 七、文档补充建议

### #16：补充性能基线

建议在 P0 阶段（未开始 P1 前）记录以下基线，P1/P2/P3 完成后做对比：
- CDP 采集一个账户耗时
- `loadPreviousSnapshots` 加载 2184 个 JSON 文件耗时
- 飞书卡片生成 + 推送端到端时延

### #17：补充回滚方案

每个阶段应有明确的回滚步骤，建议补充到"六、与现有系统的兼容性"章节。

---

## 八、风险汇总表（补充原文档第七节）

| 风险 | 概率 | 影响 | 缓解 | 优先级 |
|------|------|------|------|--------|
| Chrome CDP + Playwright context 限制 | **高** | **高** | 见 P0-#1 三方案 | P0 |
| SQLite 外键定义错误 | **高** | **高** | 见 P0-#2 修正方案 | P0 |
| OVUI 事件不兼容 playwright | 中 | 高 | 前置验证，见 P0-#3 | P0 |
| DuckDB Node API Windows 不稳 | 低 | 中 | P3 先做 PoC | P2 |
| SQLite WAL 锁竞争 | 低 | 低 | WAL 模式读不阻塞写 | P2 |
| 配置硬编码导致扩展困难 | 高 | 中 | 统一配置管理 | P1 |
| 无备份策略导致数据丢失风险 | 中 | 高 | 每日自动备份 | P1 |

---

## 九、评审结论

**综合评分**: 7.5 / 10

**核心结论**：
- 架构方向正确，技术选型务实，分层设计合理
- **P0 级别的两个技术问题（context 限制 + 外键定义）必须修正**，否则实施后会阻塞
- 建议补充配置管理、日志、备份等运维维度内容
- 文档质量较高，但部分技术细节需 PoC 验证后再写入正式方案

**下一步行动**：
1. 修正 P0-#1（context 限制）→ 调整多账户采集方案
2. 修正 P0-#2（外键定义）→ 更新 Schema 设计
3. 执行 P0-#3（OVUI 验证）→ 确认 playwright 兼容性
4. 补充配置管理 + 日志方案 → 更新架构文档
5. 完成以上后再启动 P1 实施

---

**评审模型**: WorkBuddy (AI Assistant)
**评审完成时间**: 2026-06-27T23:30+08:00
