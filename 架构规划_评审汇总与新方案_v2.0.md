# 架构规划七方评审汇总与新方案 v2.0

> **文档版本**: v2.0
> **生成日期**: 2026-06-28
> **评审输入**: 7 份独立评审报告（Qwen3.7Max / 豆包2.0 / Mimo2.5 / KimiK2.7 / WorkBuddy / M3 / DeepSeek-V4-Pro）
> **审查对象**: `架构规划_整体方案.md` v1.0
> **本档用途**: 评审意见交叉比对 + 模型排名 + 基于实际代码库的修正方案

---

## 一、七方评审模型评分排名

### 1.1 评分维度与权重

| 维度 | 权重 | 说明 |
|---|---|---|
| 现状对齐度 | 30% | 是否读代码、是否识别HTTP API主方案已落地 |
| 缺陷深度 | 25% | P0问题数量与准确性 |
| 建设性 | 20% | 给出可执行修正方案而非仅指出问题 |
| 工程完备度 | 15% | 测试/运维/回滚/容量/SLO 覆盖 |
| 文档质量 | 10% | 结构清晰、可追溯、有评分 |

### 1.2 综合排名

| 排名 | 模型 | 综合分 | 现状对齐 | 缺陷深度 | 建设性 | 工程完备 | 文档 | 关键贡献 |
|---|---|---|---|---|---|---|---|---|
| 🥇 #1 | **Qwen 3.7 Max** | **92.5** | 100 | 95 | 90 | 85 | 92 | 唯一读源码识别 P0-1 HTTP API 主方案；P0-2 connectOverCDP 限制准确；SQL 复合主键+生成列建议落地 |
| 🥈 #2 | **MiniMax-M3** | **88.5** | 95 | 90 | 88 | 82 | 88 | 与 Qwen 同样识别 P0-1；额外补"4 大生产级能力缺位"（数据质量/告警去重/SLO/灾备）；灰度策略最细 |
| 🥉 #3 | **DeepSeek V4 Pro** | **83.0** | 75 | 88 | 85 | 85 | 82 | 识别 PM2 CDP 并发竞争（新风险）；schema_migrations/snapshot_batches/db-access 三大缺失模块最具体；含 SQL 示例 |
| #4 | **WorkBuddy (自评)** | **80.0** | 70 | 85 | 82 | 78 | 85 | P0-1/P0-2 与 Qwen 同结论；配置管理/迁移机制/日志方案建议扎实；但未识别 HTTP API 已落地 |
| #5 | **豆包 2.0 Code** | **76.5** | 65 | 80 | 78 | 75 | 85 | 落地节奏 3+1 周建议合理；数据生命周期 SLA 表清晰；但未读源码，未识别 HTTP API |
| #6 | **Mimo 2.5 Pro** | **74.0** | 60 | 82 | 75 | 78 | 75 | P0 缺陷 4 项基本对齐；但 storageState 方案在 connectOverCDP 下不可行；异常检测 IQR 建议正确 |
| #7 | **Kimi K2.7 Code** | **72.0** | 55 | 78 | 72 | 75 | 80 | 落地节奏 3+1 周最早提出；但未识别 HTTP API；建议偏通用，缺源码级证据 |

### 1.3 关键差异点

**唯一读源码的 2 个模型**（Qwen / M3）：都识别出 `oceanengine-api-client.mjs` 已落地，v1.0 规划"playwright 统一接入"是重复造轮子。这是 **7 份评审中最关键的 P0 发现**，经本次代码核验确认成立。

**未读源码的 5 个模型**：都在"playwright 迁移"框架内讨论，建议虽多但建立在错误前提上。其中 DSV4Pro 虽未识别 HTTP API，但独立发现了 PM2 并发竞争问题（经核验 ecosystem.config.cjs 确认 :00/:15/:30/:45 确实冲突）。

---

## 二、评审意见交叉比对矩阵

### 2.1 P0 级问题共识度

| P0 问题 | Qwen | M3 | DSV4 | WB | 豆包 | Mimo | Kimi | 共识 | 经核验 |
|---|---|---|---|---|---|---|---|---|---|
| HTTP API 主方案被忽视 | ✅ | ✅ | — | — | — | — | — | 2/7 | ✅ 成立 |
| connectOverCDP 不支持 newContext | ✅ | ✅ | — | ✅ | ⚠️ | ✅ | ⚠️ | 5/7 | ✅ 成立 |
| SQLite 外键/主键缺陷 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 7/7 | ✅ 成立 |
| OVUI 兼容性需前置验证 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 7/7 | ✅ 成立 |
| PM2 CDP 并发竞争 | — | ✅ | ✅ | — | — | — | — | 2/7 | ✅ 成立 |
| 时区未定义 | ⚠️ | — | ✅ | — | — | ✅ | — | 2/7 | ✅ 成立 |

### 2.2 P1 级建议共识度

| P1 建议 | 提出方 | 共识 | 采纳 |
|---|---|---|---|
| Schema 版本迁移系统 | Qwen/DSV4/WB | 3/7 | ✅ |
| 统一数据访问层 DAL | Qwen/DSV4 | 2/7 | ✅ |
| 数据保留策略分层 | Qwen/豆包/Mimo/Kimi/DSV4 | 5/7 | ✅ |
| 异常检测改 IQR/MAD | Qwen/M3/Mimo | 3/7 | ✅ |
| 配置外置 | Qwen/DSV4/WB/Mimo | 4/7 | ✅ |
| 统一日志方案 | WB/Mimo/DSV4 | 3/7 | ✅ |
| 灰度策略/回滚预案 | M3/豆包/Kimi | 3/7 | ✅ |
| PM2 cron 错开 | Qwen/DSV4 | 2/7 | ✅ |
| DuckDB PoC 前置 | Qwen/M3/DSV4/豆包 | 4/7 | ✅ |
| P4 AI 安全护栏 | M3/DSV4/豆包 | 3/7 | ✅ |
| 系统自监控/metrics | Qwen/M3/DSV4/Mimo | 4/7 | ✅ |
| snapshot_batches 采集批次 | DSV4 | 1/7 | ✅（创新） |
| 告警去重 fingerprint | M3 | 1/7 | ✅（创新） |

### 2.3 工期估算对比

| 模型 | P1 | P2 | P3 | P4 | 总计 |
|---|---|---|---|---|---|
| v1.0 原方案 | 1-2d | 2-3d | 3-4d | 按需 | 2 周 |
| Qwen | 3-4d | 4-5d | 4-5d | ≥1周 | 3+ 周 |
| M3 | 2-3d | 4-5d | 4-5d | ≥1周 | 3+ 周 |
| 豆包 | 1 周 | 1 周 | 1 周 | 1 周 | 4 周 |
| Kimi | 1 周 | 1 周 | 1 周 | 1 周 | 4 周 |
| **共识** | **3-4d** | **4-5d** | **4-5d** | **按需** | **3-4 周** |

---

## 三、代码库实际核验结论

### 3.1 P0-1 HTTP API 主方案（核验：✅ 成立）

| 脚本 | 采集方式 | 状态 |
|---|---|---|
| oceanengine-monitor-v3.mjs | `import { createClient as createApiClient, collectAllData }` | ✅ HTTP API 主路径 |
| oceanengine-5min-check.mjs | 同上 | ✅ HTTP API |
| oceanengine-daily-summary.mjs | 同上 | ✅ HTTP API |
| oceanengine-shift-pusher.mjs | 同上 | ✅ HTTP API |
| pm2-smoke-test.mjs | 同上 | ✅ HTTP API |
| **oceanengine-ai-regions.mjs** | **CDP proxy HTTP (localhost:3456)** | ❌ **唯一未迁移** |
| oceanengine-api-client.mjs | Cookie 提取依赖 cdp-client.mjs | 辅助 |

**结论**：v1.0 规划"playwright 统一接入"是**重大方向错误**。5/6 主脚本已是 HTTP API，仅 ai-regions 仍走 CDP proxy（这正是 6/27 全0 bug 根因）。正确做法是把 ai-regions 也迁到 api-client，而非引入 playwright。

### 3.2 P0-2 connectOverCDP 限制（核验：✅ 成立）

`grep -l "connectOverCDP|chromium.launch"` 结果：**项目无任何 .mjs 文件使用 playwright**。v1.0 规划的"多 context 隔离"在 Chrome connectOverCDP 下确实不可行。但此问题随 P0-1 修正自动消解——走 HTTP API 不需要浏览器 context。

### 3.3 PM2 并发竞争（核验：✅ 成立）

```
pm2-5min:    */5 * * * *       → 触发于 :00/:05/:10/:15/:20/:25/:30/:35/:40/:45/:50/:55
pm2-15min:   */15 * * * *      → 触发于 :00/:15/:30/:45
pm2-daily-report: 5 23 * * *   → 23:05
pm2-daily-summary: 5 23 * * *  → 23:05  ← 与 daily-report 完全同时
pm2-ai-regions: 30 21 * * *    → 21:30（与 :30 的 5min/15min 重叠）
```

**实际冲突**：
- 每 15 分钟 5min 和 15min 同时启动（但都走 HTTP API，CDP 仅 Cookie 提取，冲突低）
- 23:05 daily-report 和 daily-summary 完全同时（需错开）
- 21:30 ai-regions 与 5min/15min 重叠（ai-regions 走 CDP proxy，5min 走 HTTP API，两者争抢 Chrome）

### 3.4 SQLite 外键缺陷（核验：✅ 成立）

v1.0 schema `campaigns` 单列主键 + `UNIQUE(campaign_id, account_id, platform)` 确实矛盾。`campaign_snapshots` 外键仅引用单列，跨账户时会关联错误。7/7 模型一致指出。

---

## 四、修正后的新方案 v2.0

### 4.1 核心方向修正

| 层 | v1.0 方案（错误） | v2.0 方案（修正） | 依据 |
|---|---|---|---|
| L1 采集层 | playwright-core 统一接入 | **HTTP API 主 + CDP 辅 + playwright 异构兜底** | 5/6 脚本已走 API，ai-regions 迁移即可 |
| L1 多账户 | connectOverCDP newContext | **API 天然隔离（不同 aadvid 不同 Cookie）** | 不需要浏览器 context |
| L1 ai-regions | 迁 playwright | **迁 api-client（复用已有逆向 API）** | 6/27 bug 根因消除 |
| L2 schema | 单列主键 | **复合主键 + 生成列分区 + snapshot_batches** | 7/7 共识 |
| L2 运维 | 无迁移系统 | **schema_migrations + db-access DAL** | 3/7 共识 |
| 工期 | 2 周 | **3-4 周** | 6/7 共识 |

### 4.2 修正后四层架构

```
┌─────────────────────────────────────────────────────────────┐
│  L1 采集层  HTTP API 主 + CDP 辅 + Playwright 异构兜底       │
│                                                             │
│  巨量引擎（已验证稳定，速度提升 30-60x）:                     │
│    oceanengine-api-client.mjs → HTTP 直连内部逆向 API        │
│    cdp-client.mjs → 仅 Cookie 提取 + 自动登录 + API 降级     │
│                                                             │
│  待迁移:                                                     │
│    oceanengine-ai-regions.mjs → 从 CDP proxy 迁到 api-client │
│                                                             │
│  异构平台（腾讯/百度，预留）:                                  │
│    playwright-core → DOM 操作兜底（非主路径，按需引入）       │
│                                                             │
│  退役: cdp-proxy.mjs HTTP 路径（ai-regions 迁移后）           │
│  保留: cdp-client.mjs（Cookie 提取）、calibrate-page.mjs      │
│  保留: wait-utils.mjs（OVUI 逻辑，API 降级时仍需）            │
└──────────────────────┬──────────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  L2 存储层  SQLite + DuckDB 双引擎 + 迁移系统                │
│                                                             │
│  SQLite WAL 主存储:                                          │
│    campaigns (复合主键) | campaign_snapshots (生成列分区)     │
│    alerts (+fingerprint) | daily_summaries | feedbacks       │
│    optimization_actions (+安全护栏)                          │
│    snapshot_batches (采集批次) ★ 新增                         │
│    system_metrics (自监控) ★ 新增                             │
│    schema_migrations (版本管理) ★ 新增                        │
│    dead_letter_jobs (死信队列) ★ 新增                         │
│                                                             │
│  DAL: db-access.mjs 统一数据访问层                            │
│  双写桥: db-writer.mjs (JSON + SQLite 并行)                   │
│  验证: verify-dual-write.mjs 三方一致性                       │
└──────────────────────┬──────────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  L3 分析层  DuckDB 聚合 + IQR 异常检测 + Parquet 归档         │
│                                                             │
│  DuckDB ATTACH SQLite (READ_ONLY) → 列式聚合                 │
│  异常检测: IQR/MAD (替代 Z-SCORE)  ← CPA 长尾分布            │
│  归档: 月度 Parquet (snapshot_date 分区)                      │
│  物化视图: daily_summary_mv (高频查询)                        │
│  联合视图: all_snapshots = SQLite ∪ Parquet                  │
│  降级: DuckDB 不可用 → SQLite 原生聚合                        │
└──────────────────────┬──────────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  L4 服务层  feedback-server 扩展 (端口8899)                  │
│                                                             │
│  现有: /report | /feedback                                   │
│  新增: /query (SELECT only + LIMIT 10000 + 关键词拦截)       │
│  新增: /api/charts/daily | /api/charts/campaign              │
│  新增: /api/export (行数上限) | /trigger (token+IP+时段)     │
│  新增: /health (浅) | /api/health/deep (DB/CDP/磁盘/进程)    │
│  新增: /poll-actions | /confirm-action (P4 AI 闭环)          │
│                                                             │
│  鉴权: Bearer token + 127.0.0.1 only + 速率限制              │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 修正后 Schema（含所有评审建议）

```sql
-- ====== 版本管理 ======
CREATE TABLE schema_migrations (
    version    INTEGER PRIMARY KEY,
    name       TEXT NOT NULL,
    applied_at TEXT DEFAULT (datetime('now','localtime')),
    checksum   TEXT
);

-- ====== 计划主表（复合主键，7/7 共识）======
CREATE TABLE campaigns (
    campaign_id       TEXT NOT NULL,
    account_id        TEXT NOT NULL,
    platform          TEXT NOT NULL DEFAULT 'oceanengine',
    campaign_name     TEXT NOT NULL,
    status            TEXT,
    daily_budget      REAL,
    lifecycle_status  TEXT DEFAULT 'active',  -- active/paused/archived/deleted (M3建议)
    created_at        TEXT DEFAULT (datetime('now','localtime')),
    updated_at        TEXT DEFAULT (datetime('now','localtime')),
    PRIMARY KEY (campaign_id, account_id, platform)
);

-- ====== 快照时序表（生成列分区 + 复合外键）======
CREATE TABLE campaign_snapshots (
    id             INTEGER PRIMARY KEY AUTOINCREMENT,
    campaign_id    TEXT NOT NULL,
    account_id     TEXT NOT NULL,
    platform       TEXT NOT NULL DEFAULT 'oceanengine',
    snapshot_time  TEXT NOT NULL,  -- ISO 8601 UTC
    snapshot_date  TEXT GENERATED ALWAYS AS (date(snapshot_time)) STORED,  -- 分区加速
    snapshot_hour  INTEGER GENERATED ALWAYS AS (strftime('%H', snapshot_time)) STORED,
    spend          REAL DEFAULT 0,  -- 统一用 spend（废弃 consume）
    leads          INTEGER DEFAULT 0,
    conversions    INTEGER DEFAULT 0,
    -- 其他字段同原方案
    quality        TEXT DEFAULT 'ok',  -- ok/partial/failed/timeout (M3建议)
    FOREIGN KEY (campaign_id, account_id, platform)
      REFERENCES campaigns(campaign_id, account_id, platform)
);

CREATE INDEX idx_snap_date_account ON campaign_snapshots(snapshot_date, account_id);
CREATE INDEX idx_snap_campaign_time ON campaign_snapshots(campaign_id, snapshot_time);
CREATE INDEX idx_snap_account_hour ON campaign_snapshots(account_id, snapshot_hour);

-- ====== 采集批次追踪（DSV4 创新）======
CREATE TABLE snapshot_batches (
    id                 INTEGER PRIMARY KEY AUTOINCREMENT,
    batch_time         TEXT NOT NULL,
    account_id         TEXT NOT NULL,
    platform           TEXT NOT NULL,
    source_script      TEXT,
    expected_campaigns INTEGER,
    actual_snapshots   INTEGER,
    collection_ms      INTEGER,
    retry_count        INTEGER DEFAULT 0,
    status             TEXT DEFAULT 'success',
    error_detail       TEXT
);

-- ====== 告警表（+fingerprint 去重，M3 创新）======
CREATE TABLE alerts (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    account_id   TEXT NOT NULL,
    platform     TEXT NOT NULL,
    campaign_id  TEXT,
    alert_type   TEXT NOT NULL,
    severity     TEXT,
    message      TEXT,
    fingerprint  TEXT,  -- MD5(campaign_id + alert_type + date_bucket)
    triggered_at TEXT DEFAULT (datetime('now','localtime')),
    resolved_at  TEXT,  -- 跨日闭环 (M3建议)
    UNIQUE(fingerprint)
);

-- ====== 系统自监控（Qwen/M3/DSV4/Mimo 共识）======
CREATE TABLE system_metrics (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    source      TEXT NOT NULL,
    metric      TEXT NOT NULL,
    value       REAL NOT NULL,
    tags        TEXT,
    recorded_at TEXT DEFAULT (datetime('now','localtime'))
);

-- ====== 死信队列（豆包建议）======
CREATE TABLE dead_letter_jobs (
    id             INTEGER PRIMARY KEY AUTOINCREMENT,
    job_type       TEXT NOT NULL,
    job_payload    TEXT,
    error_message  TEXT,
    failed_at      TEXT DEFAULT (datetime('now','localtime')),
    retry_count    INTEGER DEFAULT 0,
    max_retries    INTEGER DEFAULT 3,
    status         TEXT DEFAULT 'pending'
);

-- ====== AI 执行动作（+安全护栏，M3/DSV4 共识）======
CREATE TABLE optimization_actions (
    id                      INTEGER PRIMARY KEY AUTOINCREMENT,
    campaign_id             TEXT NOT NULL,
    account_id              TEXT NOT NULL,
    platform                TEXT NOT NULL,
    action_type             TEXT NOT NULL,
    action_payload          TEXT,
    status                  TEXT DEFAULT 'proposed',
    requires_approval       INTEGER DEFAULT 1,
    approver                TEXT,
    max_budget_change_pct   REAL DEFAULT 30.0,  -- 安全护栏
    cooldown_until          TEXT,
    proposed_at             TEXT DEFAULT (datetime('now','localtime')),
    approved_at             TEXT,
    executed_at             TEXT,
    executed_by             TEXT,
    result                  TEXT
);

-- ====== 日汇总 + 反馈（补 account_id/platform）======
CREATE TABLE daily_summaries (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    account_id   TEXT NOT NULL,
    platform     TEXT NOT NULL,
    summary_date TEXT NOT NULL,
    total_spend  REAL,
    total_leads  INTEGER,
    -- ...
    UNIQUE(account_id, platform, summary_date)
);

CREATE TABLE feedbacks (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    account_id   TEXT,
    platform     TEXT,
    suggestion_id TEXT,
    response     TEXT,
    responded_at TEXT DEFAULT (datetime('now','localtime'))
);
```

### 4.4 修正后落地路线（3-4 周）

#### P0 — 前置验证（2 天，可与 P1 并行）

| 验证项 | 方法 | 通过条件 | 失败降级 |
|---|---|---|---|
| ai-regions 迁 api-client 可行性 | 写 50 行 probe 调逆向 API 取 1 区域数据 | 返回正确 JSON | 保留 CDP proxy + 修 matchedRows 判定 |
| better-sqlite3 Windows 编译 | npm install + 建表测试 | 无 native 编译错误 | 用 sql.js 纯 JS 方案 |
| DuckDB ATTACH 性能 | 100 万行聚合 PoC | P95 < 500ms | 降级 SQLite 聚合 |

#### P1 — ai-regions 迁移 + PM2 错峰（3-4 天）

| 步骤 | 交付物 | 验证 |
|---|---|---|
| ai-regions.mjs 改用 api-client | 改后脚本 | DRY_RUN 5 区域数据正确 |
| PM2 cron 错峰（15min→2,17,32,47 / daily-summary→10 23） | ecosystem.config.cjs | 无冲突 |
| 退役 cdp-proxy.mjs HTTP 路径 | 删除 | ai-regions 仍正常 |
| 保留 cdp-client.mjs（Cookie 提取） | 不动 | — |

**关键修正**：v1.0 的"playwright 接入 + 6 脚本迁移"**全部取消**。仅迁移 ai-regions 这 1 个脚本。

#### P2 — SQLite 迁移（4-5 天）

| 步骤 | 交付物 |
|---|---|
| 安装 better-sqlite3 + 建 schema（含全部新表） | oceanengine.db |
| schema_migrations 系统 + migrations/ 目录 | 迁移脚本 |
| db-access.mjs DAL 层 | 统一数据访问 |
| db-writer.mjs 双写桥（JSON + SQLite） | 双写验证 |
| verify-dual-write.mjs 三方一致性检查 | 对账脚本 |
| 接入 6 个采集脚本 | 改后脚本 |
| JSON 历史 schema 探测 + 导入脚本 | 一次性导入 |
| /query 检索面板上线 | feedback-server 改 |

**双写期**：1 周（不在 P2 工期内，P2 结束后开始观察）

#### P3 — DuckDB 分析层（4-5 天）

| 步骤 | 交付物 |
|---|---|
| 安装 @duckdb/node-api + ATTACH PoC | PoC 报告 |
| 日报聚合改 DuckDB（降级 SQLite fallback） | daily-report 改 |
| IQR/MAD 异常检测替代 Z-SCORE | analysis.mjs |
| 7 日趋势（REGR_SLOPE 日维度 + LAG 分钟级） | analysis.mjs |
| 月度 Parquet 归档 + 联合视图 | archive 脚本 |
| /api/charts/* 上线 | feedback-server 改 |

#### P4 — AI 闭环 + 多平台（按需，≥1 周）

| 步骤 | 交付物 |
|---|---|
| optimization_actions 安全护栏激活 | 表字段补全 |
| 飞书审批流（卡片审批按钮） | feishu-listener 改 |
| CPA 预测模型（火山方舟） | prediction.mjs |
| 腾讯/百度接入（playwright 异构） | 新 platform 适配 |

### 4.5 灰度策略（M3 建议采纳）

```
P1: DRY_RUN 模式（ai-regions 采集但不推送）观察 3 天
    ↓
P2: 影子写（SQLite 双写但只读 JSON）观察 7 天
    ↓
P2 末: 切换读路径（DB 优先 → JSON fallback）观察 3 天
    ↓
P3: DuckDB 切换只读模式观察 7 天
    ↓
P4: 关闭 JSON 写入（保留 30 天再删）
```

### 4.6 SLO 定义（Qwen/M3 共识）

| 指标 | 目标 | 告警阈值 |
|---|---|---|
| 快照采集成功率 | ≥ 99% | < 95% |
| p95 采集耗时 | < 45s | > 60s |
| 飞书推送成功率 | ≥ 98% | < 90% |
| 查询 P95 | ≤ 500ms | > 2s |
| 告警延迟 | ≤ 60s | > 120s |

---

## 五、v1.0 → v2.0 修订对照表

| v1.0 内容 | 评审结论 | v2.0 修正 | 来源 |
|---|---|---|---|
| L1 playwright 统一接入 | ❌ P0 方向错误 | HTTP API 主 + CDP 辅 + playwright 异构 | Qwen/M3 |
| connectOverCDP newContext 隔离 | ❌ 不可行 | API 天然隔离 | Qwen/M3/WB |
| campaigns 单列主键 | ❌ 7/7 一致指出 | 复合主键 | 全部 |
| campaign_snapshots 单列外键 | ❌ | 复合外键 + 生成列分区 | Qwen/Mimo |
| 2 周落地 | ❌ 过于激进 | 3-4 周 | 6/7 |
| P1 即退役 cdp-client.mjs | ❌ 激进 | 保留（Cookie 提取仍需） | 豆包/Kimi |
| Z-SCORE 异常检测 | ⚠️ 不适配长尾 | IQR/MAD | Qwen/M3/Mimo |
| 无 schema 迁移系统 | ❌ 缺失 | schema_migrations + migrations/ | Qwen/DSV4/WB |
| 无统一 DAL | ❌ 缺失 | db-access.mjs | Qwen/DSV4 |
| 无数据保留策略 | ❌ 缺失 | 热30天/温12月/冷按需 | 5/7 |
| 无系统自监控 | ❌ 缺失 | system_metrics + SLO | 4/7 |
| 无采集批次追踪 | ❌ 缺失 | snapshot_batches | DSV4 |
| 无告警去重 | ❌ 缺失 | alerts.fingerprint | M3 |
| 无死信队列 | ❌ 缺失 | dead_letter_jobs | 豆包 |
| 无 AI 安全护栏 | ❌ 缺失 | max_budget_change_pct + cooldown | M3/DSV4 |
| PM2 cron 未错峰 | ❌ 隐患 | 15min→2,17,32,47 | Qwen/DSV4 |
| 字段 consume/spend 不一致 | ⚠️ | 统一 spend | Qwen/DSV4 |
| 时区未定义 | ⚠️ | UTC 存储 + 查询转换 | Qwen/Mimo/DSV4 |
| consume 退役清单含 wait-utils | ⚠️ | 保留（API 降级时仍需） | Qwen |
| 无测试计划 | ❌ 缺失 | 每阶段验证项 | 全部 |
| 无回滚预案 | ❌ 缺失 | 灰度策略 + 回滚检查点 | M3/豆包/Kimi |
| 无配置外置 | ❌ 缺失 | config.json | 4/7 |

---

## 六、最终推荐行动序列

```
立即（今天）:
  1. ai-regions.mjs 从 CDP proxy 迁到 api-client（修复 6/27 bug 根因）
  2. PM2 cron 错峰（15min: 2,17,32,47 / daily-summary: 10 23）

P1（3-4 天）:
  3. P0 前置验证 3 项 PoC
  4. ai-regions 迁移完成 + 退役 cdp-proxy
  5. 保留 cdp-client（Cookie 提取）

P2（4-5 天 + 1 周双写观察）:
  6. SQLite schema 建表（含全部新表）
  7. schema_migrations + db-access DAL
  8. db-writer 双写桥 + 6 脚本接入
  9. JSON 历史导入 + /query 上线

P3（4-5 天）:
  10. DuckDB ATTACH + 降级路径
  11. IQR 异常检测 + 7 日趋势
  12. Parquet 归档 + /api/charts

P4（按需）:
  13. AI 安全护栏 + 飞书审批
  14. CPA 预测模型
  15. 腾讯/百度异构接入
```

---

## 七、附录：各模型评审特点

| 模型 | 独特贡献 | 主要不足 |
|---|---|---|
| Qwen 3.7 Max | 唯一引用源码行号（oceanengine-monitor-v3.mjs:2）；SQL 生成列建议最专业 | 工期估算偏保守 |
| MiniMax-M3 | 4 大生产级能力框架最系统；灰度策略最细 | 与 Qwen 高度重叠 |
| DeepSeek V4 Pro | PM2 并发竞争独立发现；snapshot_batches 创新表 | 未识别 HTTP API |
| WorkBuddy | 配置管理/迁移机制最具体 | 自评未读源码 |
| 豆包 2.0 | 数据生命周期 SLA 表最清晰 | 未读源码，建议偏通用 |
| Mimo 2.5 | API 鉴权中间件示例；物化视图建议 | storageState 方案不可行 |
| Kimi K2.7 | 最早提出 3+1 周；小程序接口建议 | 未识别 HTTP API，建议最通用 |

---

**文档结束 · v2.0 可作为实施依据**
