# 巨量引擎广告计划调整 · v1.1 执行方案

> 日期：2026-07-27
> 基于：`广告计划调整_四层架构方案_v1.1_决策档案_20260727.md`（10 项决策，18 条行动清单）
> 用途：将决策档案转换为可执行的代码改造步骤

---

## 一、执行总览

```
P0（5 项，本次必须完成）→ P1（6 项，本次建议完成）→ P2（3 项，后续）
```

| 阶段 | 任务数 | 预计改动量 | 影响文件 |
|------|:---:|-----------|---------|
| P0 | 6 | ~150 行新增 + ~20 行注释 | 3-4 个文件 |
| P1 | 7 | ~250 行新增 | 5-6 个文件 |
| P2 | 3 | 暂不实施 | — |

---

## 二、P0 阶段（本次必须完成）

### 任务 1：备份 + 注释 cdp-action 三处 writeAuditLog（D1）

**文件**：`cdp-action.mjs`  
**操作**：在 3 处调用点各加一行注释，不删代码

```javascript
// cdp-action.mjs:420, 535, 657
// [v1.1] 审计归一到 action-queue-worker，writeAuditLog 不再调用
// writeAuditLog({ ... });  ← 注释掉
```

**验证**：`grep -c "writeAuditLog" cdp-action.mjs` 由 3 → 0（被注释），文件语法仍合法

---

### 任务 2：worker 审计写入字段扩展（D1 + D7）

**文件**：`action-queue-worker.mjs`  
**操作**：扩展 `writeAudit` 函数，增加 D7 要求的字段

```javascript
// action-queue-worker.mjs:78-87 改写
function writeAudit(entry) {
  const record = {
    traceRef: entry.traceRef || '',
    actionType: entry.actionType || '',
    planName: entry.planName || '',
    projectId: entry.projectId || '',
    source: entry.source || 'unknown',      // D7: auto/manual/dashboard/feishu
    beforeValue: entry.beforeValue ?? null,
    afterValue: entry.afterValue ?? null,    // D7: API 回读
    result: {
      ok: entry.result?.ok ?? false,
      method: entry.result?.method || 'none', // D7: http_api/cdp/none
      attempts: entry.result?.attempts || 0,
      error: entry.result?.error || null,
    },
    workerPid: process.pid,
    timestamp: entry.timestamp || new Date().toISOString(),
  };
  const line = JSON.stringify(record);
  fs.appendFileSync(AUDIT_LOG_FILE, line + '\n');
}
```

**验证**：执行一次操作后 `tail -1 action-audit.jsonl` 查看新字段

---

### 任务 3：引入 pending-actions.json + monitor-utils 统一导出（D4）

**文件**：`monitor-utils.mjs`（新增 4 个导出常量）

```javascript
// 在 monitor-utils.mjs 中新增以下 4 行（第 20 行附近，DATA_DIR 定义之后）
export const QUEUE_FILE = process.env.ACTION_QUEUE_FILE || path.join(DATA_DIR, '..', 'action-queue.json');
export const LOCK_FILE = process.env.ACTION_LOCK_FILE || path.join(DATA_DIR, '..', 'action-queue.json.lock');
export const AUDIT_FILE = process.env.ACTION_AUDIT_FILE || path.join(DATA_DIR, 'action-audit.jsonl');
export const PENDING_FILE = process.env.ACTION_PENDING_FILE || path.join(DATA_DIR, 'pending-actions.json');

// 初始化 pending 文件（如果不存在）
function initPendingFile() {
  if (!fs.existsSync(PENDING_FILE)) {
    fs.writeFileSync(PENDING_FILE, JSON.stringify({ pending: [] }, null, 2));
  }
}
```

**同时修改**：`action-queue-worker.mjs` 和 `feishu-listener.mjs` 中硬编码的路径常量，改用 `monitor-utils.mjs` 的导出。

```javascript
// 原来
const ACTION_QUEUE = path.join(__dirname, 'action-queue.json');
const LOCK_FILE = path.join(__dirname, 'action-queue.json.lock');

// 改为
import { QUEUE_FILE, LOCK_FILE, AUDIT_FILE, PENDING_FILE } from './monitor-utils.mjs';
```

**验证**：PM2 重启后 worker/listener 正常启动，`pending-actions.json` 自动创建

---

### 任务 4：acknowledgeStart 实现重复检测 + 二次确认（D2）

**文件**：`feishu-listener.mjs`

**4a. 新增 `checkDuplicateToday` 函数**

```javascript
// 检查当日是否已有相同计划的相同操作（基于审计日志）
function checkDuplicateToday(action) {
  try {
    if (!fs.existsSync(AUDIT_FILE)) return null;
    const today = getLocalDate();
    const lines = fs.readFileSync(AUDIT_FILE, 'utf-8').split('\n').filter(Boolean);
    const duplicates = lines
      .map(l => { try { return JSON.parse(l); } catch { return null; } })
      .filter(r => r && 
        r.timestamp?.startsWith(today) &&
        r.planName === action.planName &&
        r.actionType === action.type &&
        r.result?.ok === true
      );
    return duplicates.length > 0 ? duplicates : null;
  } catch { return null; }
}
```

**4b. 改写 `acknowledgeStart`**

```javascript
async function acknowledgeStart(chatId, action) {
  // 1. 预检查（任务 7 实现）
  const precheck = await precheckAction(action);
  if (!precheck.ok) {
    await sendMsg(chatId, `⚠️ ${precheck.reason}`);
    return null;
  }

  // 2. 检测当日重复指令
  const duplicates = checkDuplicateToday(action);
  if (duplicates) {
    const count = duplicates.length;
    const last = duplicates[duplicates.length - 1];
    const lastTime = last.timestamp?.slice(11, 19) || '未知';
    
    // 写入 pending 表
    addPending(action, chatId, { isDuplicate: true, lastCount: count, lastTime });
    
    await sendMsg(chatId,
      `🟡 当日已对「${action.planName}」执行过 ${count} 次${actionText(action.type)}操作\n` +
      `   最近一次：${lastTime}\n` +
      `   确认要再次执行吗？\n` +
      `   回复"执行"确认 · 回复"拒绝"取消`
    );
    return { status: 'pending_confirm', isDuplicate: true };
  }

  // 3. 非重复指令 → 正常入队
  const result = enqueue(action);
  await sendMsg(chatId,
    `🔵 ${actionText(action.type)}「${action.planName}」\n` +
    `   已入队 #${result.position} · traceRef: ${result.traceRef}`
  );
  return result.traceRef;
}
```

**4c. 新增 pending 表操作函数**

```javascript
function loadPending() {
  try { return JSON.parse(fs.readFileSync(PENDING_FILE, 'utf-8')); } catch { return { pending: [] }; }
}
function savePending(data) { fs.writeFileSync(PENDING_FILE, JSON.stringify(data, null, 2)); }

function addPending(action, chatId, meta = {}) {
  const data = loadPending();
  data.pending.push({
    tempId: crypto.randomUUID(),
    action: { planName: action.planName, type: action.type, amount: action.amount || null },
    chatId,
    createdAt: new Date().toISOString(),
    expiresAt: new Date(Date.now() + 3 * 60 * 1000).toISOString(),
    ...meta,
  });
  savePending(data);
}
```

**验证**：重复发送"暂停 0724-真人直播-画面直投"两次，第二次触发 🟡 确认提示

---

### 任务 5：30s pending 扫描 + 超时取消回执（D2）

**文件**：`feishu-listener.mjs`

```javascript
// 在 main() 中 setInterval 旁边新增独立的 pending 扫描定时器
setInterval(scanPending, 30000);

async function scanPending() {
  const data = loadPending();
  if (!data.pending.length) return;

  const now = new Date();
  const remaining = [];
  
  for (const item of data.pending) {
    if (new Date(item.expiresAt) < now) {
      // 超时取消
      await sendMsg(item.chatId,
        `⏰ 操作已超时取消\n` +
        `   「${item.action.planName}」${actionText(item.action.type)}确认超时（3分钟）\n` +
        `   如需执行请重新发送指令`
      );
    } else {
      remaining.push(item);
    }
  }
  
  if (remaining.length !== data.pending.length) {
    data.pending = remaining;
    savePending(data);
  }
}
```

**验证**：发一条重复指令，3 分钟后飞书群收到"操作已超时取消"消息

---

### 任务 6：ECOSYSTEM 注释（单实例约束）

**文件**：`ecosystem.config.cjs`

```javascript
// 在 action-queue-worker 配置块中加一行注释
{
  name: "action-queue-worker",
  instances: 1,  // [v1.1] 必须单实例：文件锁 wx 标志依赖单进程互斥；容器化需改 Redis 锁
  ...
}
```

**验证**：`pm2 restart action-queue-worker` 后仍为 instances=1

---

## 三、P1 阶段（本次建议完成）

### 任务 7：precheckAction 计划状态预检查

**文件**：`feishu-listener.mjs`

```javascript
async function precheckAction(action) {
  const camps = await getCampaignList();
  const target = camps.find(c => c.name.includes(action.planName));
  if (!target) return { ok: false, reason: '未找到计划 ' + action.planName };
  
  if (action.type === 'pause' && target.status !== '启用')
    return { ok: false, reason: `计划已处于「${target.status}」状态，无需重复暂停` };
  
  if (action.type === 'resume' && target.status === '启用')
    return { ok: false, reason: '计划已在投放中，无需重复恢复' };

  return { ok: true, target };
}
```

**验证**：对已暂停的计划发"暂停"指令，收到"计划已处于「暂停」状态"提示

---

### 任务 8：CDP 熔断（D3）

**文件**：`action-queue-worker.mjs`

```javascript
// 新增：共享 chrome-guard 的 9222 探活
async function isChromeHealthy() {
  try {
    const { checkCDP } = await import('./cdp-client.mjs');
    const status = await checkCDP();
    return status.reachable;
  } catch { return false; }
}

// 在 processHead 的执行逻辑中加入熔断
async function processHead() {
  const action = loadQueue().actions[0];
  if (!action) return;

  // 尝试 HTTP API
  const client = await createClient({ useCache: true });
  const result = await tryHttpApi(action, client);
  if (result.httpOk) { /* 成功，继续审计 */ return; }

  // HTTP API 失败 → 检查 Chrome 是否健康
  const chromeOk = await isChromeHealthy();
  if (!chromeOk) {
    writeAudit({ ...action, result: { ok: false, method: 'none', error: 'HTTP_API_FAILED + CHROME_UNREACHABLE' }});
    return; // 整体拒绝，不入重试循环
  }

  // Chrome 健康 → CDP 降级
  await tryCdpAction(action);
}
```

**验证**：关闭 Chrome 后执行操作，审计日志出现 `CHROME_UNREACHABLE`

---

### 任务 9：Dashboard 操作面板（D5）

**文件**：`dashboard.html` + `dashboard.js` + `feedback-server.mjs`

**9a. 新增 HTML 面板结构**

```html
<!-- dashboard.html 底部新增操作面板区域 -->
<div class="ops-panel" x-data="opsPanel()">
  <div class="ops-tabs">
    <button @click="tab='queue'" :class="{active:tab==='queue'}">当前队列</button>
    <button @click="tab='pending'" :class="{active:tab==='pending'}">待确认</button>
    <button @click="tab='audit'" :class="{active:tab==='audit'}">最近审计</button>
  </div>
  
  <div x-show="tab==='queue'" class="ops-content">
    <table> <!-- 从 /api/actions 加载队列数据 --> </table>
  </div>
  <div x-show="tab==='pending'" class="ops-content">
    <table> <!-- 从 /api/pending 加载待确认数据 --> </table>
  </div>
  <div x-show="tab==='audit'" class="ops-content">
    <table> <!-- 从 /api/audit/recent 加载最近审计 --> </table>
  </div>
</div>
```

**9b. 新增 API 路由**

```javascript
// feedback-server.mjs 新增 3 个路由

// GET /api/pending — 查询待确认列表
app.get('/api/pending', (req, res) => {
  const data = loadPending();
  res.json({ ok: true, data: data.pending });
});

// GET /api/audit/recent — 最近 20 条审计
app.get('/api/audit/recent', (req, res) => {
  const lines = readAuditLog().slice(-20);
  res.json({ ok: true, data: lines });
});

// GET /api/audit/:traceRef — 按 traceRef 查询
app.get('/api/audit/:traceRef', (req, res) => {
  const lines = readAuditLog();
  const record = lines.find(l => l.traceRef === req.params.traceRef);
  if (!record) { res.status(404).json({ ok: false, error: 'not found' }); return; }
  res.json({ ok: true, data: record });
});
```

**验证**：浏览器访问 Dashboard → 看到三个 tab → 切换显示不同数据

---

### 任务 10：ACK 卡片化（D6）

**文件**：`feishu-listener.mjs`

将二次确认从文本回复"执行/拒绝"改为飞书交互卡片按钮。

```javascript
async function sendConfirmCard(chatId, action, traceRef) {
  const card = JSON.stringify({
    config: { wide_screen_mode: true },
    header: {
      title: { tag: 'plain_text', content: '操作确认' },
      template: 'orange',
    },
    elements: [
      {
        tag: 'div',
        text: { tag: 'lark_md', content: `确认要对 **${action.planName}** 执行 **${actionText(action.type)}** 操作吗？` },
      },
      {
        tag: 'action',
        actions: [
          {
            tag: 'button',
            text: { tag: 'lark_md', content: '✅ 确认' },
            type: 'primary',
            value: JSON.stringify({ traceRef, action: 'confirm' }),
          },
          {
            tag: 'button',
            text: { tag: 'lark_md', content: '❌ 取消' },
            type: 'danger',
            value: JSON.stringify({ traceRef, action: 'cancel' }),
          },
        ],
      },
    ],
    // 3 分钟超时后卡片自动失效（客户端行为）
  });

  // 发送卡片消息（使用 pushCard 或直接 lark-cli card send）
  await pushCard(LARK_CLI, card, chatId);
}
```

**同步修改**：`feishu-listener.mjs` 的卡片回调监听中，处理 `action: 'confirm'` / `action: 'cancel'`，对应执行入队或清除 pending。

**验证**：发重复指令 → 飞书群出现卡片 → 点确认/取消按钮 → 行为正确

---

### 任务 11：audit schema source 枚举统一（D7）

**文件**：`action-queue-worker.mjs` 的 `writeAudit`

```javascript
// source 字段取值规范
const VALID_SOURCES = ['auto', 'manual', 'dashboard', 'feishu'];

function writeAudit(entry) {
  // ...
  source: VALID_SOURCES.includes(entry.source) ? entry.source : 'unknown',
  // ...
}
```

**同步修改**：`feishu-listener.mjs` 入队时设置 `source: 'feishu'`；`feedback-server.mjs` 的 `/api/actions` POST 设置 `source: 'dashboard'`。

**验证**：分别从飞书群和 Dashboard 各执行一次操作，审计日志中 source 字段正确

---

### 任务 12：CARD-FORMAT.md 增章（D6/D7）

**文件**：`CARD-FORMAT.md`

新增章节：`### 操作回执卡片 v3.x`

```
操作回执卡片格式：
- 类型：interactive card
- 有效时间：3 分钟
- 按钮：✅ 确认（type: primary） / ❌ 取消（type: danger）
- 超时处理：卡片自动失效 + 飞书文本回执
- 回调路径：card.action.trigger → /api/actions/confirm
```

**验证**：文档可读，格式与已有卡片章节一致

---

## 四、P2 阶段（后续实施）

| 任务 | 文件 | 前置条件 |
|------|------|---------|
| afterValue 回读 | `action-queue-worker.mjs` + `oceanengine-api-client.mjs` | D7 审计字段扩展完成 |
| Dashboard 一键回滚 | `dashboard.html` + `feedback-server.mjs` | afterValue 已写入审计 |
| pending 搬 SQLite | `db/` + `monitor-utils.mjs` | P0/P1 跑顺 1-2 周 |

---

## 五、验证清单

| 序号 | 验证项 | 对应任务 |
|:---:|--------|:---:|
| 1 | `cdp-action.mjs` 中 writeAuditLog 3 处已注释 | 1 |
| 2 | 审计日志新字段 traceRef/source/method/attempts 已出现 | 2 |
| 3 | `pending-actions.json` 在 monitor-data/ 自动生成 | 3 |
| 4 | 重复指令触发 🟡 二次确认提示 | 4 |
| 5 | 3 分钟后超时自动取消并通知群聊 | 5 |
| 6 | 暂停已暂停计划收到 ⚠️ 状态提示 | 7 |
| 7 | Chrome 不健康时操作被整体拒绝 | 8 |
| 8 | Dashboard 三 tab 可切换查看数据 | 9 |
| 9 | 飞书卡片按钮可正常确认/取消 | 10 |
| 10 | 审计日志 source 字段区分 feishu/dashboard | 11 |
| 11 | CARD-FORMAT.md 新增操作回执卡片章节 | 12 |

---

## 六、版本记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-07-27 | v1.0 | 初始执行方案，基于 v1.1 决策档案 10 项决策生成 |
