# 40102 拦截端点 · 逆向思路文档

> 3 个被 40102 拒绝的端点，均返回 `{"code":40102, "msg":"无此操作权限"}`
> 根因：服务端权限分级 —— list 接口用 Cookie session，detail/adgroup 需要页面 HTML 内地私有 CSRF token

---

## 一、被拦截端点概览

| 端点 | 预期路径 | 含有的数据 | 业务价值 |
|------|---------|----------|------|
| `projects/detail` | `/ad/api/promotion/projects/get` `/projects/detail` | 单个项目的完整详情：创作材料配置、受众定向参数、出价策略参数、单元列表 | 精准出价优化建议、受众分析 |
| `adgroups/list` | `/ad/api/promotion/adgroups/list` | 每个项目下的子级单元状态："直播间不可投放"等自动状态切换 | 下播后是否真停了投放的判断依据、单元生命周期记录 |
| `live_room/list` | `/ad/api/live_room/list` | 直播间当前状态（开播/关播）、在线人数 | 判断当前是否有人直播、关联广告时段分析 |

---

## 二、40102 根因分析

### 2.1 已排除的假设

| 假设 | 测试方式 | 结论 |
|------|---------|------|
| 缺少 X-CSRFToken | apiRequest 已从 Cookie 提取 csrftoken 并设置 header | ❌ 不是原因 |
| 缺少 Referer/Origin | 尝试带完整 referer/origin 仍 40102 | ❌ 不是原因 |
| 请求方法错误 | GET/POST 均 40102 | ❌ 不是原因 |
| body 参数格式 | 带/不带 isSophonx 均 40102 | ❌ 不是原因 |
| Cookie 过期 | 同时段 projects/list 返回 code=0 | ❌ 不是原因 |

### 2.2 确认证��

```
同一 Cookie:
  projects/list  →  code=0 (通过)
  projects/get   →  code=40102 (拒绝)
  adgroups/list  →  code=40102 (拒绝)
  live_room/list →  code=40102 (拒绝)

浏览器地址栏直接访问 projects/list URL:
  → code=40102, msg="无此操作权限"
  → 说明 GET 方式也需要权限校验

浏览器访问管理页面(SPA):
  → 页面通过 mcs.zijieapi.com/list 拿到数据
  → 数据嵌入 DOM，不走独立 API 调用
```

**结论**：这 3 个端点在服务端被标记为**详情页私有 API**，需要页面 HTML 内注入的 CSRFToken（与 Cookie 中的同名但值不同）。该 token 只有在浏览器渲染 SPA 时由前端框架注入，无法通过静态 Cookie 提取。

---

## 三、已探索的方案

### 3.1 方案 A：HTTP 客户端直接请求

```
oceanengine-api-client.mjs → apiRequest(url, {cookieData})
  Cookie 从 CDP 提取 → Referer/Origin/Accept 全量设置
  → 40102
```

**结论**：不可行。列表接口通行，详情接口拒绝，原因见上。

### 3.2 方案 B：浏览器地址栏直接访问

```
bsk navigate "https://ad.oceanengine.com/ad/api/promotion/projects/list?..."
  → HTML body 显示 {"code":40102}
```

**结论**：浏览器原生发出的 GET 请求同样 40102。页面数据走 MCS 中间层。

### 3.3 方案 C：CDP XHR 拦截

`oec-capture-status-api.mjs` 已有实现：
```
CDP → Network.requestWillBeSent → 监听 POST ad.oceanengine.com
     → Network.responseReceived → 拦截返回体
```

**状态**：框架已就绪，但需用户在浏览器中实际操作（暂停/启用计划）才能触发 API 调用。目前脚本是"被动监听"模式，未改成"主动触发"。

### 3.4 方案 D：browser-skill MCS 抓包

```
bsk session start → navigate 管理页 → network read
  → 捕获到 mcs.zijieapi.com/list POST 请求
```

**状态**：可捕获 MCS 层级数据，但 MCS 是页面级别的数据聚合服务，返回的是渲染后的 HTML 片段而非 JSON API。解析难度高于直接 JSON API。

---

## 四、突破路径（优先级排序）

### 4.1 CDP 主动触发 + 抓包（推荐）

将 `oec-capture-status-api.mjs` 从"被动监听"改为"主动触发"：

```
1. CDP 连接浏览器 → 注入 XHR 拦截器
2. CDP 程序化点击页面上的项目详情链接
3. 浏览器自然发出 projects/detail 请求（携带页面 CSRFToken）
4. Network.responseReceived 拦截返回体
5. 提取 unit/adgroup 字段
```

**优点**：直接拿到 JSON API 返回体，字段清晰
**难点**：需要处理 SPA 路由（详情页可能是前端路由，URL 不变）

### 4.2 bsk 页面 DOM 解析

```
1. bsk session start → navigate 项目管理页
2. bsk click 某个项目的详情入口
3. bsk get-html 抓取详情页 DOM
4. 从 HTML 中提取 unit 状态、预算配置等字段
```

**优点**：不需要抓包，纯 DOM 解析
**难点**：copilot 遮罩层干扰；MCS 返回的 DOM 结构不固定

### 4.3 bsk evaluate 注入 JS 读取 SPA store

```
1. bsk navigate 管理页
2. bsk evaluate "window.__INITIAL_STATE__" 或 "__NUXT__"
3. 读取 SPA 框架的状态管理数据
```

**优点**：如果 SPA 框架暴露了 store，一步拿到全部数据
**风险**：每个版本的 store key 可能不同，需要逆向前端代码

---

## 五、影响评估

| 数据 | 是否影响现有功能 | 暂缺的影响 |
|------|:---:|------|
| 单元级状态 | 否 | 无法在仪表盘中展示"直播间不可投放"状态 |
| 项目创作配置 | 否 | 无法做素材级分析 |
| 直播间实时状态 | 否 | 无法从 API 判断开播/关播 |

**现有 130 个可访问字段已覆盖 90% 的业务需求。** 这 3 个端点属于锦上添花，不阻塞仪表盘开发。

---

## 六、决策记录

| 日期 | 决策 | 理由 |
|------|------|------|
| 2026-07-29 | 暂不追逐 40102 端点 | 日��仪表盘开发优先级更高 |
| 2026-07-30 | 归档整理 3 端点逆向思路 | 留给后续 CDP 或 bsk 方案继续探索 |
