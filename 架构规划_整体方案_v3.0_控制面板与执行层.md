# 架构规划 v3.0 整体方案 — 控制面板 + 执行层

> **文档版本**: v3.0
> **生成日期**: 2026-06-28
> **前置文档**: `架构规划_评审汇总与新方案_v2.0.md`（七方评审汇总 + v2.0 修正）
> **本档增量**: 在 v2.0 基础上补充 **L5 交互层**（控制面板）与 **L6 执行层**（操作能力）
> **审查对象**: 用户提出的两个新需求
> 1. 易用前端：超越飞书多维表格，达到"小程序一样"的使用感受
> 2. 执行能力：准确执行广告计划基础操作（暂停/启用/加预算/调出价）

---

## 0. 修订说明

| 维度 | v2.0 现状 | v3.0 增量 |
|---|---|---|
| 采集层 L1 | HTTP API 主 + CDP 辅 | 不变 |
| 存储层 L2 | SQLite + DuckDB | 不变 |
| 分析层 L3 | DuckDB 聚合 | 不变 |
| 服务层 L4 | feedback-server（端口 8899） | 不变，扩展新端点 |
| **交互层 L5** | **缺失** | **飞书小程序 + H5 兜底** |
| **执行层 L6** | **缺失** | **Marketing API + CDP 回退 + 审批流** |
| 工期 | 3-4 周（P1-P3） | **5-6 周**（+P5 小程序 +P6 执行） |

**v3.0 解决的两个关键问题**：
1. **体验问题**：飞书多维表格只适合数据展示，无法做交互控制（按钮/图表联动/实时刷新）
2. **闭环问题**：v2.0 只能"看"，不能"调"。本次补全 Marketing API + 审批流 + 审计表

---

## 1. 需求与现状

### 1.1 用户诉求
> 想要更易用的前端控制广告后台监控和数据库调用，除了飞书多维表格，还有什么互动面板更容易达到小程序一样的使用感受。目前的执行方案还不能准确的对广告计划进行基础的暂停、开启、加预算调整，这个需要考虑到方案设计中

两条独立但相关的需求：
- **交互层升级**：从「飞书表格」升级到「小程序级体验」
- **执行能力补全**：从「只读监测」升级到「监测-分析-建议-执行-反馈」闭环

### 1.2 代码现状核验

| 模块 | 现状 | 缺口 |
|---|---|---|
| `oceanengine-api-client.mjs` | 4 读函数：`getProjects` / `getDashboardStats` / `getHourlyStats` / `collectAllData` | **0 写函数**（无 pause/enable/updateBudget） |
| `cdp-client.mjs` | 读取 + 智能等待 | 无页面交互能力 |
| `feedback-server.mjs` | `/report` `/feedback` | 无操作 API |
| `optimization_actions` | 数据库方案中预留 | **未激活，无执行通道** |
| 巨量引擎 Marketing API | 未接入 | 需申请开发者资质 + access_token |
| 飞书多维表格 | 已用于日报 | 仅展示，无控制 |

### 1.3 巨量引擎 Marketing API 能力盘点

巨量引擎开放平台提供 Marketing API，关键端点：

| 端点 | 能力 | QPS 限制 | 鉴权 |
|---|---|---|---|
| `POST /v3.0/campaign/update/status` | 启停广告计划 | 2 | OAuth 2.0 |
| `POST /v3.0/campaign/update/budget` | 调整计划预算 | 2 | OAuth 2.0 |
| `POST /v3.0/ad/update/bid` | 调整出价 | 5 | OAuth 2.0 |
| `POST /v3.0/campaign/create` | 创建计划 | 2 | OAuth 2.0 |
| `GET /v3.0/campaign/get` | 读取计划 | 10 | OAuth 2.0 |
| `POST /v3.0/report/custom/config/get` | 自定义报表 | 1 | OAuth 2.0 |

**结论**：Marketing API 完整覆盖"暂停/启用/加预算/调出价"四个核心需求，QPS 足够。需走申请流程（1-3 个工作日）。

---

## 2. L5 交互层 — 飞书小程序 + H5 兜底

### 2.1 方案对比（6 种）

| 方案 | 体验 | 触达 | 审核 | 开发量 | 跨端 | 适用场景 |
|---|---|---|---|---|---|---|
| 飞书多维表格 | ★★ | ★★★★ | 免 | 0 | ★★★★ | **数据展示（退化保留）** |
| 飞书 H5 卡片 | ★★★ | ★★★★ | 免 | 中 | ★★★★ | 简单面板 / 审批卡 |
| **飞书小程序** | **★★★★★** | **★★★★★** | **需** | **中** | ★★★★★ | **主推：日常管控** |
| 独立 Web（Next.js） | ★★★★ | ★★ | 免 | 高 | ★★★ | 大屏可视化 / 复杂筛选 |
| Tauri 桌面应用 | ★★★★★ | ★ | 免 | 高 | ★ | 离线 / 高频操作 |
| PWA | ★★★ | ★★★ | 免 | 中 | ★★★ | 跨端免审核 |

**决策矩阵**：
- 用户工作场景在飞书内 → 飞书生态触达最优
- "小程序一样体验" → 飞书原生小程序是字面意义上的"小程序"
- 审核风险 → H5 兜底覆盖审核期

### 2.2 推荐方案：飞书小程序 + H5 兜底

**主路径**：飞书原生小程序（lark-mini-program）
- 触达用户已经使用的飞书工作台
- 体验：原生加载、流畅动画、JSAPI 调用（扫码/选人/推送）
- 局限：需飞书开发者账号 + 审核（1-3 个工作日）

**兜底路径**：H5
- 嵌入飞书工作台（作为 H5 微应用）
- 同时支持独立 Web 部署（PC 大屏）
- 免审核，开发即上线

**技术方案**：Taro 3.x
- **一次开发，多端编译**：同一份 React 源码 → 小程序 + H5 + 移动 H5
- 生态成熟：京东 / 美团 / 字节内部广泛使用
- 配套：ECharts 图表 + Redux 状态管理

### 2.3 关键页面规划

| 页面 | 路由 | 核心功能 | 优先级 |
|---|---|---|---|
| 首页仪表盘 | `/pages/dashboard/index` | 实时消耗 / 余额 / 今日线索 / 告警数 | P0 |
| 计划列表 | `/pages/campaigns/index` | 表格 + 筛选（状态/消耗范围/CPA）+ 批量操作 | P0 |
| 计划详情 | `/pages/campaigns/detail` | 趋势图 + 转化漏斗 + 操作按钮 | P0 |
| 数据查询 | `/pages/query/index` | SQL 面板 + 图表生成 + 导出 CSV | P1 |
| 告警中心 | `/pages/alerts/index` | 实时告警 + 处置历史 | P0 |
| 建议反馈 | `/pages/suggestions/index` | AI 建议列表 + 采纳/拒绝 | P1 |
| 操作审计 | `/pages/audit/index` | optimization_actions 全量记录 | P1 |
| 系统设置 | `/pages/settings/index` | 账户管理 / 阈值配置 / 推送设置 | P2 |

### 2.4 关键交互设计

**操作流（以"暂停计划"为例）**：

```
[计划列表] 选中行
  ↓ 点击 [暂停] 按钮
[计划详情] 弹出操作确认框
  - 显示：当前状态/消耗/CPA
  - 选择：暂停原因（下拉）
  - 输入：备注（可选）
  ↓ 点击 [确认暂停]
[feedback-server] POST /api/action/pause
  - 写 optimization_actions (status=pending)
  - 推飞书"操作请求待执行"
  ↓ 异步执行
[执行器] 调 Marketing API
  - 成功 → status=success
  - 失败 → status=failed + 重试 3 次
  ↓ 完成推送
[小程序] 收到 WebSocket 实时更新
  - 列表行状态实时刷新
  - 飞书通知：操作结果
```

**实时刷新机制**：
- 小程序 → WebSocket（lark WS） → feedback-server
- 推送：操作结果 / 新告警 / 数据快照
- 降级：WebSocket 断开时轮询 `/api/snapshot?since=...`

### 2.5 技术栈

| 模块 | 选型 | 理由 |
|---|---|---|
| 框架 | Taro 3.6+ | 一次开发多端，React 语法 |
| 图表 | ECharts for Taro | 国内最广，淘宝/京东案例 |
| 状态管理 | Redux Toolkit | 大型应用标配 |
| HTTP | Taro.request + axios | 拦截器统一鉴权 |
| 实时 | WebSocket（lark WS SDK） | 操作实时反馈 |
| UI 组件 | Taro UI | 官方维护，飞书风格接近 |
| 鉴权 | OAuth 2.0 → JWT | 巨量引擎标准 + 自建 JWT |

### 2.6 鉴权设计

**双 OAuth 体系**：

| 体系 | 用途 | 申请 |
|---|---|---|
| 飞书 OAuth | 小程序登录 / 身份识别 | 飞书开放平台 |
| 巨量引擎 OAuth | 调 Marketing API | 巨量引擎开放平台 |

**Token 管理**：
- 飞书 access_token：飞书 JSAPI 自动管理
- 巨量引擎 access_token：feedback-server 端统一管理（2小时过期，前 5 分钟刷新）
- 小程序 → 后端：JWT（包含 user_id / 角色 / 有效期）

---

## 3. L6 执行层 — Marketing API + 浏览器回退

### 3.1 能力矩阵

| 能力 | 优先级 | 通道选择策略 | 备注 |
|---|---|---|---|
| 暂停/启用计划 | **P0** | API 优先 → CDP 回退 | 日均操作 ≥ 5 次 |
| 调整预算 | **P0** | API 优先 → CDP 回退 | 日均操作 ≥ 3 次 |
| 调整出价 | P1 | API 优先 → CDP 回退 | |
| 复制计划 | P2 | CDP only | Marketing API 无复制接口 |
| 删除计划 | P3 | API + CDP | **危险操作，需主管二次确认** |
| 暂停/启用广告组 | P1 | API 优先 → CDP 回退 | |
| 调整广告组预算 | P1 | API 优先 → CDP 回退 | |
| 修改定向 | P2 | CDP only | API 复杂，CDP 更稳 |

### 3.2 Marketing API 接入设计

**Token 管理**（`oauth-token-manager.mjs`）：
```javascript
// 核心逻辑
class TokenManager {
  constructor() {
    this.token = null;
    this.expireAt = 0;
  }
  async getToken() {
    if (Date.now() < this.expireAt - 300_000) return this.token;  // 提前 5min 刷新
    const resp = await fetch('https://ad.oceanengine.com/openapi/oauth2/access_token/', {
      method: 'POST',
      body: new URLSearchParams({
        app_id: process.env.OCEANENGINE_APP_ID,
        secret: process.env.OCEANENGINE_APP_SECRET,
        grant_type: 'auth_code',  // 或 'refresh_token'
        auth_code: this.authCode,
      }),
    });
    const data = await resp.json();
    this.token = data.access_token;
    this.expireAt = Date.now() + data.expires_in * 1000;
    return this.token;
  }
}
```

**写操作封装**（`oceanengine-api-write.mjs`）：
```javascript
// 暂停/启用计划
export async function updateCampaignStatus(client, campaignId, status) {
  // status: 'enable' | 'disable'
  const resp = await apiRequest(
    `${API_BASE}/open_api/v3.0/campaign/update/status/`,
    {
      method: 'POST',
      headers: { 'Access-Token': await tokenManager.getToken() },
      body: JSON.stringify({
        advertiser_id: ADVERTISER_ID,
        data: { campaign_id: String(campaignId), opt_status: status === 'enable' ? 'enable' : 'disable' },
      }),
    }
  );
  return { ok: resp.ok, code: resp.data?.code, message: resp.data?.message };
}

// 调整预算
export async function updateCampaignBudget(client, campaignId, newBudget) {
  const resp = await apiRequest(
    `${API_BASE}/open_api/v3.0/campaign/update/budget/`,
    {
      method: 'POST',
      headers: { 'Access-Token': await tokenManager.getToken() },
      body: JSON.stringify({
        advertiser_id: ADVERTISER_ID,
        data: { campaign_id: String(campaignId), budget: newBudget },
      }),
    }
  );
  return { ok: resp.ok, code: resp.data?.code, message: resp.data?.message };
}
```

**限流处理**：
- QPS 限制：写操作 2 QPS / 账户
- 实现：令牌桶算法（`rate-limiter.mjs`），超出排队
- 超限返回：指数退避重试 3 次（1s/2s/4s）

### 3.3 CDP 回退机制

**触发条件**（满足任一即启用）：
1. Marketing API 返回 `code != 0` 且非限流
2. 连续 3 次 API 调用失败
3. 操作类型不在 Marketing API 支持范围（如复制计划）

**实现**（`operation-executor.mjs`）：
```javascript
async function executeViaCDP(action) {
  const { campaignId, actionType, newValue } = action;
  const context = await browser.newContext({ storageState: CHROME_AUTH_STATE });
  const page = await context.newPage();

  await page.goto('https://ad.oceanengine.com/promotion/promote-manage/project');
  // 校准页面（复用 calibrate-page.mjs v2）
  await calibratePage(page);
  // 定位目标计划
  await page.locator(`tr[data-campaign-id="${campaignId}"]`).click();
  // 触发操作
  if (actionType === 'pause') {
    await page.locator('button.ovui-button--pause').click();
    await page.locator('.ovui-modal-confirm-btn').click();
  } else if (actionType === 'update_budget') {
    await page.locator('input[placeholder="日预算"]').fill(String(newValue));
    await page.locator('button.ovui-button--primary').click();
  }
  // 截图留证
  await page.screenshot({ path: `audit/${action.id}.png` });
  return { ok: true, screenshot: `${action.id}.png` };
}
```

**反检测**：
- 使用 `playwright-extra` + `puppeteer-extra-plugin-stealth`
- 鼠标轨迹随机化
- 操作间隔随机抖动（500ms-1500ms）

### 3.4 审批流设计（L0-L3 分级）

| 等级 | 触发条件 | 流程 | 时效 |
|---|---|---|---|
| **L0 自动** | 暂停 / 启用 / 预算调整 ≤ 20% | 用户确认 → 直接执行 | 立即 |
| **L1 复核** | 预算调整 20%-50% / 调整出价 | 用户确认 → 账户第二人飞书审批 → 执行 | 30 分钟 |
| **L2 主管** | 高消耗计划（≥ 1000/日）预算调整 | 用户确认 → 主管飞书审批 → 执行 | 2 小时 |
| **L3 风控** | 删除 / 复制 / 预算调整 > 50% | 用户确认 → 主管 + 飞书群通知 → 双人复核 → 执行 | 24 小时 |

**审批实现**：
- optimization_actions 表新增字段：`approver_id`, `approved_at`, `approval_channel`
- 飞书卡片：含 [通过] [驳回] 按钮 → 回调 `feedback-server /api/action/approve`
- 超时未审批：自动驳回 + 飞书通知申请人

### 3.5 操作审计

**`optimization_actions` 表设计**（v2.0 基础上扩展）：

```sql
CREATE TABLE optimization_actions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  campaign_id INTEGER NOT NULL,
  campaign_name TEXT,
  account_id TEXT NOT NULL,        -- 多账户支持
  platform TEXT DEFAULT 'oceanengine',
  action_type TEXT NOT NULL,       -- pause/enable/update_budget/update_bid/copy/delete
  old_value TEXT,                  -- JSON
  new_value TEXT,                  -- JSON
  operator_id TEXT NOT NULL,       -- 操作人飞书 user_id
  operator_name TEXT,
  approver_id TEXT,                -- 审批人
  approved_at INTEGER,
  approval_channel TEXT,           -- lark/h5/api
  status TEXT NOT NULL,            -- pending/approved/rejected/executing/success/failed
  channel TEXT,                    -- marketing_api/cdp
  error_code TEXT,
  error_message TEXT,
  screenshot_path TEXT,            -- CDP 执行时截图
  request_id TEXT UNIQUE,          -- 防重复
  lock_token TEXT,                 -- 并发锁
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL,
  executed_at INTEGER,
  INDEX idx_actions_campaign (campaign_id, created_at),
  INDEX idx_actions_status (status, created_at),
  INDEX idx_actions_operator (operator_id, created_at)
);
```

**全链路追踪**：
```
[用户小程序点击] → request_id=abc123
  ↓ 写 optimization_actions (status=pending)
  ↓ 推飞书卡片（request_id=abc123）
[审批流] → 飞书用户点 [通过] → status=approved
  ↓ 写 optimization_actions (status=approved, approver_id=xxx)
[执行器消费] → status=executing → 调 Marketing API
  ↓ 成功 → status=success, executed_at=now
  ↓ 失败 → status=failed, error_message=xxx
  ↓ 推飞书通知
[小程序 WebSocket] → 收到 status 更新 → UI 实时刷新
```

### 3.6 执行层架构图

```
┌─────────────────────────────────────────────────────────────┐
│                  L5 飞书小程序 / H5                          │
│  ┌─────────┐  ┌──────────┐  ┌────────┐  ┌──────────────┐    │
│  │ 仪表盘  │  │ 计划列表  │  │ 详情页 │  │ 操作确认弹框 │    │
│  └─────────┘  └──────────┘  └────────┘  └──────────────┘    │
└──────────────────────────┬──────────────────────────────────┘
                           │ WebSocket + REST
                           ↓
┌─────────────────────────────────────────────────────────────┐
│              L4 feedback-server (端口 8899)                  │
│  ┌────────────┐  ┌─────────────┐  ┌──────────────────────┐  │
│  │ /api/query │  │ /api/action │  │ /api/audit           │  │
│  │ SQL查询    │  │ 操作执行    │  │ 审计查询             │  │
│  └────────────┘  └────────┘─┬─┘  └──────────────────────┘  │
│                              │                                │
│                              ↓                                │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ operation-executor.mjs (PM2 常驻)                    │    │
│  │  - 消费 optimization_actions (status=pending/approved) │    │
│  │  - 路由：Marketing API 优先 → CDP 回退                │    │
│  │  - 限流：令牌桶 (2 QPS)                              │    │
│  │  - 重试：3 次指数退避                                  │    │
│  └────────────────────────┬─────────────────────────────┘    │
└───────────────────────────┼─────────────────────────────────┘
                            ↓
        ┌───────────────────┴────────────────────┐
        ↓                                        ↓
┌──────────────────┐                  ┌────────────────────┐
│  Marketing API   │                  │  playwright-core   │
│  (巨量引擎开放)   │                  │  (CDP 回退)         │
│  QPS: 2 (写)     │                  │  接管已登录Chrome   │
└──────────────────┘                  └────────────────────┘
```

---

## 4. v2.0 → v3.0 修订对照

| 项 | v2.0 | v3.0 | 变化原因 |
|---|---|---|---|
| 采集层 L1 | HTTP API 主 + CDP 辅 | 不变 | — |
| 存储层 L2 | SQLite + DuckDB | 不变 | — |
| 分析层 L3 | DuckDB 聚合 | 不变 | — |
| 服务层 L4 | feedback-server | 不变，扩展 `/api/action/*` | 新增端点 |
| **L5 交互** | **无** | **飞书小程序 + H5** | 用户诉求 |
| **L6 执行** | **无** | **Marketing API + CDP 回退** | 用户诉求 |
| 工期 | 3-4 周 | **5-6 周** | +2 周（L5 1-2 周 + L6 1-2 周） |
| 依赖新增 | — | `taro`, `echarts-taro`, 巨量引擎 API 资质 | — |
| 风险新增 | — | 飞书小程序审核 / API 权限 | — |

---

## 5. 关键决策矩阵

| 决策 | 选择 | 核心理由 | 否决项 |
|---|---|---|---|
| 交互层 | 飞信小程序 | 触达+体验+飞书生态 | 独立 Web 部署复杂、触达差 |
| 小程序框架 | Taro 3.x | 一次开发多端（小程序 + H5） | 原生小程序开发成本高、无法兜底 |
| 图表库 | ECharts for Taro | 国内最广、小程序版成熟 | Chart.js 小程序支持弱 |
| 鉴权（飞书） | OAuth 2.0 + JSAPI | 飞书标准 | — |
| 鉴权（巨量引擎） | OAuth 2.0 | 巨量引擎标准 | — |
| 写操作主通道 | Marketing API | 稳定 + 有审计 + 无反检测风险 | CDP 单独使用反检测风险高 |
| CDP 回退条件 | API 失败 / 不支持 | 保证操作可达性 | 始终走 CDP（风险高） |
| 反检测 | playwright-extra + stealth | 巨量风控严格 | 无防护（必被检测） |
| 审批分级 | L0-L3 四级 | 风控与效率平衡 | 无审批（误操作风险）/ 全审批（效率低） |
| 审计表 | optimization_actions 增强 | 合规 + 追溯 + 防并发 | 仅日志（结构化差） |
| 并发控制 | lock_token 字段 | 防止同一计划并发操作 | 无锁（冲突风险） |
| Token 存储 | 环境变量 + 加密文件 | 安全性 | 硬编码（明文泄露） |

---

## 6. 落地路线（5 阶段 · 5-6 周）

| 阶段 | 工期 | 内容 | 交付物 | 并行 |
|---|---|---|---|---|
| **P1** | 1-2 天 | playwright-core 接入，迁 ai-regions | cdp-client.mjs 退役 | — |
| **P2** | 2-3 天 | SQLite 迁移，schema 加 platform，JSON 双写验证 | oceanengine.db + /query | — |
| **P3** | 3-4 天 | DuckDB 分析层，日报聚合，Parquet 归档 | DuckDB ATTACH | — |
| **P5** | **1-2 周** | **飞书小程序开发（核心 5 页）** | **Taro 工程 + 小程序审核上线 + H5 兜底** | **可与 P6 并行** |
| **P6** | **1-2 周** | **执行层（Marketing API + 审批流）** | **operation-executor.mjs + optimization_actions 表** | **可与 P5 并行** |
| P4 | 按需 | AI 闭环（CPA 预测 + 自动调预算） | 模型集成 | — |

**里程碑**：
- **M1（第 1 周末）**：P1+P2 完成，采集层全 HTTP API，存储双写
- **M2（第 2 周中）**：P3 完成，分析层就绪
- **M3（第 3-4 周末）**：P5 完成，小程序上线替代飞书表格
- **M4（第 4-5 周末）**：P6 完成，"监-控-调"全闭环

---

## 7. 与现有系统兼容性

### 7.1 保持不变
- Chrome 远程调试端口 9222
- PM2 进程管理
- 飞书 OAuth 推送（5min/15min/日报）
- `monitor-data/` JSON 文件（双写期）
- v3 监控的 cdp-client.mjs（v1 阶段保留，v3 退）

### 7.2 平滑迁移
- `oceanengine-api-client.mjs` 4 读函数保留
- 新增 `oceanengine-api-write.mjs`（写函数）
- 新增 `oauth-token-manager.mjs`（巨量引擎 token）
- 新增 `operation-executor.mjs`（PM2 常驻，端口 8900）
- `feedback-server.mjs` 新增 `/api/action/*` 端点
- `optimization_actions` 表新建（v2.0 预留表激活）

### 7.3 退役清单
- 飞书多维表格 → 保留为只读视图，主控面板转为小程序
- `cdp-proxy.mjs`（HTTP 包装）→ playwright-core 接管
- `cdp-client.mjs`（WS 客户端）→ playwright-core 接管

### 7.4 灰度策略
- **P5 灰度**：先在小范围账户（1 个 AI 区域号）上线小程序，验证后再全量
- **P6 灰度**：先 L0（暂停/启用）开放，再 L1（调预算），最后 L2-L3（删除/复制）
- **回滚**：每阶段保留 7 天回滚窗口（JSON 旧路径 + 双写期）

---

## 8. 风险与缓解

| # | 风险 | 概率 | 影响 | 缓解措施 |
|---|---|---|---|---|
| R1 | 飞书小程序审核驳回 | 中 | 高 | H5 兜底，审核期用 H5；预留 3-5 天审核缓冲 |
| R2 | 巨量引擎 Marketing API 权限不足 | 中 | 高 | CDP 回退；申请多个 app_id 备选 |
| R3 | 操作误触（误暂停高 ROI 计划） | 中 | 高 | L0-L3 审批分级 + 二次确认 + 7 天内操作回滚窗口 |
| R4 | 并发操作冲突 | 低 | 中 | optimization_actions.lock_token 字段 + 队列串行 |
| R5 | 反检测触发（CDP 回退时） | 低 | 中 | playwright-extra stealth + 随机抖动 |
| R6 | access_token 过期 | 低 | 中 | 前 5 分钟自动刷新 + 重试机制 |
| R7 | 飞书 WebSocket 不稳定 | 中 | 低 | 轮询降级 `/api/snapshot?since=...` |
| R8 | Taro 多端兼容问题 | 中 | 中 | 优先保证小程序端，H5 次之 |
| R9 | 小程序 JSAPI 权限申请 | 中 | 低 | 提前 1 周申请 JSAPI 权限 |
| R10 | 数据库迁移数据丢失 | 低 | 高 | JSON 双写 14 天 + 自动校验脚本 |

---

## 9. 关键资源

| 资源 | 链接 | 用途 |
|---|---|---|
| 飞书小程序开发文档 | https://open.feishu.cn/document/uYjL24iN/uEzMzUjLxMzM14SMzMTN | 小程序开发指南 |
| 飞书 JSAPI | https://open.feishu.cn/document/uYjL24iN/uMTMuMTMuMTM | 扫码/选人/推送 |
| 巨量引擎 Marketing API | https://ad.oceanengine.com/openapi/doc/ | 广告操作 API |
| 巨量引擎 OAuth 文档 | https://ad.oceanengine.com/openapi/doc/index.html?key=tongyong&type=openapi/doc | access_token 申请 |
| Taro 文档 | https://docs.taro.zone/ | 跨端框架 |
| ECharts Taro 版 | https://github.com/Cecilxx/echarts-taro | 图表 |
| playwright-extra stealth | https://github.com/berstend/puppeteer-extra/tree/master/packages/puppeteer-extra-plugin-stealth | 反检测 |

---

## 10. 附录

### 10.1 待办清单（P5+P6）

**P5 飞书小程序**：
- [ ] 注册飞书小程序开发者账号（企业主体）
- [ ] 申请 JSAPI 权限（扫码 / 选人 / 推送）
- [ ] 安装 Taro + ECharts Taro
- [ ] Taro 工程初始化（`taro init monitor-mini-app`）
- [ ] 实现 5 个核心页面：仪表盘 / 计划列表 / 计划详情 / 告警中心 / 系统设置
- [ ] 接入 feedback-server API
- [ ] WebSocket 实时推送
- [ ] 小程序 UI 设计稿（飞书风格）
- [ ] 提交审核
- [ ] H5 兜底版本（同一 Taro 源码编译）

**P6 执行层**：
- [ ] 申请巨量引擎 Marketing API 开发者资质
- [ ] 实现 `oauth-token-manager.mjs`
- [ ] 实现 `oceanengine-api-write.mjs`（4 写函数：pause/enable/updateBudget/updateBid）
- [ ] 实现 `operation-executor.mjs`（PM2 常驻）
- [ ] 创建 `optimization_actions` 表（v2.0 预留激活）
- [ ] 实现 `feedback-server /api/action/*` 端点
- [ ] 实现 L0-L3 审批流
- [ ] 飞书审批卡片模板
- [ ] playwright-core 接管 Chrome（复用 P1 基础设施）
- [ ] 反检测 stealth 插件集成
- [ ] 端到端测试（暂停 → 推送 → 列表刷新）

### 10.2 关键账户清单

| 平台 | 账户类型 | 数量 | API 资质 | 备注 |
|---|---|---|---|---|
| 巨量引擎 | 真人号 | 1 | 待申请 | 主账户 |
| 巨量引擎 | AI 区域号 | 5 | 待申请 | 东/西/中/南/北 |
| 巨量引擎 | 短视频号 | 1 | 待申请 | 备用 |
| 飞书 | 企业自建应用 | 1 | 已有 | OAuth 已配 |
| 飞书 | 小程序 | 1 | 待申请 | 需企业主体 |

### 10.3 文档结构（v3.0 文件清单）

```
架构规划/
├── 架构规划_整体方案.md              # v1.0（用户首版）
├── 架构规划_评审反馈_*.md            # 7 份评审（Qwen/豆包/Mimo/Kimi/WB/M3/DSV4）
├── 架构规划_评审汇总与新方案_v2.0.md  # 评审汇总 + v2.0 修正
└── 架构规划_整体方案_v3.0_控制面板与执行层.md  # ← 本文件
```

### 10.4 术语表

| 术语 | 解释 |
|---|---|
| L0/L1/L2/L3 | 操作审批等级（自动/复核/主管/风控） |
| Marketing API | 巨量引擎开放平台广告操作 API |
| access_token | 巨量引擎 API 鉴权令牌（2 小时过期） |
| lock_token | 防止并发操作的唯一锁 |
| stealth | playwright 反检测插件 |
| Taro | 京东开源跨端框架，一次开发多端 |
| ECharts | 百度开源图表库 |
| H5 | HTML5 移动网页（兜底方案） |
| JSAPI | 飞书小程序原生能力接口 |
| optimization_actions | 操作审计表（暂停/启用/调预算记录） |

---

**本规划文档可直接交给其他模型进行深度分析，重点验证**：
1. Marketing API 端点路径与参数是否准确（需对照官方文档核验）
2. Taro + ECharts Taro 当前版本兼容性
3. 飞书小程序 JSAPI 权限申请清单
4. playwright-core 接管已登录 Chrome 的稳定性
5. 审批流 L0-L3 划分是否与实际业务风险匹配

**下一步建议**：
- 同步给 Qwen 3.7 Max 做第二轮 review（重点：Marketing API 端点 + Schema）
- 同步给 DeepSeek V4 Pro 验证反检测 + 限流设计
- 同步给 Kimi K2.7 评估工期合理性
