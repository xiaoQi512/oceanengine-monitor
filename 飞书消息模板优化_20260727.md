# 飞书群 @回复模板 & 建议反馈内容优化

> 日期：2026-07-27
> 范围：`feishu-listener.mjs` 的 `callAI` / `handleAtMention` / `dispatch` 回复内容
> 当前问题：@回复内容单薄、System Prompt 缺少实时数据、指令回复缺少计划状态上下文

---

## 一、当前问题

| 问题 | 示例 | 影响 |
|------|------|------|
| AI prompt 无实时数据 | System Prompt 只有固定账户名和日预算 | 回复空洞，无法回答具体数据 |
| @回复先浪费一句 | "收到，思考中..." 无信息量 | 多一次消息打扰 |
| 指令回复无上下文 | 暂停回复只提队列位置 | 不知道计划当前状态、验证是否合理 |
| info 展示过于简陋 | 队列 3 条待处理，无类型和名称 | 无法快速判断 |
| 建议类型单一 | 只有暂停/关停/加预算 | 缺改出价、缺批量操作 |

---

## 二、AI System Prompt 改造

### 当前（第 397 行）

```javascript
const systemPrompt = "你是小七，巨量引擎广告投放监控助手（账户：极狐-区域福利号-直播，日预算45000元）。你已在这个群聊中，直接自然回答问题，不要自我介绍。中文回复，不超过3句话。";
```

### 改造后

```javascript
async function buildSystemPrompt() {
  // 读取实时账户数据
  let dataCtx = '';
  try {
    const stats = JSON.parse(fs.readFileSync(path.join(DATA_DIR, 'latest-stats.json'), 'utf-8'));
    const alerts = JSON.parse(fs.readFileSync(path.join(DATA_DIR, 'latest-alerts.json'), 'utf-8'));
    dataCtx = `
当前账户状态（${stats.time}）:
- 今日消耗 ¥${stats.totalSpend} / 日预算 ¥${stats.budget}（${(stats.totalSpend/stats.budget*100).toFixed(0)}%）
- CPL ¥${stats.avgCPA} · 线索 ${stats.totalConversions}条 · 投放中 ${stats.activeCount}条
- 节奏: ${stats.pacingHealth} · 时段: ${stats.timeSlot}
- 余额: ¥${stats.balance} (可支撑约${stats.balanceDays}天)
- 当前告警: ${alerts.length}条 (${alerts.filter(a=>a.severity==='high').length}条严重)
`;
  } catch {}

  return `你是小七，巨量引擎广告投放监控助手。

账户: 极狐-区域福利号-直播
账户ID: 1842681352509635
日预算: ¥60,000

${dataCtx}

核心指标含义:
- CPL (线索成本) = 总消耗 / 线索数，越低越好
- CPM = 千次展示成本
- CTR = 点击率 · CVR = 转化率
- 零转化 = 计划消耗了但没产生转化，通常建议暂停
- 高CPL = 线索成本远超均值，建议关停或降预算
- 预算撞线 = 接近日预算上限，可能被系统自动暂停
- 掉量 = 计划消耗显著下降，可能进入衰退

回复要求:
1. 中文回复，每条不超过5句
2. 如果用户问数据问题，基于上述实时数据回答
3. 如果用户要求操作，引导使用指令格式: 暂停/关停/恢复/加预算 「计划名」
4. 如果不知道答案，诚实说不知道，不要编造数据
5. 用简洁的列表格式呈现多项信息`;
}
```

---

## 三、@回复模板（handleAtMention）

### 当前

```javascript
await sendMsg(chatId, "收到，思考中...");
const reply = await callAI(cleaned);
if (reply) { await sendMsg(chatId, reply); }
```

### 改造后

直接回复，去掉"收到，思考中..."占用一条消息。

```javascript
async function handleAtMention(text, chatId) {
  const cleaned = cleanAtText(text);
  if (!cleaned) {
    await sendMsg(chatId, 
      '👋 我在。\n\n' +
      '我可以帮你：\n' +
      '· 查询数据 — @小七 今天消耗多少？\n' +
      '· 执行操作 — 暂停 「计划名」\n' +
      '· 查看状态 — 状态 / 队列\n' +
      '· 调整预算 — 加预算 「计划名」8000\n' +
      '· 调整出价 — 改出价 「计划名」95.5');
    return;
  }
  
  const reply = await callAI(cleaned);
  if (reply) { await sendMsg(chatId, reply); }
  else { await sendMsg(chatId, '抱歉，暂时无法处理。请尝试: 状态 查看帮助'); }
}
```

---

## 四、指令回复模板（dispatch 各分支）

### 4.1 暂停/关停

**触发**: `暂停 「0724-真人直播-画面直投_1」`

```
🔵 暂停「0724-真人直播-画面直投_1」
   当前状态：投放中 · 今日消耗 ¥2,150 · CPL ¥119
   已入队（#3），等待 worker 执行
   📊 队列共 3 条 · 你的排第 3 位
```

**完成（worker 回调）**:  
```
✅ 已暂停「0724-真人直播-画面直投_1」
   今日最终消耗 ¥2,150 · 线索 18条 · CPL ¥119.44
   队列剩余 2 条待处理
```

### 4.2 恢复

**触发**: `恢复 「计划名」`

```
🔵 恢复「计划名」
   当前状态：已暂停 · 上次消耗 ¥850 · CPL ¥95
   已入队（#2），等待 worker 执行
```

### 4.3 加预算

**触发**: `加预算 「0724-真人直播-画面直投_1」 10000`

```
🔵 修改预算「0724-真人直播-画面直投_1」
   当前 ¥8,000（已用 ¥7,200 · 90%）→ ¥10,000
   已入队（#4），等待 worker 执行
```

### 4.4 改出价（新增）

**触发**: `改出价 「计划名」 97.5`

```
🔵 修改出价「计划名」
   当前 ¥95.00 → ¥97.50
   已入队（#3），等待 worker 执行
```

### 4.5 执行/采纳

**触发**: `执行 「计划名」` 或直接 `执行`（取队首）

```
🔵 执行「计划名」— 已采纳，等待 worker 处理
   队列位置：#1 · 类型：暂停 · 入队时间：14:30
```

### 4.6 拒绝

**触发**: `拒绝 「计划名」`

```
✅ 已拒绝「计划名」
   该建议不再推送 · 同类建议被拒 2 次将全局抑制
   📋 今日建议：采纳 3 · 拒绝 2 · 忽略 1
```

### 4.7 状态/帮助

**当前**: 一行文本，无队列详情

**改造后**：
```
📊 队列状态
   待处理 3 条：
   1. [暂停] 0724-真人直播-画面直投_1 — 14:30 入队
   2. [加预算→¥10,000] 0726-真人直播-简单投 — 14:35 入队
   3. [关停] 0724-真人直播-简单投 — 14:38 入队
   
📋 今日统计
   执行 5 次 · 成功 5 · 失败 0
   类型: 暂停 2 · 恢复 1 · 加预算 2
   建议采纳率: 67% (8/12)
   
⌨️ 指令
   暂停/关停/恢复 「计划名」
   加预算 「计划名」 [金额]
   改出价 「计划名」 [金额]
   拒绝 「计划名」 · 执行
   状态 · 帮助
```

---

## 五、自然语言 Q&A 回复示例

### 数据查询

| 用户问 | 回复 |
|--------|------|
| @小七 今天消耗多少 | 今日消耗 ¥17,100 / ¥60,000（29%）· CPL ¥96 · 线索 178条 · 节奏正常 |
| @小七 哪个计划跑最好 | TOP3（按转化）：计划A 45条 ¥85CPL · 计划B 38条 ¥92CPL · 计划C 30条 ¥98CPL |
| @小七 有没有问题 | 3条告警待处理：🟡 计划X 零转化 ¥85 · 🟡 计划Y CPL ¥248(2.6x) · 🟡 计划Z 预算撞线90% |
| @小七 建议关停哪个 | 建议优先关停「计划Y」— CPL ¥248 远超均值2.6倍，今日已消耗 ¥3,500 |
| @小七 余额还有多少 | ¥614,043，按日均¥78,000消耗，可支撑约7.9天 |
| @小七 今天跟昨天比怎么样 | 消耗↑8% · CPL↓3% · 线索↑12%，整体略优于昨日 |

### 操作类

| 用户问 | 回复 |
|--------|------|
| @小七 帮我把零转化的都停了 | 当前零转化告警的计划：1.「计划A」消耗¥85 · 2.「计划B」消耗¥120。如果确定，请回复：暂停「计划A」、暂停「计划B」 |
| @小七 CPL太高的关一下 | CPL超过均值2倍的：1.「计划A」¥248(2.6x) · 2.「计划B」¥210(2.2x)。可以关停，请回复指令确认 |
| @小七 给XX加到15000 | 加预算 「计划名」 15000（请确认计划名） |

---

## 六、建议/反馈内容

### 6.1 建议卡片文本

当前监控卡片中的建议 Section 只是简单罗列告警。改造后每条建议附带数据支撑：

```
💡 盯盘建议

1️⃣ 🔴 暂停「计划A」— 消耗 ¥85 零转化，已运行 2.5h
   同类计划平均 1.5h 出首单，建议释放预算投其他

2️⃣ 🔴 暂停「计划B」— CPL ¥248（均值 ¥96 的 2.6 倍）
   按此成本亏损，建议关停等待次日重新学习

3️⃣ 🟡 追加「计划C」— 已用 90% 预算（¥7,200/¥8,000）
   当前 CPL ¥95 正常，建议追加到 ¥12,000 继续跑量

4️⃣ 🟡 观察「计划D」— 近 30min 掉量 45%
   可能进入衰退，如下一轮未恢复建议暂停
```

### 6.2 用户对建议的反馈回复

```
用户点击「采纳」按钮:
→ 入队，回复：✅ 已采纳，暂停「计划A」交由 worker 执行

用户回复「采纳 1」或「采纳计划A」:
→ 同上，按序号或计划名定位

用户回复「拒绝 1」:
→ 回复：✅ 已拒绝建议 #1，不再推送此计划同类告警
   该类型累计被拒 2 次，即将全局抑制

用户回复「全部采纳」:
→ 挨个入队，回复：✅ 已采纳 3 条建议，依次交由 worker 执行
```

---

## 七、实施要点

### 7.1 callAI 改造

```javascript
async function callAI(userMessage) {
  const systemPrompt = await buildSystemPrompt();  // 动态生成，含实时数据
  const prompt = systemPrompt + "\n\n用户: " + userMessage + "\n小七:";
  // ... 调用 codebuddy
}
```

### 7.2 实时数据缓存

新增定时任务（每 5 分钟），从 `monitor-data/` 读取最新监控数据，写入 `latest-stats.json` 和 `latest-alerts.json`，供 System Prompt 使用。

### 7.3 改出价命令注册

在 `CMD_RULES` 中新增:
```javascript
{ id: 'adjust_bid', re: /^\s*(改出价|调整出价|修改出价|adjust\s*bid)\s*/i, needAmount: true },
```

### 7.4 dispatch 增强

`acknowledgeStart` 中增加计划当前状态查询；`reportResult` 中增加队列剩余数量。

---

## 八、版本记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-07-27 | v1.0 | 初始方案，聚焦 @回复模板和建议反馈内容 |
