# 巨量引擎广告计划调整 · v1.1 架构方案 · 评审意见

> 评审人：爆量君（WorkBuddy Agent）
> 评审日期：2026-07-27（周一 21:06 GMT+8）
> 评审对象：`广告计划调整_四层架构方案_v1.1_20260727.md`
> 评审范围：本机运行环境适配、前端展示、架构优缺点与优化空间

---

## 0. 总评（一句话）

v1.1 把"自动幂等去重"换成"重复指令二次确认"是**对的业务取舍**——巨量广告后台对同一计划频繁暂停/恢复/调价会触发模型重新学习期，无脑去重是隐性 bug。架构走向与 v3.1 / v5.0 的"代码已走得通保留，不硬拆不新增空概念"原则一致，**继续走**。真正落地的风险集中在 4 个地方：审计归一时的字段不兼容、pending 介质选择、Dashboard 操作面板缺位、CDP 降级无健康熔断。

---

## 1. 本机运行环境 — 需要注意和适配

### 1.1 文件路径不对称（重要）

**事实**：
- `action-queue.json` 在项目根目录（`action-queue-worker.mjs:17`）
- `action-queue.json.lock` 在项目根目录（`action-queue-worker.mjs:18`）
- `monitor-data/action-audit.jsonl` 在 `monitor-data/`（`action-queue-worker.mjs:19`）
- v1.1 新增 `pending-actions.json` —— **路径没约定**

**建议**：
- 与现有 `monitor-data/` 一致，把 `pending-actions.json` 也放 **`monitor-data/`**，并在 `monitor-utils.mjs` 里集中导出 `PENDING_FILE`、`AUDIT_FILE`、`QUEUE_FILE`、`LOCK_FILE`，避免散落。`monitor-utils.mjs` v3.2 已经在做这件事，正好顺势补齐。
- 反过来考虑：审计已有 `monitor-data/action-audit.jsonl`，锁文件 `action-queue.json.lock` 留在根目录可以理解（与被锁对象在同一目录），但**没有任何一致性的强约束**，建议在 `monitor-utils.mjs` 顶部加一段"目录约定表"，让后人不用 grep 找。

### 1.2 Windows 文件锁的边界

**事实**：
- `acquireLock()` 用 `fs.writeFileSync(LOCK_FILE, ..., { flag: 'wx' })` 原子创建，10 分钟僵死回收。
- 当前 PM2 配置 `action-queue-worker` 是 `instances: 1`（`ecosystem.config.cjs:176`）—— 单进程，单机 OK。

**风险**：
- Windows 上 `wx` 标志等价于 `O_CREAT | O_EXCL`，但**没有强制锁**（advisory lock）。如果未来有人手工跑 `node action-queue-worker.mjs`，或者 PM2 因 max_restarts 重启时产生第二个 worker 进程，会出现"两个 worker 抢同一队列"。当前看似无害，但**v1.1 没把这条边界标到 PM2 配置注释里**。
- 僵死回收依赖 mtime，Windows 上 mtime 会有 2 秒精度漂移（FAT 上甚至更大，NTFS 一般是 100ns），不影响功能但要意识到不依赖精确秒级。

**建议**：
- 在 `ecosystem.config.cjs` 的 `action-queue-worker` 配置上加注释：
  ```js
  // ⚠️ 单实例约束：.lock 文件锁是 advisory lock，多实例会产生竞态
  instances: 1,
  ```
- 持续监控 PM2 启动日志里有没有出现 `[worker] 检测到僵死锁` —— 出现次数飙升说明有进程在打架。

### 1.3 CDP 降级路径的依赖（关键）

**事实**：
- v1.1 P2-1 设计"API 优先 + CDP 降级"。
- 已有 `chrome-guard.mjs`（PM2 常驻 60s 探活 9222 端口）。

**风险**：
- 如果 Chrome 9222 挂了，`tryHttpApi` → false → `tryCdpAction` → 直接失败。**v1.1 没设计第三层 fail-safe**。
- 巨量广告 API 偶发 503 是有先例的（`oceanengine-api-client.mjs` 抓过），叠上 Chrome 死掉 = 整条执行链断。

**建议**：
- 在 worker 里加一道 CDP 熔断：
  ```js
  const cdpAlive = await pingChrome9222();   // 类似 chrome-guard 的 TCP 探活
  if (!cdpAlive) {
    return { ok: false, err: 'HTTP API 失败 + CDP 不可达，执行中止', method: 'none' };
  }
  ```
- 这条要写进 `monitor-utils.mjs`，与 chrome-guard 共享同一份探活函数，避免重复实现。
- 顺手把 `chrome-guard` 健康状态写到 `monitor-data/daemon-health.json`，worker 可以直接读，不要每探一次都连 Chrome。

### 1.4 PM2 listener 进程与 pending 超时扫描

**事实**：
- v1.1 3.2 提到"每 30s 扫描 pending 表"。
- 实施位置在 `feishu-listener.mjs`（PM2 常驻，`exec_mode: 'fork'`）。

**风险**：
- `setInterval(callback, 30000)` 在 Node 里**不会自动 rescheduling**——如果某次 callback 因为 await 抛异常延迟到 1 分钟后，下一次扫描不会补跑，但回调时机会往后推。
- PM2 重启 listener 进程时（autorestart: true + max_restarts: 10），**未超时的 pending 记录会留在 JSON 文件里**，但新进程启动不会重发"已超时"通知。

**建议**：
- 用 `setTimeout` 自循环：
  ```js
  async function tick() {
    try { await scanPending(); } catch (e) { /* swallow */ }
    setTimeout(tick, 30000).unref();
  }
  setTimeout(tick, 30000);
  ```
- listener 启动时扫一次 pending，找 `expiresAt < now()` 的 → 立刻飞书告警"X 操作已超时取消"，不等到下一个 30s 周期。

### 1.5 traceRef 与 cron 任务的竞合

**事实**：
- audit 写入路径：`monitor-data/action-audit.jsonl`。
- `pm2-15min` 和 `pm2-5min` 是 cron 类（`autorestart: false`），跑完即退。

**风险**：
- 如果操作队列在 cron 监控脚本的"按计划预算 80%"触发后**短期内被人工加入**，"扣减到目标预算"和"人工加预算"可能在时间上紧贴，且都写 audit 同一文件。
- 单线程没有竞态，但**审计语义上**区分"系统建议 vs 人工操作"会更有用。

**建议**：
- audit schema 加 `source: 'auto' | 'manual' | 'dashboard' | 'feishu'` 字段（v1.1 已有 `source: 'feishu'` 痕迹），统一 4 个枚举值。
- 真正做"建议采纳"链路时（v5.0 suggestion → action 闭环），source 字段就是关键审计线索。

### 1.6 abi127 / 原生模块的擦边风险

**事实**：
- `CODEBUDDY.md` 强调 `better-sqlite3@12.11.1` ABI 127。
- v1.1 没有引入新原生依赖（pending-actions.json 用 fs 即可）。

**建议**：
- 落地时显式约定："pending 用 JSON 不用 SQLite"，理由：JSON 足够小（pending 通常 0-3 条），避免引入新表 schema、避免物化刷新流程调整。**但要保持 JSON 写盘的 fsync**（用 `fs.writeFileSync` 已做到），不要换成 `appendFile` 这种非原子操作。

### 1.7 环境变量集中化

**事实**：
- `monitor-utils.mjs` v3.2 已经内嵌 `.env` 加载器，集中导出 `FEISHU_CHAT_ID / ACCOUNT_ID / AI_REGIONS / CDP_PORT / FEEDBACK_PORT` 等。
- v1.1 新增的 `acknowledgeStart` / `pending scan` 需要读 `FEISHU_CHAT_ID`、`OCEANENGINE_HOME_CHAT_ID`，避免散落在 listener。

**建议**：
- 在 `monitor-utils.mjs` 再加两行导出：
  ```js
  PENDING_FILE: path.join(DATA_DIR, 'pending-actions.json'),
  AUDIT_FILE: path.join(DATA_DIR, 'action-audit.jsonl'),
  ```
- 这样 listener 和 worker 不再各自拼 `path.join(__dirname, ...)`。

### 1.8 飞书推送熔断器复用

**事实**：
- worker 的反馈走 `feishu-push-guard.mjs` 的 `pushText`（已带熔断）。

**建议**：
- v1.1 飞书回推 ACK 超时取消、操作完成、操作失败三条消息，**全部走 `pushText`**（不要直接 `lark-cli im +messages-send` 绕过熔断），避免测试时反复发送把 lark-cli 限流。

### 1.9 日志通道

**事实**：
- 部分进程用 `OEC_SILENT=1`，stdout → `monitor-data/monitor.log`。
- `action-queue-worker` / `feishu-listener` / `feedback-server` / `chrome-guard` **没有** `OEC_SILENT`。

**建议**：
- `feishu-listener` 加 `OEC_SILENT=1`，理由：pending 扫描是 30s tick + ack 回调的高频消息，单独打 `monitor.log` 比塞 `pm2-feishu-listener-out.log` 更方便排障。
- `action-queue-worker` 已经有审计 JSON 落盘，stdout 反而要保持可见——保留原样。

---

## 2. 前端展示 — 优缺点与优化空间

### 2.1 Dashboard（dashboard.html + dashboard.js）

#### 优点

1. **可读性强**：KPI grid + mid-grid + alerts 三段式布局，移动 4 断点响应式已经过测试。
2. **CSRF 双验证**（`feedback-server.mjs:73-75` sanitize + escHtml）保证入参安全。
3. **/api/snapshots /api/campaigns /api/alerts** 三套 API 已经给 dashboard 提供主视图，15s 轮询节奏合理。

#### 缺位

1. **没有"操作执行"面板** —— 这是 v1.1 上线后**最大的展示缺口**。
   - 现在 dashboard 是只读视图：看快照、看告警、看排班、看趋势。
   - "操作面板"应包含：
     - **当前队列**（队首 + 队尾 traceRef + 待预计时间）
     - **pending 待确认**（planName + 操作 + 确认按钮 → 调 `/api/actions`）
     - **最近审计**（traceRef + 操作 + 成功/失败 + 重试次数 + 一键查看完整链路）

2. **traceRef 在 dashboard 端没有入口** —— 用户收到飞书"已入队 #3 · traceRef: a1b2c3..."但无从查。

   **优化**：加 `/api/audit/:traceRef` 接口，从 `action-audit.jsonl` 直接 grep 那一行返回。dashboard "最近审计" 行点击 → 弹一个 modal（用 Alpine 不需要新框架）。

3. **重复指令确认的 Dashboard 形态不明**。
   - v1.1 飞书端文案是"回复'执行' / '拒绝'"。
   - dashboard 走 HTTP 调 `/api/actions` 直接入队，**要不要也做二次确认**？
   - 建议：`/api/actions` 增加 query 参数 `confirmToken`，要求先调 `/api/actions/preview` 拿到 token，再调 `/api/actions/execute` 完成确认。两次请求之间 timeout 3min。

#### 优化空间

| 优化点 | 价值 | 工作量 |
|--------|------|--------|
| 加"操作执行"页面（队列/pending/审计 tab） | ★★★ 解锁 dashboard 主操作闭环 | 中 |
| `/api/audit/:traceRef` 接口 | ★★ 让 traceRef 可点击 | 低 |
| 批量操作（多选计划 → 一次暂停） | ★ 提高效率但放大误操作风险 | 中（带二次确认） |
| 一键回滚（基于 audit beforeValue 生成反向指令） | ★★ 救火场景最有用 | 中 |
| 操作历史时间轴（按 planName 归档） | ★ 复盘友好 | 低 |
| 移动端 traceRef 折叠展示（UUID 太长） | ★ 微改进 | 低 |

### 2.2 飞书卡片（CARD-FORMAT.md）

#### 优点

1. v3 已经实现的 5 分钟/15 分钟/日报/AI 区域汇总卡片体系成熟，CARD-FORMAT.md 规范约束强。
2. 已有操作反馈的 [是] / [否] 按钮（卡片交互 callback）—— 这是 v5.0 闭环的范例。

#### 缺位

1. **重复指令 ACK 卡片没设计**。
   - v1.1 当前只是文本消息："回复'执行'确认 · 回复'拒绝'取消"。
   - 建议改为**带按钮的卡片**：
     - 主标题：`🟡「女装-春装上新」当日已暂停 2 次（最近 14:22）`
     - 副标题：`确认再次暂停？`
     - 按钮：`[✅ 确认暂停]` `[❌ 取消]`
   - 这与 v5.0 建议反馈的 [是] / [否] **完全同构**，可直接复用 callback 路由 → POST /api/actions/confirm。

2. **CARD-FORMAT.md 没有 traceRef 字段约定**。
   - 当前 5min/15min 卡片不带 traceRef（数据是计划 ID）。
   - 操作类消息必须带 traceRef 否则无法回溯。建议在 CARD-FORMAT.md 加一节"操作回执卡 v3.x 规范"。

3. **失败回执消息缺少 `attempts` 字段**。
   - 当前 worker 失败时飞书回推只说"原因: result.err"，用户不知道重试 3 次全失败了还是 1 次快速失败。
   - 建议卡片模板加上重试次数 + 失败耗时。

#### 优化空间

| 优化点 | 价值 | 工作量 |
|--------|------|--------|
| 操作回执卡片加入 traceRef + attempts 显示 | ★★★ 可观测性 | 低 |
| ACK 卡片化（带按钮替代文本匹配） | ★★★ 用户体验 | 中（卡片 callback 实现） |
| 操作回执卡片加入"查看详情"链接 → http://127.0.0.1:8899/audit?ref=xxx | ★★ 联动 dashboard | 低 |
| 飞书消息里的 JSON 卡片 SDK 版本钉死 | ★ 防止 lark-cli 升级格式变化 | 低 |

### 2.3 跨端一致性

| 维度 | 飞书端 | Dashboard | 改进建议 |
|------|--------|-----------|----------|
| 重复确认 | 文本回复"执行/拒绝" | （无） | 统一为按钮 + 双通道 |
| 看到队列 | 仅 #N 编号 | 完整列表 | Dashboard 加全量 |
| 看到 traceRef | 文本中夹一长串 UUID | 无 | Dashboard 自动截断 + 详情 modal |
| 操作失败 | 简短原因 | （无） | Dashboard 错误详情 + traceRef + retries |

---

## 3. 架构 — 优缺点与优化空间

### 3.1 优点（值得坚持的设计点）

1. **traceRef 贯通全链路** —— 入队/执行/审计/回推一条龙，`jq 'select(.traceRef == "xxx")'` 一句话回查。这是 v1.0→v1.1 最值得保留的部分。
2. **执行层自检替代独立校验层** —— 不强行拆，避免双校验打架，且尊重 `verifyPlanStateWithRetry` 已在 `finally` 里的事实。
3. **API 优先 + CDP 降级** —— 与 chrome-guard 守护体系无缝衔接，符合"依赖平台越稳定越好"。
4. **暂不引入学习期保护/接口风控** —— "代码已走得通保留，不硬拆不新增空概念"原则的实例化。

### 3.2 缺点 / 风险 / 优化点（按优先级）

#### P0 — 必须改

**P0-1 同上：审计双写（事实）**
- `cdp-action.mjs:420/535/657` 三处 `writeAuditLog`，worker 还有自己的 `writeAudit`。同一 action 生命周期可能被劈到 2 个文件。
- v1.1 已经写"worker 作为唯一审计写入点"——但落地时要小心：
  - cdp-action 现有 `writeAuditLog` 函数（line 83）写的是**不同的字段**（cdp-action 是它自己的 traceback，worker 是 lifecycle）。
  - **必须先 grep `action-audit.jsonl`，确认两个写入点的字段不重复**，否则归一会丢数据。
  - 落地步骤建议：① 备份 audit.jsonl 到 `monitor-data/.archive/action-audit-2026-07-27.jsonl`；② 注释掉 cdp-action 的 3 处 `writeAuditLog` 调用；③ worker 的 `writeAudit` 加上 traceRef、attempt、method、cdpError 全部字段；④ 跑 3 天对比归档。

**P0-2 重复指令确认的超时与重试**

- 当前 v1.1 的 3 分钟 ACK 超时，是个不错的默认值，但**没有"过期取消 → 飞书告警"的完整闭环示例**。
- 落地要让用户感知到"我刚才说的那条超时被取消了"—— 这条飞书消息**必须有**，否则用户会以为还在等。

#### P1 — 应该改

**P1-1 pending 表介质选择**

- v1.1 用 JSON，但项目已有 `monitor-data/oceanengine.db`（SQLite WAL 模式 + 物化视图）。
- 当前设计的 pending 通常 0-3 条，JSON 足够。但**好处是 SQL 可以查**，例如查询"近 7 天所有用户的 ACK 接受率"—— 这需要 SQL。
- 建议：如果 audit 已经在 SQLite（v5.0 已经做了），pending 也搬进 SQLite 新建表 `pending_actions (temp_id, action_json, chat_id, created_at, expires_at, status, is_duplicate)`，并复用物化视图统计。**但这一步不是必须的，可以 P2 跟进**。

**P1-2 live-watcher 联动 + 命令拦截（2026-07-27 主人决策：暂缓，待智能提醒项目一并设计）**

- **业务澄清**：直播中暂停高成本计划是**常规救火动作**（流量贵 + 计划成本高 = 必须立刻停），不是误操作——"直播中"不能作为拦截条件。
- **决策**：本次 v1.1 不做 `precheckAction` 接入 `live-watcher`，也不上 `risk-classifier.mjs` 的"学习期/主力消耗/CPA 突降"分级拦截骨架。
- **拦截规则留到"智能提醒"项目**：届时再讨论"主力计划"定义（top 5 vs top 10）、"学习期"边界（7 天 vs 14 天）、与 v3.0 建议系统的耦合方式。
- **当前 v1.1 已有兜底**：当日重复指令二次确认机制已覆盖"单日多次调整同计划"场景——这正是主人强调"暂时只做同一计划单日多次调整的人工确认"的边界。
- **本条 P1 移除**：相关 worker / feedback-server / live-watcher 改造任务不列入 v1.1 sprint。

**P1-3 traceRef 在失败路径的传递**

- worker 失败时写 audit，但 audit 里 `result.ok: false` 没有 traceRef 是否到达执行层的标记。
- 如果用户在队列停留 30 分钟突然开 traceRef 查，会发现 audit 里根本没有那行（因为 ack 超时被取消）。
- 建议：ack 超时取消的 pending 也**写一条 audit `result: 'cancelled_by_timeout'`**，让审计可追到。

**P1-4 飞书 ACK 按钮替代文本匹配（卡片交互升级）**

- 现在文本"执行/拒绝"极易误识别（用户在群里聊"执行 xxx"会被命中）。
- 用卡片按钮 + token 校验替代文本匹配，**误操作率大幅降低**。
- 这个改动对前/后端都有，但 worker 逻辑不变——只把"二次确认"的触发从文本匹配换成按钮 callback。

#### P2 — 按需改

**P2-1 接口风险管控**——v1.1 明确暂不考虑。建议至少在 worker 出口加一个 `console.log` 标记 metric，方便未来量化。

**P2-2 多维表格入口**——按需补充，优先级低。

**P2-3 容器化锁升级**——按需。

**P2-4 自动回滚**——v1.1 明确不做。但建议加一个 dashboard "一键回滚"按钮（手动但工具化），基于 audit.beforeValue 生成反向操作—— **回滚本身不自动化，只是触发界面友好化**。

**P2-5 before/after 自动从 API 回读**

- v1.1 文档 `afterValue: null, // 执行后可从 API 回读` —— 已经预留空间。
- 建议落地时**真正实现**这一步：从 `getProjects()` 读回 `status` 和 `budget`，填到 audit 的 afterValue。这让回滚、统计、未来 ML 反事实分析都有原料。

---

## 4. 优先级行动清单

| 序号 | 任务 | 优先级 | 涉及文件 | 备注 |
|:---:|------|:---:|------|------|
| 1 | 备份审计文件 → 注释 cdp-action 三处 writeAuditLog | P0 | `cdp-action.mjs` | 先验证无字段冲突再注释 |
| 2 | worker 审计写入字段扩展（traceRef/attempts/method） | P0 | `action-queue-worker.mjs` | 与 v1.1 5.3 文档一致 |
| 3 | 引入 pending-actions.json，放 monitor-data/，走 monitor-utils 统一 | P0 | 新增 `pending-actions.mjs` + `monitor-utils.mjs` | 路径约定明确 |
| 4 | acknowledgeStart 实现重复检测 + 二次确认 | P0 | `feishu-listener.mjs` | v1.1 3.1 已设计 |
| 5 | 30s pending 扫描 + 启动扫描补跑 | P0 | `feishu-listener.mjs` | setTimeout 自循环 |
| 6 | ACK 超时取消飞书回执（含 traceRef） | P0 | `feishu-listener.mjs` | 用户感知 |
| 7 | ECOSYSTEM 注释：worker instances=1 单实例约束 | P1 | `ecosystem.config.cjs` | 防 ad-hoc 启动 |
| 8 | 加 CDP 熔断：HTTP API 失败 + Chrome 不健康 → 整体拒绝 | P1 | `action-queue-worker.mjs` + `monitor-utils.mjs` | 共享 chrome-guard 的 ping |
| 9 | ~~precheckAction 接入 live-watcher 直播状态~~ | ~~P1~~ 暂缓 | — | 主人决策：等智能提醒项目一起做规则（2026-07-27） |
| 10 | Dashboard "操作执行"面板（队列 + pending + 最近审计） | P1 | `dashboard.html` + `dashboard.js` + `feedback-server.mjs` | 解锁 dashboard 主闭环 |
| 11 | `/api/audit/:traceRef` 接口 | P1 | `feedback-server.mjs` | traceRef 可点 |
| 12 | CARD-FORMAT.md 增加 "操作回执卡 v3.x" 章节 | P1 | `CARD-FORMAT.md` | traceRef/attempts 上卡片 |
| 13 | ACK 卡片化（按钮替代文本匹配） | P1 | `feishu-listener.mjs` | 防误识别 |
| 14 | audit schema `source` 枚举统一（auto/manual/dashboard/feishu） | P1 | `action-queue-worker.mjs` | 区分人机 |
| 15 | afterValue 从 API 回读（pause→status、budget→budget） | P2 | `action-queue-worker.mjs` + `oceanengine-api-client.mjs` | 留好钩子 |
| 16 | Dashboard "一键回滚"按钮（手动但工具化） | P2 | dashboard + feedback-server | 不自动化回滚 |
| 17 | pending 搬 SQLite + 物化视图统计 ACK 接受率 | P2 | `db/` + `monitor-utils.mjs` | 不是必须 |
| 18 | 学习期保护 + 多账户接口风险 | 按需 | — | v1.1 明确暂缓，标 follow-up |

---

## 5. 评审之外的小发现

1. **`monitor-daemon.mjs` 不在 `ecosystem.config.cjs` 里** —— 工作记忆里写"v2 守护程序健康检查"，但当前 PM2 配置 13 个进程中没有 daemon 名字。可能起在别处（系统级 cron / Windows 任务计划），建议确认它的存活位置并同步到 README。

2. **`_check_daily.mjs` / `_check_lines.mjs` 等临时探针文件残留**（目录里看到多个 `_*` 调试脚本）—— 建议归档到 `archive/debug-scripts/` 或 `.trash/`，避免新人 grep 时被误导。

3. **`feishu-listener.mjs.bak-20260727` 备份文件存在** —— 评审与同事协作时不要读 `.bak` 文件。但应该纳入 `.gitignore` 或定期清理。

4. **CDP 客户端统一基础设施已具备** —— 记忆中 `cdp-client.mjs` 已是统一封装（3 次指数退避 / 30s 心跳）。v1.1 落地时要让 `tryCdpAction` **走这个模块**，不要 worker 又自己重写一份 ws 连接。

5. **SQLite 物化视图收益没在审计维度复用** —— v5.0 已把 snapshots/alerts/feedback 物化，但 action-audit 还是 jsonl。如果未来要从 audit 查"近 30 天各计划的暂停次数"等指标，建议同步把 audit 搬 SQLite（与 P2 的 pending 合并考虑，一起做）。

---

## 6. 评审小结

**架构走向（v1.1）**：正确。继续保持"代码已走得通保留"的原则。

**最大缺口**：
1. Dashboard 没有操作执行面板（用户只能在飞书端操作，dashboard 缺手臂）
2. cdp-action 与 worker 的审计归一执行前要先核字段不丢
3. CDP 降级没有第三层 fail-safe（Chrome 也挂了会全断）

**最高 ROI 改造**：
- P0 全部是基础设施类，改动可控，风险集中在"字段兼容性"——做好备份 + 对比再落地。
- P1 的"操作执行面板"和"ACK 卡片化"是用户感知最强的两块，但工作量中等，可作为后续冲刺的优先级主题。

**本机注意**：一切围绕"路径约定集中化（monitor-utils.mjs）"和"Windows 文件锁的边界标注（PM2 注释）"，这两个做完就不会再有新坑。
