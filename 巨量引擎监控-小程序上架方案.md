# 巨量引擎监控 · 数据库 + 小程序上架方案

> 2026-06-25 | 当前形态：本地脚本 + JSON + 飞书推送
> 目标形态：云端数据库 + 微信小程序看板（限定租户·自用）

---

## 一、现状本质

| 维度 | 现在 | 上架需要 |
|---|---|---|
| 数据源 | 逆向内部 API + 浏览器 cookie 抓取（CDP 提取，2h 过期，依赖 Chrome 在线） | 官方开放 OAuth 或保留 cookie |
| 存储 | monitor-data/*.json 散文件（每 15min 快照） | 关系型数据库 |
| 计算 | Windows 任务计划 cron 触发 | 云端常驻 worker |
| 服务 | localhost:8899 本地 HTTP | 公网 HTTPS 后端 |
| 鉴权 | 无（单用户） | 多租户：小程序用户 ↔ 广告主 |
| 输出 | 飞书卡片 + 本地 HTML | 小程序 UI + 订阅消息 |

---

## 二、目标架构（三层分离）

```
本机采集层              云端服务层                 小程序
┌───────────────┐ 推送增量 ┌───────────────┐ HTTPS ┌──────────┐
│ monitor-v3    │────────▶│ Postgres      │◀─────▶│ 看板/告警 │
│ cookie抓取    │         │ 后端 API      │       │ 建议/操作 │
│ + 同步agent   │◀────────│ + 指令队列     │       └──────────┘
└───────────────┘ 指令回传 └───────────────┘
   (WS长连)
```

- **采集层留本机**：cookie 抓取依赖 Chrome 登录态 + 9222 端口，必须本机跑。
- **服务层上云**：小程序前端只能连 HTTPS + 备案域名的后端。
- **指令回流**：小程序点「暂停计划」→ 云端入队 → 本机 WS 拉取执行 → 回传结果。

---

## 三、数据库设计（从现有 JSON 迁移）

现有快照结构：
- `daily-YYYY-MM-DD.json` = 账户级时序快照（19 条/天，~36 汇总字段）
- `2026-06-2xT...json` = 单次采集明细（active/allSpending 计划数组 + summary + alerts）

迁移为 6 张表：

| 表 | 内容 | 替代 |
|---|---|---|
| advertisers | 广告主账户 + token | .oec-cookies.json |
| snapshots | 账户级时序快照 | daily-*.json |
| campaign_metrics | 计划级时序（16 指标） | active/allSpending 数组 |
| alerts | 告警 | alerts 数组 |
| suggestions | 建议反馈 | suggestion-history.json |
| push_logs | 推送记录 | — |

体量：15min × 16h × ~10 计划 ≈ 640 行/天，Postgres 轻松。

---

## 四、分阶段路线

| 阶段 | 内容 | 复用资产 |
|---|---|---|
| 1 | 建库 + 历史导入 | 全部历史 JSON |
| 2 | 采集层加同步 agent（JSON→DB） | monitor-v3、api-client 字段映射 |
| 3 | 后端 API（feedback-server 重构） | server 框架、报表逻辑 |
| 4 | 小程序前端（HTML→组件） | 卡片结构、分析维度 |
| 5 | 部署：云服务器 + 域名 + HTTPS + ICP备案 | — |

可直接搬的：analyzeData 分析逻辑、告警规则、pacing 计算、趋势检测（纯 JS，迁到后端 worker）。

---

## 五、卡点（按严重程度）

### 🔴 卡点1 · 小程序必须 HTTPS + 备案域名
微信小程序正式版/体验版强制后端走 https + 备案域名，不接受 IP、不接受自签证书。
- 代价：域名 + ICP备案（企业主体，1-2 周）+ 云服务器 + SSL证书
- 规避：开发期用「不校验域名」+ 内网穿透临时调通；稳定用必须备案
- **替代路线**：若只自用，做移动端 H5 + 飞书内嵌网页，零备案，复用飞书身份

### 🔴 卡点2 · cookie 抓取与云端架构冲突
cookie 方案要求 Chrome 持续在线、2h 过期需本机刷新：
- 采集永远只能在本机跑 → 电脑关机数据就断
- 云端只当存储 + API，离开本机是空壳
- 小程序操作指令依赖本机 WS 在线 → 出门在外电脑没开，指令卡队列
- 彻底解决须切官方开放 API（oceanengine-oauth/ 已有原型），长期必修

### 🟡 卡点3 · 双写同步
monitor-v3 仍写 JSON，又要写 DB → 需明确以谁为准，JSON 退成备份或废弃

### 🟡 卡点4 · feedback-server 安全层
现 localhost:8899 无鉴权、靠飞书回调。迁云要加用户鉴权、多租户隔离、公网安全，逻辑可复用但安全层重做

### 🟢 卡点5 · 飞书推送去留
上小程序后与订阅消息重叠，需定策略（飞书做告警 / 小程序做看板）

### 🟢 卡点6 · 指令闭环改造
现 feishu-listener 轮询群消息执行 CDP。迁小程序后指令来源变「小程序→云端队列→本机 WS」，解析逻辑可复用但触发入口要改

---

## 六、建议方向

- **若自用、尽快见效**：跳过备案，走「移动端 H5 + 飞书内嵌」，阶段 1-3 照做，前端用 H5 不做小程序，一周内跑通。
- **若确有上架/多用户打算**：备案 + 切官方 OAuth 绕不开，接受 1-2 周基建周期。
