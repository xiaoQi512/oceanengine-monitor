# 直播状态巡检协议规范

> 日期：2026-07-26
> 状态：设计阶段，待评审
> 用途：定义直播间状态巡检的完整状态机、多轮验证流程、边界规则与日终触发逻辑
> 关系：本文档替代《多租户架构设计_线索直播广告监控.md》第八节 8.3，作为状态联动的精确执行规范

---

## 一、设计目标

### 1.1 现状问题

当前系统存在三套互相割裂的直播状态判断机制：

| 机制 | 位置 | 行为 | 问题 |
|------|------|------|------|
| live-watcher.mjs | 60s 轮询 | 探测开播/下播写入 live-state.json | 孤儿状态，无人读取，状态变迁无消费者 |
| 5min 速报 | cron 触发 | 只认排班时间窗口，不查实际开播 | 排班时段内未开播也空跑采集推送 |
| 15min 监控 | cron 触发 | 每次自己现查开播状态 | 不复用 live-watcher 结果，重复查询 |

三套机制各干各的：live-watcher 抓了状态没人用，5min 只认排班不管实际，15min 重复查询不复用。导致：排班时段未开播时 5min 空跑采集推送垃圾数据；拖堂/跨夜直播时日终任务在排班结束时刻触发而非实际下播后触发，日报数据不完整。

### 1.2 设计目标

建立单一状态源（状态管理器），所有巡检（5min/15min/日终）都以它为前置门控：

1. **排班表与实际开播互验**：两个标准交叉验证，任一偏离都触发对应行为
2. **状态驱动巡检频率**：未开播低频、开播高频，避免空跑
3. **动态扩展排班时间**：拖堂/跨夜时继续监控，直到真正下播才触发日终
4. **班次粒度判断**：按当班班次（含当班主播）判断，而非整天一个窗口

---

## 二、两个标准

### 标准 A：排班表（计划）

来源：`monitor-data/shifts-YYYY-MM-DD.json` 的 `shifts[]` 数组。

结构示例（一天 8 个班次）：

```json
{
  "shifts": [
    { "label": "06:30-08:30", "hours": [6,7,8], "anchorName": "张萌" },
    { "label": "08:30-10:30", "hours": [8,9,10], "anchorName": "芝芝" },
    { "label": "14:30-16:30", "hours": [14,15,16], "anchorName": "李咪" },
    { "label": "20:30-23:30", "hours": [20,21,22,23], "anchorName": "三水" }
  ]
}
```

每个班次含起止时间和当班主播。判断当前是否在排班时段，需遍历 `shifts[]` 找出当前时间落在哪个班次的 `label` 区间内，并取出当班主播。

### 标准 B：实际开播状态（现实）

来源：`getOnlineRoomList(client)` + `getLiveRoomStatus(client, roomId)`。

返回 `isLive: bool` 和 `roomTitle: string`。查询失败时返回 `null`（保持上一状态，不误判）。

### 交叉验证逻辑

两个标准独立判断，组合成四状态：

```
标准A(排班)  标准B(开播)  ->  状态
  是           是          ->  LIVE_MONITORING（正常监控）
  是           否          ->  WAITING（等开播）
  否           是          ->  UNSCHEDULED_LIVE（异常开播告警）
  否           否          ->  SILENT（静默）
```
---

## 三、多轮验证流程

### 轮 1：巡检入口查排班（标准 A）

每次巡检启动时，先读 `shifts-YYYY-MM-DD.json`，判断当前时间是否落在某个班次的 `label` 区间内：

```
getCurrentShift(now) -> {
  inShift: bool,
  currentShift: { label, anchorName } | null,
  nextShiftStart: "HH:MM" | null   // 下一个班次开始时间
}
```

遍历 `shifts[]`，用班次 label 的起止时间（如 14:30-16:30）与当前时间比较。取到当班主播名。

**关键修正：** 现有 `getTodayShiftWindow()` 只取首尾拼成 06:30-23:30 整天窗口，丢了中间班次结构。本方案直接遍历 `shifts[]` 数组，按班次粒度判断。

### 轮 2：对应时间查开播状态（标准 B）

在排班时段内，查直播间实际开播状态，与排班互验：

```
isLive = await getLiveStatus(client)
组合 inShift + isLive -> 进入四状态之一
```

这一步决定进入 WAITING（排班了没开）还是 LIVE_MONITORING（排班了也开了）。

### 轮 3：汇报内确认（持续互验）

进入 LIVE_MONITORING 后，每次 5min/15min 数据汇报都附带一次状态确认：

```
汇报前：再查一次 isLive
  在线 -> 正常汇报，附直播状态
  下播 -> 状态回退到 WAITING，本次汇报标注"已下播"
  排班已结束但仍在线 -> 转入 UNSCHEDULED_LIVE，继续汇报但触发告警
```

### 轮 4：非排班时段反向巡检

SILENT 状态下保持低频巡检（30min），反向发现异常开播：

```
非排班时段巡检：
  isLive == true -> 立即推告警："直播间开播了，请注意广告投放计划调整"
  isLive == false -> 继续静默
```

这是标准 B 反向验证标准 A：排班表说不开播，但实际开播了，说明排班错漏或主播私自开播，需提醒运营。

---

## 四、四状态定义与转移

### 4.1 状态定义

| 状态 | 标准A(排班) | 标准B(开播) | 含义 |
|------|------------|------------|------|
| SILENT | 否 | 否 | 非排班且未开播，静默低频巡检 |
| WAITING | 是 | 否 | 排班时段但未开播，等待开播 |
| LIVE_MONITORING | 是 | 是 | 排班时段且已开播，正常监控汇报 |
| UNSCHEDULED_LIVE | 否 | 是 | 非排班时段但已开播，异常告警 |

### 4.2 状态转移表

| 当前状态 | 条件 | 目标状态 | 行为 |
|----------|------|----------|------|
| SILENT | 进入排班时段，未开播 | WAITING | 开始等待，5min 巡检 |
| SILENT | 非排班，发现开播 | UNSCHEDULED_LIVE | 推告警卡片 |
| SILENT | 非排班，未开播 | SILENT | 30min 后再巡检 |
| WAITING | 排班时段内查到开播 | LIVE_MONITORING | 开始正常监控汇报 |
| WAITING | 排班时段内仍未开播 | WAITING | 5min 后再查，每轮推送"未开播"告警 |
| WAITING | 排班结束，仍未开播 | SILENT | 排班时段过了，回到静默 |
| LIVE_MONITORING | 汇报中确认仍在线 | LIVE_MONITORING | 持续 5min/15min 汇报 |
| LIVE_MONITORING | 汇报中发现已下播 | WAITING | 回到等待（可能换班重开） |
| LIVE_MONITORING | 排班结束但仍在线（拖堂） | UNSCHEDULED_LIVE | 继续每 5min 监控推送，动态扩展排班时间 |
| UNSCHEDULED_LIVE | 进入下一排班时段，仍在线 | LIVE_MONITORING | 恢复正常监控（主播可能已接上） |
| UNSCHEDULED_LIVE | 检测到已下播 | SILENT | 下播后 4 分钟触发日终任务（日报+汇总） |

### 4.3 巡检频率

| 状态 | 巡检间隔 | 采集 | 推送 |
|------|---------|------|------|
| SILENT | 30min | 不采集 | 不推送（仅反向探活） |
| WAITING | 5min | 不采集 | 推"未开播"告警（含当班主播） |
| LIVE_MONITORING | 5min 速报 + 15min 详细 | 正常采集 | 正常汇报卡片 |
| UNSCHEDULED_LIVE | 5min | 正常采集 | 继续监控推送 + 异常开播告警 |

---

## 五、班次粒度判断

### 5.1 当前班次查询

```
function getCurrentShift(now):
  shifts = loadShifts(today)
  for shift in shifts:
    [startH, startM, endH, endM] = parse(shift.label)  // "14:30-16:30"
    nowMin = now.hour * 60 + now.minute
    startMin = startH * 60 + startM
    endMin = endH * 60 + endM
    if nowMin >= startMin and nowMin < endMin:
      return { inShift: true, currentShift: shift, nextShiftStart: null }
  // 不在任何班次内，找下一个班次
  nextShift = findFirst(shifts where shift.startTime > now)
  return { inShift: false, currentShift: null, nextShiftStart: nextShift?.label?.start }
```

### 5.2 班次交接处理

班次交接时刻（如 08:30，张萌班结束、芝芝班开始）：

- 上一班下播、下一班未开 -> WAITING，当班主播更新为下一班的 anchorName
- 上一班没下播、下一班主播接上 -> 仍 LIVE_MONITORING，主播字段更新为下一班 anchorName
- 上一班下播、下一班也还没开 -> WAITING，等下一班开播

班次粒度让告警和汇报能带上"当前应值班主播"，异常开播时运营能判断是排班错漏还是主播提前开了。
---

## 六、边界规则

### 6.1 迟迟不开播（WAITING 状态持续）

排班时段已到但直播间迟迟未开播时的处理：

- **巡检频率**：保持每 5 分钟一次巡检
- **每次告警**：每 5 分钟推送一条告警，内容包含当场排期主播姓名和"还未开播"状态
- **告警文案**：`排班时段已到，当场排期主播{anchorName}，还未开播（{已等待N分钟}）`
- **持续条件**：只要仍在排班时段内且未开播，持续每 5 分钟告警，不静默
- **终止**：查到开播 -> 转 LIVE_MONITORING；排班时段结束仍未开播 -> 转 SILENT

### 6.2 拖堂（排班结束仍在播）

排班最后一场结束时间已过，但直播间仍在播时的处理：

- **不立即触发日终**：排班结束时刻不触发日报/汇总（这是与现有逻辑的关键区别）
- **动态扩展排班时间**：将实际直播结束时间视为当日排班的真实结束时间
- **继续监控推送**：转入 UNSCHEDULED_LIVE 状态，继续每 5 分钟监控推送，采集和汇报照常
- **状态标注**：汇报卡片标注"排班已结束，直播间仍在播（拖堂）"
- **下播检测**：每 5 分钟巡检中检测是否已下播
- **下播后触发日终**：检测到下播后，等待 4 分钟（广告后台数据延迟），触发：
  - 监控日报（oceanengine-daily-report-scheduler.mjs）
  - 换班群日汇总（oceanengine-daily-summary.mjs）
  - AI 区域号汇总（ai-regions-http.mjs）
  - 次日排班同步（sync-tomorrow-shifts.mjs）

```
时间线示例（拖堂）：
  23:30  排班结束，仍在播 -> 转 UNSCHEDULED_LIVE，继续 5min 监控
  23:35  仍在播，推送汇报（标注拖堂）
  ...
  00:15  检测到已下播 -> 等 4 分钟
  00:19  触发日报 + 日汇总 + AI区域号 + 次日排班同步
```

### 6.3 跨夜直播

直播跨越午夜 00:00 的处理：

- **与拖堂一致**：动态扩展排班时间，继续每 5 分钟监控推送，不因跨午夜而中断
- **日期归属**：跨夜直播的数据归属开播当日（非下播次日），日报和汇总仍按开播日期生成
- **日终触发**：检测到下播后 4 分钟触发，时间戳可能在次日，但日报文件名和汇总标记用开播日期
- **次日排班衔接**：次日排班的 sync-tomorrow 任务在日终触发链中执行，确保次日班次缓存就绪

```
时间线示例（跨夜）：
  07/26 20:30  开播（当日最后一场）
  07/26 23:30  排班结束，仍在播 -> UNSCHEDULED_LIVE
  07/27 00:30  仍在播，继续 5min 监控（日期标记仍为 07/26）
  07/27 01:15  检测到下播 -> 等 4 分钟
  07/27 01:19  触发 07/26 的日报 + 日汇总
               + sync-tomorrow（同步 07/27 排班）
```

### 6.4 状态查询失败

`getOnlineRoomList` / `getLiveRoomStatus` 调用失败时的处理：

- **单次失败**：在汇报内容中注明"未检测到直播间开播情况"，不误判为下播，保持上一状态
- **汇报继续**：数据采集和汇报照常进行，仅直播状态字段标注为"未知"
- **连续失败计数**：每次查询失败累加计数器，查询成功时清零
- **连续三次失败**：推一条告警提醒人工排查
- **告警文案**：`连续 3 次未检测到直播间开播情况，请人工排查（直播间状态查询异常）`
- **告警频率**：触发一次后不重复，直到查询恢复或人工确认

```
计数器逻辑：
  查询成功(isLive=true)   -> counter=0, 状态按实际值
  查询成功(isLive=false)  -> counter=0, 状态按实际值
  查询失败(null)          -> counter++
    counter < 3           -> 汇报中标注"未检测到开播情况"
    counter == 3          -> 推人工排查告警
    counter > 3           -> 不重复推告警，继续在汇报中标注
```

---

## 七、日终触发改造

### 7.1 现状（基于排班结束）

现有 `oceanengine-shift-pusher.mjs` 的日终触发逻辑：

```
triggerEndOfDayTasks(shift):
  if shift 是排班表最后一场 and shift.label 已结束:
    触发日终任务链（sync-tomorrow 立即, daily-report 4分钟后, daily-summary 4分30秒后）
```

问题：触发条件是排班表最后一场的结束时间（`isShiftEnded` 比较班次 label 的结束时刻），不是实际下播。拖堂/跨夜时日终在 23:34 触发但直播还在进行，日报数据不完整。

### 7.2 改造（基于实际下播）

日终触发条件改为：状态机检测到 LIVE_MONITORING / UNSCHEDULED_LIVE -> SILENT 的转移（即实际下播）。

```
onStateChange(from=LIVE_MONITORING|UNSCHEDULED_LIVE, to=SILENT):
  // 实际下播，等 4 分钟数据延迟
  setTimeout(() => triggerEndOfDayTasks(), 4 * 60 * 1000)

triggerEndOfDayTasks():
  生成当日日报（oceanengine-daily-report-scheduler.mjs）
  推送换班群日汇总（oceanengine-daily-summary.mjs）
  AI 区域号汇总（ai-regions-http.mjs）
  次日排班同步（sync-tomorrow-shifts.mjs）
```

### 7.3 复用现有机制

- 4 分钟延迟：复用现有 `DATA_DELAY = 4 * 60_000`
- 任务链顺序：sync-tomorrow -> daily-report -> daily-summary -> ai-regions（复用现有顺序）
- 防重复标记：复用 `monitor-data/daily-summary-done-YYYY-MM-DD.json`
- 防重复触发：复用 `_eodTriggered` Set，按开播日期去重

**关键区别：** 触发时机从"排班最后一场结束时刻"改为"实际检测到下播后 4 分钟"。日终任务用开播日期（非下播时刻的日期）作为文件名和标记键，确保跨夜直播归属正确。

---

## 八、告警文案规范

| 场景 | 状态转移 | 告警类型 | 文案模板 |
|------|---------|---------|---------|
| 非排班开播 | SILENT -> UNSCHEDULED_LIVE | 异常开播 | `直播间开播了，请注意广告投放计划调整` |
| 排班未开播 | WAITING 持续 | 等待开播 | `排班时段已到，当场排期主播{anchorName}，还未开播（已等待{N}分钟）` |
| 拖堂 | LIVE_MONITORING -> UNSCHEDULED_LIVE | 拖堂提醒 | `排班已结束，直播间仍在播（拖堂），继续监控` |
| 查询连续失败 | counter >= 3 | 查询异常 | `连续 3 次未检测到直播间开播情况，请人工排查` |
| 正常汇报含状态 | LIVE_MONITORING | 汇报附注 | 汇报卡片底部标注 `直播状态：{在线/未检测到}` |

---

## 九、与现有代码的对应关系

| 现有模块 | 现有行为 | 本协议要求 |
|----------|---------|-----------|
| live-watcher.mjs | 60s 轮询写 live-state.json，无人读 | 改为状态管理器，作为唯一状态源，所有巡检的前置门控 |
| getTodayShiftWindow() | 取首尾拼整天窗口 06:30-23:30 | 改为 getCurrentShift()，遍历 shifts[] 按班次粒度判断 |
| 5min-check.mjs | 只认排班时间窗口，不查实际开播 | 先过状态门控：WAITING 推未开播告警，LIVE_MONITORING 才采集 |
| monitor-v3.mjs | 每次自己现查开播状态 | 改为读状态管理器的状态，不重复查询 |
| shift-pusher EOD | 排班最后一场结束触发日终 | 改为实际下播后 4 分钟触发，动态扩展排班时间 |

---

## 十、验收清单

- [ ] 状态管理器作为唯一状态源，5min/15min 巡检均以其为前置门控
- [ ] getCurrentShift() 按班次粒度判断，取到当班主播名
- [ ] 四状态转移正确（SILENT/WAITING/LIVE_MONITORING/UNSCHEDULED_LIVE）
- [ ] 非排班时段发现开播 -> 推告警"直播间开播了，请注意广告投放计划调整"
- [ ] 排班时段未开播 -> 每 5 分钟告警"当场排期主播XX，还未开播"
- [ ] 拖堂 -> 动态扩展排班时间，继续 5min 监控推送，下播后 4 分钟触发日终
- [ ] 跨夜直播 -> 数据归属开播日期，下播后 4 分钟触发日终
- [ ] 状态查询失败 -> 汇报中标注"未检测到开播情况"，连续 3 次推人工排查告警
- [ ] 日终任务链触发时机为实际下播后 4 分钟，非排班结束时刻
- [ ] 日终任务用开播日期作为文件名和防重复标记键
---

## 十一、日汇报去重修复（补充）

### 11.1 问题

`oceanengine-daily-summary.mjs` 用 `daily-summary-done-YYYY-MM-DD.json` 标记文件防重复推送：

```
第 272-273 行：
  const todayMarker = `daily-summary-done-${getLocalDate()}.json`;
  if (!OEC_FORCE && !OEC_DRY_RUN && fs.existsSync(todayMarker)) {
    log("⚠️ 日汇报今日已推送过，跳过");
    return;
  }

第 314 行（推送成功后）：
  fs.writeFileSync(todayMarker, JSON.stringify({ doneAt: ... }));
```

标记文件只判断"今天有没有推过"，不区分触发来源。白天手动触发一次会创建标记，晚上日终任务触发日汇报时检查到标记存在就被跳过，导致晚上下播后的正式日汇报被屏蔽。

### 11.2 修复方案：区分触发来源

引入 `EOD_TRIGGER=1` 环境变量，专标识日终触发（下播后 4 分钟的状态机触发）。日终触发强制推送，不受标记文件限制；手动/测试触发仍受标记保护，且可选拆不创建标记。

**改造点 1：shift-pusher.mjs 日终任务链加 EOD_TRIGGER**

```
// oceanengine-shift-pusher.mjs 第 318 行附近
const child = spawn(nodeExe, [path.join(cwd, task.script)], {
  cwd, stdio: 'ignore', detached: true,
  env: { ...process.env, OEC_SILENT: '1', EOD_TRIGGER: '1' },  // 新增 EOD_TRIGGER
});
```

**改造点 2：daily-summary.mjs marker 检查加 EOD_TRIGGER 绕过**

```
// oceanengine-daily-summary.mjs 第 24 行附近
const EOD_TRIGGER = process.env.EOD_TRIGGER === '1';

// 第 272-273 行改为
const todayMarker = path.join(DATA_DIR, `daily-summary-done-${getLocalDate()}.json`);
if (!OEC_FORCE && !EOD_TRIGGER && !OEC_DRY_RUN && fs.existsSync(todayMarker)) {
  log("⚠️ 日汇报今日已推送过，跳过");
  return;
}
```

日终触发时 `EOD_TRIGGER=1`，绕过标记检查强制推送，并在第 314 行更新标记为日终时间戳。

**改造点 3：手动测试触发的标记处理**

手动白天测试时加 `OEC_NO_MARK=1`，推送成功也不创建标记，避免污染晚上日终：

```
// oceanengine-daily-summary.mjs 第 314 行改为
if (!process.env.OEC_NO_MARK) {
  try { fs.writeFileSync(todayMarker, JSON.stringify({ doneAt: new Date().toISOString(), source: EOD_TRIGGER ? 'eod' : 'manual' })); } catch {}
}
```

标记文件内容增加 `source` 字段，记录是 eod（日终）还是 manual（手动），便于排查。

### 11.3 触发来源矩阵

| 触发来源 | 环境变量 | 标记检查 | 创建标记 | 行为 |
|---------|---------|---------|---------|------|
| 日终（下播后4分钟） | EOD_TRIGGER=1 | 绕过 | 更新（source=eod） | 固定推送，不被历史屏蔽 |
| 手动测试（白天） | OEC_NO_MARK=1 | 检查 | 不创建 | 推送但不污染日终标记 |
| 手动正式（白天） | 无 | 检查 | 创建（source=manual） | 推送并占标记（慎用） |
| OEC_FORCE 测试 | OEC_FORCE=1 | 绕过 | 创建 | 测试用，不推荐日常 |

### 11.4 保证晚上日终固定生效

核心保证：状态机检测到下播后 4 分钟触发的日终任务链，daily-summary 带 `EOD_TRIGGER=1`，强制推送不受标记文件限制。

即使白天手动触发过创建了标记，晚上日终触发仍会：
1. 绕过第 273 行的标记检查
2. 执行完整数据采集和推送
3. 覆盖标记文件为日终时间戳（source=eod）

这样晚上下播后的第一个日汇报固定生效，不会被历史推送屏蔽。

### 11.5 验收清单（补充）

- [ ] shift-pusher 日终任务链 spawn daily-summary 时传 EOD_TRIGGER=1
- [ ] daily-summary.mjs marker 检查加 EOD_TRIGGER 绕过条件
- [ ] 手动测试可加 OEC_NO_MARK=1 不创建标记
- [ ] 标记文件记录 source 字段（eod/manual）
- [ ] 白天手动触发后，晚上日终仍能正常推送日汇报
- [ ] 日终触发的标记 doneAt 时间为实际下播后 4 分钟
