# 巨量引擎监控系统整体架构规划

> **文档版本**: v1.0
> **生成日期**: 2026-06-27
> **状态**: 待评审（稍后由其他模型做深度分析）
> **前置输入**: 数据库方案设计报告 + CDP方案四方案对比评估

---

## 一、实现目标

### 1.1 核心目标
建立一套支持多平台、多账户、可长期演进的广告投放监控分析系统，覆盖"监测 → 分析 → 建议 → 执行 → 反馈"全闭环。

### 1.2 量化目标
| 目标 | 当前 | 目标 | 衡量 |
|---|---|---|---|
| 采集稳定性 | 5个区域3-4个首采 matchedRows=0 | 0次竞态失败 | playwright auto-waiting |
| 账户覆盖 | 1真人号 + 5AI区域号 | +腾讯/百度接入 | 单一接入层 |
| 数据查询 | 2184+ JSON文件线性扫描 | 秒级SQL检索 | SQLite索引+DuckDB分析 |
| 跨日分析 | 无（仅T-15/30/60min环比） | 7日趋势+异常检测 | DuckDB列式聚合 |
| 执行闭环 | 只监测不执行 | 自动调预算(可选) | optimization_actions激活 |
| 长期归档 | JSON无限增长 | 月度Parquet(5-10x压缩) | DuckDB COPY TO |

### 1.3 非目标
- 不做实时流式处理（5min采集粒度足够）
- 不做多机分布式（单机Windows+PM2即可）
- 不自建前端框架（复用 feedback-server + HTML报表）

---

## 二、前置条件与环境

### 2.1 硬件环境
- **OS**: Windows 11 (win32)
- **运行时**: Node.js 22.22.2 (managed)
- **Python**: 3.13.12 (DuckDB python绑定备用)
- **Chrome**: v149 `D:\Program Files\Google\Chrome\Application\chrome.exe`
- **CDP专用数据**: `D:\ChromeCDP\User Data\` (Profile 4)
- **CDP端口**: 9222

### 2.2 软件依赖现状
| 包 | 版本 | 状态 | 规划 |
|---|---|---|---|
| ws | 已装 | 在用 | 保留至P1结束退役 |
| better-sqlite3 | — | 未装 | P2安装 |
| @duckdb/node-api | — | 未装 | P3安装 |
| playwright-core | — | 未装 | P1安装 |
| pm2 | 7.0.1 | 在用 | 保留 |
| lark-cli | 已装 | 在用 | 保留 |

### 2.3 运行环境约束
- PM2 全家桶已托管8个进程（详见 MEMORY.md）
- 监控时段 7:00-23:00（16小时）
- 飞书推送走 lark-cli OAuth 通道
- Chrome 9222 由 chrome-guard.mjs 60秒探活守护

### 2.4 数据现状
- `monitor-data/` 目录已有 2184+ JSON 快照文件
- 当前 schema 仅支持单账户(account_id 默认值)
- 无跨日聚合能力
- 检索窗口未实现

---

## 三、架构设计

### 3.1 四层架构总览

```
┌─────────────────────────────────────────────────────────┐
│  L1 采集层  playwright-core 统一接入                     │
│      connectOverCDP → context隔离 → auto-waiting         │
│      真人号 | AI区域号5账户 | 日报 | [腾讯/百度预留]      │
└──────────────────────┬──────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│  L2 存储层  SQLite + DuckDB 双引擎                       │
│      SQLite WAL 主存储  +  DuckDB ATTACH 列式分析        │
│      snapshots | alerts | feedbacks | summaries | actions│
└──────────────────────┬──────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│  L3 分析层  DuckDB 聚合 + AI 优化引擎                    │
│      日报聚合 | 跨日趋势 | CPA异常检测 | 时段效率         │
│      Parquet 月度归档  +  AI调优建议(预留)               │
└──────────────────────┬──────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│  L4 服务层  feedback-server 扩展 (端口8899)              │
│      /query SQL面板 | /report HTML | /feedback 反馈      │
│      /api/charts/* | /api/export | /trigger 采集触发     │
└─────────────────────────────────────────────────────────┘
```

### 3.2 各层设计注释

#### L1 采集层 — playwright-core 统一接入

**设计决策**：
- **选择 playwright-core 而非 puppeteer-core**：auto-waiting 是默认行为，根治今日 ai-regions.mjs 的 matchedRows=0 竞态 bug；context 隔离天然支撑多账户
- **connectOverCDP 接管已登录Chrome**：登录态零成本复用，避免重复扫码
- **不用 stealth 插件**：连接的是用户真实Chrome（非headless），`navigator.webdriver` 默认 false；只读监控风险极低
- **加随机人类延迟 2-8s**：降低被识别为自动化的概率

**接入模式**：
```javascript
const { chromium } = require('playwright-core');
const browser = await chromium.connectOverCDP('http://127.0.0.1:9222');
const context = browser.contexts()[0];  // 或 newContext() 隔离
const page = await context.newPage();
await page.goto(url);
// auto-waiting 自动等元素就绪
const data = await page.locator('table.ovui-table').evaluate(/* ... */);
await page.close();  // 关 page 不关 browser
```

**退役清单**：
- `cdp-client.mjs`（自研WS客户端）
- `wait-utils.mjs`（手写智能等待）
- `cdp-proxy.mjs` HTTP路径（ai-regions 专用）
- `calibrate-page.mjs`（playwright 自带校准能力）

**多账户 context 策略**：
| 账户 | context 名 | profile | 备注 |
|---|---|---|---|
| 真人号(1842681352509635) | `ocean-real` | Profile 4 | 主监控 |
| AI东区 | `ocean-ai-east` | 同Chrome不同tab | 复用登录态 |
| AI西区 | `ocean-ai-west` | 同上 | |
| AI中区 | `ocean-ai-mid` | 同上 | |
| AI南区 | `ocean-ai-south` | 同上 | |
| AI北区 | `ocean-ai-north` | 同上 | |

---

#### L2 存储层 — SQLite + DuckDB 双引擎

**设计决策**：
- **SQLite 作主存储**：事务安全、WAL并发读不阻塞写、单文件零运维、better-sqlite3 同步API简单
- **DuckDB 作分析引擎**：列式向量化聚合比SQLite快100x、`ATTACH` 直接读SQLite无需搬迁
- **不做 PostgreSQL/MySQL**：单机场景过度设计，增加运维负担

**Schema 调整（相对数据库方案设计报告）**：

```sql
-- campaigns 表
CREATE TABLE campaigns (
  campaign_id    TEXT PRIMARY KEY,
  campaign_name  TEXT NOT NULL,
  account_id     TEXT NOT NULL,           -- 去掉 DEFAULT '1842681352509635'
  platform       TEXT NOT NULL DEFAULT 'oceanengine',  -- 新增：oceanengine/tencent/baidu
  status         TEXT,
  daily_budget   REAL,
  created_at     TEXT DEFAULT (datetime('now', 'localtime')),
  UNIQUE(campaign_id, account_id, platform)
);

-- campaign_snapshots 时序表
CREATE TABLE campaign_snapshots (
  id             INTEGER PRIMARY KEY AUTOINCREMENT,
  campaign_id    TEXT NOT NULL,
  account_id     TEXT NOT NULL,           -- 新增
  platform       TEXT NOT NULL,           -- 新增
  snapshot_time  TEXT NOT NULL,
  consume        REAL DEFAULT 0,
  leads          INTEGER DEFAULT 0,
  conversions    INTEGER DEFAULT 0,
  -- ...其他字段同原方案
  FOREIGN KEY (campaign_id) REFERENCES campaigns(campaign_id)
);

-- 关键索引（新增 idx_snapshots_account_time）
CREATE INDEX idx_snapshots_account_time
  ON campaign_snapshots(account_id, platform, snapshot_time);
CREATE INDEX idx_snapshots_campaign_time
  ON campaign_snapshots(campaign_id, snapshot_time);

-- optimization_actions 表（激活，原方案为预留）
CREATE TABLE optimization_actions (
  id             INTEGER PRIMARY KEY AUTOINCREMENT,
  campaign_id    TEXT NOT NULL,
  account_id     TEXT NOT NULL,
  platform       TEXT NOT NULL,
  action_type    TEXT NOT NULL,           -- pause/budget_adjust/bid_adjust
  action_payload TEXT,                    -- JSON: {new_budget: 5000, reason: '...'}
  status         TEXT DEFAULT 'proposed', -- proposed/approved/executed/rejected
  proposed_at    TEXT DEFAULT (datetime('now', 'localtime')),
  executed_at    TEXT,
  executed_by    TEXT,                    -- playwright/manual
  result         TEXT                     -- JSON: 执行结果
);
```

**数据迁移策略**：
1. 现有 JSON 双写期：playwright 采集后同时写 JSON + SQLite（1周验证）
2. 验证通过后停 JSON 写入
3. 历史 JSON 数据导入 SQLite（一次性脚本）
4. 启动 Parquet 月度归档

**文件位置**：
- `monitor-data/oceanengine.db`（SQLite 主库）
- `monitor-data/archive/YYYY-MM.parquet`（DuckDB 月度归档）

---

#### L3 分析层 — DuckDB 聚合 + AI 优化引擎

**设计决策**：
- **DuckDB ATTACH SQLite**：无需数据搬迁，直接在SQLite上跑列式聚合
- **月度Parquet归档**：5-10x压缩，DuckDB直接查询，老数据不占主库
- **AI调优建议留接口不实现**：先积累数据，后续接火山方舟模型

**核心分析能力**：
| 能力 | SQL 示例 | 触发 |
|---|---|---|
| 日报聚合 | `SELECT account_id, SUM(consume), SUM(leads) FROM snapshots WHERE date(snapshot_time)=? GROUP BY account_id` | 23:05 |
| 跨日趋势 | 7日滚动窗口 + REGR_SLOPE 线性回归 | 日报触发 |
| CPA异常检测 |STDDEV+Z-SCORE，>2σ 标记异常 | 实时 |
| 时段效率 | `EXTRACT(HOUR FROM snapshot_time)` 分组 | 周报 |
| 归档 | `COPY (SELECT * FROM snapshots WHERE month=?) TO '?.parquet'` | 每月1日 |

**DuckDB 连接模式**：
```javascript
const duckdb = require('@duckdb/node-api');
const db = await duckdb.createConnection('monitor-data/analysis.duckdb');
await db.run(`ATTACH 'monitor-data/oceanengine.db' AS ocean (READ_ONLY)`);
const result = await db.run(`
  SELECT account_id, SUM(consume), SUM(leads)
  FROM ocean.campaign_snapshots
  WHERE date(snapshot_time) = $1
  GROUP BY account_id
`, [today]);
```

---

#### L4 服务层 — feedback-server 扩展 (端口8899)

**设计决策**：
- **复用 feedback-server 而非新建服务**：PM2 已守护、技术栈一致、零运维增量
- **从"反馈收集器"升级为"数据服务层"**：承载 SQL检索 + 采集调度 + 图表API
- **不引入 Express/Koa**：保持原生 http 模块，避免依赖膨胀

**端点规划**：
| 端点 | 方法 | 状态 | 用途 |
|---|---|---|---|
| `/report` | GET | 现有 | HTML报表 |
| `/feedback` | POST | 现有 | 飞书卡片反馈收集 |
| `/query` | GET/POST | 新增 | SQL检索面板（暗色主题） |
| `/api/charts/daily` | GET | 新增 | 日维度图表JSON |
| `/api/charts/campaign` | GET | 新增 | 单计划趋势JSON |
| `/api/export` | GET | 新增 | CSV导出 |
| `/trigger` | POST | 新增 | HTTP触发playwright采集（外部调度） |
| `/health` | GET | 新增 | 健康检查 |

**安全约束**：
- 仅监听 127.0.0.1，不暴露公网
- `/query` 仅允许 SELECT，拦截DDL/DML
- `/trigger` 需 token 校验（环境变量）

---

## 四、落地路线（4阶段 · 2周）

### P1 — playwright-core 接入（1-2天）
**目标**：统一采集层，根除竞态bug

| 步骤 | 交付物 | 验证 |
|---|---|---|
| 安装 playwright-core | package.json | `npm ls playwright-core` |
| 封装 `pw-client.mjs`（统一接入层） | pw-client.mjs | 单元测试连接9222 |
| 迁移 ai-regions.mjs | 改后脚本 | DRY_RUN验证5区域 |
| 迁移 daily-report.mjs | 改后脚本 | 23:05日报正常 |
| 退役 cdp-client.mjs / wait-utils.mjs | 删除 | v3监控仍正常 |
| 迁移 v3监控 + 5min-check | 改后脚本 | 15min周期正常 |

**风险**：OVUI组件需真实鼠标事件（见MEMORY.md），playwright 的 `page.mouse` API 需验证

### P2 — SQLite 迁移（2-3天）
**目标**：结构化存储 + 检索能力

| 步骤 | 交付物 | 验证 |
|---|---|---|
| 安装 better-sqlite3 | package.json | 连接测试 |
| 建 schema（含platform字段） | oceanengine.db | 表结构检查 |
| 写 `db-writer.mjs`（双写桥） | db-writer.mjs | JSON+SQLite一致 |
| 接入所有采集脚本 | 改后脚本 | 双写1周验证 |
| 检索窗口 `/query` 上线 | feedback-server改 | SQL查询正常 |
| 历史 JSON 导入 | 一次性脚本 | 行数校验 |

**风险**：2184+ JSON 导入需保证幂等，重复导入要ON CONFLICT IGNORE

### P3 — DuckDB 分析层（3-4天）
**目标**：列式聚合 + 归档

| 步骤 | 交付物 | 验证 |
|---|---|---|
| 安装 @duckdb/node-api | package.json | ATTACH测试 |
| 日报聚合改 DuckDB | daily-report改 | 与SQLite结果一致 |
| 7日趋势 + 异常检测 | analysis.mjs | 异常计划标记 |
| Parquet 月度归档 | archive脚本 | 6月数据归档 |
| `/api/charts/*` 上线 | feedback-server改 | 图表JSON返回 |

**风险**：DuckDB Node API 较新，需验证 Windows 兼容性

### P4 — AI 闭环（按需，非阻塞）
**目标**：监测→分析→建议→执行→反馈

| 步骤 | 交付物 |
|---|---|
| CPA预测模型（火山方舟） | prediction.mjs |
| optimization_actions 激活 | playwright执行层 |
| 自动调预算（需用户确认） | /trigger + 飞书审批 |
| 腾讯/百度接入 | 新 platform 适配 |

---

## 五、关键决策矩阵

| 决策点 | 选择 | 否决项 | 核心理由 |
|---|---|---|---|
| CDP接入 | playwright-core | puppeteer/selenium/自研WS | auto-waiting根除竞态、context隔离、MS官方日更 |
| 主存储 | SQLite (better-sqlite3) | PostgreSQL/MySQL | 单机零运维、WAL并发、事务安全 |
| 分析引擎 | DuckDB | ClickHouse/Spark | 轻量单机、ATTACH直接读SQLite、列式快100x |
| 数据服务 | feedback-server扩展 | 新建Express服务 | 零新进程、技术栈一致、PM2已守护 |
| 归档格式 | Parquet | CSV/JSON | 5-10x压缩、DuckDB原生查询 |
| AI执行通道 | playwright-core操作页面 | 纯API调用 | 巨量无开放API、playwright已在采集层复用 |
| 长期归档 | 月度Parquet | 永久SQLite | 主库保持轻量、老数据冷存储 |

---

## 六、与现有系统的兼容性

### 6.1 保持不变
- PM2 ecosystem.config.cjs（8进程结构不变）
- 飞书推送 lark-cli OAuth 通道
- 飞书群分配（监控群/上架群）
- chrome-guard.mjs 60秒探活
- 监控逻辑（环比/速率/告警类型/建议反馈系统）
- OVUI组件交互规则（真实鼠标事件）
- HTML报表结构

### 6.2 平滑迁移
- JSON双写期1周，验证通过才停JSON
- cdp-client.mjs 退役前先验证 playwright 全功能
- 检索窗口上线后 JSON 文件保留30天再清理

### 6.3 退役清单
| 文件 | 退役阶段 | 替代 |
|---|---|---|
| cdp-client.mjs | P1结束 | pw-client.mjs |
| wait-utils.mjs | P1结束 | playwright auto-waiting |
| cdp-proxy.mjs HTTP路径 | P1结束 | playwright 直连 |
| calibrate-page.mjs | P1结束 | playwright 自带 |
| monitor-data/*.json（2184+） | P2结束 | oceanengine.db |
| data-consistency-check.mjs | P3评估 | DuckDB一致性查询 |

---

## 七、风险与缓解

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| playwright OVUI事件不兼容 | 中 | 高 | P1先验证分页/排序，失败则保留CDP `Input.dispatchMouseEvent` |
| DuckDB Node API Windows不稳 | 低 | 中 | P3先做ATTACH PoC，失败退回SQLite聚合 |
| SQLite WAL 锁竞争 | 低 | 低 | 5min采集间隔足够，WAL模式读不阻塞写 |
| 历史JSON导入缺字段 | 中 | 低 | 导入脚本做字段映射，缺字段填NULL |
| 飞书限流 | 低 | 中 | shouldPush()节流逻辑保留 |

---

## 八、附录

### 8.1 现有脚本清单（待迁移）
| 脚本 | CDP路径 | 迁移优先级 |
|---|---|---|
| oceanengine-ai-regions.mjs | proxy HTTP | P1-高（今日bug根因） |
| oceanengine-daily-report.mjs | proxy HTTP | P1-高 |
| oceanengine-monitor-v3.mjs | cdp-client WS | P1-中 |
| oceanengine-5min-check.mjs | cdp-client WS | P1-中 |
| oceanengine-daily-summary.mjs | proxy HTTP | P1-低 |
| oceanengine-shift-pusher.mjs | 无CDP | 不迁移 |

### 8.2 账户清单
| 平台 | 账户名 | aadvid | context名 |
|---|---|---|---|
| oceanengine | 极狐-区域福利号-直播 | 1842681352509635 | ocean-real |
| oceanengine | 极狐-AI东区 | 1842681994872135 | ocean-ai-east |
| oceanengine | 极狐-AI西区 | 1842681830951944 | ocean-ai-west |
| oceanengine | 极狐-AI中区 | 1842663909080452 | ocean-ai-mid |
| oceanengine | 极狐-AI南区 | 1842682454270468 | ocean-ai-south |
| oceanengine | 极狐-AI北区 | 1842683071403332 | ocean-ai-north |

### 8.3 相关文档
- 数据库方案设计报告.md（详细schema）
- 数据库方案设计报告.html（可视化版）
- MEMORY.md（项目工作记忆）
- ecosystem.config.cjs（PM2配置）

---

**文档结束 · 待其他模型深度分析**
