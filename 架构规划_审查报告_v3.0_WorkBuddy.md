# 架构规划 v3.0 审查报告 — WorkBuddy

> **审查版本**: v2.0（全面修订版）
> **审查日期**: 2026-06-28
> **审查对象**: `架构规划_整体方案_v3.0_控制面板与执行层.md`
> **审查方法**: 逐项对照项目源码核验 + 历史文档溯源 + 架构设计评审
> **审查范围**: 功能完整性、技术准确性、设计缺陷、遗漏风险、**现实约束核验**
> **核验源码**: 18 个核心文件 + 4 个测试文件 + 历史方案文档 3 份

---

## 0. 审查摘要

| 类别 | 数量 | 严重度 |
|---|---|---|
| 🔴 P0 事实性错误 / 阻塞性约束 | **7** | 阻塞开发，必须修正 |
| 🟡 P1 设计缺陷 | **8** | 影响可靠性，需修正后再实施 |
| 🔵 P2 优化建议 | **9** | 提升健壮性，建议采纳 |
| ⚪ P3 文档质量 | **3** | 可读性/一致性问题 |

**总体评价**: v3.0 架构方向正确（Marketing API 做执行主通道、审批分级做风控、前端交互升级），但存在 **7 个 P0 级问题**，其中最关键的两个：

1. **飞书/微信小程序均需企业资质才能上架**——这是一个现实商业约束，直接否定了 v3.0 主推的"飞书小程序"方案的可行性（除非用户有企业资质可用）
2. **完全忽视已有的 10+ 个执行层/自治层模块**——`cdp-action.mjs`（416行）已实现暂停/恢复/调预算，`feishu-listener.mjs` 已实现三阶段闭环，`autonomous-router.mjs` / `shadow-arena.mjs` / `adaptive-scheduler.mjs` 已构成完整自治体系

如果不修正直接交给其他 agent 实施，会导致：**方案选型不可行 + 已有能力重复建设 + 架构断裂**。

---

## 1. 🔴 P0 级问题（必须修正）

### P0-1: 飞书/微信小程序均需企业资质——主推方案可能根本不可行

**文档 §2.2 推荐**: "飞书原生小程序" 作为主推方案。

**现实约束**:

| 平台 | 上架要求 | 个人可用? |
|---|---|---|
| 飞书小程序 | 必须企业主体开发者账号 + 审核 | ❌ 需企业资质 |
| 微信小程序 | 必须企业主体 + HTTPS 备案域名 + ICP | ❌ 需企业资质 |
| 飞书 H5 微应用 | 需企业管理员在飞书开放平台创建应用 | ⚠️ 需企业飞书管理权限 |
| 独立 Web | 无特殊要求 | ✅ 可用 |
| PWA | 无特殊要求 | ✅ 可用 |

**项目已有的认知**: `巨量引擎监控-小程序上架方案.md`（2026-06-25）已明确指出此约束，并建议 "若自用、尽快见效：跳过备案，走移动端 H5 + 飞书内嵌网页"。

**v3.0 的问题**: §2.1 方案对比表中 "飞书小程序" 审核列标为 "需"，但未展开说明"需"什么（企业资质）。§2.2 直接推荐飞书小程序为主推，**未评估用户是否具备企业资质**。

**修正建议**:
- **在 §2.1 方案对比表新增一列** "资质要求"，明确：飞书小程序=企业资质、微信小程序=企业资质+ICP备案、飞书 H5=企业管理员权限、独立 Web/PWA=无
- **主推方案应改为** "飞书 H5 微应用"（如果用户有企业飞书管理权限）或 "独立 Web + PWA"
- **H5 从"兜底"升级为"主路径"**，小程序降为"有条件时升级"
- 保留 Taro 跨端选型，但主编译目标应为 H5 而非小程序
- §10.1 待办清单第一条应从 "注册飞书小程序开发者账号（企业主体）" 改为 "确认企业资质可用性，不可用则切 H5 方案"

---

### P0-2: 完全忽视已有的 cdp-action.mjs（416行成熟执行层）

**项目已有**:

| 文件 | 行数 | 现有能力 | 状态 |
|---|---|---|---|
| `cdp-action.mjs` | 416 | `togglePlanStatus()`（暂停/关停/恢复）、`adjustBudget()`（调预算）、搜索/翻页/弹窗确认/截图留证 | **已生产使用** |
| `pause-campaign.mjs` | 182 | CDP 暂停计划的多策略实现（`ovui-switch__thumb` 精确定位） | 实验/备用 |
| `adjust-budget.mjs` | 113 | 预算调整实验（v12 network capture 逆向 API） | 实验 |
| `action-executor.mjs` | 250 | 飞书群消息轮询 → 命令解析 → CDP 执行 | v1 原型 |
| `feishu-listener.mjs` | ~300 | 飞书群消息监听 + 三阶段闭环（确认→执行→报告）+ 8 种指令 | **已生产使用** |

**文档描述**: §1.2 代码现状核验表仅列出 `oceanengine-api-client.mjs`（"0 写函数"）、`cdp-client.mjs`（"无页面交互能力"）、`feedback-server.mjs`。完全没有提及 `cdp-action.mjs`、`feishu-listener.mjs` 等已有执行层模块。

**问题**: 文档声称 "0 写函数" 是基于只看了 `oceanengine-api-client.mjs` 的读函数。实际上 `cdp-action.mjs` 已经有写操作（通过 CDP 操作页面执行暂停/恢复/调预算），且已在 `feishu-listener.mjs` 的生产链路中使用。

**修正建议**:
- §1.2 现状表新增：
  | `cdp-action.mjs` | 416行，暂停/恢复/调预算 + 搜索/翻页/弹窗确认/截图 | 基于 raw CDP（非 Playwright）；已在 feishu-listener 中使用 |
  | `feishu-listener.mjs` | 飞书群指令监听 + 三阶段闭环 | 8种指令，已生产；依赖 cdp-action |
  | `pause-campaign.mjs` | 暂停计划多策略实现 | 备用 |
- §3 执行层设计应明确 "基于 cdp-action.mjs 扩展" 而非从零新建 `operation-executor.mjs`
- §3.3 CDP 回退代码示例应直接引用 `cdp-action.mjs` 的现有实现

---

### P0-3: CDP 回退代码示例全部基于 Playwright API，与现有架构不兼容

**文档 §3.3** 的 CDP 回退代码：

```javascript
const context = await browser.newContext({ storageState: CHROME_AUTH_STATE });
const page = await context.newPage();
await page.locator('tr[data-campaign-id="..."]').click();
```

**项目实际**（`cdp-action.mjs` + `calibrate-page.mjs` + `MEMORY.md`）:

| 维度 | 文档写法 (Playwright) | 项目实际 (raw CDP) |
|---|---|---|
| 连接方式 | `browser.newContext()` | WebSocket 直连 `ws://localhost:9222` |
| 页面操作 | `page.locator().click()` | `cdpSend('Input.dispatchMouseEvent', {...})` |
| 导航 | `page.goto()` | `cdpSend('Page.navigate', {url})` |
| OVUI 兼容 | 合成事件 | **必须用真实鼠标事件**（OVUI Vue 组件不响应合成事件） |
| 校准 | 无 | `calibratePage()` v2（3次重试+验证+多策略回退） |
| 等待 | Playwright 内置 | `wait-utils.mjs`（DOM 轮询/行数检查/值稳定检测） |

**关键**: 项目 MEMORY.md 明确记录 "OVUI Vue组件不响应合成事件"。Playwright 的 `locator.click()` 是合成事件，**在 OVUI 上会失效**。`cdp-action.mjs` 之所以用 416 行代码专门处理这些，就是因为必须用 raw CDP 的真实鼠标事件。

**修正建议**:
- §3.3 全部删除 Playwright 代码示例，改为基于 `cdp-client.mjs` + `calibrate-page.mjs` + `cdp-action.mjs` 的已有实现
- §7.3 退役清单中 "cdp-client.mjs → playwright-core 接管" 必须删除——现有 6 个监控脚本全部依赖 cdp-client.mjs
- 如确需引入 Playwright，应作为 v2 评审已识别的 "connectOverCDP 不支持 newContext" P0 问题的后续处理

---

### P0-4: "飞书 WebSocket" 概念混淆

**文档 §2.4**:
> 小程序 → WebSocket（lark WS） → feedback-server

**问题**: 飞书开放平台**不提供通用 WebSocket 推送通道**。

| 方案 | 实现 | 适用 |
|---|---|---|
| 飞书长连接 (lark-ws SDK) | 订阅事件回调（消息/审批），**不是通用 WS** | 接收飞书事件 |
| **自建 WebSocket Server** | feedback-server 升级为 WS Server（项目已用 `ws` 包） | 前端实时推送 |
| SSE (Server-Sent Events) | 单向推送，HTTP 兼容 | 数据更新推送 |
| 轮询降级 | 定时 GET `/api/snapshot` | 兜底 |

**注意**: 项目已安装 `ws` npm 包（用于 cdp-client.mjs 的 WebSocket 连接），feedback-server 升级 WS Server 的依赖已具备。

**修正建议**:
- 删除 "lark WS" 描述
- 明确为自建 WebSocket Server（基于 `ws` 包，feedback-server 扩展）
- 补充 WS 鉴权（JWT 握手验证 + 心跳保活 30s）

---

### P0-5: Marketing API 与现有 API Client 的鉴权体系共存方案缺失

**现状**:

| 维度 | 现有 (Cookie 内部 API) | 新增 (OAuth Marketing API) |
|---|---|---|
| 鉴权 | 浏览器 Cookie（CDP 提取，2h 过期） | OAuth 2.0 access_token |
| 基址 | `ad.oceanengine.com`（内部） | `ad.oceanengine.com/open_api`（开放） |
| 数据格式 | 内部 JSON 结构 | 开放 API 标准结构 |
| 计划 ID | 内部 ID（CDP 采集的 `data[0]`） | 开放 API campaign_id（**可能不同**） |
| 已有原型 | `oceanengine-api-client.mjs`（生产） | `oceanengine-oauth/` Python 脚本（原型） |

**关键缺失**: 同一计划在两个体系下的 ID 是否一致？如果不一致，执行层 `campaignId` 关联将断裂。

**额外遗漏**: `oceanengine-oauth/` 目录下已有 Python 版 OAuth 原型（`callback_server.py` + `exchange_token.py`），v3.0 未提及可复用。

**修正建议**:
- 新增 "鉴权体系共存" 小节
- 复用 `oceanengine-oauth/` 原型做 Node.js 迁移
- PoC 验证 plan ID 在两个体系下的一致性
- 补充 `refresh_token` 刷新路径（当前示例仅有 `auth_code`）

---

### P0-6: 忽视已有的自治体系（autonomous-router / shadow-arena / adaptive-scheduler）

**项目已有一套完整的自治优化体系**:

| 模块 | 功能 | v3.0 引用 |
|---|---|---|
| `autonomous-router.mjs` | 智能路由（Provider 评分/熔断/自动降级） | ❌ 未提及 |
| `shadow-arena.mjs` | 阴影竞技场（5% 流量做影子测试） | ❌ 未提及 |
| `adaptive-scheduler.mjs` | 动态调度器（基于时段/活跃度调频） | ❌ 未提及 |
| `feishu-push-guard.mjs` | 推送熔断守卫（lark-cli 包装+降级） | ❌ 未提及 |
| `data-consistency-check.mjs` | 数据一致性校验 | ❌ 未提及 |
| `oec-auto-login.mjs` | SSO 自动登录（Cookie 过期自动刷新） | ❌ 未提及 |
| `oceanengine-shift-pusher.mjs` | 换班推送（node-cron 8时段） | ❌ 未提及 |
| `overview.md` | 四层治理架构设计文档 | ❌ 未引用 |

**v3.0 §P4 "AI 闭环"** 部分提出要做 CPA 预测+自动调预算，但没有引用已有的自治体系。这些模块在 `overview.md` 中有完整的设计文档，且部分已实现。

**修正建议**:
- §1.2 现状核验新增自治模块清单
- §P4 AI 闭环应明确 "基于 autonomous-router + shadow-arena 扩展" 而非从零构建
- `overview.md` 的四层治理架构应作为 v3.0 的架构基础之一

---

### P0-7: §5 关键决策矩阵 "飞信小程序" 笔误

**文档 §5 第 1 行**: "飞信小程序"

"飞信" 是中国移动的 IM 产品，不是飞书。关键决策矩阵出现此笔误，交给其他 agent 执行会造成误解。

**修正**: "飞信小程序" → "飞书小程序"（或根据 P0-1 修正为 "H5 微应用"）

---

## 2. 🟡 P1 级问题（设计缺陷）

### P1-1: operation-executor.mjs 的端口冲突与职责不清

**文档 §7.2**: "新增 `operation-executor.mjs`（PM2 常驻，端口 8900）"

**问题**:
- 端口 8900 用途未定义（HTTP？WS？内部通信？）
- 纯消费者（拉取 optimization_actions）还是也提供 API？
- 与 feedback-server (8899) 的职责边界？
- **已有 `feishu-listener.mjs` 已在做"消费指令→调 cdp-action→回传结果"，两者关系？**

**修正建议**:
- 推荐：operation-executor 为纯消费者，不监听端口；API 通过 feedback-server 转发
- 明确与 `feishu-listener.mjs` 的关系：合并还是并行？如果并行，指令来源需去重

### P1-2: 审批流 L1 "账户第二人" 未定义

**文档 §3.4**: "L1 复核 → 用户确认 → 账户第二人飞书审批"

**问题**:
- "第二人" 是谁？当前监控系统只有单一用户（主人/爆量君）
- 飞书群审批需指定群 ID 和审批人列表
- 超时 30 分钟对直播盯盘太长（15 分钟可能错过窗口）

**修正建议**:
- L1 审批人改为可配置
- 盯盘时段（7-23）L1 超时缩至 10-15 分钟
- 增加"自动审批白名单"——历史采纳率 > 90% 的操作类型自动降级为 L0

### P1-3: lock_token 并发控制方案不完整

**文档 §3.5**: `lock_token` 字段做并发锁。

**问题**:
- 锁获取/释放算法未说明
- SQLite WAL 模式下写串行，lock_token 实现方式与 PostgreSQL 不同
- 锁超时未定义——进程崩溃后锁如何释放？

**修正建议**:
```sql
UPDATE optimization_actions 
SET lock_token = :new_token, status = 'executing'
WHERE id = :id AND lock_token IS NULL AND status = 'approved';
-- affected rows = 1 → 获锁成功
```
- 增加 `lock_expire_at` 字段，超时自动释放

### P1-4: Marketing API 限流设计缺失细节

**问题**:
- 令牌桶容量未定义
- 多 PM2 进程可能同时调 API，令牌桶需跨进程共享
- operation-executor 是否单实例未确认

**修正建议**:
- 令牌桶实现为 SQLite 表（`rate_limit_state`），跨进程安全
- 明确 PM2 `instances: 1`
- 补充：写操作 2 QPS → 令牌桶容量 5，补充速率 2/s

### P1-5: 前端框架选型需重新评估（Taro 目标变更）

**P0-1 修正后**，主推 H5 而非小程序，Taro 选型需要重新评估：

| 方案 | 适合场景 | 开发量 |
|---|---|---|
| **React + Vite（纯 H5）** | 无需小程序，纯 Web 部署 | 低（无跨端编译复杂度） |
| **Taro 3.x** | 需要同时编译小程序 + H5 | 中（跨端抽象层增加复杂度） |
| **Next.js** | 需要 SSR / 大屏可视化 | 中 |
| **Vue 3 + Vite** | 团队更熟悉 Vue | 低 |

如果确定不需要小程序，Taro 的跨端优势消失，纯 React + Vite 或 Vue 3 + Vite 更简单。

**修正建议**:
- P0-1 确认后重新评估框架选型
- 如果 H5 为主，推荐 React + Vite（或 Vue 3 + Vite），放弃 Taro
- 保留 "未来可升级小程序" 的架构预留即可

### P1-6: 错误重试策略不区分可重试/不可重试

**文档 §3.2**: "指数退避重试 3 次"

**问题**: Marketing API 错误需分类：

| 错误类型 | 示例 | 重试? |
|---|---|---|
| 限流 429 | QPS 超限 | ✅ 退避 |
| 服务端 5xx | 系统繁忙 | ✅ 退避 |
| 参数错误 400 | campaign_id 不存在 | ❌ 不重试 |
| 权限错误 403 | token 无权限 | ❌ 不重试 |
| 业务错误 | 计划已暂停 | ❌ 不重试 |

### P1-7: operation-executor 健康监控缺失

**问题**: 缺少健康检查端点、失败告警推送、执行延迟监控。

**修正建议**:
- 复用现有 `monitor-daemon.mjs` 健康检查框架（已有 daemon-health.json）
- 复用 `feishu-push-guard.mjs` 做失败告警推送
- 记录 `executed_at - approved_at` 作为执行延迟指标

### P1-8: 已有方案文档未被引用——v3.0 遗漏了前序设计

项目中已有两份与 v3.0 直接相关的设计文档，但 v3.0 完全未引用：

| 文档 | 内容 | v3.0 引用 |
|---|---|---|
| `巨量引擎监控-小程序上架方案.md` | 三层架构（本机采集→云端服务→小程序）+ 6 个卡点分析 + HTTPS/备案约束 | ❌ 未提及 |
| `oec-data-strategy-analysis.md` | HTTP API 完整迁移方案（3 个端点已逆向验证，提速 27-63 倍） | ❌ 未提及 |
| `overview.md` | 四层治理架构 + SMART 优化目标 + autonomous-router 设计 | ❌ 未提及 |

**`巨量引擎监控-小程序上架方案.md` 已明确识别的关键约束**:
- 微信小程序强制 HTTPS + 备案域名（企业主体，1-2 周）
- Cookie 抓取与云端架构的冲突
- 建议 "若自用：走移动端 H5 + 飞书内嵌网页"

**`oec-data-strategy-analysis.md` 已验证的 API 能力**:
- `POST /ad/api/promotion/projects/list` — 1-2 秒完整数据采集
- `isSophonx:1` 关键参数发现
- 与 CDP 数据的一致性验证

**修正建议**:
- v3.0 应在 §1 需求与现状中引用这两份文档
- §L1 采集层应引用 `oec-data-strategy-analysis.md` 的 API 端点清单
- §L5 交互层应引用 `巨量引擎监控-小程序上架方案.md` 的约束分析

---

## 3. 🔵 P2 级优化建议

### P2-1: 现有 calibrate-page.mjs v2 + cdp-action.mjs 可直接复用

CDP 回退路径应直接复用已有模块：

```
operation-executor → calibratePage() → cdp-action.mjs（已有）
                                        ├── togglePlanStatus(client, planName, 'pause'/'resume')
                                        ├── adjustBudget(client, planName, amount)
                                        └── 新增: adjustBid(client, planName, bid) — 待开发
```

`cdp-action.mjs` 已有的能力（416行）：
- `searchPlanInTable()` — 搜索+翻页
- `confirmPopupIfAny()` — 二次确认弹窗
- `screenshotOnError()` — 失败截图留证
- `togglePlanStatus()` — 暂停/恢复（真实鼠标事件+OVUI兼容）
- `adjustBudget()` — 调预算

### P2-2: feedback-server 应有完整 API 端点设计

| 端点 | 方法 | 用途 | 鉴权 |
|---|---|---|---|
| `/api/action/create` | POST | 创建操作请求 | JWT |
| `/api/action/list` | GET | 查询操作历史 | JWT |
| `/api/action/approve` | POST | 审批通过/驳回 | JWT |
| `/api/action/cancel` | POST | 取消操作 | JWT |
| `/api/action/status` | GET | 查询单条状态 | JWT |
| `/api/query` | POST | SQL 查询（只读） | JWT（已有 `/query`） |
| `/api/snapshot` | GET | 最新快照 | JWT |
| `/api/health` | GET | 健康检查 | 无（已有） |
| `/ws` | WS | 实时推送 | JWT (握手) |

**注意**: feedback-server 已有 `/health`、`/report`、`/feedback`、`/query` 四个端点，v3.0 §1.2 遗漏了 `/query` 和 `/health`。

### P2-3: 灰度策略需要量化验证标准

- 每个灰度阶段需定义成功率阈值（如 ≥ 95%）
- 灰度期间的回退触发条件（如连续 3 次失败）
- 灰度阶段的监控指标（延迟 P95、错误率）

### P2-4: 前端离线/弱网场景

- 操作请求本地缓存，网络恢复后重发
- 数据展示缓存兜底（显示最后更新时间）
- `request_id` 幂等设计（已有 `request_id UNIQUE` 字段）

### P2-5: 数据库 Schema 补充

`optimization_actions` 表建议补充：
- `retry_count INTEGER DEFAULT 0` — 重试次数
- `retry_at INTEGER` — 下次重试时间
- `idempotency_key TEXT UNIQUE` — 幂等键
- `source TEXT` — 来源（h5/feishu_card/feishu_listener/daemon）
- `cdp_session_id TEXT` — CDP 会话标识（审计追溯）
- `CHECK(status IN (...,'cancelled','expired'))` — 状态约束

### P2-6: 前端分包与性能（如仍用 Taro）

飞书小程序首包限制 2MB（如果仍走小程序方案）：
- Taro 分包加载（subpackages）
- ECharts 按需引入（仅 bar/line/pie/gauge）
- 首屏只加载仪表盘

### P2-7: 审计表数据保留策略

- 热数据：最近 90 天 SQLite
- 冷数据：90 天前归档到 Parquet（复用 DuckDB）
- 与 v2.0 数据保留策略对齐

### P2-8: Marketing API PoC 应前置到 P1 阶段

- P1 阶段同步提交 Marketing API 申请
- P3 之前完成 PoC（验证 pause/enable/updateBudget 三个端点）
- 验证 plan ID 在 Cookie API 和 Marketing API 下的一致性
- **复用 `oceanengine-oauth/` Python 原型做快速验证**

### P2-9: 已有测试文件可复用

项目 `tests/` 目录已有 4 个测试文件，v3.0 的端到端测试应基于扩展：

| 测试文件 | 覆盖范围 |
|---|---|
| `autonomous-router.test.mjs` | 路由器/Provider/熔断 |
| `feishu-listener.test.mjs` | 飞书指令解析 |
| `feishu-listener-e2e.test.mjs` | 端到端指令执行 |
| `feishu-push-guard.test.mjs` | 推送熔断 |

---

## 4. ⚪ P3 文档质量问题

### P3-1: 工期阶段编号不连续

P1 → P2 → P3 → P5 → P6 → P4。建议按时间顺序重编号。

### P3-2: §10.1 待办清单与正文不对应

P5 待办 "5 个核心页面" vs §2.3 有 8 个页面。建议明确首期范围。

### P3-3: §9 资源链接可能过时

- ECharts Taro 版 `github.com/Cecilxx/echarts-taro` 需确认维护状态
- playwright-extra stealth 链接指向 puppeteer-extra 仓库

---

## 5. 完整源码核验对照表

| 文档声明 | 代码/文档实际 | 核验结果 |
|---|---|---|
| `oceanengine-api-client.mjs` 有 4 读函数 | 4 读函数 + Cookie 管理 + 分页抓取 + 过期自动刷新 | ⚠️ 基本准确，遗漏 Cookie 管理 |
| `cdp-client.mjs` 无页面交互能力 | `checkCDP` / `getOceanEngineTab` / CDP 命令发送 / 心跳重连 | ⚠️ 有基础交互，无复杂页面操作 |
| `feedback-server.mjs` 有 `/report` `/feedback` | 实际有 `/health` `/report` `/feedback` `/query` | ❌ 遗漏 `/query` `/health` |
| `optimization_actions` 预留未激活 | `action-queue.json` + `action-executor.mjs` + `cdp-action.mjs` 已存在 | ❌ 已有完整原型链未提及 |
| `cdp-proxy.mjs` 存在且需退役 | **项目中不存在此文件** | ❌ 文件不存在 |
| `calibrate-page.mjs` v2 已有 | 确认存在，raw CDP + wait-utils | ✅ 准确 |
| 无执行层写能力 | `cdp-action.mjs` 已有 togglePlanStatus + adjustBudget | ❌ 严重低估 |
| 无自治/路由能力 | `autonomous-router.mjs` + `shadow-arena.mjs` + `adaptive-scheduler.mjs` | ❌ 完全遗漏 |
| 无推送熔断 | `feishu-push-guard.mjs` 已有 | ❌ 遗漏 |
| 无自动登录 | `oec-auto-login.mjs` 已有（SSO + 验证码通知） | ❌ 遗漏 |
| 无 OAuth 原型 | `oceanengine-oauth/` Python 原型已存在 | ❌ 遗漏 |
| 无前序方案文档 | `小程序上架方案.md` + `oec-data-strategy-analysis.md` + `overview.md` | ❌ 未引用 |
| 工期 5-6 周 | P5/P6 各 1-2 周 + P1-P3 共 1-2 周 | ⚠️ P1-P3 工期与 v2.0 的 3-4 周不一致 |
| PM2 9 进程 | 实际 9 进程（含 pm2-test 手动触发） | ⚠️ 未提及 pm2-test |

---

## 6. 已有模块与 v3.0 的映射关系

v3.0 设计的新模块，与已有模块的对应关系（**实施时必须参考**）：

| v3.0 新模块 | 已有模块 | 关系 |
|---|---|---|
| `operation-executor.mjs` | `action-executor.mjs` + `cdp-action.mjs` + `feishu-listener.mjs` | **应合并升级，非新建** |
| `oceanengine-api-write.mjs` | `oceanengine-api-client.mjs`（读）+ `cdp-action.mjs`（写） | 新增 Marketing API 写函数，与读函数同文件 |
| `oauth-token-manager.mjs` | `oceanengine-oauth/exchange_token.py`（原型）| Node.js 迁移 + 扩展 refresh_token |
| `rate-limiter.mjs` | `autonomous-router.mjs` 内置限流逻辑 | 复用或提取 |
| `calibrate-page.mjs` 复用 | `calibrate-page.mjs` v2 已有 | ✅ 直接复用 |
| `wait-utils.mjs` 复用 | `wait-utils.mjs` 已有 | ✅ 直接复用 |
| 健康检查 | `monitor-daemon.mjs` + `daemon-health.json` + `chrome-guard.mjs` | 扩展现有框架 |
| 推送通道 | `feishu-push-guard.mjs`（lark-cli 包装+降级）| 复用现有推送 |
| 自治路由 | `autonomous-router.mjs`（Provider 评分/熔断） | P4 AI 闭环应基于此 |
| 阴影测试 | `shadow-arena.mjs`（5% 流量影子测试） | P4 AI 闭环应基于此 |
| 动态调度 | `adaptive-scheduler.mjs`（时段/活跃度调频） | 与执行层调度协调 |
| WebSocket Server | `cdp-client.mjs` 已用 `ws` 包 | feedback-server 升级 WS Server |
| 前端（小程序/H5） | **无任何前端代码** | 全新建设 |

---

## 7. 建议的修正行动清单

| # | 行动 | 对应问题 | 优先级 |
|---|---|---|---|
| 1 | **确认企业资质可用性**，不可用则主推 H5 + 飞书内嵌 | P0-1 | **阻塞** |
| 2 | §1.2 补充 cdp-action / feishu-listener / autonomous-router 等 10+ 个已有模块 | P0-2, P0-6 | **阻塞** |
| 3 | §3.3 CDP 回退代码改为基于 cdp-action.mjs + calibrate-page.mjs | P0-3 | **阻塞** |
| 4 | §2.4 删除 "lark WS"，改为自建 WebSocket Server | P0-4 | 高 |
| 5 | 新增 "鉴权体系共存" 小节 + 复用 oauth 原型 | P0-5 | 高 |
| 6 | §5 "飞信小程序" → 修正（或改为 H5） | P0-7 | 高 |
| 7 | 引用 `小程序上架方案.md` + `oec-data-strategy-analysis.md` + `overview.md` | P1-8 | 高 |
| 8 | 明确 operation-executor 与 feishu-listener 的关系 | P1-1 | 高 |
| 9 | 审批流超时与盯盘时段对齐 | P1-2 | 中 |
| 10 | lock_token 实现补充 SQL 示例 | P1-3 | 中 |
| 11 | 前端框架根据 P0-1 结论重新评估 | P1-5 | 中 |
| 12 | 错误码分类表 | P1-6 | 中 |
| 13 | Marketing API PoC 前置 + 复用 oauth 原型 | P2-8 | 中 |
| 14 | 基于 tests/ 已有测试扩展端到端测试 | P2-9 | 低 |

---

## 8. 可直接采纳的部分

以下设计合理，无需大改：

- ✅ **Marketing API 优先 + CDP 回退** 的双通道策略（前提是 CDP 回退改用 raw CDP）
- ✅ **L0-L3 审批分级** 的风控思路
- ✅ **optimization_actions 全链路追踪** 的审计设计
- ✅ **双 OAuth 体系**（飞书 + 巨量引擎）的鉴权架构
- ✅ **灰度策略** 的分阶段上线思路
- ✅ **operation-executor** 作为 PM2 常驻进程的定位
- ✅ **§3.1 能力矩阵** 的操作分级（P0-P3）

---

## 9. 给后续 agent 的使用指引

**⚠️ 实施前必读**:

1. **先解决 P0-1（企业资质）**：这决定了前端方案选型（小程序 vs H5），影响后续所有 L5 工作
2. **先读 `cdp-action.mjs`（416行）**：这是执行层的核心，不是从零开始。v3.0 说的"0 写函数"是错的
3. **先读 `feishu-listener.mjs`**：已有完整的"指令监听→确认→执行→报告"三阶段闭环
4. **先读 `autonomous-router.mjs` + `overview.md`**：P4 AI 闭环的基础设施已存在
5. **先读 `巨量引擎监控-小程序上架方案.md`**：已识别 HTTPS/备案/云端架构等约束
6. **先读 `oec-data-strategy-analysis.md`**：HTTP API 的 3 个端点已逆向验证，含关键参数 `isSophonx:1`
7. **CDP 回退开发时**：绝对不要用 v3.0 中的 Playwright 代码示例，用 cdp-action.mjs 的真实鼠标事件方案
8. **数据库迁移时**：参考 `数据库方案设计报告.md` 的完整 Schema，v3.0 的 SQL 有遗漏字段
9. **Marketing API 接入前**：先 PoC 验证 plan ID 一致性，复用 `oceanengine-oauth/` 原型

---

## 10. 修正后的推荐架构（基于 P0-1 不可用的假设）

如果用户**无企业资质**（最大概率场景），推荐架构调整为：

```
┌─────────────────────────────────────────────────────────┐
│  L5 交互层：独立 Web (React+Vite) + 飞书内嵌 H5          │
│  ┌──────────┐  ┌──────────┐  ┌────────┐  ┌───────────┐  │
│  │ 仪表盘   │  │ 计划列表  │  │ 详情页  │  │ 操作确认  │  │
│  └──────────┘  └──────────┘  └────────┘  └───────────┘  │
└──────────────────────┬──────────────────────────────────┘
                       │ REST + WebSocket（自建 WS Server）
                       ↓
┌─────────────────────────────────────────────────────────┐
│  L4 feedback-server (端口 8899)                          │
│  /api/action/* + /api/query + /ws（基于 ws 包）          │
│                                                          │
│  operation-executor（PM2 常驻，无端口，纯消费者）         │
│  ← 合并: feishu-listener + action-executor + cdp-action  │
└──────────────────────┬──────────────────────────────────┘
                       │
          ┌────────────┴────────────┐
          ↓                         ↓
┌──────────────────┐    ┌────────────────────────┐
│  Marketing API   │    │  cdp-action.mjs（raw CDP）│
│  OAuth 2.0       │    │  真实鼠标事件 + OVUI 兼容 │
│  (主通道)        │    │  (回退通道，非 Playwright) │
└──────────────────┘    └────────────────────────┘
```

**关键变化**:
- L5: 飞书小程序 → 独立 Web + 飞书内嵌 H5（免企业资质）
- L6 CDP 回退: Playwright → cdp-action.mjs（raw CDP 真实鼠标事件）
- operation-executor: 新建 → 合并已有 feishu-listener + action-executor + cdp-action
- 自治层: 引用 autonomous-router + shadow-arena + adaptive-scheduler（已有）

---

*报告结束。本文档可直接作为 v3.1 修订的输入。*
