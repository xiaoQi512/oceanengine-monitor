# 架构规划评审意见 v1.0

> **评审对象**: `架构规划_整体方案.md` v1.0
> **评审人**: 爆量君 (基于 MiniMax-M3 模型)
> **评审日期**: 2026-06-27
> **评审范围**: 四层架构 / 三引擎 / 四阶段路线 / 风险与工期
> **使用建议**: 本意见可直接由架构 agent 拆解为 v1.1 修订项，按"3.1 → 4.1-4.6 → 5.1-5.4 → 6.1-6.4"优先级逐条落实

---

## 1. 总体评分与定性

| 维度 | 评分 | 评价 |
|---|---|---|
| 架构分层清晰度 | A | L1-L4 分工合理、职责无重叠 |
| 路径选择合理性 | **C+** | 致命盲点：已落地的 HTTP API 未纳入方案 |
| 生产能力完备度 | B- | 缺数据质量/告警去重/SLO/灾备 |
| 工期估算准确度 | B- | 整体偏乐观 30-50% |
| 风险意识 | B+ | 风险表存在但不够细，缺回滚预案 |
| 文档质量 | B | 缺目录/术语表/ADR/版本迭代 |

**核心结论**: 框架扎实、阶段划分合理，但 (1) 路径选择存在"已有资产被绕开"问题，(2) **生产级四大能力（数据质量/告警去重/SLO/灾备）缺位**，(3) 工期低估。建议 v1.1 优先修正路径选择 + 补齐 4 大能力。

---

## 2. 优点（应保留的设计）

| # | 优点 | 说明 |
|---|---|---|
| 1 | 四层架构分层清晰 | L1采集 / L2存储 / L3分析 / L4服务 职责无重叠 |
| 2 | SQLite 主写 + DuckDB 分析 | 双引擎分工规避了 DuckDB 频繁小事务痛点 |
| 3 | playwright-core context 隔离 | 多账户登录态隔离的最佳方案 |
| 4 | Parquet 列式归档 | 数据长期治理的标准做法 |
| 5 | 渐进式 4 阶段路线 | 降低迁移风险，模块退役有先后 |
| 6 | 保持 PM2 进程结构 | 减少迁移面，运维负担可控 |
| 7 | 保留 JSON 兜底 | 历史 2184 文件 0 损耗，rollback 友好 |

---

## 3. 致命盲点（必须修正）

### 3.1 已落地的 HTTP API 被完全忽视

**证据**:
- `oceanengine-monitor-v3.mjs:2` 注释: **"v4: HTTP API主方案 + CDP降级"**
- `oceanengine-api-client.mjs` 已实现 3 个逆向 API 端点
- `monitor-v3.mjs:24` 已 import `oceanengine-api-client.mjs`

**问题**: 规划却把 "playwright-core 统一接入" 作为 L1 主路径。`oceanengine-api-client.mjs` 在文档中**完全没出现**。这会导致 P1 走 playwright 重复造轮子，把已稳定的 "HTTP 主 + CDP 兜底" 方案废掉。

**修正建议**:
```
L1 采集层 重写为:
  ├── 内部 API 客户端（主路径，已实现）   ← oceanengine-api-client.mjs
  ├── CDP 客户端（Cookie 提取 + 自动登录）← 仅保留 cdp-client.mjs
  └── playwright-core（异构平台兜底，腾讯/百度用，非主路径）
```

**理由**:
- 巨量走 API（已验证稳定）→ 不必再攻 OVUI 真实鼠标事件这个已记录在案的雷区
- API 速度提升 30-60 倍（生产实测）
- CDP 仅承担 Cookie 提取 + 自动登录 + API 失效兜底
- playwright-core 留给真正需要 DOM 操作的异构平台

---

## 4. 重要缺项（六项生产级能力）

### 4.1 数据质量维度缺失

| 缺口 | 后果 | 建议 |
|---|---|---|
| `snapshot_quality` 字段未设计 | 无法区分"采集失败但被记录"vs"真实空数据" | 加 `enum('ok','partial','failed','timeout')` |
| 补采机制缺失 | 监控脚本崩溃 N 小时，数据空洞无修补 | 加 `data_gap` 表 + PM2 任务触发补采脚本 |
| 采集耗时/成功率无 metrics | 性能退化无预警 | `monitor_metrics` 表 + `/api/metrics` 端点 |

### 4.2 告警系统能力不足

| 缺口 | 后果 | 建议 |
|---|---|---|
| 同类型告警 N 分钟去重 | 飞书刷屏、用户疲劳 | 滑动窗口聚合（5min/15min 同 plan+type 只报一次） |
| 告警升级机制 | 高优告警无人工响应时无降级策略 | 5min 未读升级到日报置顶 / 飞书 @at |
| 跨日告警闭环 | "昨天 CPA 异常"无后续跟踪 | `alerts.resolved_at` + 根因标记 |
| AI 区域号告警噪声 | 5 区域同规则会触发 5 次 | 按 (account_group, alert_type) 聚合 |

### 4.3 AI 区域号深度分析缺位

当前 `oceanengine-ai-regions.mjs` 是 5 区域数据**简单合并**。建议加：
- 区域横向对比（东区 CPA vs 西区 CPA）
- 区域 leaderboard（消耗 / 转化 / ROI 排名）
- 区域异常检测（某区 CPA 偏离均值 >2σ，建议改用 **MAD** 而非 Z-SCORE）
- 时段叠加分析（5 区域在同时段的差异）

### 4.4 容量规划与 SLO 缺位

| 维度 | 估算 | 备注 |
|---|---|---|
| 5min 采集 × 16h × 6 账户 | ~5,760 行/天 | 1年 ≈ 210 万行，3年 ≈ 630 万行 |
| 索引后单行 200B → 5min 表 3 年 | ≈ 12 GB | 单 SQLite 文件风险高，**需月度归档** |
| DuckDB ATTACH 性能 | **数据复制**非零拷贝 | 需先在 PoC 中验证 100 万行聚合延迟 |

**建议**:
- 补"容量规划"章节
- 定义 SLO：采集成功率 ≥99% / 查询 P95 ≤500ms / 告警延迟 ≤60s
- 冷热分层：0-30 天走 SQLite / 30-90 天走 DuckDB on Parquet / >90 天 archive/

### 4.5 监控守护自身的可观测性

`monitor-daemon.mjs` 检查所有 PM2 任务，但**没人监控它自己**。

**建议**:
- 加 `daemon-guard`（每 5min 心跳写文件）
- 或直接复用 `chrome-guard` 心跳机制上报到飞书
- 守护进程 dead → 触发 `feedback-server` `/health` 检测 → 飞书告警

### 4.6 回滚 / 灾备方案缺失

P2 SQLite 迁移是单点风险：
- 2184 JSON 导入脚本写错 → 重复导入？丢数据？
- SQLite WAL 文件损坏 → 如何恢复？
- 迁移中途服务崩溃 → 半写状态？

**建议**:
- 迁移前 `monitor-data/` 全量 tar.gz 备份
- SQLite 启用 `PRAGMA journal_mode=WAL` + 每日 `VACUUM INTO` 备份
- 灰度开关：`USE_SQLITE_WRITER=0/1` 环境变量（默认 0 → 双写期开 1）

---

## 5. 模块级优化建议

### 5.1 L1 采集层

| 建议 | 理由 |
|---|---|
| **Cookie 失效自愈**前置 | api-client 已有 autoLogin，但放在每次调用前才检测会拖慢采集；建议在 PM2 启动序列里做一次预热 |
| **采集时间预算硬上限** | 单次采集 >90s 强制退出（避免堆积 PM2 cron 任务） |
| **采集幂等键** | `(account_id, snapshot_time, page_no)` 唯一约束 → 双写期避免重复 |
| **OVUI 鼠标事件工具单独保留** | `calibrate-page.mjs` 不要退役，作为异构平台兜底 |
| **采集失败重试策略** | 网络错误指数退避 × 3（不要在主流程里 sleep） |

### 5.2 L2 存储层（Schema 改进）

```sql
-- 加分区视图（按月），加速归档
CREATE VIEW snapshots_2026_06 AS
  SELECT * FROM campaign_snapshots
  WHERE snapshot_time >= '2026-06-01' AND snapshot_time < '2026-07-01';

-- 索引补全（规划里 idx_snapshots_account_time 不够）
CREATE INDEX idx_snapshots_time_active
  ON campaign_snapshots(snapshot_time, is_active);  -- 时段效率查询
CREATE INDEX idx_snapshots_account_type_time
  ON campaign_snapshots(account_id, alert_type, snapshot_time);  -- 告警分析

-- alerts 表加 fingerprint 字段（告警去重）
ALTER TABLE alerts ADD COLUMN fingerprint TEXT;  -- MD5(campaign_id + alert_type + day_bucket)
CREATE UNIQUE INDEX idx_alerts_fingerprint ON alerts(fingerprint);
```

**额外建议**:
- `campaigns` 表加 `lifecycle_status`（active/paused/archived/deleted）
- `optimization_actions` 加 `requires_approval` 和 `approver` 字段（人审环节）
- `monitor_metrics` 表（采集耗时 / 成功率 / 错误码分布）

### 5.3 L3 分析层

| 建议 | 理由 |
|---|---|
| **DuckDB PoC 提前到 P1 末** | 验证 ATTACH 真实延迟（不是文档假设的"零拷贝"） |
| **异常检测算法选型** | Z-SCORE 适合正态分布，CPA 是右偏长尾——改用 **MAD（中位数绝对偏差）** 或 **IQR** |
| **REGR_SLOPE 7日趋势** | 适合日维度；分钟级趋势用 `LAG()` 窗口函数即可 |
| **Parquet 分片** | 月度单文件 vs 按周分片——**按周更利于细粒度查询** |
| **DuckDB 升级到 v1.0+** | 当前 `@duckdb/node-api` 是 0.x → 1.0 前 API 变化频繁，建议等稳定 |
| **better-sqlite3 native 编译** | Windows 上需要 prebuild 验证（Node 22 的 node-gyp 兼容） |

### 5.4 L4 服务层

| 建议 | 理由 |
|---|---|---|
| **`/query` 强约束** | ① 最大行数 10000 ② 超时 10s ③ 结果白名单（仅 read-only 表）④ 关键词拦截（INSERT/UPDATE/DELETE/DROP/ATTACH/PRAGMA） |
| **`/trigger` 鉴权强化** | token + IP 白名单（仅 127.0.0.1）+ 时间窗口（仅 7-23）+ 速率限制（每分钟 ≤3 次） |
| **新增 `/api/health/deep`** | 比 `/health` 深入：DB 可达 / 最近采集时间 / CDP 状态 / 磁盘空间 / 进程数 |
| **静态资源分离** | `feedback-server` 当前混了 API + 静态 HTML，未来加图表时建议分文件 |
| **WebSocket 推送** | 报表"实时刷新"用 WS 而非轮询（HTTP 太重） |
| **API 鉴权中间件统一** | 所有写操作走同一鉴权层（不重复 token 校验） |

---

## 6. 风险提醒（含工期修正）

### 6.1 工期估算偏乐观

| 阶段 | 规划估时 | 修正估时 | 偏差原因 |
|---|---|---|---|
| P1 playwright 接入 | 1-2 天 | **2-3 天** | 需先做 PoC 验证 OVUI 真实鼠标事件；AI 区域号 5 账户串行/并行都要测 |
| P2 SQLite 迁移 | 2-3 天 | **4-5 天** | 2184 JSON 导入 + 6 脚本改造 + 1 周双写期不算在 P2 内 |
| P3 DuckDB 分析 | 3-4 天 | **4-5 天** | Node API 较新 + Windows 兼容性需额外调试 |
| P4 AI 闭环 | 按需 | **至少 1 周** | 飞书审批流 + 火山方舟对接 + playwright 执行层 |

### 6.2 隐含依赖

- **P2 依赖 P1 验证**: 写 P2 之前必须先确认 playwright 真的可用（或确认走 API 路径）
- **DuckDB 升级到 v1.0+**: 当前 `@duckdb/node-api` 是 0.x → 1.0 前 API 变化频繁
- **better-sqlite3 native 编译**: Windows 上需要 prebuild 验证

### 6.3 灰度策略建议

```
P1: DRY_RUN 模式（采集但不写库）观察 3 天
    ↓
P2: 影子写（写双份但只读 JSON）观察 7 天
    ↓
P2 末: 切换读路径（DB 优先 → JSON fallback）观察 3 天
    ↓
P3: DuckDB 切换只读模式观察 7 天
    ↓
P4: 关闭 JSON 写入（保留 30 天再删）
```

### 6.4 安全风险

| 风险 | 等级 | 建议 |
|---|---|---|
| `/query` SQL 端点开放 | **高** | 上面已提白名单+超时+行数+关键词拦截 |
| `/trigger` token 泄漏 | 中 | 改短时效 token（5min）+ 一次性 + 来源 IP 绑定 |
| 环境变量散落敏感信息 | 中 | OEC_EMAIL/OEC_PASSWORD 应走 keytar 加密存储 |
| 飞书卡片泄漏 aadvid | 低 | 飞书群本身半私域，可接受；如需脱敏在 L4 层做 |

---

## 7. 必须立刻回答的 3 个问题（v1.1 需回答）

1. **是否真的需要 playwright-core？**（如果 HTTP API 已稳，建议 L1 取消 playwright 主路径）
2. **DUCKDB 零拷贝是真的吗？**（要求 PoC 验证，否则回退到 SQLite 聚合 + JSON 索引）
3. **P4 飞书审批流如何设计？**（chat_id 新增 bot 监听 vs webhook 模式）

---

## 8. 文档本身改进点

| 缺项 | 建议补什么 |
|---|---|
| 没有 ADR 格式 | 用 `MADR` 模板记录 7 个关键决策（已表格化但缺"上下文/权衡/后果"） |
| 没有变更日志 | 每次评审后追加"评审意见 + 采纳情况" |
| 没有目录 | 8 章无 TOC，401 行文档阅读负担重 |
| 没有术语表 | `OVUI / CDP / context 隔离 / WAL / 列式存储` 等概念首次出现应注释 |
| 没有引用现有 ADR | 监控逻辑 v3 / 守护 v2 这些已存在设计应链接 |
| v1.0 状态写"待评审" | 评审完应改为"已评审 v1.0 → v1.1"明确迭代 |
| 没有架构图 | 建议加 SVG 架构图（数据流向一目了然） |

---

## 9. 业务侧建议（架构没覆盖的"价值层"）

| 能力 | 价值 | 优先级 |
|---|---|---|
| 跨账户 ROI 对比 | 真人号 vs 5 AI 区域号横向排名，发现潜力账户 | P3 |
| 历史相似计划推荐 | "3 个月前类似计划，跑量 X，建议预算 Y" | P4 |
| A/B 计划效果评估 | 同素材不同出价的 ROI 对比 | P4 |
| 投放员操作审计 | 谁在何时改了哪个计划，结果如何 | P2 |
| 飞书交互式看板 | 飞书卡片"查看历史"按钮 → 弹出 7 日趋势图 | P3 |

---

## 10. 评审人信息

- **模型名称**: MiniMax-M3
- **模型开发商**: MiniMax
- **评审人代号**: 爆量君
- **工作模式**: Ask 模式评审 + Craft 模式落盘
- **文档命名**: `架构规划_评审意见_M3_v1.0.md`
