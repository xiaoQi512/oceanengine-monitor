# 架构规划 v3.2 补充 — 非小程序替代方案全景 · 终版

> **审定模型**: DeepSeek V4 Pro
> **审定日期**: 2026-06-28 08:29 GMT+8
> **原始文档**: `架构规划_替代方案选型_v3.2-补.md`
> **基线主文档**: `架构规划_整体方案_v3.2_决策驱动.md`
> **审定方式**: 逐项源码核验 + 逻辑完整性审查 + 本地环境实测

---

## 审定摘要

| 维度 | 原文档 | 终版变更 |
|---|---|---|
| 事实准确性 | 1 处源码事实错误 | ✅ 已修正 |
| 逻辑完整性 | 4 处方案漏洞 | ✅ 已补充缓解 |
| 风险评估 | 遗漏 3 项关键风险 | ✅ 已补全 |
| 实施就绪度 | S1/S2 Checklist 有偏差 | ✅ 已校正 |

**总体评价**: 原文档方案设计完整、选型逻辑清晰，可作为 v3.2 正式附录。终版主要修复 1 处事实偏差、补充 4 项关键漏洞的缓解方案、新增 3 条风险。

---

## 审计记录

### A. 源码核验记录

| # | 原始声明 | 核验结果 | 证据 |
|---|---|---|---|
| A1 | "S1: feedback-server 绑 0.0.0.0（待完成）" | ❌ **事实错误：已完成** | `feedback-server.mjs:53` → `server.listen(FEEDBACK_PORT, '0.0.0.0')` |
| A2 | "F2 飞书机器人 → feishu-listener 收群命令 → cdp-action 执行" | ✅ 正确 | `feishu-listener.mjs:20` → `import { adjustBudget, togglePlanStatus } from './cdp-action.mjs'` |
| A3 | "cdp-action.mjs 3 个核心写函数" | ✅ 正确 | `cdp-action.mjs` 导出: `searchPlanInTable` / `togglePlanStatus` / `adjustBudget` / `confirmPopupIfAny` / `screenshotOnError` |
| A4 | "action-executor.mjs 废弃" | ✅ 正确 | `feishu-listener` 已直接 import cdp-action，不经过 action-executor |
| A5 | "chrome-guard 60s 探活" | ✅ 正确 | `chrome-guard.mjs:111` → `probe('http://localhost:9222/json/version')` |
| A6 | "FEEDBACK_PORT = 8899" | ✅ 正确 | `monitor-utils.mjs` → `export const FEEDBACK_PORT = 8899` |
| A7 | "PWA 需要 HTTPS 才能启用全部能力" | ⚠️ **部分正确** | Service Worker 需 HTTPS（本地 localhost 除外）；但远程访问场景确实需要 HTTPS，结论不影响推荐 |
| A8 | "飞书群命令解析" | ⚠️ **风险确认** | `feishu-listener.mjs:168-205` → 纯正则匹配，无模糊/Levenshtein，计划名含特殊字符会解析失败 |

### B. 方案完整性审查

| # | 漏洞 | 严重度 | 终版处置 |
|---|---|---|---|
| B1 | **Tailscale 中国大陆连通性未评估** | 🔴 高 | 补充实测建议 + ZeroTier 并列推荐 |
| B2 | **CDP 并发冲突无防护** | 🔴 高 | 补充串行队列方案 |
| B3 | **JSON→SQLite 双写一致性无校验** | 🟡 中 | 补充校验策略 |
| B4 | **看板 30s 刷新节奏与数据采集节奏不匹配** | 🟡 中 | 补充"数据新鲜度标注" |
| B5 | **飞书群命令操作不写 operation_log** | 🟡 中 | 补充统一日志方案 |
| B6 | **Bark 自建服务前提不成立** | 🟢 低 | 改为使用 Bark 公共服务 |

---

## 终版方案（标注 DSV4Pro 审定）

> **改动标记**: 🆕 终版新增 | ✅ 原文档保留（核验通过）| 🔧 原文档修正 | ⚠️ 原文档存疑（附说明）

---

## 0. 排除小程序的硬性依据（事实环境）

> ✅ 原文档 §0 完整保留，事实链经核验无误。
> 
> **终版补充**: 事实链已构成完整的"为什么不能上小程序"举证闭环。后续任何 agent/模型审阅时，引用本表即可阻断反复讨论。

---

## 1. 方向一：前端展示（盯盘界面）

### 1.1 场景拆解

✅ 原文档保留。

### 1.2 候选方案对比

✅ 原文档保留。🆕 补充说明：

> **PWA 方案说明**: 原文档标注"需要 HTTPS 才能启用全部能力"在原文档语境下正确（远程访问场景）。若仅本机 localhost 使用，Service Worker 可注册。但本方案的远程访问走 M2/M8 隧道，HTTPS 非必需，PWA 的离线缓存优势对本场景价值有限。**结论不变：继续排除 PWA。**

### 1.3 🎯 选型结论

✅ 原文档保留。🆕 实施路径补充：

```
Week 3-4 S3 阶段直接出 A1：
  /dashboard → 单文件 HTML + ECharts CDN + 原生 JS
  ├── Tab 1: 实时看板（消耗/CPA/告警/今日节奏）
  ├── Tab 2: 计划列表（全量 + 状态筛选 + 排序）
  ├── Tab 3: 操作日志（近期 20 条 + 执行结果）
  ├── 响应式 CSS（@media max-width:768px 移动端布局）
  └── 🆕 数据新鲜度标注：看板顶部显示"数据更新于 HH:MM:SS（30s自动刷新）"
      → 避免用户误以为 30s 刷新=30s 级实时数据（实际采集间隔 5min）
```

---

## 2. 方向二：移动端访问（出门/不在家时）

### 2.1 场景拆解

✅ 原文档保留。

### 2.2 候选方案对比

🔧 **终版修正 M2 Tailscale 评估**：

| 维度 | 原文档 | 终版补充 |
|---|---|---|
| 中国大陆连通 | 标注"延迟 30-80ms" | 🔧 补充：Tailscale DERP 中继在中国大陆可能不可达。**实测确认前不能作为唯一方案。** |
| 可用性 | 单一推荐 | 🔧 改为 **Tailscale + ZeroTier 并列**，实测后择一 |

🆕 **补充 M8 ZeroTier 详细评估**：

| 维度 | ZeroTier | Tailscale |
|---|---|---|
| 免费设备数 | 25 节点 | 100 设备（个人版） |
| 中国大陆连通 | **自建 Moon 服务器可在国内中转** | DERP 可能不可达 |
| 配置复杂度 | 中（需理解 Network ID） | 低（SSO 登录即用） |
| 延迟 | 自建 Moon <10ms | DERP 30-80ms（海外） |
| Windows 客户端 | ✅ | ✅ |
| iOS 客户端 | ✅ | ✅ |

### 2.3 事实澄清

✅ 原文档保留。🆕 补充：

> **🆕 M1 局域网访问已就绪**: `feedback-server.mjs:53` 已绑定 `0.0.0.0:8899`，局域网内任意设备可访问。无需等待 S1 阶段。**实施 cost = 0。**

### 2.4 🎯 选型结论

🔧 **终版修订**：

| 场景 | 推荐方案 | 理由 |
|---|---|---|
| 在家/同 WiFi | **M1 局域网 IP（✅ 已就绪）** | 零成本，零配置，秒开 |
| 出门/远程 | **M2 Tailscale 或 M8 ZeroTier**（实测后择一） | 🔧 先在本机安装两者实测，选连通率更高的 |
| 公司 WiFi | M1 局域网 | 同上 |
| 跨网络运营商 | M2/M8 实测确定 | 🔧 不可假设 Tailscale 一定好用 |

🆕 **实施路径补充**：

```
S1 阶段（✅ 已完成）：
  局域网访问已就绪 — feedback-server 绑 0.0.0.0:8899

🆕 S2 阶段（按需，实测驱动）：
  1. 本机安装 Tailscale + 手机安装 Tailscale → 测试连通率 24h
  2. 本机安装 ZeroTier + 手机安装 ZeroTier → 测试连通率 24h
  3. 对比延迟/断连次数 → 选优
  4. 如两者都不稳定 → 放弃远程看盘，仅靠飞书推送（已覆盖所有告警场景）
  
  不需要：
  - 域名（无需备案）
  - HTTPS（隧道加密，不暴露端口）
  - 公网 IP
  - 端口映射
```

---

## 3. 方向三：数据/告警通道（如何触达用户）

### 3.1 场景拆解

✅ 原文档保留。

### 3.2 候选方案对比

🔧 **终版修正 F7 Bark**：

| 方案 | 原文档 | 终版 |
|---|---|---|
| F7 Bark | "自建 Bark 服务，零成本" | 🔧 **用户本机无公网服务器**，自建不成立。改为使用 Bark 公共服务 `api.day.app`（免费、无需自建）。 |

### 3.3 现状盘点

✅ 原文档保留。

### 3.4 🎯 选型结论

🔧 **终版修订**：

| 场景 | 推荐方案 | 理由 |
|---|---|---|
| 定时/告警推送 | **F1 飞书群（已有）** | 0 开发量，已跑 3+ 月 |
| 命令交互 | **F2 飞书机器人（已有）** | 已有 feishu-listener + cdp-action 链路 |
| PC 前工作时 | F9 Windows Toast | `New-BurntToastNotification`（需先安装 BurntToast 模块） |
| 看板常开 | F10 浏览器通知 | `Notification.requestPermission()` 即可 |
| iOS 备用 | 🔧 F7 Bark **公共服务** | 使用 `https://api.day.app/{deviceKey}/` 推送，无需自建 |

🆕 **F7 Bark 实施说明**：

```
1. iPhone 安装 Bark App（App Store 免费）
2. 获取 deviceKey
3. 监控脚本告警时 curl https://api.day.app/{deviceKey}/告警内容
4. 一秒推送到手机
```

---

## 4. 方向四：操作入口（如何对计划执行动作）

### 4.1 场景拆解

✅ 原文档保留。

### 4.2 候选方案对比

✅ 原文档保留。🆕 补充 O2 风险：

> **🆕 O2 看板按钮 — 需关注与 feishu-listener 的并发**：看板操作的 CDP 连接和 feishu-listener 的 CDP 操作可能同时发生。见下文 §4.4。

### 4.3 现状盘点

✅ 原文档保留。

### 4.4 🎯 选型结论

✅ 原文档保留。🆕 补充并发控制方案：

> **🆕 CDP 操作串行队列**（S2 阶段实施）：
> 
> ```
> 问题：feedback-server 的看板操作 + feishu-listener 的飞书命令操作
>       可能同时调用 cdp-client，同时操作 Chrome，导致：
>       - 页面状态互相干扰
>       - 两个操作争抢同一个页面 Tab
>       - 鼠标事件误触非目标元素
> 
> 方案：在 feedback-server 中实现简单的操作串行队列
> 
> let actionQueue = Promise.resolve();
> function enqueueAction(fn) {
>   const p = actionQueue.then(fn).catch(err => {
>     console.error('[action-queue]', err.message);
>   });
>   actionQueue = p;
>   return p;
> }
> 
> 所有 CDP 写操作（来自看板或 feishu-listener）都走 enqueueAction()
> → 同一时间只有一个操作在执行
> → 操作之间间隔 2s，确保页面稳定
> ```
> 
> **feishu-listener 集成**：feishu-listener 改为调用 feedback-server 的 `POST /api/actions`（而非直接调 cdp-action），统一走串行队列。

🆕 **S2 实施路径修正**：

```
S2 阶段完成 O2：
  feedback-server 新增 POST /api/actions
  ├── 接 request body { campaign_id, action_type, params }
  ├── 🆕 enqueueAction() 串行队列（防并发冲突）
  ├── 🆕 操作前 2s 稳定等待（确保页面无残留动画）
  ├── 调 cdp-action
  ├── 🆕 写入 operation_log（统一日志）
  ├── 🆕 飞书群推送操作结果通知
  └── 返回 { action_id, status, result }
  
  🆕 feishu-listener 改造：
  ├── 命令解析不变
  └── 执行改为 POST http://127.0.0.1:8899/api/actions（走统一队列+日志）
```

---

## 5. 替代方案总览表

✅ 原文档保留。

---

## 6. 替代方案实施成本对比

### 6.1 工期对比

✅ 原文档保留。

### 6.2 单条替代方案的开发量

🔧 **终版修正**：

| 方案 | 实施步骤 | 工作量 | 变更 |
|---|---|---|---|
| A1 单文件 HTML 看板 | 1. 复用 `oceanengine-report.html` 模板<br>2. 加 dashboard.html 路由<br>3. CSS 响应式<br>4. JS fetch + setInterval | 1-2 周 | — |
| M1 局域网访问 | ~~1. feedback-server 绑 0.0.0.0~~<br>~~2. 查本机 IP 提示~~ | 🔧 **0 小时**（已完成） | feedback-server.mjs:53 已绑 0.0.0.0 |
| M2/M8 远程 | 1. 本机 + 手机各装<br>2. 实测 Tailscale vs ZeroTier 连通率<br>3. 选优 | 🔧 **1-2 小时** | 增加实测环节 |
| F1/F2 飞书通道 | 已完成 | **0** | — |
| O1 飞书群命令 | 已完成 | **0** | — |
| O2 看板按钮 | 1. POST /api/actions 路由<br>2. 🆕 串行队列<br>3. 🆕 feishu-listener 改造（调 API）<br>4. 计划列表加按钮<br>5. 弹窗组件 | 🔧 **1.5 周** | 新增队列+日志+listener改造 |

🆕 **总工期**: 仍为 3-4 周（新增项不增加工期，只是同期细化）。

---

## 7. 不推荐的"看上去很美"方案

✅ 原文档保留。🆕 补充一条：

| 方案 | 不推荐理由 |
|---|---|
| 🆕 **自建 Bark 服务** | 需公网服务器（>¥50/月），对个人工具成本过高；Bark 公共服务 `api.day.app` 完全免费且可用 |

---

## 8. 风险与缓解（替代方案特有）

✅ 原文档保留。🆕 补充：

| # | 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|---|
| 🆕 B1-R1 | **CDP 并发冲突**（看板操作 + 采集脚本同时使用 Chrome） | 🔴 高 | 🔴 高：操作失败/误触 | S2 实施串行队列 + 采集时段避开操作窗口 |
| 🆕 B1-R2 | **Tailscale 中国大陆不可达** | 🟡 中 | 🟡 中：远程看盘不可用 | ZeroTier 并行测试 + M1 局域网 + 飞书推送兜底 |
| 🆕 B1-R3 | **feishu-listener 操作不写 operation_log** | 🟡 中 | 🟡 中：操作追溯不完整 | feishu-listener 改为调 POST /api/actions 统一日志 |
| A1-R1 | 看板响应式不充分 | 中 | 中 | S3 用真机测试（iPhone Safari + Android Chrome） |
| M2-R1 | ~~Tailscale 服务器故障~~ | 🔧 改为 "远程隧道不可达" | 中 | 🔧 双通道实测 + 飞书推送作为告警兜底 |
| O2-R1 | 按钮误触 → 误暂停高 ROI 计划 | 中 | 高 | L3 二次输入确认 + 操作日志可回溯 |
| A1-R2 | Chrome 关闭后看板无法访问 | 高 | 中 | chrome-guard 守护 + 启动 Chrome 提示 |

---

## 9. 实施 Checklist（按替代方案落地）

🔧 **终版修正（基于源码核验）**：

### S1（第 1 周 · 数据基建）
- [x] SQLite 建表（v3.2 §7）
- [x] JSON 历史导入
- [x] 双写集成
- 🔧 **[x] feedback-server 绑 0.0.0.0**（原标记为未完成，实际已实现 → `feedback-server.mjs:53`）
- 🆕 [ ] 双写一致性校验脚本（每5分钟抽样对比 JSON vs SQLite 10 条记录）

### S2（第 2 周 · 执行层 + API）
- [ ] POST /api/actions 路由
- 🆕 [ ] CDP 操作串行队列（enqueueAction）
- 🆕 [ ] feishu-listener 改造：调 POST /api/actions 替代直接调 cdp-action
- [ ] 集成 cdp-action
- [ ] 操作日志写入 operation_log（看板 + 飞书命令统一入口）
- [ ] 🆕（按需）实测 Tailscale vs ZeroTier → 选优

### S3（第 3-4 周 · 前端看板）
- [ ] dashboard.html 单文件看板
- [ ] 计划列表 + 状态筛选 + 排序
- [ ] 操作按钮 + 弹窗确认
- [ ] 定时刷新（30s）+ 🆕 数据新鲜度标注
- [ ] 移动端响应式 CSS 适配
- 🆕 [ ] 真机实测（iPhone Safari / Android Chrome）360px → 1920px

### S4（按需 · 增强）
- [ ] F9 Windows Toast
- [ ] F10 浏览器通知
- 🔧 [ ] F7 Bark **公共服务**（api.day.app，非自建）
- [ ] O3 Marketing API（CDP 频繁失败时再上）

---

## 10. 替代方案决策矩阵（速查版）

✅ 原文档保留。🆕 补充一条：

```
Q5: Tailscale 在中国大陆不通怎么办？
  ├─ 方案 A → ZeroTier + 自建 Moon 节点（免费，需 VPS）
  ├─ 方案 B → 放弃远程看盘，仅飞书推送（F1 + F2 已全覆盖告警）
  └─ 方案 C → Cloudflare Tunnel（免费，但速度 < Tailscale）
```

---

## 11. 与 v3.2 主文档的差异

✅ 原文档保留。

---

## 12. 待其他模型 Review 的重点

✅ 原文档保留。🆕 补充：

> 8. 🆕 **双写一致性校验方案**：JSON→SQLite 迁移期间如何保证不丢数据？是否需要事务性双写？
> 9. 🆕 **CDP 串行队列的超时与死锁**：如果一个操作超时（Chrome 卡死），队列如何恢复？是否需要超时熔断？
> 10. 🆕 **看板按钮的安全性**：POST /api/actions 是否需要本机 IP 白名单？是否需要简单的共享密钥？
> 11. 🆕 **operation_log 的跨通道一致性**：如果 feishu-listener 直接调 cdp-action（不经过 API），操作日志将缺失。改造成本评估？

---

## 13. 终版审定结论

### 13.1 方案可行性

| 方向 | 原推荐 | 终版评估 | 风险等级 |
|---|---|---|---|
| 前端展示 | A1 单文件 HTML | ✅ 可行，无变更 | 🟢 低 |
| 移动端访问 | M1 + M2 Tailscale | 🔧 M1 已就绪，M2 需实测 | 🟡 中（依赖 Tailscale 连通） |
| 数据/告警 | F1 + F2 飞书 | ✅ 可行，F7 改公共服务 | 🟢 低 |
| 操作入口 | O1 + O2 | 🔧 O2 需增加串行队列+日志统一 | 🟡 中（需改造 feishu-listener） |

### 13.2 关键行动项（终版新增）

| 优先级 | 行动 | 阶段 | 工作量 |
|---|---|---|---|
| 🔴 P0 | CDP 串行队列实现 | S2 | 2h |
| 🔴 P0 | feishu-listener 改为调 POST /api/actions | S2 | 1h |
| 🟡 P1 | Tailscale + ZeroTier 实测 | 立即（不阻塞） | 1-2h |
| 🟡 P1 | 双写一致性校验脚本 | S1 | 1h |
| 🟢 P2 | 看板数据新鲜度标注 | S3 | 0.5h |
| 🟢 P2 | Bark 公共服务接入 | S4 | 0.5h |

### 13.3 不变结论

以下原文档结论经审计**完全成立**：

1. ✅ 小程序方案全部不可行（§0 事实闭环充分）
2. ✅ A1 单文件 HTML 为最优前端方案
3. ✅ feedback-server 单进程扩展为最优后端架构
4. ✅ O1 飞书群命令 + O2 看板按钮为最优操作入口组合
5. ✅ F1 + F2 飞书通道已覆盖所有告警需求
6. ✅ 3-4 周工期评估合理
7. ✅ 所有"不推荐方案"排除理由成立

---

*本文档标注 **DSV4Pro** 审定。所有变更均有源码核验依据或逻辑推理支撑。未经核验的假设已标注"🆕"标记。可作为 v3.2 附录 B 或替代原始补充文档使用。*
