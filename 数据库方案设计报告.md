# 巨量引擎监控 — 数据库方案设计报告

> 综合日期：2026-06-27 | 方案来源：三路独立分析合成

---

## 一、现状诊断

### 1.1 当前存储架构

```
monitor-data/
├── 2026-06-12T09-00-21.json    ← 15分钟完整快照 (~40KB/个)
├── 2026-06-12T09-15-00.json    ← 累计 2184+ 个独立文件
├── 5m-2026-06-27T12-55-00.json ← 5分钟轻量快照
├── daily-2026-06-27.json       ← 每日趋势日志(追加数组)
├── suggestion-history.json     ← 告警反馈(56条,单文件)
├── daemon-health.json          ← 守护健康报告
├── campaign-lifecycle.json     ← 计划生命周期
├── action-queue.json           ← 操作队列
├── last-push.json / last-5m-push.json
└── monitor.log                 ← 文本日志
```

### 1.2 核心数据字段（从 tbody 采集）

| 列索引 | 字段 | 类型 | 说明 |
|--------|------|------|------|
| data[7] | spend | number | 消耗金额(元) |
| data[8] | leads | number | 线索数 |
| data[9] | conversions | number | 转化数 |
| data[10] | privateMsgOpen | number | 私信开口 |
| data[11] | privateMsgRetain | number | 私信留资 |
| data[12] | formSubmit | number | 表单提交 |
| data[13] | ctr | number | 点击率(%/100) |
| data[14] | cpm | number | CPM(元) |
| data[15] | cvr | number | 转化率(%/100) |
| data[16] | liveViews | number | 直播观看 |
| data[17] | liveOver1Min | number | >1min观看 |
| data[18] | liveComments | number | 评论数 |
| — | cpa | computed | spend/conversions |

### 1.3 痛点

- **碎片化**：2184+ 独立 JSON 文件，文件系统压力大
- **无索引查询**：无法按计划ID/时间段/告警类型高效检索
- **日报生成低效**：需遍历全天 JSON 文件全部解析到内存
- **跨日分析困难**：无法做"过去7天CPA趋势""同计划不同日对比"
- **AI优化无数据基础**：没有结构化历史数据库供AI学习
- **并发不安全**：虽用 tmp+rename，但无真正数据库事务

---

## 二、主方案：SQLite + DuckDB 双引擎

### 2.1 架构总览

```
┌─────────────────────────────────────────────────┐
│              PM2 进程集群 (6个进程)                │
│  monitor-v3 │ 5min-check │ daily-report │ ...    │
└──────┬──────┴─────┬──────┴──────┬───────┴────────┘
       │            │             │
       ▼            ▼             ▼
┌─────────────────────────────────────────────────┐
│    better-sqlite3  (WAL 模式，一写多读并发)        │
│           oceanengine.db                         │
│  ┌────────────────────────────────────────┐     │
│  │ campaign_snapshots  时间序列核心       │     │
│  │ campaigns           计划元数据          │     │
│  │ alerts              告警记录           │     │
│  │ alert_feedbacks     用户反馈           │     │
│  │ daily_summaries     日汇总            │     │
│  │ optimization_actions AI优化动作(预留)   │     │
│  └────────────────────────────────────────┘     │
└──────────────────────┬──────────────────────────┘
                       │  DuckDB ATTACH (零拷贝)
                       ▼
┌─────────────────────────────────────────────────┐
│         @duckdb/node-api (列式分析引擎)            │
│  7日趋势 │ CPA异常检测 │ 时段效率 │ 月度归档       │
│  COPY TO 'archive/2026-06.parquet'               │
└─────────────────────────────────────────────────┘
```

### 2.2 分工原则

| 引擎 | 角色 | 优势 | 适用场景 |
|------|------|------|---------|
| **SQLite** (better-sqlite3) | 主存储 | 行式存储、事务安全、WAL并发 | 快照写入、反馈记录、元数据管理 |
| **DuckDB** (@duckdb/node-api) | 分析引擎 | 列式存储、向量化并行、聚合快100x | 日报聚合、趋势分析、异常检测、AI训练数据提取 |

### 2.3 为什么不是其他方案

| 方案 | 拒绝理由 |
|------|---------|
| 纯 JSON 文件(现在) | 碎片化、无索引、无法扩展 |
| 只用 SQLite | 聚合查询慢，日报生成仍需全表扫描 |
| 只用 DuckDB | 不适合频繁小事务并发写入 |
| MySQL/PostgreSQL | 需独立服务，PM2管理复杂，杀鸡用牛刀 |
| MongoDB | 文档数据库，时序聚合性能差 |
| InfluxDB/TimescaleDB | 专项时序库，功能丰富但部署重 |

---

## 三、数据库 Schema

### 3.1 核心表

```sql
-- 计划元数据
CREATE TABLE campaigns (
    id              TEXT PRIMARY KEY,
    name            TEXT NOT NULL,
    account_id      TEXT NOT NULL DEFAULT '1842681352509635',
    status          TEXT DEFAULT 'active',      -- active/paused/deleted
    budget_type     TEXT,                        -- '按日预算'/'总预算'
    budget_amount   REAL,
    bid_type        TEXT,                        -- oCPM/oCPC
    bid_amount      REAL,
    created_at      TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    updated_at      TEXT NOT NULL DEFAULT (datetime('now','localtime'))
);

-- 时间序列核心表
CREATE TABLE campaign_snapshots (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    campaign_id     TEXT NOT NULL,
    snapshot_time   TEXT NOT NULL,               -- ISO 8601
    time_slot       TEXT,                        -- 冷启动期/早高峰/午高峰/午后/晚高峰/夜间收尾

    -- 消耗指标
    spend           REAL DEFAULT 0,
    budget_daily    REAL,
    budget_used_pct REAL,

    -- 转化漏斗
    conversions     INTEGER DEFAULT 0,
    leads           INTEGER DEFAULT 0,
    private_msg_open  INTEGER DEFAULT 0,
    private_msg_retain INTEGER DEFAULT 0,
    form_submit     INTEGER DEFAULT 0,
    cpa             REAL,
    cvr             REAL,

    -- 曝光互动
    ctr             REAL,
    cpm             REAL,

    -- 直播指标
    live_views      INTEGER DEFAULT 0,
    live_over_1min  INTEGER DEFAULT 0,
    live_comments   INTEGER DEFAULT 0,

    -- 汇总(冗余加速)
    active_count    INTEGER,
    total_spend     REAL,
    total_conversions INTEGER,

    created_at      TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    FOREIGN KEY (campaign_id) REFERENCES campaigns(id)
);

CREATE INDEX idx_snapshots_campaign_time ON campaign_snapshots(campaign_id, snapshot_time);
CREATE INDEX idx_snapshots_date ON campaign_snapshots(date(snapshot_time));
CREATE INDEX idx_snapshots_time_slot ON campaign_snapshots(time_slot, date(snapshot_time));

-- 告警记录
CREATE TABLE alerts (
    id              TEXT PRIMARY KEY,
    alert_type      TEXT NOT NULL,               -- budget_cap/zero_conv/high_cpa/...
    campaign_id     TEXT,
    severity        TEXT DEFAULT 'warning',      -- low/medium/high
    suggestion      TEXT,
    metric_context  TEXT,                        -- JSON: 触发时指标快照
    time_slot       TEXT,
    created_at      TEXT NOT NULL DEFAULT (datetime('now','localtime'))
);

-- 反馈记录(替代 suggestion-history.json)
CREATE TABLE alert_feedbacks (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    alert_id        TEXT NOT NULL,
    response        TEXT NOT NULL,               -- accept/reject/ignored
    response_time   TEXT,
    created_at      TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    FOREIGN KEY (alert_id) REFERENCES alerts(id)
);

-- 日汇总(替代 daily-YYYY-MM-DD.json)
CREATE TABLE daily_summaries (
    date            TEXT PRIMARY KEY,            -- YYYY-MM-DD
    total_spend     REAL,
    total_conversions INTEGER,
    avg_cpa         REAL,
    avg_ctr         REAL,
    avg_cvr         REAL,
    active_count    INTEGER,
    alert_count     INTEGER,
    budget_used_pct REAL,
    time_slot       TEXT,
    pacing_health   TEXT,                        -- healthy/warning/danger
    created_at      TEXT NOT NULL DEFAULT (datetime('now','localtime'))
);

-- AI优化动作(预留)
CREATE TABLE optimization_actions (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    campaign_id     TEXT NOT NULL,
    action_type     TEXT NOT NULL,               -- pause/resume/budget_adjust/bid_adjust
    reason          TEXT,
    confidence      REAL,
    before_state    TEXT,                        -- JSON
    after_state     TEXT,                        -- JSON
    result          TEXT DEFAULT 'pending',      -- pending/success/failed
    created_at      TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    executed_at     TEXT,
    FOREIGN KEY (campaign_id) REFERENCES campaigns(id)
);
```

### 3.2 数据量估算

| 维度 | 数值 |
|------|------|
| 快照频率 | 每5-15分钟，约96次/天 |
| 活跃计划 | 5-15个/次 |
| 日写入行数 | ~1,000行 |
| 月写入行数 | ~30,000行 |
| 年写入行数 | ~365,000行 |
| 预估年库大小 | 200-500 MB |

---

## 四、检索窗口方案

### 4.1 推荐：扩展 feedback-server (端口8899)

在现有 `feedback-server.mjs` 上新增端点，零额外服务、零新端口。

| 端点 | 方法 | 功能 |
|------|------|------|
| `/query` | GET | **检索窗口** — 暗色主题 SQL 查询面板 |
| `/api/query` | POST | 参数化 SQL 查询(只读)，返回 JSON |
| `/api/charts/daily` | GET | 按日聚合趋势数据 |
| `/api/charts/campaign?id=` | GET | 单计划时序数据 |
| `/api/export?format=csv` | GET | 数据导出 |

### 4.2 检索窗口预览

```
┌─────────────────────────────────────────────────┐
│  检索窗口 — SQL 查询                   端口 8899  │
├─────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────┐    │
│  │ SELECT c.name, s.spend, s.cpa          │    │
│  │ FROM campaign_snapshots s              │    │
│  │ JOIN campaigns c ON c.id = s.campaign_id│   │
│  │ WHERE s.cpa > 100                      │    │
│  │   AND date(s.snapshot_time) = '2026-06-27'│ │
│  │ ORDER BY s.spend DESC LIMIT 20         │    │
│  └─────────────────────────────────────────┘    │
│  [执行查询]  [导出CSV]  [常用查询▼]              │
├─────────────────────────────────────────────────┤
│  name          │ spend  │ cpa   │ conversions   │
│  ──────────────┼────────┼───────┼────────────── │
│  0609-真人...  │ 920.66 │ 115.08│ 8             │
│  0604-真人...  │ 756.30 │ 98.45 │ 7             │
│  ...           │ ...    │ ...   │ ...           │
│                                     共 12 行     │
└─────────────────────────────────────────────────┘
```

### 4.3 辅助工具：DuckDB CLI

直接从 monitor-data/ 目录的 JSON 文件原地查询，无需迁移：

```bash
# 直接查 JSON 快照 — 查近3天 CPA > 100 的计划
duckdb -json -c "
  SELECT snapshot_time, campaign_id, spend, cpa
  FROM read_json('monitor-data/2026-06-2*.json')
  WHERE cpa > 100
  ORDER BY spend DESC
  LIMIT 20
"
```

### 4.4 备选方案对比

| 方案 | 评分 | 理由 |
|------|------|------|
| **扩展 feedback-server** | ★★★★★ | 零新依赖、技术栈一致、PM2已守护 |
| DuckDB CLI 辅助 | ★★★★☆ | 安装简单、直接查JSON、适合临时分析 |
| Datasette (Python) | ★★☆☆☆ | Windows无官方支持、引入Python技术栈 |
| Grafana | ★★☆☆☆ | 重部署、过度设计 |
| SQLite Web (Python) | ★★☆☆☆ | 功能单一、Python依赖 |

---

## 五、分阶段落地计划

### 阶段一：SQLite 替换 JSON 存储（1-2天）

```
1. npm install better-sqlite3
2. 创建 db/schema.sql + db/index.js（连接单例）
3. 修改 monitor-utils.mjs：新增 db 操作函数
4. 修改 oceanengine-monitor-v3.mjs：快照写入改 INSERT
5. 修改 feedback-server.mjs：反馈记录改 SQL
6. 编写迁移脚本：现有 JSON → SQLite 导入
7. 验证：双写对比 1 天，确认数据一致后切纯 SQLite
```

### 阶段二：DuckDB 分析层（3-4天）

```
1. npm install @duckdb/node-api
2. 实现日报聚合查询(替换现有 JSON 遍历)
3. 7日趋势、CPA异常检测、计划效率排名
4. 月度 Parquet 归档脚本
```

### 阶段三：检索窗口上线（与阶段一同步）

```
1. feedback-server.mjs 新增 /query + /api/query 端点
2. 查询面板 HTML 页面
3. 常用查询模板（日消耗TOP10、CPA飙升、掉量计划等）
```

### 阶段四：AI 自动调优（按需）

```
1. 基于 SQLite 历史数据训练 CPA 预测
2. 实现规则引擎：预算调整/暂停建议
3. optimization_actions 表记录所有 AI 决策
4. 通过反馈数据持续优化
```

---

## 六、实施要点

### 6.1 关键配置

```javascript
// db/index.js — WAL 模式(多进程并发)
db.pragma('journal_mode = WAL');
db.pragma('synchronous = NORMAL');
db.pragma('cache_size = 64000');       // 64MB
db.pragma('busy_timeout = 5000');      // 5s 等待锁
db.pragma('foreign_keys = ON');
```

### 6.2 npm 依赖

| 包 | 版本 | 用途 |
|----|------|------|
| `better-sqlite3` | ^11.0 | SQLite 主存储 |
| `@duckdb/node-api` | ^1.4 | DuckDB 分析引擎 |

### 6.3 向后兼容

- 现有 JSON 文件保留 7 天作为回滚备份
- 迁移脚本可逆：SQLite → JSON 导出
- 监控脚本双写验证后再切换

---

## 七、决策总结

| 决策 | 选择 | 核心理由 |
|------|------|---------|
| 主存储 | **SQLite** (better-sqlite3) | 事务安全、WAL并发、单文件、零运维 |
| 分析引擎 | **DuckDB** (@duckdb/node-api) | 列式聚合快100倍、直接 ATTACH SQLite |
| 检索窗口 | **扩展 feedback-server** | 零新服务、技术栈一致、PM2已守护 |
| 长期归档 | **月度 Parquet** | 5-10倍压缩、DuckDB 直接查询 |
| Python方案 | **不用** | Windows无官方支持、引入双语言维护 |
| Grafana等 | **不用** | 过度设计、杀鸡用牛刀 |

> **一句话**：SQLite 负责存，DuckDB 负责算，feedback-server 负责查。一个 npm install 搞定全部。
