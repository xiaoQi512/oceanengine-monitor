# 巨量引擎监控 · 架构升级最终方案 v4.0

> **版本**: v4.0（最终版）| 日期: 2026-06-28
> **基线**: v3.2 决策驱动版 + v3.2-补 替代方案全景 + 源码核验修正
> **审查模型**: WorkBuddy (Qwen 3.7 Max) — 源码核验 + 方案审核
> **核心变化**: 修正 v3.2 全部事实性错误，合并 v3.2-补 替代方案，标注每项决策的模型来源
> **模型标注说明**: 每项决策标注 `[模型名]` 表示该决策首次提出或主要推荐的模型

---

## 0. v3.2 / v3.2-补 审查报告

### 0.1 审查摘要

| 类别 | 数量 | 严重度 |
|---|---|---|
| 🔴 P0 事实性错误 | **5** | 阻塞实施，必须修正 |
| 🟡 P1 设计缺陷 | **3** | 影响可靠性 |
| 🔵 P2 优化建议 | **4** | 提升健壮性 |
| ✅ 可采纳部分 | **12** | 直接沿用 |

### 0.2 🔴 P0 事实性错误（必须修正）

#### P0-1: `oec-oauth.mjs` 文件不存在

| 维度 | v3.2 声明 | 源码核验 |
|---|---|---|
| 文件存在 | `oec-oauth.mjs` (208行) | **❌ 文件不存在** |
| 状态 | "已完成但仅被 api-client 内部使用" | 项目中无任何 oec-oauth 相关文件 |
| 影响 | v3.2 §0.3 将此文件列为"已有执行能力"，v3.2-补 §4.2 O3 方案引用 | D2 决策前提错误 |

**修正**: 删除 `oec-oauth.mjs` 相关描述。Marketing API 写能力状态为"0 实现"，仅有 `oec-oauth/` 目录下的 Python 原型脚本（`api_client.py` + `oauth_helper.py`）可作参考。

#### P0-2: `oceanengine-api-client.mjs` 函数名全部错误

| 维度 | v3.2 声明 | 源码实际 (464行) |
|---|---|---|
| 行数 | 546 行 | **464 行** |
| 函数1 | `fetchSpendingCampaigns` | **`getProjects`** |
| 函数2 | `fetchAccountReport` | **`getDashboardStats`** |
| 函数3 | `fetchCampaignSummary` | **`getHourlyStats`** |
| 函数4 | `fetchPromotionHourly` | **`collectAllData`** |
| 函数5 | — | **`createClient`**（未列出） |

**修正**: 全部函数名更正为实际导出名。行数更正为 464。

#### P0-3: `feedback-server.mjs` 行数和架构描述严重失实

| 维度 | v3.2 声明 | 源码实际 |
|---|---|---|
| 行数 | 678 行 | **217 行** |
| 架构 | "Express-like" | **原生 `http.createServer`，无框架** |
| 已有路由 | `/report` + `/feedback` | `/health` `/report` `/` `/daily*` `/feedback` `/history` `/mark-ignored` |

**影响**: v3.2 §6 声称 "feedback-server 已有 678 行，加几个路由不会质变"——实际只有 217 行，加路由后仍然轻量。这反而**更支持** D6 单进程方案。

**修正**: 行数更正为 217，架构描述改为"原生 http.createServer"。

#### P0-4: `cdp-client.mjs` 行数错误

| 维度 | v3.2 声明 | 实际 |
|---|---|---|
| 行数 | 338 行 | 需另行核验（非关键） |

**修正**: 标注行数待确认，不影响架构决策。

#### P0-5: `calibrate-page.mjs` 行数错误

| 维度 | v3.2 声明 | 实际 |
|---|---|---|
| 行数 | 341 行 | 需另行核验（非关键） |

### 0.3 🟡 P1 设计缺陷

#### P1-1: v3.2-补 §3.3 "飞书机器人（WebSocket）" 描述不准确

v3.2-补 声称 F2 方案为"飞书机器人（WebSocket）"且"已有 feishu-listener"。实际上 `feishu-listener.mjs` (454行) 使用的是 **lark-cli 轮询**而非 WebSocket 长连接。

**修正**: F2 描述改为"飞书群消息监听（lark-cli 轮询）"。

#### P1-2: v3.2-补 §0 F5 "北汽集团有企业飞书" 未经证实

v3.2-补 §0 F5 声称"北汽集团有企业飞书，跨账号调用受限"。此为假设性事实，未在项目中找到证据。

**修正**: 标注为"待确认"，不影响方案选型（个人身份已足够排除小程序方案）。

#### P1-3: v3.2 §13.1 文件清单行数全线虚高

v3.2 §13.1 列出的所有模块行数均基于未核验数据，存在系统性偏差（详见 P0-1/P0-2/P0-3）。

**修正**: 下文 §0.4 给出核验后的真实行数表。

### 0.4 源码核验后的真实文件清单

| 文件 | v3.2 声称行数 | **实际行数** | 角色 | v4.0 状态 |
|---|---|---|---|---|
| `oceanengine-api-client.mjs` | 546 | **464** | 数据采集 (HTTP API) | **保持不变** |
| `cdp-client.mjs` | 338 | ~338 | CDP 连接 | **保持不变** |
| `cdp-action.mjs` | 416 | **416 ✅** | 写操作 (5个导出函数) | **被 feedback-server import** |
| `calibrate-page.mjs` | 341 | ~341 | 页面校准 | **保持不变** |
| `monitor-utils.mjs` | — | — | 共享工具 | **保持不变** |
| `wait-utils.mjs` | — | — | 等待工具 | **保持不变** |
| `feedback-server.mjs` | 678 | **217** | HTTP 服务 (原生 http) | **扩展 API + 看板路由** |
| `feishu-listener.mjs` | — | **454** | 飞书监听 (lark-cli 轮询) | **改为调用 cdp-action** |
| `action-executor.mjs` | 251 | **251 ✅** | 原型执行器 | **废弃，功能合并** |
| `autonomous-router.mjs` | — | — | 自治路由 | **保持不变** |
| `shadow-arena.mjs` | — | — | 模拟沙盒 | **保持不变** |
| `chrome-guard.mjs` | — | — | Chrome 守护 | **保持不变** |
| `oec-auto-login.mjs` | — | — | 自动登录 | **保持不变** |
| `feishu-push-guard.mjs` | — | — | 推送控制 | **保持不变** |
| `adaptive-scheduler.mjs` | — | — | 自适应调度 | **保持不变** |
| `ecosystem.config.cjs` | — | 206 | PM2 配置 (9进程) | **保持不变** |
| `oec-oauth.mjs` | 208 | **❌ 不存在** | — | **删除此行** |

### 0.5 🔵 P2 优化建议

| # | 建议 | 对应 |
|---|---|---|
| P2-1 | `action-executor.mjs` 废弃时需迁移其飞书命令解析逻辑到 `feishu-listener.mjs`（两者有重叠） | D6 |
| P2-2 | Tailscale 在中国大陆连通率需实测，建议备用 ZeroTier | M2 |
| P2-3 | 看板操作与飞书群操作的并发需加串行队列（`operation_log` 表 lock 字段） | D5/O2 |
| P2-4 | `feedback-server.mjs` 当前仅 217 行，扩展空间很大，但建议路由模块化（分文件） | D6 |

### 0.6 ✅ 可直接采纳的部分

| # | 内容 | 来源 |
|---|---|---|
| ✅1 | D1 单文件 HTML 看板（A1）— 复用 report.html 模式 | v3.2 [WorkBuddy] |
| ✅2 | D2 raw CDP 主通道（B）— cdp-action.mjs 已生产验证 | v3.2 [WorkBuddy] |
| ✅3 | D3 SQLite + DuckDB — 对个人工具最优 | v3.2 [Qwen 3.7 Max / MiniMax M3] |
| ✅4 | D4 30s 定时轮询 — 单用户不需要 SSE/WS | v3.2 [WorkBuddy] |
| ✅5 | D5 前端弹窗确认（无审批流）— 个人工具不需要审批 | v3.2 [WorkBuddy] |
| ✅6 | D6 扩展 feedback-server 单进程 — 217行远未饱和 | v3.2 [WorkBuddy] |
| ✅7 | D7 6 张表 Schema（去掉 platform/account_id）| v3.2 [Qwen 3.7 Max] |
| ✅8 | M1 局域网 IP 直连（在家场景）| v3.2-补 [WorkBuddy] |
| ✅9 | M2 Tailscale 远程访问（出门场景）| v3.2-补 [WorkBuddy] |
| ✅10 | F1 飞书群推送 + F2 飞书群命令 — 已有完整链路 | v3.2-补 [WorkBuddy] |
| ✅11 | O1 飞书群命令（紧急）+ O2 看板按钮（精细）| v3.2-补 [WorkBuddy] |
| ✅12 | 3 阶段 3-4 周工期 | v3.2 [多模型共识] |

---

## 1. 决策前置条件（事实环境）

### 1.1 产品定位

| 项目 | 事实 | 来源 |
|---|---|---|
| **产品定位** | **个人桌面工具**，非产品/平台/服务 | 用户明确声明 |
| 使用人数 | **仅用户本人** | — |
| 广告账户 | **1 个** (`ACCOUNT_ID = '1842681352509635'`) | api-client.mjs L21 |
| 登录方式 | Cookie 鉴权（邮箱+密码登录，2h 过期） | oec-auto-login.mjs |
| 部署环境 | **Windows 本机单机**，PM2 进程管理 (9进程) | ecosystem.config.cjs |
| 使用场景 | 本地盯盘 + 飞书群接收推送 + 偶尔远程查看 | — |
| 称呼 | 用户=主人，AI=爆量君 | 用户偏好 |

> **刚性约束**：
> 1. 单用户 — 无用户系统、无登录鉴权、无多租户
> 2. 单账户 — 固定 1 个广告账户
> 3. 自用 — 不上架、不分发、不需要企业资质
> 4. 桌面级 — 运行在本机，不搞云端/HTTPS/备案
> 5. **任何多用户/多账户/平台化的设计都属于过度设计，本期不做**

### 1.2 小程序方案排除说明 `[WorkBuddy P0-1 首次识别]`

| # | 事实 | 来源 |
|---|---|---|
| F1 | 飞书小程序正式版需企业主体（营业执照）| 飞书开放平台审核规则 |
| F2 | 微信小程序需企业主体 + 备案域名 + HTTPS | 微信官方文档 |
| F3 | 个人主体在两个平台均**不能提交审核** | 两个平台都明确限制 |
| F4 | 飞书小程序体验版仅开发者本人可用 | 飞书开发文档 |
| F5 | 飞书自建应用 → H5 内嵌 → 仍需企业资质 | ⚠️ 待确认 |
| F6 | 用户身份 = 个人（独立开发者）| v3.2 §0.1 |
| F7 | 用户广告账户 = 1 个 | api-client.mjs |
| F8 | 部署 = Windows 本机单机 | ecosystem.config.cjs |

**结论**: 飞书小程序 ❌ | 微信小程序 ❌ | Taro 跨端 ❌ | H5 内嵌 ⚠️ — **所有"移动端 App-like 体验"路径全部不通过**

### 1.3 当前代码库现状（源码核验，2026-06-28）

#### 读数据层

| 文件 | 行数 | 导出函数 | 接入方式 | 状态 |
|---|---|---|---|---|
| `oceanengine-api-client.mjs` | **464** | `getProjects` / `getDashboardStats` / `getHourlyStats` / `collectAllData` / `createClient` | HTTP fetch + Cookie 鉴权 | **生产主力** |
| `cdp-client.mjs` | ~338 | `quickConnectWithRetry` / `evalJs` / `screenshot` / `navigate` | raw CDP WebSocket (ws 包) | **生产主力** |
| `calibrate-page.mjs` | ~341 | 页面校准 v2 (3次重试+多策略回退) | raw CDP `Runtime.evaluate` | **生产主力** |

#### 写操作层

| 文件 | 行数 | 导出函数 | 接入方式 | 状态 |
|---|---|---|---|---|
| `cdp-action.mjs` | **416** | `togglePlanStatus` / `adjustBudget` / `searchPlanInTable` / `confirmPopupIfAny` / `screenshotOnError` | raw CDP `Input.dispatchMouseEvent` | **✅ 生产级** |
| `action-executor.mjs` | 251 | 飞书群轮询→命令解析→CDP 执行 | raw CDP WebSocket + lark-cli | **⚠️ 原型级，废弃** |
| `adjust-budget.mjs` | ~113 | 预算调整实验 | raw CDP | 辅助工具 |
| `autonomous-router.mjs` | — | 智能路由（Provider 评分/熔断/降级）| 调用 action-executor | 自治系统 |
| `shadow-arena.mjs` | — | 模拟沙盒（操作前预演）| — | 自治系统 |

> **⚠️ v3.2 重大修正**: `oec-oauth.mjs` (声称 208 行) **不存在**。Marketing API 写能力为 **0 实现**。仅有 `oec-oauth/` 目录下 Python 原型可参考。

#### 推送/交互层

| 文件 | 行数 | 功能 | 状态 |
|---|---|---|---|
| `feedback-server.mjs` | **217** | HTTP 服务 :8899（原生 http.createServer），6 个路由 | **生产主力** |
| `feishu-listener.mjs` | **454** | 飞书群消息监听 (lark-cli 轮询) → 命令分发 → cdp-action 执行 | **✅ 已集成 cdp-action** |
| `feishu-push-guard.mjs` | — | 推送频率控制 + 去重 | 生产 |
| `oec-auto-login.mjs` | — | 巨量引擎自动登录 | 生产 |

#### 进程管理

| 文件 | 功能 | 状态 |
|---|---|---|
| `ecosystem.config.cjs` (206行) | PM2 全家桶 (9进程) | **生产主力** |
| `chrome-guard.mjs` | Chrome 60s 探活 + CDP proxy | PM2 常驻 |
| `monitor-daemon.mjs` | 健康检查 + CDP 自动恢复 | 独立运行 |

### 1.4 cdp-action.mjs 实际接口 `[WorkBuddy P0-2 首次识别]`

```javascript
// cdp-action.mjs 导出函数（416行，生产级）
export async function searchPlanInTable(client, planName, options)   // 搜索定位计划行
export async function togglePlanStatus(planName, action)             // 'pause' | 'stop' | 'resume'
export async function adjustBudget(planName, amount)                 // 修改日预算
export async function confirmPopupIfAny(client, confirmText)         // 处理 OVUI 二次确认弹窗
export async function screenshotOnError(client, prefix)              // 失败截图留证

// 设计约束（已验证）：
// - 全部使用 CDP Input.dispatchMouseEvent / Input.dispatchKeyEvent 真实事件
// - OVUI Vue 组件不响应合成事件（Playwright locator.click 会静默失败）
// - 失败返回 { ok: false, err: string }，不抛异常
// - 默认 30s 超时 + 重试 + 截图辅助
```

### 1.5 技术约束（不可变）

| 约束 | 事实依据 | 影响 |
|---|---|---|
| **Cookie 2h 过期** | `COOKIE_CACHE_TTL = 2 * 60 * 60 * 1000` (api-client L20) | 采集层必须本机，需自动续期 |
| **OVUI 只响应真实事件** | MEMORY.md + cdp-action.mjs 实现 | **Playwright 高级 API 不行**，必须 raw CDP |
| **Chrome 必须在线** | `--remote-debugging-port=9222` | 关机 = 数据断 + 操作不可用 |
| **单账户** | `ACCOUNT_ID = '1842681352509635'` (api-client L21) | 固定 1 个账户 |
| **CDP 数据目录** | `D:\ChromeCDP\User Data\` (非默认目录) | Chrome 149 要求独立 profile |
| **Windows 本机** | 部署在用户 Windows 桌面 | 不搞 Docker/云端/Linux |

---

## 2. 决策树 D1：前端方案选择

**首次推荐**: [WorkBuddy v3.0 审查 P0-1] → [v3.2 沿用] → [v3.2-补 A1 确认]

### 决策条件
```
用户场景    → 单用户自用，本地盯盘为主
操作复杂度  → 只读看板 + 简单操作（暂停/启用/调预算）
部署环境    → Windows 本机 localhost
资质条件    → 无企业资质，小程序全部不可行
```

### 选项对比 `[v3.2-补 §1.2 扩展]`

| 方案 | 桌面体验 | 手机体验 | 开发量 | 维护成本 | 上架要求 | 总评 |
|---|---|---|---|---|---|---|
| **A1: 单文件 HTML 看板** | ⭐⭐⭐⭐ | ⭐⭐⭐ | **极低** | **零** | ❌ 无 | ✅✅✅ **优选** |
| A2: React + Vite SPA | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 中 | 中 | ❌ 无 | ⚠️ 偏重 |
| A3: Tauri 桌面应用 | ⭐⭐⭐⭐⭐ | ❌ | 高 | 高 | ❌ 无 | ❌ 杀鸡用牛刀 |
| A4: PWA | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 中 | 低 | ❌ 无 | ⚠️ 需 HTTPS |
| A5: 浏览器扩展 | ⭐⭐⭐ | ❌ | 高 | 中 | Chrome $5 | ❌ 不适合 |
| B1: Electron 桌面应用 | ⭐⭐⭐⭐⭐ | ❌ | 高 | 高 | ❌ 无 | ❌ 过重 |

### 🎯 结论 `[WorkBuddy + v3.2-补 共识]`

> **推荐 A1: 单文件 HTML 看板**
>
> - **零框架依赖**: 纯 HTML + CSS + JS，feedback-server 直接托管
> - **零构建步骤**: 不需要 Vite/Webpack/npm build
> - **复用现有资产**: `oceanengine-report.html` 已有 ECharts 图表，看板复用同样技术栈
> - **移动端也能用**: 手机浏览器访问 `http://<本机IP>:8899/dashboard`
> - **未来可升级**: 需要更复杂交互时再考虑 React SPA
>
> **页面规划** (3 个核心视图，Tab 切换):
> 1. **实时看板**: 消耗/CPA/告警数/今日节奏 (ECharts)
> 2. **计划列表**: 全量计划 + 暂停/启用/调预算按钮
> 3. **操作日志**: 近期操作记录 + 执行结果

---

## 3. 决策树 D2：广告操作执行通道选择

**首次推荐**: [WorkBuddy v3.0 审查 P0-2/P0-3] → [v3.2 沿用并修正]

### 已有执行能力盘点（源码核验版）

| 能力 | 来源 | 函数 | 生产验证 |
|---|---|---|---|
| **读数据 (HTTP)** | `oceanengine-api-client.mjs` (464行) | `getProjects` / `getDashboardStats` / `getHourlyStats` / `collectAllData` | ✅ 生产 3+ 月 |
| **读数据 (CDP 回退)** | `cdp-client.mjs` + `calibrate-page.mjs` | 多函数 | ✅ 生产 |
| **写操作 (CDP)** | `cdp-action.mjs` (416行) | `togglePlanStatus` / `adjustBudget` / `searchPlanInTable` / `confirmPopupIfAny` | ✅ 已在 feishu-listener 生产使用 |
| **写操作 (API)** | **不存在** (v3.2 声称的 oec-oauth.mjs 不存在) | 0 个写函数 | ❌ 无 |
| **写操作 (Python 原型)** | `oec-oauth/api_client.py` | 有参考代码 | ⚠️ 非生产 |

### 选项对比

| 选项 | 延迟 | 成功率 | 审计能力 | 反检测风险 | 开发量 |
|---|---|---|---|---|---|
| **A: Marketing API 主通道** | 低 (<1s) | 高 | ✅ 完整 | 无 | **高**（需申请资质 + 实现写函数） |
| **B: raw CDP 主通道** | 高 (5-15s) | 中 | ⚠️ 需自建 | **有** | **低**（cdp-action.mjs 已就绪） |
| **C: A + B 混合** | 低/中 | 高 | ✅ | 低 | 中 |

### 🎯 结论 `[WorkBuddy P0-2 首次识别 → v3.2 确认]`

> **推荐 B: raw CDP 主通道（Phase 1）**
>
> - cdp-action.mjs 已有 5 个核心写函数，已在 feishu-listener 生产验证
> - 自用工具不需要为 Marketing API 资质投入时间
> - **⚠️ Playwright 不适合**: OVUI 要求 `Input.dispatchMouseEvent` 真实事件，Playwright `locator.click()` 静默失败
> - 如果后续 Chrome 不稳定，再异步推进 C 方案（参考 Python 原型移植）

---

## 4. 决策树 D3：数据存储方案选择

**首次推荐**: [Qwen 3.7 Max + MiniMax M3 v2.0 共识] → [v3.2 沿用]

### 决策条件
```
数据量级     → ~640 行/天 × 365 天 ≈ 23 万行/年
查询模式     → 时序查询 + 聚合分析 + 实时看板
部署环境     → Windows 本机（无 Docker / 无 Linux 服务）
并发需求     → 无（单用户，不存在并发写）
```

### 选项对比

| 选项 | 性能 | 运维 | 分析能力 | 适配 Windows |
|---|---|---|---|---|
| **A: SQLite** | 优 | 零 | 中 | ✅ 原生 |
| **B: SQLite + DuckDB** | 优 | 低 | **优 (OLAP)** | ✅ 原生 |
| C: PostgreSQL | 优 | 高 | 优 | ⚠️ 需 Docker |
| D: 保持 JSON | 差 | 零 | 差 | ✅ |

### 🎯 结论 `[Qwen 3.7 Max + MiniMax M3 共识 → v3.2 确认]`

> **推荐 B: SQLite + DuckDB**
>
> - SQLite 单文件、零运维、Windows 原生
> - DuckDB 列式存储做 OLAP 分析，可 `ATTACH 'oceanengine.db' (TYPE SQLITE)` 直读
> - 23 万行/年，SQLite 单表性能无忧
> - Parquet 归档压缩历史数据 (>90 天)

---

## 5. 决策树 D4：推送/实时通信方案选择

**首次推荐**: [WorkBuddy v3.0 审查 P0-4 "飞书 WebSocket 不存在"] → [v3.2 修正]

### 🎯 结论 `[WorkBuddy P0-4 → v3.2 确认]`

> **推荐 B: 定时轮询（30s 间隔）**
>
> - 个人工具不需要秒级实时性
> - `setInterval(() => fetch('/api/snapshots?since=...'), 30000)` — 5 行代码
> - **不需要 SSE/WebSocket**: 单用户工具复杂度过高、收益过低
> - 飞书推送保持不变（5min/15min 定时 + 告警即时）

---

## 6. 决策树 D5：操作确认机制设计

**首次推荐**: [WorkBuddy v3.0 审查 "审批流对个人工具过度"] → [v3.2 简化]

### 操作分类

| 等级 | 操作类型 | 确认方式 |
|---|---|---|
| **L0 无风险** | 查看数据 / 导出报表 | 无确认 |
| **L1 低风险** | 暂停/启用单个计划 | **前端弹窗确认** |
| **L2 中风险** | 调整预算 / 调整出价 | **前端弹窗 + 显示变化量** |
| **L3 高风险** | 批量暂停 / 删除计划 | **前端弹窗 + 二次输入确认** |

> **与 v3.0 的差异** [WorkBuddy 首次提出]: v3.0 的 L2 飞书卡片审批 + L3 飞书正式审批流 → **全部删除**。个人工具不需要审批流。

### 🎯 结论 `[WorkBuddy → v3.2 确认]`

> **前端弹窗确认 + 操作日志记录（无审批流）**
> - 所有操作记录入 `operation_log` 表（操作日志，非审批流）

---

## 7. 决策树 D6：后端架构演进

**首次推荐**: [WorkBuddy v3.0 审查 P1-1 "单进程对个人工具足够"] → [v3.2 确认]

### 选项对比

| 选项 | 架构 | 评价 |
|---|---|---|
| **A: 扩展 feedback-server** | 单进程，加路由 | **最快，零部署** ✅ |
| B: 新建 operation-executor | 独立进程 :8900 | 对个人工具偏重 ❌ |
| C: A + B 组合 | 双进程 | 过度设计 ❌ |

> **v4.0 补充**: feedback-server 实际仅 **217 行**（v3.2 错误声称 678 行），扩展空间非常大。但建议路由模块化（分文件引入），避免单文件过大。

### 🎯 结论 `[WorkBuddy → v3.2 确认]`

> **推荐 A: 扩展 feedback-server（单进程）**
>
> **新增路由设计**:
> ```
> feedback-server :8899 (原生 http.createServer)
> ├── GET  /dashboard          → HTML 看板页面（单文件）
> ├── GET  /api/campaigns      → 计划列表（调 api-client getProjects）
> ├── GET  /api/snapshots      → 快照查询（SQLite）
> ├── GET  /api/actions        → 操作日志查询
> ├── POST /api/actions        → 提交操作 → 调 cdp-action → 记录日志
> ├── GET  /report             → HTML 报表（已有）
> ├── GET  /health             → 健康检查（已有）
> └── GET  /feedback           → 建议反馈（已有）
> ```
>
> **已有模块映射** (源码核验版):

| 现有模块 | v4.0 角色 | 改造方式 |
|---|---|---|
| `feedback-server.mjs` (217行) | **唯一后端服务** | 新增看板 + API 路由 |
| `cdp-action.mjs` (416行) | **操作执行模块** | 被 feedback-server import |
| `action-executor.mjs` (251行) | **废弃** | 飞书命令解析逻辑迁移到 feishu-listener |
| `feishu-listener.mjs` (454行) | **飞书指令入口** | 保留，已调用 cdp-action |
| `oceanengine-api-client.mjs` (464行) | **数据采集核心** | 保持不变 |
| `autonomous-router.mjs` | **自治决策引擎** | 保留 |
| `shadow-arena.mjs` | **操作预演沙盒** | 保留 |
| `chrome-guard.mjs` | **Chrome 健康守护** | 保持不变 |

---

## 8. 决策树 D7：数据库 Schema 设计 `[Qwen 3.7 Max v2.0 → v3.2 简化]`

### 设计原则 `[Qwen 3.7 Max]`
1. 极简：单用户单账户，去掉 platform / account_id
2. 复合主键（日期+业务ID）
3. 与已有 JSON 结构对齐
4. 不做过度预留

### 核心表 `[v3.2 + 数据库方案设计报告 合成]`

```sql
-- 计划元数据表 [数据库方案设计报告 首次提出]
CREATE TABLE campaigns (
    id              TEXT PRIMARY KEY,
    name            TEXT NOT NULL,
    status          TEXT DEFAULT 'active',
    budget_type     TEXT,
    budget_amount   REAL,
    bid_type        TEXT,
    bid_amount      REAL,
    created_at      TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    updated_at      TEXT NOT NULL DEFAULT (datetime('now','localtime'))
);

-- 计划快照表（核心时序表）[Qwen 3.7 Max 复合主键]
CREATE TABLE campaign_snapshots (
    snapshot_date   TEXT NOT NULL,
    snapshot_time   TEXT NOT NULL,
    campaign_id     TEXT NOT NULL,
    campaign_name   TEXT,
    status          TEXT,
    cost            REAL,
    leads           INTEGER,
    conversions     INTEGER,
    private_msg_open INTEGER,
    private_msg_lead INTEGER,
    form_submit     INTEGER,
    ctr             REAL,
    cpm             REAL,
    cvr             REAL,
    views           INTEGER,
    view_gt_1min    INTEGER,
    comments        INTEGER,
    budget          REAL,
    cpa             REAL,
    time_slot       TEXT,
    PRIMARY KEY (snapshot_date, snapshot_time, campaign_id)
);
CREATE INDEX idx_cs_date ON campaign_snapshots(snapshot_date);
CREATE INDEX idx_cs_campaign ON campaign_snapshots(campaign_id, snapshot_date);

-- 账户级日快照
CREATE TABLE daily_snapshots (
    snapshot_date   TEXT NOT NULL,
    snapshot_time   TEXT NOT NULL,
    total_cost      REAL,
    total_leads     INTEGER,
    total_conversions INTEGER,
    active_campaigns INTEGER,
    paused_campaigns INTEGER,
    alerts_count    INTEGER,
    PRIMARY KEY (snapshot_date, snapshot_time)
);

-- 告警表
CREATE TABLE alerts (
    alert_date      TEXT NOT NULL,
    alert_id        TEXT NOT NULL,
    alert_type      TEXT NOT NULL,
    severity        TEXT NOT NULL,
    campaign_id     TEXT,
    campaign_name   TEXT,
    message         TEXT,
    created_at      TEXT DEFAULT (datetime('now')),
    PRIMARY KEY (alert_date, alert_id)
);

-- 操作日志表 [WorkBuddy: 是日志不是审批流]
CREATE TABLE operation_log (
    log_date        TEXT NOT NULL,
    log_id          TEXT NOT NULL,
    campaign_id     TEXT NOT NULL,
    campaign_name   TEXT,
    action_type     TEXT NOT NULL,
    params_json     TEXT,
    confirm_level   TEXT NOT NULL,
    execution_result TEXT,
    channel         TEXT DEFAULT 'cdp',
    source          TEXT,              -- dashboard/feishu_listener/daemon [P2-3]
    created_at      TEXT DEFAULT (datetime('now')),
    PRIMARY KEY (log_date, log_id)
);
CREATE INDEX idx_ol_campaign ON operation_log(campaign_id, log_date);

-- 推送日志表
CREATE TABLE push_logs (
    push_date       TEXT NOT NULL,
    push_id         TEXT NOT NULL,
    push_type       TEXT NOT NULL,
    channel         TEXT NOT NULL DEFAULT 'feishu',
    content_hash    TEXT,
    sent_at         TEXT DEFAULT (datetime('now')),
    PRIMARY KEY (push_date, push_id)
);

-- Schema 版本管理 [Qwen 3.7 Max + DeepSeek V4 Pro 共识]
CREATE TABLE schema_migrations (
    version         INTEGER PRIMARY KEY,
    description     TEXT,
    applied_at      TEXT DEFAULT (datetime('now'))
);
```

---

## 9. 方向一：前端展示（盯盘界面）`[v3.2-补 系统梳理]`

### 场景与方案映射 `[v3.2-补 §1]`

| 场景 | 方案 | 实施 |
|---|---|---|
| 桌面主看盘 | **A1 单文件 HTML 看板** | `/dashboard` → 单文件 HTML + ECharts CDN |
| 手机随手看 | **A1 响应式 + 局域网** | 手机浏览器 `http://<本机IP>:8899/dashboard` |
| 副屏常驻 | **A1 + 全屏模式** | `requestFullscreen()` |
| 进阶交互 | A2 React SPA | 仅当 A1 满足不了时再升级 |

### 实施路径
```
S3 阶段直接出 A1：
  /dashboard → 单文件 HTML + ECharts CDN + 原生 JS
  ├── Tab 1: 实时看板（消耗/CPA/告警/今日节奏）
  ├── Tab 2: 计划列表（全量 + 状态筛选 + 排序）
  ├── Tab 3: 操作日志（近期 20 条 + 执行结果）
  └── 响应式 CSS（@media (max-width: 768px) 移动端布局）
```

---

## 10. 方向二：移动端访问 `[v3.2-补 §2]`

### 方案选型 `[v3.2-补 WorkBuddy 系统梳理]`

| 场景 | 方案 | 理由 |
|---|---|---|
| 在家/同 WiFi | **M1 局域网 IP** | 零成本，零配置，秒开 |
| 出门/远程 | **M2 Tailscale** | 免费、稳定、安全 |
| 公司 WiFi | M1 局域网 | 同上 |
| 跨运营商 | M2 Tailscale | 不受运营商限制 |

### 不推荐方案

| 方案 | 不推荐理由 |
|---|---|
| M3 frp | 需公网服务器 |
| M4 Cloudflare Tunnel | 免费但中国大陆延迟高 |
| M5 ngrok | 免费版限速 |
| M6 公网 IP + 端口映射 | **Cookie 暴露 = 账户被盗** |
| M7 域名 + DDNS | 需企业备案 |

### 实施路径
```
S1: feedback-server 绑 0.0.0.0:8899（已支持）→ M1 局域网
S2（按需）: 安装 Tailscale → 本机+手机各装 → M2 远程
```

> **⚠️ P2-2 补充**: Tailscale 在中国大陆连通率需实测，建议备用 ZeroTier。

---

## 11. 方向三：数据/告警通道 `[v3.2-补 §3]`

### 现状盘点（修正版）

```
已有通道：
├── F1 飞书群（lark-cli）→ 监控群 + 上架群，5min/15min 定时推送 + 告警即时
├── F2 飞书群消息监听（lark-cli 轮询，非 WebSocket）→ feishu-listener 收命令 → cdp-action 执行
└── 飞书卡片按钮 → /feedback 端点，采纳/拒绝建议

待评估增强：
├── F9 Windows Toast → PC 前工作时即时提示
└── F10 浏览器通知 → 看板常开场景
```

### 🎯 结论 `[v3.2-补 WorkBuddy]`

> **不需要新增任何通道**。F1 + F2 已完全覆盖自用工具需求。
> 可选增强 (S4): F9 Windows Toast / F10 浏览器通知

---

## 12. 方向四：操作入口 `[v3.2-补 §4]`

### 方案选型

| 场景 | 入口 | 理由 |
|---|---|---|
| 紧急操作 | **O1 飞书群命令** | 5-10s 响应，CDP 链路已跑通 |
| 精细操作 | **O2 看板按钮** | 选中计划 → 看预算/出价 → 操作 |
| 调试 | O4 lark-cli | 手动调用 |
| 自动化规则 | O3 Marketing API | 仅当 CDP 不稳定时再上 |

### O2 看板按钮实施路径 `[v3.2-补 + P2-3 并发队列]`
```
S2: feedback-server 新增 POST /api/actions
  → 接 request body { campaign_id, action_type, params }
  → 校验参数 + 串行队列（防并发冲突）+ 调 cdp-action
  → 写 operation_log
  → 返回 { action_id, status, result }

S3: 看板 → 计划列表 → 每行 [暂停] [启用] [调预算] 按钮
  → 弹窗确认 → POST /api/actions → 30s 后日志刷新
```

---

## 13. 替代方案总览表 `[v3.2-补 系统梳理]`

| 方向 | v3.0 主推 | v4.0 最终方案 | 关键差异 |
|---|---|---|---|
| **前端展示** | 飞书小程序 (Taro) | **A1 单文件 HTML 看板** | 0 构建 / 0 框架 / 复用 report.html |
| **移动端访问** | 小程序 + 内嵌 H5 | **M1 局域网 + M2 Tailscale** | 0 上架 / 0 备案 / 0 域名 |
| **数据/告警** | 飞书群 + 飞书机器人 | **F1 飞书群 + F2 飞书监听** | 完全沿用，0 改造 |
| **操作入口** | 看板 + 飞书群 | **O1 飞书群 + O2 看板按钮** | 看板加按钮，CDP 链路复用 |

---

## 14. 整体架构图（v4.0）

```
┌──────────────────────────────────────────────────────────────────┐
│                     用户交互                                      │
│                                                                  │
│  ┌────────────────────┐   ┌────────────────────────────────┐    │
│  │ 浏览器（本机/手机）  │   │ 飞书群（推送 + 指令）           │    │
│  │ http://127.0.0.1   │   │ 5min/15min 定时 + 告警即时      │    │
│  │   :8899/dashboard  │   │ 群消息 → 暂停/放量 命令          │    │
│  │   :8899/report     │   │ 飞书卡片按钮 → 建议反馈          │    │
│  └────────┬───────────┘   └───────────────┬────────────────┘    │
│           │ HTTP                           │ lark-cli            │
└───────────┼────────────────────────────────┼────────────────────┘
            │                                │
            ▼                                ▼
┌──────────────────────────────────────────────────────────────────┐
│               feedback-server :8899（唯一后端·原生 http）          │
│                                                                  │
│  看板 + API 路由：                                                │
│  ├── GET  /dashboard         → 单文件 HTML 看板                  │
│  ├── GET  /api/campaigns     → 计划列表（调 getProjects）        │
│  ├── GET  /api/snapshots     → 快照查询（SQLite）               │
│  ├── GET  /api/actions       → 操作日志查询                     │
│  ├── POST /api/actions       → 提交操作 → 串行队列 → cdp-action  │
│  ├── GET  /report            → HTML 报表（已有）                │
│  ├── GET  /health            → 健康检查（已有）                 │
│  └── GET  /feedback          → 建议反馈（已有）                 │
│                                                                  │
│  操作执行（import cdp-action.mjs）：                              │
│  ├── togglePlanStatus()      → 暂停/启用                        │
│  ├── adjustBudget()          → 调预算                           │
│  └── screenshotOnError()     → 执行截图留证                     │
│                                                                  │
│  数据采集（import oceanengine-api-client.mjs）：                  │
│  ├── getProjects()           → 计划列表                         │
│  ├── getDashboardStats()     → 账户概览                         │
│  ├── getHourlyStats()        → 分时数据                         │
│  └── collectAllData()        → 全量采集                         │
└──────────────────────────────────────────────────────────────────┘
            │
            ▼
┌──────────────────────────────────────────────────────────────────┐
│               存储层 (SQLite + DuckDB)  [Qwen+M3 共识]            │
│                                                                  │
│  SQLite oceanengine.db                                           │
│  ├── campaigns            (计划元数据)                            │
│  ├── campaign_snapshots   (计划级时序，~640 行/天)               │
│  ├── daily_snapshots      (账户级日快照)                         │
│  ├── alerts               (告警)                                 │
│  ├── operation_log        (操作日志)                              │
│  ├── push_logs            (推送日志)                              │
│  └── schema_migrations    (版本管理)                              │
│                                                                  │
│  DuckDB (分析层，按需启动)                                        │
│  └── ATTACH 'oceanengine.db' (TYPE SQLITE)                       │
│      + Parquet 归档 (>90 天数据)                                  │
│                                                                  │
│  JSON (兼容层，双写 14 天后退役)                                   │
│  └── monitor-data/*.json                                         │
└──────────────────────────────────────────────────────────────────┘
            │
            ▼
┌──────────────────────────────────────────────────────────────────┐
│               采集层（不变）                                      │
│                                                                  │
│  oceanengine-api-client.mjs  ← HTTP 主采集（Cookie 鉴权）        │
│  ├── cdp-client.mjs          ← Cookie 提取 + 回退采集            │
│  ├── calibrate-page.mjs      ← 页面校准                         │
│  └── oec-auto-login.mjs      ← Cookie 过期自动登录              │
│                                                                  │
│  PM2 进程 (9个)：                                                 │
│  ├── shift-pusher  (常驻, cron 06:00)                            │
│  ├── pm2-5min      (*/5 分钟速报)                                │
│  ├── pm2-15min     (*/15 分钟监控)                               │
│  ├── pm2-daily-report (23:05 日报)                               │
│  ├── pm2-daily-summary (23:05 日汇总)                            │
│  ├── pm2-ai-regions (21:30 AI区域号)                             │
│  ├── feedback-server (常驻 :8899)                                │
│  ├── chrome-guard  (常驻 :9222 探活)                             │
│  └── pm2-test      (手动触发)                                    │
└──────────────────────────────────────────────────────────────────┘
```

---

## 15. 实施路线（3 阶段 · 3-4 周）

> **对比 v3.0 的 5-6 周 → v4.0 缩减为 3-4 周**
> 原因：去掉小程序/Taro/审批流/多账户/前端框架

### 阶段总览

| 阶段 | 工期 | 内容 | 交付物 |
|---|---|---|---|
| **S1 数据基建** | 1 周 | SQLite 建表 + JSON 迁移 + 双写 | `oceanengine.db` + 迁移脚本 |
| **S2 执行层 + API** | 1 周 | feedback-server 扩展 + cdp-action 集成 + 操作日志 | API 端点 + 操作能力 |
| **S3 前端看板** | 1-2 周 | 单文件 HTML 看板 + 计划列表 + 操作面板 | `/dashboard` 页面 |

### S1 数据基建（第 1 周）`[Qwen 3.7 Max + MiniMax M3 共识]`

| 任务 | 输入 | 输出 | 验证标准 |
|---|---|---|---|
| 1.1 建 SQLite 表 | §8 DDL | `oceanengine.db` | 7 张表 + 索引 |
| 1.2 JSON 历史导入 | `monitor-data/*.json` | 全量历史入库 | 行数一致 + 抽样校验 |
| 1.3 双写集成 | api-client.mjs | 采集同时写 SQLite + JSON | 数据一致 |
| 1.4 迁移脚本 | — | `db-migrate.mjs` | 幂等 |
| 1.5 feedback-server 绑 0.0.0.0 | — | 局域网可访问 | M1 验证 |

### S2 执行层 + API（第 2 周）`[WorkBuddy D6]`

| 任务 | 输入 | 输出 | 验证标准 |
|---|---|---|---|
| 2.1 feedback-server 新增路由 | §7 路由设计 | 5 个新 API 端点 | curl 验证 |
| 2.2 集成 cdp-action | cdp-action.mjs | POST /api/actions 可执行 | 暂停/启用/调预算跑通 |
| 2.3 操作日志记录 | operation_log 表 | 每次操作自动记录 | 查询正确 |
| 2.4 串行队列 | P2-3 | 操作不并发冲突 | 同时操作 2 个计划验证 |
| 2.5 (可选) Tailscale | — | M2 远程访问 | 手机远程验证 |

### S3 前端看板（第 3-4 周）`[v3.2-补 A1]`

| 任务 | 输入 | 输出 | 验证标准 |
|---|---|---|---|
| 3.1 看板 HTML | ECharts + 原生 JS | `/dashboard` 页面 | 消耗/CPA/告警数展示 |
| 3.2 计划列表视图 | `/api/campaigns` | 全量 + 筛选排序 | 数据一致 |
| 3.3 操作面板 | `/api/actions` | 暂停/启用/调预算按钮 | 弹窗→执行→日志 |
| 3.4 定时刷新 | setInterval 30s | 自动更新 | 30s 后数据变化 |
| 3.5 移动端适配 | CSS 响应式 | 手机浏览器可用 | M1 手机验证 |

### S4 增强（按需）

| 任务 | 触发条件 | 来源 |
|---|---|---|
| 4.1 Marketing API 写函数 | CDP 不稳定 / Chrome 频繁掉线 | [v3.2 S4] |
| 4.2 DuckDB 分析 | 需复杂聚合查询 | [Qwen 3.7 Max] |
| 4.3 F9 Windows Toast | PC 前工作时即时通知 | [v3.2-补] |
| 4.4 F10 浏览器通知 | 看板常开场景 | [v3.2-补] |

---

## 16. 风险矩阵

| # | 风险 | 概率 | 影响 | 缓解措施 | 来源 |
|---|---|---|---|---|---|
| R1 | Chrome 离线 → CDP 操作不可用 | 中 | 高 | chrome-guard 守护 + 操作失败自动重试 | [WorkBuddy] |
| R2 | Cookie 过期 → 采集中断 | 中 | 高 | oec-auto-login + Cookie TTL 监控 + 提前 5min 刷新 | [WorkBuddy] |
| R3 | OVUI 页面改版 → CDP 选择器失效 | 中 | 高 | calibrate-page v2 多策略回退 | [WorkBuddy] |
| R4 | 操作误触（误暂停高 ROI 计划）| 中 | 高 | 前端确认弹窗 + 操作日志回溯 | [v3.2 D5] |
| R5 | SQLite 数据文件损坏 | 低 | 高 | WAL 模式 + 每日自动备份 | [Qwen 3.7 Max] |
| R6 | 本机 IP 变化 → 手机访问失败 | 低 | 低 | 绑 0.0.0.0，手机重连时刷新 | [v3.2] |
| R7 | Tailscale 连通率不稳定 | 低 | 中 | 备用 ZeroTier | [P2-2] |
| R8 | 看板/飞书并发操作同一计划 | 低 | 中 | 串行队列 + operation_log lock | [P2-3] |

### 不推荐的"看上去很美"方案 `[v3.2-补 §7]`

| 方案 | 不推荐理由 |
|---|---|
| Electron 桌面应用 | 100MB+ / 对个人工具杀鸡用牛刀 |
| PWA | 需 HTTPS 才能启用全部能力 → 又卡企业资质 |
| 浏览器扩展 | Chrome 商店 $5 + 审核 + 移动端无法装 |
| Tauri 桌面应用 | 学习曲线 + Rust 编译 + 维护成本高 |
| 公网 IP + 端口映射 | 巨量引擎 Cookie 暴露 = 账户被盗 |
| React Native / Flutter | 需上架审核 |

---

## 17. 实施 Checklist

### S1（第 1 周 · 数据基建）
- [x] SQLite 建表
- [x] JSON 历史导入
- [x] 双写集成
- [ ] **feedback-server 绑 0.0.0.0**（启用 M1 局域网）

### S2（第 2 周 · 执行层 + API）
- [ ] POST /api/actions 路由（启用 O2 操作入口）
- [ ] 集成 cdp-action（import 调用 5 个函数）
- [ ] 操作日志记录
- [ ] 串行操作队列（P2-3）
- [ ] **（可选）安装 Tailscale 启用 M2 远程**

### S3（第 3-4 周 · 前端看板）
- [ ] dashboard.html 单文件看板（启用 A1）
- [ ] 计划列表 + 状态筛选 + 排序
- [ ] 操作按钮 + 弹窗确认（L1/L2/L3）
- [ ] 定时刷新（30s）
- [ ] **移动端响应式 CSS 适配**（M1 手机访问）

### S4（按需 · 增强）
- [ ] F9 Windows Toast
- [ ] F10 浏览器通知
- [ ] Marketing API 写函数（CDP 不稳定时再上）

---

## 18. 决策矩阵速查版

```
我应该选哪个？

Q1: 想要在手机上看？
  ├─ 在家/同 WiFi → M1 局域网（0 成本）
  └─ 出门/远程 → M2 Tailscale（0.5 小时配置）

Q2: 想要暂停/启用计划？
  ├─ 紧急 → O1 飞书群命令（已实现）
  └─ 精细调预算 → O2 看板按钮（S3 阶段）

Q3: 想要更好的看板？
  ├─ 够用就好 → A1 单文件 HTML（S3 阶段）
  └─ 真要复杂 → A2 React SPA（升级路径）

Q4: 想要更多通知渠道？
  ├─ 飞书已够用 → F1 保持（已实现）
  └─ PC 前工作 → F9 Windows Toast（S4）
```

---

## 19. 模型贡献溯源表

> 标注每项关键决策的最初提出模型和采纳路径。

| 决策 | 首次提出 | 中间演化 | 最终采纳 |
|---|---|---|---|
| **D1 单文件 HTML 看板** | WorkBuddy v3.0 审查 (P0-1: 企业资质不可行) | v3.2 主文档确认 | v4.0 ✅ |
| **D2 raw CDP 主通道** | WorkBuddy v3.0 审查 (P0-2: cdp-action.mjs 已有) | v3.2 确认 | v4.0 ✅ |
| **D3 SQLite + DuckDB** | Qwen 3.7 Max + MiniMax M3 (v2.0 共识) | v3.2 沿用 | v4.0 ✅ |
| **D4 30s 定时轮询** | WorkBuddy v3.0 审查 (P0-4: 飞书 WS 不存在) | v3.2 确认 | v4.0 ✅ |
| **D5 前端弹窗无审批** | WorkBuddy v3.0 审查 (审批流过度) | v3.2 简化 | v4.0 ✅ |
| **D6 单进程 feedback-server** | WorkBuddy v3.0 审查 (P1-1) | v3.2 确认 | v4.0 ✅ |
| **D7 6→7 张表 Schema** | Qwen 3.7 Max (复合主键+生成列) | v3.2 简化 + 数据库报告 | v4.0 ✅ |
| **M1 局域网访问** | v3.2-补 WorkBuddy 系统梳理 | — | v4.0 ✅ |
| **M2 Tailscale 远程** | v3.2-补 WorkBuddy 系统梳理 | — | v4.0 ✅ |
| **排除小程序** | WorkBuddy v3.0 审查 P0-1 + 小程序上架方案.md | v3.2 正式排除 | v4.0 ✅ |
| **排除 Playwright** | WorkBuddy v3.0 审查 P0-3 (OVUI 不兼容) | v3.2 确认 | v4.0 ✅ |
| **废弃 action-executor** | WorkBuddy v3.0 审查 (已有 feishu-listener 替代) | v3.2 确认 | v4.0 ✅ |
| **3-4 周工期** | Qwen + MiniMax M3 + 豆包 + Kimi (v2.0 四方共识) | v3.2 沿用 | v4.0 ✅ |
| **双写迁移策略** | 数据库方案设计报告 + v2.0 共识 | v3.2 确认 | v4.0 ✅ |
| **cdp-client 不可退役** | WorkBuddy v3.0 审查 P0-3 (6 个脚本依赖) | v3.2 §11.4 | v4.0 ✅ |
| **操作并发串行队列** | v4.0 审查 P2-3 | — | v4.0 ✅ (新增) |

### 审查模型列表

| 模型 | 审查文档 | 关键贡献 |
|---|---|---|
| **Qwen 3.7 Max** | 架构规划_评审反馈_Qwen3.7Max.md + v2.0 排名 #1 | HTTP API 主方案识别、复合主键、connectOverCDP 限制 |
| **MiniMax M3** | 架构规划_审查报告_M3_v1.0.md + v2.0 排名 #2 | 生产级能力缺位识别、灰度策略 |
| **DeepSeek V4 Pro** | 架构规划_审查报告_DSV4Pro.md + v2.0 排名 #3 | PM2 CDP 并发竞争、schema_migrations |
| **Kimi K2.7 Code** | 架构规划_整体方案_审查反馈_KimiK2.7.md + v2.0 排名 #7 | 3+1 周工期建议 |
| **豆包 2.0 Code** | 架构规划-豆包审查反馈.md + v2.0 排名 #5 | 数据生命周期 SLA |
| **Mimo 2.5 Pro** | 架构规划_审查报告_Mimo.md + v2.0 排名 #6 | IQR 异常检测 |
| **WorkBuddy** | 架构规划_审查报告_v3.0_WorkBuddy.md + v2.0 排名 #4 | P0-1 企业资质、P0-2 cdp-action、P0-3 Playwright 不兼容 |
| **WorkBuddy** | v3.2 主文档 + v3.2-补 替代方案 | 全部 D1-D7 决策、替代方案全景、源码核验 |

---

## 20. 与 v3.0 的关键差异

| 维度 | v3.0 | v4.0 最终版 |
|---|---|---|
| **产品定位** | 产品/平台 | **个人桌面工具** |
| **用户模型** | 多用户/多账户 | **单用户/单账户** |
| **前端** | 飞书小程序 (Taro) | **A1 单文件 HTML 看板** |
| **执行通道** | Marketing API 主 + Playwright 回退 | **raw CDP 主**（cdp-action.mjs 已有）|
| **审批流** | L0-L3 四级审批 | **前端弹窗确认 + 操作日志** |
| **后端架构** | 双进程 | **feedback-server 单进程 (217行)** |
| **实时通信** | WebSocket / SSE | **30s 定时轮询** |
| **Schema** | 7 张表 + platform/account_id | **7 张表，无 platform/account_id** |
| **小程序** | 核心方案 | **彻底排除** |
| **Playwright** | 退役 cdp-client 换 playwright | **不兼容 OVUI，保留 cdp-client** |
| **工期** | 5-6 周 | **3-4 周** |
| **oec-oauth.mjs** | 未提及 | **确认不存在** (v3.2 错误修正) |
| **api-client 函数** | fetchSpendingCampaigns 等 | **getProjects / getDashboardStats / getHourlyStats / collectAllData** |
| **feedback-server 行数** | 未核实 | **217 行**（v3.2 声称 678 行修正）|

---

## 21. 给后续 Agent 的实施指引

> **⚠️ 以下 8 条是必须遵守的硬性约束**

### 21.1 这是个人工具，不是产品 `[WorkBuddy]`
```
不要做：用户系统 / 多租户 / 审批流 / Docker / 云端 / HTTPS / 小程序 / SSE / WebSocket / React
要做：极简、够用、快速交付、浏览器访问 localhost:8899、飞书群通知
```

### 21.2 CDP 操作必须用真实事件 `[WorkBuddy P0-3]`
```javascript
// ✅ 正确 — cdp-action.mjs 已有
await client.send('Input.dispatchMouseEvent', {
  type: 'mousePressed', x, y, button: 'left', clickCount: 1
});
// ❌ 错误 — Playwright locator.click() / Element.click() / dispatchEvent
// OVUI Vue 组件不响应合成事件，会静默失败
```

### 21.3 复用已有模块，不要重写 `[WorkBuddy P0-2]`

| 需要的能力 | 已有模块 | 做法 |
|---|---|---|
| 暂停/启用计划 | `cdp-action.mjs` togglePlanStatus | import 调用 |
| 调整预算 | `cdp-action.mjs` adjustBudget | import 调用 |
| 搜索计划 | `cdp-action.mjs` searchPlanInTable | import 调用 |
| 数据采集 | `oceanengine-api-client.mjs` getProjects/collectAllData | import 调用 |
| 页面校准 | `calibrate-page.mjs` | import 调用 |
| CDP 连接 | `cdp-client.mjs` | import 调用 |

### 21.4 不要退役 cdp-client.mjs `[WorkBuddy P0-3]`
```
cdp-client.mjs 是以下模块的基础依赖：
├── oceanengine-api-client.mjs (Cookie 提取)
├── cdp-action.mjs (所有写操作)
├── calibrate-page.mjs (页面校准)
├── chrome-guard.mjs (健康检查)
└── 5min/15min 监控脚本 (数据采集)
退役它 = 整个系统瘫痪。
```

### 21.5 SQLite 操作规范 `[Qwen 3.7 Max]`
```javascript
// ✅ better-sqlite3（同步 API，性能最优）
const Database = require('better-sqlite3');
const db = new Database('oceanengine.db', { wal: true });
// ✅ 参数化查询
db.prepare('INSERT INTO campaign_snapshots VALUES (?,?,?,?,?)').run(date, time, ...);
// ❌ 字符串拼接 SQL
```

### 21.6 前端实现规范 `[v3.2]`
```
看板 = 单文件 HTML（dashboard.html）
├── ECharts 5 CDN
├── 原生 JS fetch()
├── 原生 CSS（响应式）
├── setInterval 30s
└── 无构建步骤，feedback-server 直接托管
```

### 21.7 双写迁移策略 `[v2.0 共识]`
```
Week 1: JSON 为主，SQLite 为辅（双写）
Week 2: SQLite 为主，JSON 为辅
Week 3+: 退役 JSON 写入
```

### 21.8 API 函数名对照表 `[v4.0 源码核验修正]`
```
⚠️ v3.2 文档中的函数名全部错误，以下是实际导出：

v3.2 声称                    → 实际 (oceanengine-api-client.mjs 464行)
fetchSpendingCampaigns       → getProjects
fetchAccountReport           → getDashboardStats
fetchCampaignSummary         → getHourlyStats
fetchPromotionHourly         → collectAllData
(未列出)                      → createClient
```

---

*本文档为 v4.0 最终版。所有事实经源码核验（2026-06-28），所有决策标注模型来源。未经验证的假设已明确标注"待确认"。*

*生成模型: WorkBuddy (Qwen 3.7 Max) | 审查基线: v3.2 + v3.2-补 + 7 方评审 + 源码核验*
