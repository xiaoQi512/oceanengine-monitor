// src/domain/report-html-body-lists.mjs - 报表列表区块

export function buildReportHtmlLists({ summary, d, alerts, alertsHTML, planList, allPlanRows, histRows, funnelBar, campaignRows, rampingUp, dropping }) {
  return `<div class="section"><h2>📊 转化漏斗 <span class="count">线索来源对照</span></h2>${funnelBar(summary.totalPrivateMsgOpen||0, '💬 私信开口', '#8b5cf6')}${funnelBar(summary.totalPrivateMsgRetain||0, '📝 私信留资', '#3b82f6')}${funnelBar(summary.totalFormSubmit||0, '📋 表单提交', '#10b981')}${funnelBar(summary.totalLeads||0, '📌 线索数 (留资+表单)', '#f59e0b')}${funnelBar(summary.totalConversions||0, '🎯 转化数 (≈线索)', '#ef4444')}</div>
<div class="section"><h2>⚠️ 异常告警 <span class="badge ${alerts.filter(a=>a.severity==='high').length>0?'bg-red':'bg-green'}">${alerts.length} 条</span></h2><table><thead><tr><th>类型/名称</th><th colspan="4">详情</th><th>级别</th></tr></thead><tbody>${alertsHTML}</tbody></table></div>
<div class="section"><h2>📋 全量计划明细 <span class="count">${(planList||[]).length} 条 · 有消耗 · 按消耗降序</span></h2><div class="scroll-table"><table><thead><tr><th>计划名称/ID</th><th>状态</th><th>消耗</th><th>CPL</th><th>转化</th><th>线索</th><th>私信留资</th><th>表单</th><th>开口</th><th>CTR</th><th>CVR</th><th>计划预算</th><th>周期</th></tr></thead><tbody>${allPlanRows}</tbody></table></div></div>
<div class="section"><h2>📊 近${Math.round(d.age15||15)}分钟新增消耗 TOP</h2><table><thead><tr><th>计划名称/ID</th><th>趋势</th><th>累计消耗</th><th>${Math.round(d.age15||15)}m新增</th><th>环比变化</th><th>转化(条)</th><th>CPL</th><th>预算使用</th></tr></thead><tbody>${campaignRows}</tbody></table></div>
<div class="grid2"><div class="section"><h2>🚀 起量 <span class="count">>30% | ${(rampingUp||[]).length}条</span></h2><table><thead><tr><th>计划</th><th>消耗</th><th>${Math.round(d.age15||15)}m新增</th><th>环比</th><th>CPL</th></tr></thead><tbody>${(rampingUp||[]).length > 0 ? (rampingUp||[]).map(c => `<tr><td title="${c.name}">${c.name.slice(0,35)}</td><td>¥${c.spend.toFixed(0)}</td><td style="color:#e74c3c">¥${(c.spendDelta||0).toFixed(0)}</td><td style="color:#e74c3c">+${((c.changeRate||0)*100).toFixed(0)}%</td><td>¥${(c.cpa||0).toFixed(0)}</td></tr>`).join('') : '<tr><td colspan="5" style="text-align:center;color:#94a3b8">暂无起量</td></tr>'}</tbody></table></div><div class="section"><h2>📉 掉量 <span class="count"><-30% | ${(dropping||[]).length}条</span></h2><table><thead><tr><th>计划</th><th>消耗</th><th>${Math.round(d.age15||15)}m新增</th><th>环比</th><th>CPL</th></tr></thead><tbody>${(dropping||[]).length > 0 ? (dropping||[]).map(c => `<tr><td title="${c.name}">${c.name.slice(0,35)}</td><td>¥${c.spend.toFixed(0)}</td><td style="color:#27ae60">¥${(c.spendDelta||0).toFixed(0)}</td><td style="color:#27ae60">${((c.changeRate||0)*100).toFixed(0)}%</td><td>¥${(c.cpa||0).toFixed(0)}</td></tr>`).join('') : '<tr><td colspan="5" style="text-align:center;color:#94a3b8">暂无掉量</td></tr>'}</tbody></table></div></div>
<div class="section"><h2>📋 建议反馈记录 <span class="count">最近20条</span></h2>${histRows ? `<div class="scroll-table"><table><thead><tr><th>时间</th><th>类型</th><th>计划</th><th>建议</th><th>结果</th></tr></thead><tbody>${histRows}</tbody></table></div>` : '<p style="color:#94a3b8;padding:20px;text-align:center">暂无建议记录</p>'}</div>`;
}
