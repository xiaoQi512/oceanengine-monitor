# 巨量引擎监控 · 项目完整文档

> 版本：v1.0 · 2026-08-01
> 定位：项目全貌总览 —— 环境依赖 / 项目目标 / 功能模块 / 数据逻辑 / 重构方案 / 路线图 / 文档地图
> 关联文档：《完整重构方案_20260801.md》《数据中枢与AI调优架构设计.md》《前端数据抓取重构方案.md》《多租户架构设计_线索直播广告监控.md》《工程化优化方案_20260725.md》

---

## 一、项目概述

**巨量引擎监控**（oceanengine-monitor）是一套面向"线索直播广告"场景的实时监控与调优系统：

- 每 5/15 分钟自动采集巨量引擎（字节跳动广告平台）各广告计划数据
- 通过飞书交互卡片推送 5min 速报、15min 完整监控、主播换班数据、日汇总、日报
- 支持在飞书群内以自然语言指令执行广告操作（暂停/恢复/调预算/调出价），形成「指令 → 审计 → 执行 → 回读 → 报告」闭环
- 提供本地 Dashboard（Alpine.js + Chart.js）可视化、操作队列与审计、一键回滚、AI 建议
- 配套 AI 区域号汇总、周报 PPT 生成、多 Agent 协作研发流水线

**核心链路（现状）**：

```
采集（HTTP API 主 / CDP 降级）
  → 快照落盘（JSON 双写 + SQLite）
  → 分析（delta/告警/生命周期）
  → 推送（飞书卡片）
  → 群指令（暂停/预算/出价）→ 队列 → 执行 → 审计回读 → 效果追踪
  → Dashboard 可视化 + AI 建议
```

**目标链路（重构后，详见第八章）**：

```
采集独立（oec-collector 只写库，HTTP API 唯一路径；失败每分钟重试 → 飞书群告警 → 恢复通知）→ SQLite 数据中枢（唯一事实源）
  → 汇报层（卡片/日报/Dashboard 全部读库）
  → 规则引擎（trigger_rules 条件触发）→ AI 决策 → 操作入库 → 效果追踪回写
```

---

## 二、环境与运行依赖

### 2.1 运行时

| 项 | 值 |
|---|---|
| Node.js | **22.22.2（ABI 127）**，路径 `C:\Users\HTF2026\.workbuddy\binaries\node\versions\22.22.2\node.exe` |
| 模块规范 | ESM（`.mjs`），`package.json` 声明 `"type": "module"` |
| 引擎约束 | `"engines": { "node": ">=22 <23" }`，`.nvmrc` = 22 |
| 警告 | 本机另有 Node 24（ABI 137），**PM2 必须绑定 Node 22**；原生模块安装/重建用 Node 22 的 npm |

### 2.2 依赖（package.json）

| 包 | 版本 | 用途 |
|---|---|---|
| better-sqlite3 | ^12.11.1 | 唯一原生依赖，SQLite 访问（ABI 127 预编译） |
| jszip | ^3.10.1 | PPT 模板解压 |
| node-cron | ^4.5.0 | 定时任务 |
| pptxgenjs | ^4.0.1 | 周报 PPT 生成 |
| ws | ^8.21.0 | CDP WebSocket 客户端 |

### 2.3 进程管理（PM2 · 12 进程）

配置源：`ecosystem.config.cjs`（`MONITOR_DIR` / `NODE_EXE` / `LOG_DIR` 环境变量可覆盖）。

| 进程 | 脚本 | 调度 | 内存上限 |
|---|---|---|---|
| pm2-15min | `oceanengine-monitor-v3.mjs` | cron `*/15 * * * *` | — |
| pm2-5min | `oceanengine-5min-check.mjs` | cron `*/5 * * * *` | — |
| shift-pusher | `oceanengine-shift-pusher.mjs` | 常驻 60s 轮询 + 每日 6:00 重启 | 300M |
| pm2-daily-report | `oceanengine-daily-report-scheduler.mjs` | cron `5 23 * * *` | — |
| pm2-daily-summary | `oceanengine-daily-summary.mjs` | cron `35 23 * * *` | — |
| pm2-sync-tomorrow | `sync-tomorrow-shifts.mjs` | cron `0 23 * * *` | — |
| pm2-ai-regions | `ai-regions-http.mjs` | cron `30 21 * * *` | — |
| live-watcher | `live-watcher.mjs` | 常驻 60s 轮询 | 256M |
| feishu-listener | `feishu-listener.mjs` | 常驻 10s 轮询 | 200M |
| action-queue-worker | `action-queue-worker.mjs --watch` | 常驻 15s 轮询 | 200M |
| feedback-server | `feedback-server.mjs` | 常驻 HTTP（端口 8899） | 200M |
| chrome-guard | `chrome-guard.mjs` | 常驻 10s 探活 | 256M |

### 2.4 数据库

- **引擎**：SQLite 3（WAL + better-sqlite3），`PRAGMA synchronous=FULL`（写入侧）
- **文件**：`monitor-data/oceanengine.db`（唯一 DB 文件，约 7MB）
- **Schema**：v2.0，9 张基础表 + 3 张物化聚合表（详见第六章）
- **代码**：当前三套并存 —— `db/`（旧）、`巨量引擎监控数据库/`（v2 模块化）、`巨量引擎快照数据库/`（快照差计算）；重构后统一为 `src/db/`

### 2.5 浏览器（Chrome CDP）

| 项 | 值 |
|---|---|
| CDP 端口 | 9222（`chrome-guard` 守护，崩溃自动拉起） |
| 用户数据目录 | `CHROME_USER_DATA_DIR=D:\ChromeCDP\User Data` |
| Profile | `CHROME_PROFILE_DIRECTORY=Profile 4` |
| 用途 | Cookie 提取/刷新（占比 ~0.2%）、写操作（暂停/预算/出价）、逆向工具（发现新端点）；**不用于生产数据抓取降级** |

### 2.6 推送通道（飞书）

- **工具**：`lark-cli`（`findLarkCli()` 自动发现），推送封装 `feishu-push-guard.mjs`
- **群**：监控群 `oc_8deeb3061bdbd43608de252a44c97a25`、主播群 `oc_b245ee4b255c7b25b7f8d953802c49ff`
- **Bot**：`cli_a92d0bfc68f89cb2`（feishu-listener 使用）
- **排班表**：飞书电子表格（`SHIFT_SPREADSHEET_TOKEN` / `SHIFT_SHEET_ID`）；每日 23:00 同步次日排班 + **校对当日排班与当日已填写的主播换班数据**（缺班次/多余/主播不一致 → 飞书告警）

### 2.7 前端服务

- `feedback-server.mjs`（8899）：静态资源 + REST API（`/api/snapshots`、`/api/campaigns`、`/api/actions`、`/api/alerts`、`/api/ai/learning-data`、`/api/live-status` 等 30+ 路由）
- `dashboard-v2.html/js/css`：Alpine.js + Chart.js（vendor/ 本地化），PWA（sw.js / manifest.json）

### 2.8 配置（.env）

| 变量 | 说明 |
|---|---|
| `OEC_EMAIL` / `OEC_PASSWORD` | 巨量引擎登录凭据 |
| `OEC_ACCOUNT_ID` / `OEC_VIDEO_ACCOUNT_ID` | 广告账户 / 视频账户 |
| `LARK_MONITOR_CHAT_ID` / `LARK_ANCHOR_CHAT_ID` | 监控群 / 主播群 |
| `SHIFT_SPREADSHEET_TOKEN` / `SHIFT_SHEET_ID` | 飞书排班表 |
| `OEC_DAILY_BUDGET` | 日预算（默认 45000） |
| `CHROME_USER_DATA_DIR` / `CHROME_PROFILE_DIRECTORY` | Chrome 配置 |
| `MONITOR_DIR` / `NODE_EXE` | PM2 路径覆盖 |
| `LOG_RETENTION_DAYS` | 日志保留天数（默认 14） |
| `SIMULATE_CDP=1` | 测试模式：跳过真实 CDP 操作 |



### 2.9 远程运维通道（CodeBuddy 网关）

系统可通过 CodeBuddy 远程网关从任意位置访问（Web UI / webCLI），已投入运行：

| 项 | 现状 |
|---|---|
| 网关鉴权 | CodeBuddy `settings.json` → `gateway: { auth: "password" }`，密码保护 Web UI |
| 隧道 | cloudflared trycloudflare 快速隧道（2 个进程常驻），本地 CodeBuddy Web 服务端口（如 59687 / 61278）→ 公网 `https://<随机>.trycloudflare.com/?password=***` |
| 状态文件 | `C:\Users\HTF2026\.codebuddy\tunnel-state.json`（url / localPort / cliPid） |
| 地址通知 | `E:\炼丹炉\WorkBuddy\.codebuddy\check_gateway_hook.sh`（CodeBuddy Stop 事件）→ 从 transcript 提取最新 URL → `send_gateway_to_feishu.py` 推飞书私聊 → `.gateway_last_url.txt` 去重 |
| 关联文件 | `E:\炼丹炉\WorkBuddy\网关地址.txt`（Web UI / Tunnel / 本地三地址记录） |

> 说明：该通道属于 WorkBuddy 层运维基础设施，独立于监控系统自身；监控 Dashboard（8899）目前仅本机访问，未走隧道。重构方案将其纳入统一治理（见 6.5）。

### 2.10 目录结构（现状）

```
巨量引擎监控/
├── *.mjs（150 个核心脚本，扁平散落）     # 重构后迁入 src/
├── ecosystem.config.cjs                 # PM2 12 进程
├── monitor-data/                        # 运行数据（快照/日志/状态/DB）
├── db/ + 巨量引擎监控数据库/ + 巨量引擎快照数据库/  # 三套数据库代码（重构后统一）
├── dashboard*.html/js/css               # 前端（新旧两版，重构后只留 v2）
├── tests/（8 个） scripts/ .github/workflows/ci.yml
├── docs 体系（115 个 md：方案/评审/规范/历史）
├── prompts/ + opencode.json             # 多 Agent 研发流水线
└── archive/（cdp-probes / debug-scripts / deprecated-prototypes）
```

---

## 三、项目目标

### 3.1 业务目标

1. **实时监控**：5/15min 卡片稳定推送，异常消耗/CPL 波动即时可见
2. **降本提效**：CPL（线索成本）为核心指标，监测留资转化链路（观看 → 私信打开 → 私信留资 → 表单 → 线索 → 转化）
3. **操作闭环**：运营在飞书群直接调优，全部操作留痕、可确认、可回滚
4. **数据沉淀**：历史快照/操作/效果可查，支撑 AI 诊断与规则学习

### 3.2 重构目标（完整重构方案）

1. **目录分层**：根目录 150 文件 → `src/` 分层骨架（config/platform/domain/services/web/db/feishu/cdp/utils）
2. **模块化**：巨型文件拆分（163KB / 72KB / 33KB → <30KB）
3. **去重**：8 处 httpPost、7 处 WebSocket、3 处 Cookie 提取 → 统一实现
4. **进程合并**：12 进程 → ≤6 进程
5. **配置外置**：20+ 处硬编码（chat_id/aadvid/账户）→ 配置化
6. **测试强化**：单测 8 → ≥30，CI 双平台 + import 检查

### 3.3 数据中枢与 AI 调优目标（核心深化）

1. **采集独立**：`oec-collector` 只写库，与汇报解耦
2. **唯一事实源**：汇报/监控 100% 读库，JSON 降级为灾备回放
3. **操作闭环入库**：`operations` + `operation_effects` 替代文件队列与 audit
4. **条件触发 AI 调优**：`trigger_rules` 配置化 → 命中触发 → 飞书确认/自动执行 → 效果回写规则置信度

### 3.4 量化指标（目标态）

| 指标 | 现状 | 目标 |
|---|---|---|
| 根目录生产代码文件数 | ~150 | < 20 |
| 最大单文件 | 163KB | < 30KB |
| PM2 进程数 | 12 | ≤ 6 |
| httpPost/WebSocket/Cookie 重复实现 | 8/7/3 | 0 |
| 单测文件数 | 8 | ≥ 30 |
| 硬编码配置 | 20+ 处 | 0 |
| 消费方读 JSON 文件 | 全部 | 0（仅灾备） |
| AI 自动调优 | 无 | confirm 灰度 → auto 受控放量 |

---

## 四、功能模块清单

### 4.1 数据采集层

| 模块 | 文件 | 职责 |
|---|---|---|
| HTTP API 客户端 | `oceanengine-api-client.mjs` | 18 个导出函数：getProjects/getDashboardStats/getHourlyStats/collectAllData/getSessionStats/updateProjectStatus/updateProjectBudget/updateProjectBid/updateAccountBudget/createClient/getOnlineRoomList/getLiveRoomStatus 等 |
| CDP 连接 | `cdp-client.mjs` | WebSocket 封装、checkCDP、getOceanEngineTab |
| 页面校准 | `calibrate-page.mjs` | （已废弃生产抓取用途，随 DOM 抓取归档） |
| 抓包逆向 | `oec-capture-status-api.mjs`、`capture-ai-regions-api.mjs`、`oec-reverse-*.mjs`、`oec-internal-api-discovery.mjs` | Network 域监听捕获新端点 |
| 自动登录 | `oec-auto-login.mjs` | Cookie 提取/刷新 |
| 一次性抓取 | `fetch-ai-regions*.mjs`、`fetch-video-account-cost.mjs`、`fetch-short-video-cost.mjs` | 专项数据抓取（游离体系外，待收敛） |

### 4.2 监控与汇报层（PM2 业务进程）

| 模块 | 文件 | 职责 |
|---|---|---|
| 15min 完整监控 | `oceanengine-monitor-v3.mjs`（163KB） | 采集+分析+卡片+推送一体；状态变化检测（投放中→超出预算） |
| 5min 速报 | `oceanengine-5min-check.mjs`（33KB） | 轻量速报卡片（消耗/转化/CPL），HTTP API 唯一路径 |
| 换班推送 | `oceanengine-shift-pusher.mjs` | 主播班次结束推送，写飞书排班表 |
| 日汇总 | `oceanengine-daily-summary.mjs` | 23:35 全天数据核对后推送 |
| 日报 | `oceanengine-daily-report-scheduler.mjs` | 23:05 生成日报并推送 |
| 排班同步+校对 | `sync-tomorrow-shifts.mjs`（重构后 `sync-shifts`） | 23:00 同步次日排班 + 校对当日排班/当日换班数据（缺班次/主播不一致告警） |
| AI 区域号 | `ai-regions-http.mjs` / `oceanengine-ai-regions.mjs` | 21:30 区域号汇总推送（5 账户） |
| 直播状态 | `live-watcher.mjs` | 60s 轮询开播/下播，下播二次确认 |

### 4.3 操作与反馈闭环

| 模块 | 文件 | 职责 |
|---|---|---|
| 飞书监听 | `feishu-listener.mjs`（32KB） | 10s 轮询群消息，指令解析：暂停/恢复/加预算/拒绝/取消/状态/帮助；三阶段反馈（确认→执行→报告） |
| 操作队列 | `action-queue-worker.mjs` | 15s 轮询文件队列（action-queue.json），单实例锁，双路径执行（HTTP API 主 / CDP 降级），审计写入 |
| CDP 操作 | `cdp-action.mjs` | 浏览器内暂停/预算/出价操作 |
| 推送守卫 | `feishu-push-guard.mjs` | 推送熔断/重试/去重 |
| 回滚 | Dashboard + worker | beforeValue/afterValue 回读，一键回滚 |
| AI 学习 | `/api/ai/learning-data` | 读审计+快照，按 (deliveryType, actionType) 统计成功率/置信度/15min 消耗变化 |

### 4.4 服务与展示层

| 模块 | 文件 | 职责 |
|---|---|---|
| HTTP 服务 | `feedback-server.mjs`（72KB） | 静态资源 + 30+ REST 路由 |
| Dashboard | `dashboard-v2.html/js/css` | 实时看板：计划列表/操作队列/审计/回滚/AI 建议/异常高亮 |
| PWA | `sw.js`、`manifest.json` | 离线缓存 |

### 4.5 基础设施层

| 模块 | 文件 | 职责 |
|---|---|---|
| 共享工具 | `monitor-utils.mjs` | 配置常量、快照工具、推送工具、排班、锁、原子写 |
| 日志 | `logger.mjs` | 统一聚合：运行日志 + agent 变更日志（`[CHANGE]`）双写 monitor.log + ndjson，按日轮转 |
| 守护 | `monitor-daemon.mjs` | Windows 任务/日志错误/数据间隔/服务健康检查，CDP 自动拉起 |
| Chrome 守护 | `chrome-guard.mjs` | 9222 探活与拉起 |
| 动态调度 | `adaptive-scheduler.mjs` | 排班驱动：排班时段内按消耗 5min/15min 采集，**非排班时段=低活跃期** 30min 低频或暂停（待并入 scheduler） |
| 路由熔断 | `autonomous-router.mjs` | provider 选择 + CircuitBreaker + 成本 guardrail |
| 影子竞技 | `shadow-arena.mjs` | 5% 输入影子测试新路径 |
| 安全 | `csrf-utils.mjs`、`wait-utils.mjs`、`data-consistency-check.mjs` | CSRF、等待、一致性校验 |

### 4.6 数据库模块（三套现状）

| 模块 | 文件 | 职责 |
|---|---|---|
| 旧版 | `db/`（schema.sql、writer.mjs、init.mjs、backfill.mjs、refresh-materialized.mjs） | v1 双写接口 |
| v2 模块化 | `巨量引擎监控数据库/`（dal.mjs、writer.mjs、index.mjs、schema.sql、schema-views.sql、migrations/） | 统一入口 connect/createAPI、双写、物化刷新 |
| 快照差 | `巨量引擎快照数据库/`（snapshot-db.mjs） | getShiftDelta/getSnapshotAt/getDailyShiftReport，HTTP 回退 |

### 4.7 辅助与研发支撑

| 模块 | 文件 | 职责 |
|---|---|---|
| 周报 PPT | `gen-anchor-ppt*.mjs`、`gen-from-template*.mjs` | pptxgenjs/jszip 生成周报 |
| 测试 | `tests/`（8 个） | csrf、autonomous-router、feishu-listener、action-queue-lock、refresh-materialized-lock、push-guard、e2e |
| CI | `.github/workflows/ci.yml` | ubuntu：单测 + 备份验证 |
| 多 Agent | `prompts/*` + `opencode.json` | PM/架构/工程师/测试/评审/发布角色流水线 |

---



### 4.8 远程运维与访问通道

| 组件 | 文件/位置 | 职责 |
|---|---|---|
| CodeBuddy 远程网关 | `settings.json`（gateway 配置） | 密码鉴权的远程访问入口 |
| cloudflared 隧道 | 系统进程（2 个） | 本地 CodeBuddy Web 服务 → 公网 trycloudflare URL |
| Web UI / webCLI | 隧道 URL（带 password） | 远程查看/操作本机（pm2、日志、监控命令） |
| 地址变更通知 | `check_gateway_hook.sh` + `send_gateway_to_feishu.py` | 隧道地址变化 → 飞书私聊通知 |
| 监控 Dashboard（可选） | `feedback-server` 8899 | 当前仅本机；可决策是否纳入远程通道 |

## 五、数据逻辑（现状）

### 5.1 数据流总览

```
采集（monitor-v3 / 5min-check）
  ├─ 15min 快照 JSON：monitor-data/{UTC时间}.json（完整分析结果，含 summary/delta/active/_multiDay/alerts）
  ├─ 5min 快照 JSON：monitor-data/5m-{时间}.json（轻量）
  ├─ SQLite 双写：db/writer.mjs → insertSnapshot（JSON 失败不阻塞 SQLite）
  ├─ 状态文件：live-state.json / listener-state*.json / action-queue.json
  └─ 排班：shifts-{date}.json（sync-tomorrow 23:00 同步；重构后入 shifts 表，23:00 同时做当日排班/换班校对）
消费（汇报/AI 诊断）
  ├─ 主要读 JSON 快照文件（daily-summary / shift-pusher / AI 诊断）
  └─ Dashboard 部分读 DB（/api/snapshots /api/campaigns /api/actions）
操作（feishu-listener → action-queue-worker）
  ├─ 队列：action-queue.json（文件，单实例锁）
  ├─ 待确认：monitor-data/pending-actions.json
  ├─ 审计：monitor-data/action-audit.jsonl（beforeValue/afterValue/snapshotBefore）
  └─ DB：actions 表（状态机 pending→executing→succeeded/failed）
```

### 5.2 数据文件清单（monitor-data/）

| 类型 | 命名 | 内容 |
|---|---|---|
| 15min 快照 | `{YYYY-MM-DD}T{HH-mm-ss}.json` | 完整分析：summary/delta/active/allSpending/paused/_multiDay/alerts/topNewSpenders/rampingUp/dropping/budgetExceededChanges |
| 5min 快照 | `5m-*.json` | 轻量指标 |
| 日汇总 | `daily-{date}.json`、`daily-summary-done-{date}.json`、`daily-report-done-{date}.json` | 日数据 + 完成标记 |
| 排班 | `shifts-{date}.json` | 班次/主播/时段 |
| 操作 | `action-queue.json`、`pending-actions.json`、`action-audit.jsonl` | 队列/待确认/审计 |
| 状态 | `live-state.json`、`listener-state.json`、`listener-state-anchor.json`、`last-5m-push.json`、`shift-push-lock.json`、`.fail-log-state.json` | 各进程状态与去重 |
| Cookie | `.oec-cookies.json` | 会话缓存（2h TTL） |
| 日志（统一聚合） | `monitor.log` + `logs/monitor-YYYY-MM-DD.ndjson` | 运行日志与 agent 变更日志（`[CHANGE] agent=... 原因:... 方法:... 结果:...`）统一聚合；按日轮转，`LOG_RETENTION_DAYS` 控制保留 |
| 遥测 | `telemetry.jsonl` | autonomous-router 执行记录 |
| DB | `oceanengine.db` | SQLite 唯一文件 |

**统一日志规范**：任何 agent 对项目更改后执行 `node scripts/log-change.mjs --agent <agent> --reason <原因> --method <方法> --files <文件> --result <done|partial|failed>`，记录与运行日志聚合在同一文件（`[CHANGE]` 前缀），实现「运行 + 修改」单一日志入口。

### 5.3 数据保留策略

| 表/数据 | 保留期 | 策略 |
|---|---|---|
| `snapshots` | 90 天 | 超期按日归档/清理（分批 DELETE + 每月 VACUUM） |
| `hourly_stats` / `daily_stats` / `daily_summaries` / `shifts` | 长期 | 不清理 |
| `operations` / `operation_effects` / `ai_decisions` | 永久 | 审计要求 |
| `alerts` / `feedback` | 1 年 | 归档后清理 |
| `telemetry` | 90 天 | 清理 |
| 原始报文 JSON | 30 天 | 灾备回放窗口 |

实现：`scripts/purge-data.mjs` 每日 04:00 执行，保留期存 `config` 表可配置。90 天窗口下 DB 规模控制在 ~120-150MB。

### 5.4 数据库表结构（v2，9 表 + 3 物化视图）

| 表 | 关键字段 | 说明 |
|---|---|---|
| schema_migrations | version/applied_at | 迁移记录 |
| campaigns | campaign_id UNIQUE、name、status、daily_budget、bid | 计划主表 |
| snapshots | snapshot_time/snapshot_cst、campaign_id、cost、leads、conversions、msg_open、msg_lead、form_submit、ctr、cpm、cvr、views、views_1min、comments、**learning_status / learning_done（学习期）**、source_type(api/cdp/dom)、page_summary_json、raw_json | 快照明细（核心事实表）；learning_done=0 时禁止/延迟调优 |
| daily_summaries | date UNIQUE、total_cost、total_leads、total_conversions、avg_cpa、avg_ctr、avg_cpm、active_count、peak_hour、peak_cost、source | 账户级日汇总 |
| shift_metrics | date、shift_label、anchor_name、spend、leads、cpl、UNIQUE(date,shift_label) | 主播班次指标 |
| alerts | alert_time、alert_type、severity、campaign_id、message、resolved | 告警事件 |
| actions | action_time、action_type、campaign_id、before_value、after_value、source、status、executed_at | 操作记录（重构后升级为 operations） |
| feedback | feedback_time、alert_id、response、note | 告警反馈 |
| config | key/value | 键值配置 |
| telemetry | check_time、write_lag_ms、db_size_mb、snap_count、wal_size_mb、checks_ok | 数据库自监控 |
| hourly_stats（物化） | stat_date/stat_hour/campaign_id、delta_cost、delta_leads、peak_cost、snap_count | 按计划×小时聚合 |
| daily_stats（物化） | stat_date/campaign_id、total_cost、total_leads、avg_ctr/cpm/cvr、peak_hour、peak_cost | 按计划×日聚合 |
| alert_timeline（物化） | stat_date/stat_hour/campaign_id、alert_count、severity 计数 | 告警时间线 |

刷新：`refresh-views.mjs` 增量（基于 `config.last_materialized_refresh`）；`backfill.mjs` 历史回灌。

### 5.5 学习期字段
学习期字段（计划级）：
- `learningStatus`：巨量引擎原始枚举 `learning`（学习中）/ `learned`（学习完成）/ `learning_failed`（学习失败）/ `not_learning`（无需学习）/ 空
- `learningDone`（**学习期是否完成**）：布尔 —— `learned`/`learning_failed`/`not_learning` → `true`（学习期已结束）；`learning` → `false`（学习中）
- 来源：HTTP API 提供 `learning_status` 时直取；当前逆向端点未提供时由 `oec-normalize` 按规则推断（投放时长 ≥ 24h 且转化数 ≥ 阈值 → 视为已出学习期），推断规则以实际 API 校准
- 用途：卡片「学习中」标记；`learning_failed` 计划告警；trigger-engine 条件支持 `learning_done=0` 时**禁止/延迟调优**（学习中转化不稳定）

### 5.5 关键计算逻辑

| 逻辑 | 位置 | 算法 |
|---|---|---|
| 15min delta | monitor-v3 analyze | 当前累计 - 上一窗口累计 → spendLast15min/spendLastHour/speedCurrent |
| CPL | 各卡片 | 消耗 / 线索数（leads = msg_lead + form_submit） |
| CPM / CTR / CVR | 卡片构建 | 千次展示成本 / 点击率 / 转化率 |
| 班次消耗 | snapshot-db getShiftDelta | 班次开始/结束快照差，快照缺失回退 HTTP getHourlyStats |
| 消耗节律基线 | AI 诊断 Rule-001 | 近 15 天同时段 mean/stdev → 5 档分级（过慢/偏慢/正常/偏快/过快），仅诊断不输出操作建议 |
| 操作效果 | worker writeAudit | snapshotBefore（最新 5m 快照），before/after 15min accountSpend delta；阈值：pause 后 delta<200=high_positive、<600=positive、<1000=neutral、否则 negative |
| 数值解析 | 各脚本散落 | `parseFloat(x.replace(/,/g,''))` —— **存在 ¥9459→¥7 事故，重构后统一 parseNum** |
| 数据一致性 | data-consistency-check.mjs | JSON vs SQLite 对账 |

### 5.7 AI 诊断与学习

- **规则手册**：`AI诊断规则手册.md`（Rule-001 起），原则「仅诊断标注异常，不输出操作建议」
- **数据源**：最新快照 JSON（`_multiDay` 近 3 天）+ 历史快照文件（15/30 天基线）+ `shifts-{date}.json`
- **学习回路**：`/api/ai/learning-data` 读审计 + 快照 → 按 (deliveryType, actionType) 分组 → successRate/confidence/avgDeltaSpend15min → 规则/证据列表
- **重构目标**：规则从静态文档迁移到 `trigger_rules` 表，数据源全部改读 DB，规则效果自动回写

---

## 六、重构方案总览

### 6.1 完整重构（S 系列）

- **目录分层**：`src/` 骨架 + 分层依赖规则（platform 只依赖 utils/config；domain 纯逻辑；services 只编排；禁止跨层 import）
- **进程合并**：cron 6 进程 → `oec-scheduler`；换班+直播 → `live-pusher`；监听+队列 → `feishu-bus`；12 → ≤6
- **monitor-data 分区**：snapshots/daily/shifts/state/logs/push-fallback + oceanengine.db
- **文档治理**：115 个 md → 有效 <15 + `docs/archive/` + ADR

### 6.2 数据中枢与 AI 调优（D 系列）

- **采集独立**：`oec-collector` 5min 粒度，只写库；live-watcher 并入
- **数据模型**：`snapshots` 5min + account_id；新增 `shifts` / `operations`（状态机 proposed→pending→confirmed→executing→succeeded/failed→rolled_back）/ `operation_effects`（15/30/60/120min 窗口）/ `trigger_rules` / `ai_decisions`
- **触发引擎**：条件求值 → 冷却/日上限/互斥检查 → confirm（飞书确认）或 auto（低风险）→ 执行 → 效果追踪 → 规则置信度回写
- **安全边界**：审批兜底、参数护栏、熔断、一键回滚、全量审计

### 6.3 前端抓取重构（P 系列，已被 D 系列吸收）

- 会话层（oec-session）→ 传输层（oec-http/oec-capture）→ 数据层（oec-normalize/oec-dom-scraper）→ 编排层（oec-router）→ 40102 逆向沉淀

### 6.4 多租户（S7，独立立项）

- 运营 1:N 直播间 1:N 广告账户；accounts.json 配置化接入；数据/推送隔离；Chrome Profile 池

---



### 6.5 远程运维通道治理（新增主题 K）

**决策（2026-08-01）**：不采用固定域名/固化地址的远程网关方案（无固定公网域名等环境条件），远程通道维持 CodeBuddy 网关 + cloudflared trycloudflare 动态隧道 + 地址变更飞书通知的现状，仅做治理与安全加固。

1. **纳入健康监控**：隧道进程（cloudflared）、隧道状态文件、网关 hook 执行结果纳入 `monitor-daemon`/`watchdog.ps1` 检查项，异常告警飞书。
2. **地址通知标准化**：`check_gateway_hook.sh` + `send_gateway_to_feishu.py` 收敛到 `scripts/`，记录结构化日志；密码轮换策略写入文档。
3. **安全加固**：隧道密码与飞书通知凭据入库 `.env`（不写死脚本）；可选仅允许只读 webCLI 会话；Dashboard 走隧道时强制密码 + CSRF（现有 csrf-utils 已具备）。
4. **文档化**：远程访问手册（如何获取最新地址、如何连接 webCLI、如何重启隧道）写入 README 附录。
5. **可选演进**：把监控 Dashboard（8899）纳入远程通道，实现「手机/异地查看监控大盘 + webCLI 运维」。

## 七、路线图总览

| 阶段 | 主题 | 关键交付 |
|---|---|---|
| S0 | 冻结基线 | PM2/DB/卡片样例存档 + git 标签 |
| S1 | 安全清理 | 临时脚本/日志/备份归档 |
| S2 | 基础设施 | 数据库统一、配置外置、monitor-data 分区 |
| S3 | 采集收敛 | oec-session/http/normalize 上线（P0-P2） |
| S4 | 业务拆分 | monitor-v3/5min-check/feedback-server 拆分；删旧前端 |
| D0 | 数据模型 | schema 迁移：新表 + account_id；operations 兼容 actions |
| D1 | 采集器独立 | oec-collector + 卡片/汇总改读库（并行 diff） |
| D2 | 操作闭环入库 | operations/operation_effects 替换文件队列与 audit |
| D3 | 规则引擎 | trigger_rules + trigger-engine（confirm 模式，Rule-001 试点） |
| S5 | 进程合并 | 12 → 6 进程 |
| S6 | 测试强化 | 单测 ≥30 + windows CI + import 检查 |
| D4 | AI 自动执行 | auto 模式灰度（低风险） |
| S7 | 多租户 | accounts.json + 数据/推送隔离 |
| S8 | 远程通道治理 | 隧道/网关/地址通知纳入健康监控 + 安全加固 + 文档化 |

依赖：D1 依赖 S3；D2 依赖 S2；D3 依赖 D1+D2；D4 依赖 D3；S7 可与 D4 并行。

---

## 八、风险与回滚

| 风险 | 等级 | 缓解 |
|---|---|---|
| 主链路断更（15/5min 卡片） | 高 | 基线存档 + 新旧并行 diff + PM2 逐进程回滚 |
| 数值/字段解析事故 | 高 | 统一 parseNum + 列头映射 + 结构化告警 |
| 数据库迁移损坏历史 | 高 | VACUUM INTO 备份 + migrations 增量 |
| AI 自动操作失控 | 高 | 审批兜底 + 冷却/日上限/护栏/熔断 + 一键回滚 + 全量审计 |
| 40102 端点突破失败 | 低 | 仅 HTTP API 抓取；缺失端点记录缺失标记 + 飞书告警，不阻塞主线 |
| 多租户范围蔓延 | 中 | 独立立项，S7 前不动主链路 |

---

## 九、文档地图

### 9.1 有效文档（维护中）

| 文档 | 内容 |
|---|---|
| `完整重构方案_20260801.md` | 全项目重构方案（S 系列） |
| `数据中枢与AI调优架构设计.md` | 采集独立 + 唯一事实源 + AI 调优闭环（D 系列） |
| `前端数据抓取重构方案.md` | 采集链路收敛（P 系列） |
| `多租户架构设计_线索直播广告监控.md` | 多租户演进 |
| `AI诊断规则手册.md` | 诊断规则基线（将配置化） |
| `API字段全量档案.md` | 已逆向端点字段 |
| `CARD-FORMAT.md` | 卡片格式规范 |
| `直播状态巡检协议规范.md` | 直播巡检协议 |
| `工程化优化方案_20260725.md` | 工程化基线 |
| `CHANGELOG.md` | 版本演进 v0.1~v1.3 |
| `README.md` / `CODEBUDDY.md` | 快速开始 / 项目规范 |

### 9.2 归档建议（docs/archive/）

`架构规划_*` 系列（20+）、`*_评审*`、`*_审查*`、`*_替代方案选型*`、`*_决策档案*`、`40102端点逆向思路.md`（已并入 API 档案）、各临时 `_*.mjs` 脚本说明等历史文档。

---

## 附录 A · 常用命令

```bash
pm2 start ecosystem.config.cjs          # 启动全部
pm2 status                              # 查看状态
pm2 restart pm2-15min pm2-5min          # 重启核心监控
tail -f monitor-data/monitor.log        # 主日志
# 重建原生模块（Node 升级后）
C:/Users/HTF2026/.workbuddy/binaries/node/versions/22.22.2/npm.cmd rebuild better-sqlite3
# 代码量统计
bash scripts/count-lines.sh --markdown
```

## 附录 B · 测试入口

```bash
node scripts/ci-test.mjs                # CI 逻辑测试（csrf/router/push-guard）
node scripts/backup-verify.mjs          # 备份完整性/灾备演练
node tests/action-queue-lock.test.mjs   # 队列锁测试（单测）
```
