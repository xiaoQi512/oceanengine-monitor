# 巨量引擎监控系统架构规划 - 豆包审查反馈

> **审查对象**: `架构规划-整体方案.md` v1.0  
> **审查模型**: 豆包2.0 Code (WorkBuddy)  
> **审查日期**: 2026-06-28  
> **前置参考**: Kimi K2.7 Code 初步审查反馈 + 现有代码库深度阅读

---

## 一、总体评价

### 1.1 优点肯定

| 维度 | 评价 |
|------|------|
| **技术选型合理** | playwright-core + connectOverCDP 是解决当前 `matchedRows=0` 竞态问题的正确方向；SQLite + DuckDB 双引擎贴合单机场景 |
| **兼容性设计良好** | 保持现有 PM2 进程、lark-cli 推送、Chrome 9222 架构不变，平滑迁移设计值得肯定 |
| **业务闭环完整** | 覆盖监测-分析-建议-执行-反馈全流程，并预留 AI 调优接口 |
| **分层架构清晰** | 采集-存储-分析-服务四层结构逻辑分明 |

### 1.2 缺点与问题

| 优先级 | 问题 | 风险等级 | 说明 |
|--------|------|----------|------|
| 🔴 高 | 2周落地节奏过于激进 | 极高 | 兼职推进 + 多模块依赖 + 回归风险，需3周缓冲 |
| 🔴 高 | 退役策略过于激进 | 高 | P1结束即删除 `cdp-client.mjs`，未保留降级方案 |
| 🟡 中 | Schema 主键缺陷 | 高 | `campaigns` 表单主键会导致多账户/平台冲突 |
| 🟡 中 | 数据生命周期不清晰 | 中 | 只提归档，未明确清理策略与数据保留SLA |
| 🟡 中 | 测试计划缺失 | 中 | 全文档无单元/集成/回归测试方案 |
| 🟢 低 | 服务层路由可维护性 | 低 | 继续手写原生路由会导致 `feedback-server.mjs` 膨胀 |

---

## 二、改进建议

### 2.1 落地节奏重规划（3周 + 1周缓冲）

| 阶段 | 周期 | 核心目标 | 关键交付物 |
|------|------|----------|------------|
| P1 | 第1周 | playwright接入 + 灰度双跑 | `pw-client.mjs`、ai-regions playwright版、保留CDP降级方案 |
| P2 | 第2周 | SQLite结构化 + 双写期 | 修正Schema、`db-writer.mjs`、三方一致性检查 |
| P3 | 第3周 | DuckDB分析层 + 聚合 | 日报DuckDB聚合、7日趋势、归档脚本 |
| P4 | 第4周 | 全量切换 + 回归 | 测试报告、更新后的文档 |

**关键调整**：P1不删除 `cdp-client.mjs`，保留至少4周作为降级方案。

---

### 2.2 Schema 复合主键修正

**问题**：原方案 `campaigns` 表仅 `campaign_id` 主键，无法处理同计划跨账户/平台场景。

**修正核心**：
```sql
-- campaigns 表（复合主键）
CREATE TABLE campaigns (
  campaign_id    TEXT NOT NULL,
  account_id     TEXT NOT NULL,
  platform       TEXT NOT NULL DEFAULT 'oceanengine',
  campaign_name  TEXT,
  status         TEXT,
  daily_budget   REAL,
  created_at     TEXT DEFAULT (datetime('now', 'localtime')),
  updated_at     TEXT DEFAULT (datetime('now', 'localtime')),
  PRIMARY KEY (campaign_id, account_id, platform)
);

-- 新增死信队列表
CREATE TABLE dead_letter_jobs (
  id             INTEGER PRIMARY KEY AUTOINCREMENT,
  job_type       TEXT NOT NULL,
  job_payload    TEXT,
  error_message  TEXT,
  failed_at      TEXT DEFAULT (datetime('now', 'localtime')),
  retry_count    INTEGER DEFAULT 0,
  max_retries    INTEGER DEFAULT 3,
  status         TEXT DEFAULT 'pending'
);
```

---

### 2.3 数据生命周期 SLA 设计

| 数据类型 | 热存储 | 温存储 | 冷存储 |
|----------|--------|--------|--------|
| snapshots | 30天 SQLite | 12个月 Parquet | 按需保留 |
| alerts | 90天 SQLite | 36个月 Parquet | 按需保留 |
| feedbacks | 365天 SQLite | 永久 Parquet | - |
| actions | 365天 SQLite | 永久 Parquet | - |

**建议**：前3个月归档不删SQLite数据，仅双写验证。

---

### 2.4 多账户隔离方案

推荐采用 **独立 Profile + playwright BrowserContext** 方案，避免"同Chrome不同tab"的串号风险。

```javascript
// 伪代码示例
const ACCOUNT_PROFILES = {
  '1842681352509635': { name: '真人号', profile: 'Profile 4' },
  '1842681994872135': { name: 'AI东区', profile: 'Profile 5' },
  // ...
};
```

---

### 2.5 配置中心设计

新增 `config.json` 统一管理，避免硬编码：
- 账户配置（预算、告警阈值）
- 监控时段配置
- 推送配置
- 数据库路径配置

---

### 2.6 自动执行安全链

```
proposed → approved（飞书确认）→ executing → success（或 failed）
     ↓
  rejected（飞书拒绝）
```

**关键**：执行前二次校验当前状态 + 保存回滚信息 + 操作日志完整记录。

---

### 2.7 测试方案补充

| 层级 | 内容 |
|------|------|
| 单元测试 | `db-writer.mjs`、`monitor-utils.mjs`、`analysis.mjs` |
| 集成测试 | 采集→存储→分析端到端 |
| 回归测试 | 告警逻辑、推送策略、日报生成 |

---

## 三、风险提醒

| 风险 | 概率 | 影响 | 缓解方案 |
|------|------|------|----------|
| playwright 与 OVUI 真实鼠标事件不兼容 | 中 | 高 | P1 先 POC 验证，失败则保留 CDP 方案 |
| DuckDB Node API Windows 不稳定 | 中 | 中 | P3 先 POC，失败则用 `better-sqlite3` 聚合 |
| 多账户串号 | 中 | 高 | 独立 Profile + 隔离 context，POC 验证 cookie |
| SQLite WAL 锁竞争（双写期） | 低 | 中 | 5min 间隔足够，仍建议用队列串行化写入 |
| 历史 JSON 导入失败 | 中 | 低 | 导入脚本做字段映射 + ON CONFLICT IGNORE + 行数校验 |
| 2周节奏导致回滚困难 | 高 | 高 | 延长至4周，每阶段设检查点 + 备份 |
| 自动执行误操作 | 低 | 极高 | 必须人工确认 + 执行前二次校验 + 回滚机制 |

---

## 四、立即可以做的 3 个 POC 验证

1. **playwright-core OVUI 交互 POC**：连接9222，测试分页/排序/表格读取
2. **better-sqlite3 + DuckDB POC**：建表、插入、ATTACH 查询
3. **多账户隔离 POC**：验证不同账户的 Cookie/LocalStorage 是否隔离

---

## 五、审查模型

**豆包2.0 Code** (WorkBuddy)

---

**文档结束**
