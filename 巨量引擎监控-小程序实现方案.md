# 巨量引擎监控 · 小程序实现方案

> **日期**: 2026-06-28 | **版本**: v1.1（审查修正版）
> **关联文档**: `巨量引擎监控-小程序上架方案.md`（6.25，卡点分析）| `架构规划_整体方案_v3.2_决策驱动.md`
> **定位**: 小程序从立项到上线的完整实施指南，可直接转发给其他 agent 执行
> **前提**: 已获得企业营业执照（小程序上架的必要条件）
> **⚠️ 当前状态**: 用户为单用户自用、无企业资质，小程序方案暂不可行。v3.2 架构规划主推"单文件 HTML 看板"。本文档为条件满足时的预备方案。

---

## 一、可行性分析

### 1.1 前置条件检查清单

| # | 条件 | 当前状态 | 获取方式 | 周期 | 是否阻塞 |
|---|---|---|---|---|---|
| C1 | **企业营业执照** | ❌ 待确认 | 用户需自行获取（个人无法申请） | 不确定 | **是（硬性）** |
| C2 | **微信小程序开发者账号（企业）** | ❌ 待注册 | https://mp.weixin.qq.com → 注册 → 企业类型 → 对公打款验证 | 1-3 个工作日 | 是 |
| C3 | **已备案域名** | ❌ 待确认 | 域名购买 + ICP 备案（企业主体） | **2-4 周**（备案是最大瓶颈） | **是（硬性）** |
| C4 | **HTTPS 证书** | ❌ 待申请 | Let's Encrypt 免费 / 阿里云免费证书 | 即时 | 否（依赖 C3） |
| C5 | **云服务器（公网）** | ❌ 待部署 | 阿里云/腾讯云轻量 ECS | 即时 | 否 |
| C6 | **微信支付商户号** | — | 不需要，自用工具无支付需求 | — | — |

> **⚠️ 个人开发者不能上架正式版小程序**。微信虽然允许个人注册小程序，但：
> - 个人小程序不能选择"工具"类目（广告监控需要此类目）
> - 个人小程序无法使用大部分 JSAPI（如 `wx.login` 获取用户信息）
> - 审核时工具类小程序几乎必定驳回
> - **结论：必须企业主体，个人不行**

### 1.2 可行性判定

```
if (C1 + C2 + C3 + C4 + C5 全部满足) {
    → 可行，进入实施阶段（正式版）
} else if (C1 满足但 C3/C5 不满足) {
    → 开发版可行（微信开发者工具不校验域名 + 内网穿透调试）
    → 正式版需补齐 C3/C5
    → 注意：开发版仅开发者本人可用，不能分发给其他人
} else if (C1 不满足) {
    → ❌ 完全不可行，走替代方案（§1.3）
}
```

### 1.3 替代方案（无企业资质时 → 当前推荐）

| 方案 | 资质要求 | 体验 | 开发量 | 推荐度 |
|---|---|---|---|---|
| **单文件 HTML 看板** | 无 | 中 | **低（1-2 周）** | ★★★★★（**v3.2 推荐**） |
| **H5 + 飞书内嵌** | 无 | 中 | 低 | ★★★★ |
| **PWA（渐进式 Web 应用）** | 无（需 HTTPS） | 高 | 中 | ★★★ |
| **独立 Web SPA** | 无 | 高 | 中 | ★★★（对自用偏重） |
| **Electron 桌面应用** | 无 | 高 | 高 | ★★（对个人工具过重） |

> **v3.2 架构规划结论**：当前走"单文件 HTML 看板"（feedback-server `/dashboard`），零资质、零框架、零构建。
> **本文档目的**：如果未来获得企业资质或转为企业产品，可直接按本方案执行。

---

## 二、技术选型

### 2.1 小程序平台选择

| 平台 | 资质 | 后端要求 | 推送能力 | 用户触达 | 备注 |
|---|---|---|---|---|---|
| **微信小程序** | 企业主体 + 备案域名 | HTTPS + 备案域名 | 订阅消息（需用户授权） | 最广 | **主推** |
| **飞书小程序** | 企业主体 | HTTPS | 飞书消息卡片 | 仅飞书用户 | 备选 |
| **支付宝小程序** | 企业主体 | HTTPS | 模板消息 | 中等 | 不推荐 |

> **推荐：微信小程序**（触达最广，手机随时看数据）
> **备选：飞书小程序**（如果日常办公全在飞书，可后续用 Taro 编译飞书端）

### 2.2 开发框架选择

| 框架 | 语言 | 跨端能力 | 包体积 | 生态活跃度 | 适合场景 |
|---|---|---|---|---|---|
| **Taro 3.x** | React / Vue | 微信/飞书/H5/支付宝 | 中 | ★★★★ | 需要多端编译 |
| **uni-app** | Vue | 全平台 | 中 | ★★★★★ | Vue 技术栈 |
| **原生小程序** | WXML + WXSS | 仅微信 | **小** | 官方 | 只做微信、追求最小包 |

> **推荐：Taro 3.x + React**
> - 与后端 Node.js 技术栈统一（减少语言切换成本）
> - 一次开发，编译微信 + H5（H5 用于开发调试 + 兜底）
> - 京东开源，社区活跃
>
> **⚠️ 如果只做微信不需要跨端**，原生小程序包体积最小、兼容性最好。
> Taro 的跨端优势在仅需微信时消失，反而增加编译复杂度。
> **建议**：先评估是否真的需要 H5 兜底，如果不需要，选原生小程序。

### 2.3 技术栈总览

| 层 | 技术 | 理由 |
|---|---|---|
| **小程序前端** | Taro 3.x + React 18 + TypeScript | 跨端 + 类型安全 |
| **UI 组件** | NutUI 4（Taro 版） | 比 Taro UI 更活跃，京东官方维护 |
| **图表** | **echarts-for-weixin**（原生方案） | 见 §2.4 说明 |
| **状态管理** | Zustand | 轻量，React 生态 |
| **HTTP** | Taro.request | 原生 API，无额外依赖 |
| **后端 API** | feedback-server :8899 扩展 | 复用已有 |
| **数据库** | SQLite（本机主存）+ 云端只读副本 | 单机轻量 |
| **部署** | 云服务器 + Nginx 反代 + HTTPS | 小程序强制 HTTPS |

### 2.4 ⚠️ ECharts 小程序方案选择（重要）

| 方案 | 包体积 | Taro 兼容 | 维护状态 | 推荐 |
|---|---|---|---|---|
| `echarts-taro` | ~800KB | ✅ | ⚠️ 维护不活跃 | ★★ |
| `echarts-for-weixin` (微信官方适配) | ~1MB（可按需裁剪到 300KB） | ❌ 原生方案 | ✅ 微信团队维护 | ★★★★ |
| `uCharts` | ~100KB | ✅ Taro 适配 | ✅ 活跃 | ★★★★★ |

> **推荐：uCharts**（轻量 + Taro 兼容 + 活跃维护）
> - 小程序首包限制 2MB，ECharts 完整版 ~1MB 太奢侈
> - uCharts 仅 ~100KB，支持折线图/柱状图/饼图/进度条，满足看板需求
> - 如果图表需求复杂（漏斗图/热力图），再考虑 echarts-for-weixin 按需引入
>
> **⚠️ 首包体积预算**：
> ```
> 微信小程序首包限制: 2MB
> Taro 运行时:        ~200KB
> React 运行时:       ~150KB
> uCharts:            ~100KB
> NutUI 组件:         ~200KB
> 业务代码:           ~300KB
> ──────────────────────────
> 合计:               ~950KB（安全，< 2MB）
>
> 如果用 echarts-for-weixin 完整版:
> ECharts:            ~1MB
> 合计:               ~1.65MB（危险，接近 2MB 限制）
> → 必须用分包加载（subpackages），图表页放子包
> ```

---

## 三、小程序页面设计

### 3.1 页面结构

```
pages/
├── index/              # Tab1: 实时看板（首页）
│   ├── 顶部指标卡片（消耗/CPA/线索/告警数）
│   ├── 消耗节奏进度条（时间进度 vs 预算消耗）
│   ├── 今日趋势折线图（uCharts）
│   └── 最新告警列表（最近 5 条）
│
├── campaigns/          # Tab2: 计划列表
│   ├── 筛选栏（状态: 全部/投放中/已暂停 | 排序: 消耗/CPA）
│   ├── 计划卡片列表
│   │   ├── 计划名 + 状态标签
│   │   ├── 消耗/预算 进度条
│   │   ├── CPA + 转化数
│   │   └── 操作按钮组（暂停/启用/调预算）
│   └── 下拉刷新 + 触底加载
│
├── campaign-detail/    # 计划详情（从列表点击进入，非 Tab 页）
│   ├── 基础信息（名称/ID/状态/预算）
│   ├── 指标卡片（消耗/CPA/线索/转化/CTR/CVR）
│   ├── 时序趋势图（近 24h 消耗曲线）
│   ├── 转化漏斗（线索→开口→留资→表单）
│   └── 操作历史（该计划的近期操作记录）
│
├── alerts/             # Tab3: 告警中心
│   ├── 告警列表（按时间倒序）
│   │   ├── 告警类型图标 + 严重度颜色
│   │   ├── 告警消息
│   │   └── 时间 + 关联计划
│   └── 筛选（严重度/类型/日期）
│
└── settings/           # Tab4: 系统设置
    ├── 数据刷新频率（15s/30s/60s）
    ├── 推送开关（订阅消息授权）
    ├── 操作确认等级（L1-L3 配置）
    └── 关于 + 版本信息
```

> **⚠️ 分包策略**（如果首包超 2MB）：
> ```json
> // app.config.ts
> {
>   subpackages: [
>     {
>       root: 'pages/campaign-detail',  // 详情页放子包
>       pages: ['index']
>     }
>   ]
> }
> ```

### 3.2 页面设计稿（核心页面说明）

#### 首页 - 实时看板

```
┌─────────────────────────────────┐
│  巨量引擎监控          🔄 15s   │  ← 顶部栏 + 自动刷新倒计时
├─────────────────────────────────┤
│ ┌──────┐ ┌──────┐ ┌──────┐     │
│ │ ¥12.3k│ │CPA 42│ │ 线索38│     │  ← 指标卡片（3列）
│ │ +18%↑ │ │ -5%↓ │ │ +12%↑│     │     红涨绿跌（中国惯例）
│ └──────┘ └──────┘ └──────┘     │
├─────────────────────────────────┤
│ 消耗节奏                        │
│ ┌─────────────────────────────┐ │
│ │ 时间进度 ████████░░ 65%     │ │  ← 双进度条
│ │ 预算消耗 ██████░░░░ 48%     │ │
│ │ 节奏评估: ✅ 健康（偏慢）    │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ 今日消耗趋势                    │
│ ┌─────────────────────────────┐ │
│ │     /\                      │ │  ← uCharts 折线图
│ │    /  \    /\               │ │
│ │   /    \  /  \              │ │
│ │ 7  9  11  14  17  20  23   │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ 最新告警                    全部>│
│ 🔴 14:30 [CPA过高] xxx计划 CPA=85  │
│ 🟡 14:15 [消耗过快] yyy计划 +35%   │
│ 🟢 13:45 [掉量] zzz计划 环比-40%   │
└─────────────────────────────────┘
```

#### 计划列表页

```
┌─────────────────────────────────┐
│ 计划列表                        │
├─────────────────────────────────┤
│ [全部] [投放中] [已暂停]  排序▼ │  ← 筛选 + 排序
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │ 🟢 极狐-东区-信息流-001      │ │
│ │ 投放中                      │ │  ← 状态标签（绿=投放中/灰=暂停）
│ │ 消耗 ¥3,200 / 预算 ¥10,000  │ │
│ │ ████████░░░░ 32%            │ │  ← 预算进度条
│ │ CPA: ¥38  转化: 84  CTR: 2.1%│ │
│ │ [暂停] [调预算]              │ │  ← 操作按钮
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ ⚪ 极狐-南区-直播-003        │ │
│ │ 已暂停                      │ │
│ │ 消耗 ¥8,500 / 预算 ¥10,000  │ │
│ │ ██████████████ 85%          │ │
│ │ CPA: ¥65  转化: 130  CTR: 1.8%│
│ │ [启用] [调预算]              │ │
│ └─────────────────────────────┘ │
│ ... 下拉加载更多               │
└─────────────────────────────────┘
```

#### 操作确认弹窗

```
L1/L2 常规操作:
┌───────────────────────────────┐
│       确认操作                 │
│                               │
│  操作类型: 暂停计划            │
│  目标计划: 极狐-东区-信息流-001│
│  当前消耗: ¥3,200             │
│  当前状态: 投放中              │
│                               │
│  ⚠️ 暂停后该计划将停止消耗     │
│     预计影响: 日消耗减少 ¥3,200│
│                               │
│  [取消]          [确认暂停]    │
└───────────────────────────────┘

L3 高风险操作（如批量暂停/删除计划）:
┌───────────────────────────────┐
│       ⚠️ 高风险操作确认        │
│                               │
│  操作: 批量暂停 3 个计划       │
│  影响消耗: ¥15,000/天         │
│                               │
│  请输入 "确认" 以继续:        │
│  ┌─────────────────────────┐  │
│  │                         │  │  ← 二次输入确认
│  └─────────────────────────┘  │
│                               │
│  [取消]          [确认执行]    │
└───────────────────────────────┘
```

---

## 四、后端 API 设计

### 4.1 小程序专用 API 端点

> 所有端点新增到 feedback-server :8899，通过 Nginx HTTPS 反代对外暴露
> **⚠️ 微信小程序后台需配置 request 合法域名**：设置 → 开发管理 → 服务器域名 → request合法域名 → 添加 `https://your-domain.com`

```
基础路径: https://your-domain.com/api/v1

GET  /campaigns              # 计划列表
     参数: ?status=all|active|paused&sort=cost|cpa&limit=50&offset=0
     响应: { campaigns: [...], total: 10 }

GET  /campaigns/:id          # 计划详情
     响应: { campaign: {...}, snapshots_24h: [...], alerts: [...] }

GET  /snapshots/summary      # 看板汇总数据
     参数: ?date=2026-06-28
     响应: {
       total_cost: 12300,
       total_leads: 38,
       cpa: 42,
       pacing: { time_progress: 65, budget_used: 48, health: 'good' },
       alerts_count: 3,
       trend: [{ hour: 7, cost: 200 }, ...]
     }

GET  /alerts                 # 告警列表
     参数: ?severity=high|medium|low&date=2026-06-28&limit=20
     响应: { alerts: [...] }

POST /actions                # 提交操作
     Body: { campaign_id, action_type, params }
     响应: { action_id, status: 'executed'|'queued', result: {...} }
     确认等级由后端根据 action_type 自动判定:
       L1: pause / enable → 立即执行（前端已弹窗确认）
       L2: update_budget（±20% 以内）→ 立即执行（前端已显示变化量）
       L3: update_budget（>20%）/ batch_pause / delete → 立即执行（前端已二次输入确认）

GET  /actions                # 操作日志
     参数: ?campaign_id=xxx&date=2026-06-28&limit=20
     响应: { actions: [...] }

GET  /health                 # 健康检查
     响应: { chrome: 'online', cdp: 'connected', last_snapshot: '2026-06-28T14:30:00' }

GET  /pending-actions        # 待执行操作队列（供本机 agent 轮询）
     响应: { actions: [{ action_id, campaign_id, action_type, params }] }

POST /actions/:id/result     # 回传执行结果（供本机 agent 调用）
     Body: { status: 'executed'|'failed', result: {...} }
```

### 4.2 认证方案（自用简化版）

```
单用户自用 → 不需要用户系统 → 简单 token 防外网扫描

方案: Header X-Token
- feedback-server 启动时读取 monitor-data/api-token.txt（文件不存在则生成并写入）
- Token 持久化，不会因重启失效
- 小程序首次使用时手动输入 token，存储在 wx.setStorageSync('apiToken')
- 每次请求携带 Header: X-Token: <stored_token>
- Token 不匹配返回 403

⚠️ 这不是安全鉴权，只是防止公网扫描器随意访问
   真正的安全靠 HTTPS + 不公开域名
```

### 4.3 HTTPS 部署方案（关键架构问题）

> **核心矛盾**：小程序后端必须公网 HTTPS，但采集层（Chrome/CDP）必须在本机 Windows。
> 这意味着不能简单地把 feedback-server 搬到云服务器。

#### 推荐架构：云端轻量 API + 本机全功能

```
                    微信小程序
                       │ HTTPS
                       ▼
            ┌─── 云服务器 (ECS) ───┐
            │  Nginx :443          │
            │  ├── HTTPS 反代      │
            │  ├── 静态资源托管     │
            │  └── 轻量 API 代理   │
            │      :8901           │
            │  (仅做转发+缓存)     │
            └─────────┬────────────┘
                      │ frp/Tailscale 隧道
                      ▼
            ┌─── 本机 Windows ────┐
            │  feedback-server    │
            │  :8899              │
            │  ├── 数据采集       │
            │  ├── CDP 操作       │
            │  ├── SQLite 读写    │
            │  └── 全功能 API     │
            └─────────────────────┘
```

**为什么不用"云端 SQLite 副本 + rsync"方案**：
- rsync SQLite .db 文件有数据一致性风险（WAL 模式下需同时同步 .db-wal + .db-shm，共 3 个文件）
- Windows 没有原生 rsync（需安装 cwRsync 或 Git Bash 的 rsync）
- 操作指令回传需要额外的队列机制，增加复杂度
- **frp/Tailscale 隧道方案更简单**：云端只做 HTTPS 终端 + 流量转发，本机做全部业务逻辑

#### 内网穿透方案对比

| 方案 | 延迟 | 稳定性 | 成本 | 易用性 | 推荐 |
|---|---|---|---|---|---|
| **Tailscale** | 低 | 高 | 免费（个人） | **极简**（装好即用） | ★★★★★ |
| **frp** | 低 | 高 | 免费 | 中（需配置 frps/frpc） | ★★★★ |
| **ngrok** | 中 | 中 | 免费额度有限 | 简单 | ★★★ |

> **推荐 Tailscale**：
> - 本机 Windows + 云服务器都安装 Tailscale
> - 自动组网，两端获得 100.x.x.x 内网 IP
> - 云服务器 Nginx 反代到 `http://100.x.x.x:8899`（本机 feedback-server）
> - 零配置、零端口暴露、端到端加密

---

## 五、分阶段实施计划

### 阶段总览（8 周）

| 阶段 | 工期 | 内容 | 交付物 | 前置条件 |
|---|---|---|---|---|
| **P0 基建准备** | **2-4 周** | 企业资质 + 域名备案 + 云服务器 + Tailscale | 营业执照 + 备案号 + 隧道通 | C1-C5 |
| **P1 数据层** | 1 周 | SQLite 建表 + JSON 迁移 + 双写 | oceanengine.db + 迁移脚本 | — |
| **P2 后端 API** | 1 周 | feedback-server 新增 API 端点 + Nginx 反代 | REST API + HTTPS 可访问 | P0 + P1 |
| **P3 小程序开发** | 2-3 周 | Taro 工程 + 4 个页面 + 操作交互 | 小程序代码 + 开发版可体验 | P2 |
| **P4 测试上线** | 1 周 | 提审 + 灰度 + 正式发布 | 小程序正式版 | P3 |
| **P5 执行层** | 1 周 | CDP 操作集成 + 操作日志 | 暂停/启用/调预算闭环 | P2（可与 P3 并行） |

> **⚠️ P0 是最大瓶颈**：ICP 备案需要 2-4 周，不可压缩。P1/P2 可在备案期间并行推进。

### P0: 基建准备（2-4 周，备案是瓶颈）

| 任务 | 详情 | 产出 | 耗时 |
|---|---|---|---|
| 0.1 注册微信小程序企业开发者 | mp.weixin.qq.com → 注册 → 企业类型 → 对公打款验证 | AppID + AppSecret | 1-3 天 |
| 0.2 域名购买 + ICP 备案 | 域名购买 → 提交备案（企业主体） → 管局审核 | 备案号 | **2-4 周** |
| 0.3 云服务器 | 阿里云轻量 ECS（2C2G 足够），选与备案一致的区域 | 公网 IP | 即时 |
| 0.4 HTTPS 证书 | Let's Encrypt (`certbot`) 或阿里云免费证书 | cert.pem + key.pem | 即时（需域名） |
| 0.5 Nginx 部署 | 安装 Nginx + 配置 HTTPS 反代 + SSL | `https://api.xxx.com` 可访问 | 半天 |
| 0.6 Tailscale 组网 | 本机 Windows + 云服务器安装 Tailscale，互通 | 隧道连通 | 半天 |
| 0.7 微信后台配置 | 服务器域名白名单 + 业务域名 + JS 安全域名 | 小程序可请求后端 | 半天 |

> **P0 期间的并行工作**（不等备案）：
> - 微信开发者工具 → 本地开发（不校验域名）
> - 本机 feedback-server 直接作为后端
> - 开发完成后切到 HTTPS 域名只需改一个配置

### P1: 数据层（1 周）

| 任务 | 详情 | 产出 |
|---|---|---|
| 1.1 SQLite 建表 | 按 v3.2 Schema DDL 创建 6 张表 | `oceanengine.db` |
| 1.2 JSON 历史导入 | `monitor-data/*.json` → SQLite | 全量历史入库 |
| 1.3 双写集成 | 采集脚本同时写 JSON + SQLite | 双写 + 一致性校验脚本 |
| 1.4 数据清理策略 | >365 天数据归档为 Parquet，SQLite 仅保留近 1 年 | 清理脚本 + cron |

### P2: 后端 API（1 周）

| 任务 | 详情 | 产出 |
|---|---|---|
| 2.1 GET /api/v1/snapshots/summary | 看板汇总（消耗/CPA/节奏/趋势） | curl 验证 |
| 2.2 GET /api/v1/campaigns | 计划列表 + 筛选排序 | curl 验证 |
| 2.3 GET /api/v1/campaigns/:id | 计划详情 + 时序数据 | curl 验证 |
| 2.4 GET /api/v1/alerts | 告警列表 | curl 验证 |
| 2.5 POST /api/v1/actions | 提交操作（含确认等级判定） | curl 验证 |
| 2.6 GET /api/v1/pending-actions | 待执行队列（本机 agent 轮询） | curl 验证 |
| 2.7 POST /api/v1/actions/:id/result | 回传执行结果 | curl 验证 |
| 2.8 X-Token 认证中间件 | 持久化 token 文件 + Header 校验 | 无 token → 403 |
| 2.9 HTTPS 联调 | 小程序开发者工具 → Nginx → Tailscale → feedback-server | 全链路通 |

### P3: 小程序开发（2-3 周）

| 子阶段 | 工期 | 任务 | 产出 |
|---|---|---|---|
| 3.1 工程初始化 | 0.5 天 | `npx @tarojs/cli init monitor-mini-app` + 依赖 + 目录 | 可运行空壳 |
| 3.2 公共组件 | 2 天 | MetricCard / ProgressBar / AlertItem / CampaignCard / ConfirmDialog | 组件库 |
| 3.3 首页看板 | 3 天 | 指标卡片 + 节奏进度条 + uCharts 趋势图 + 告警列表 | 首页可展示数据 |
| 3.4 计划列表 | 2 天 | 筛选排序 + 卡片列表 + 下拉刷新 + 操作按钮 | 列表页可用 |
| 3.5 计划详情 | 2 天 | 基础信息 + 指标 + 趋势图 + 漏斗 + 操作历史 | 详情页可用 |
| 3.6 告警中心 | 1 天 | 告警列表 + 筛选 | 告警页可用 |
| 3.7 操作面板 | 2 天 | L1/L2/L3 确认弹窗 + 调预算输入 + POST /actions | 操作可执行 |
| 3.8 系统设置 | 1 天 | 刷新频率 + 推送开关 + Token 配置 + 关于 | 设置页可用 |
| 3.9 联调测试 | 2 天 | 真机调试 + 数据校验 + 操作闭环 + 弱网测试 | 开发版可体验 |

> **3.9 弱网测试**：手机切 4G 测试（非 WiFi），验证：
> - 请求超时处理（超时提示 + 重试按钮）
> - 数据缓存（`wx.setStorageSync` 缓存上次数据，断网时显示缓存）
> - 操作提交失败时不重复提交（幂等性）

### P4: 测试上线（1 周）

| 任务 | 详情 | 产出 |
|---|---|---|
| 4.1 隐私协议 | 编写隐私政策页面（小程序审核强制要求） | `/pages/privacy/index` |
| 4.2 类目选择 | 选"工具 > 信息查询"或"商业服务 > 企业管理" | 类目确认 |
| 4.3 代码审查 | 检查代码规范、敏感信息（无硬编码密钥）、包体积 | 代码就绪 |
| 4.4 提交审核 | 微信后台提交审核（1-3 个工作日） | 审核通过 |
| 4.5 体验版发布 | 设置体验版 → 添加体验成员 → 真机验证 | 体验版可用 |
| 4.6 正式发布 | 全量发布 | 小程序正式上线 |

### P5: 执行层（1 周，可与 P3 并行）

| 任务 | 详情 | 产出 |
|---|---|---|
| 5.1 集成 cdp-action | feedback-server import cdp-action.mjs | POST /actions → CDP 执行 |
| 5.2 操作日志 | 每次操作记录入 operation_log 表 | 日志可查 |
| 5.3 指令回传闭环 | 小程序 → HTTPS → 本机执行 → 结果写回 → 小程序刷新可见 | 全链路闭环验证 |
| 5.4 失败处理 | CDP 操作失败 → 自动重试 1 次 + 截图留证 + 标记 status=failed | 失败不卡队列 |
| 5.5 离线队列 | Chrome 离线时操作入队，恢复后自动续执行 | 队列持久化（SQLite） |

---

## 六、Taro 工程结构

```
monitor-mini-app/
├── config/
│   ├── index.ts              # Taro 编译配置
│   ├── dev.ts                # 开发环境（API 指向 localhost:8899）
│   └── prod.ts               # 生产环境（API 指向 https://your-domain.com）
├── src/
│   ├── app.tsx               # 入口
│   ├── app.config.ts         # 全局配置（页面路径/TabBar/分包）
│   ├── pages/
│   │   ├── index/            # 首页看板
│   │   │   ├── index.tsx
│   │   │   ├── index.config.ts
│   │   │   └── index.module.css
│   │   ├── campaigns/        # 计划列表
│   │   │   ├── index.tsx
│   │   │   └── index.module.css
│   │   ├── campaign-detail/  # 计划详情
│   │   │   └── index.tsx
│   │   ├── alerts/           # 告警中心
│   │   │   └── index.tsx
│   │   ├── settings/         # 系统设置
│   │   │   └── index.tsx
│   │   └── privacy/          # 隐私协议（审核必须）
│   │       └── index.tsx
│   ├── components/
│   │   ├── MetricCard/       # 指标卡片
│   │   ├── ProgressBar/      # 进度条
│   │   ├── AlertItem/        # 告警条目
│   │   ├── CampaignCard/     # 计划卡片
│   │   └── ConfirmDialog/    # 操作确认弹窗
│   ├── services/
│   │   ├── api.ts            # HTTP 请求封装（Taro.request + X-Token）
│   │   ├── campaigns.ts      # 计划相关 API
│   │   ├── snapshots.ts      # 快照相关 API
│   │   └── actions.ts        # 操作相关 API
│   ├── stores/
│   │   ├── useCampaignStore.ts  # Zustand 状态管理
│   │   └── useAlertStore.ts
│   ├── utils/
│   │   ├── format.ts         # 数字/金额/百分比格式化
│   │   └── time.ts           # 时间处理
│   └── styles/
│       └── global.css        # 全局样式
├── package.json
├── tsconfig.json
└── project.config.json       # 微信小程序项目配置
```

### app.config.ts 配置

```typescript
export default defineAppConfig({
  pages: [
    'pages/index/index',
    'pages/campaigns/index',
    'pages/alerts/index',
    'pages/settings/index',
    'pages/privacy/index'       // 隐私协议
  ],
  // 详情页放子包（减小首包体积）
  subpackages: [
    {
      root: 'subpackages',
      pages: ['pages/campaign-detail/index']
    }
  ],
  tabBar: {
    color: '#999999',
    selectedColor: '#1890ff',
    backgroundColor: '#ffffff',
    list: [
      { pagePath: 'pages/index/index', text: '看板',
        iconPath: 'assets/tab-dashboard.png',
        selectedIconPath: 'assets/tab-dashboard-active.png' },
      { pagePath: 'pages/campaigns/index', text: '计划',
        iconPath: 'assets/tab-campaigns.png',
        selectedIconPath: 'assets/tab-campaigns-active.png' },
      { pagePath: 'pages/alerts/index', text: '告警',
        iconPath: 'assets/tab-alerts.png',
        selectedIconPath: 'assets/tab-alerts-active.png' },
      { pagePath: 'pages/settings/index', text: '设置',
        iconPath: 'assets/tab-settings.png',
        selectedIconPath: 'assets/tab-settings-active.png' }
    ]
  },
  window: {
    navigationBarTitleText: '巨量引擎监控',
    navigationBarBackgroundColor: '#ffffff',
    navigationBarTextStyle: 'black',
    backgroundColor: '#f5f5f5'
  }
})
```

---

## 七、API 请求封装示例

```typescript
// src/services/api.ts
import Taro from '@tarojs/taro'

// 开发环境: http://127.0.0.1:8899/api/v1
// 生产环境: https://your-domain.com/api/v1
const BASE_URL = process.env.NODE_ENV === 'development'
  ? 'http://127.0.0.1:8899/api/v1'
  : 'https://your-domain.com/api/v1'

function getToken(): string {
  return Taro.getStorageSync('apiToken') || ''
}

async function request<T>(path: string, options?: {
  method?: 'GET' | 'POST'
  data?: Record<string, any>
}): Promise<T> {
  const token = getToken()
  if (!token) {
    Taro.navigateTo({ url: '/pages/settings/index' })
    throw new Error('请先配置 API Token')
  }

  try {
    const res = await Taro.request({
      url: `${BASE_URL}${path}`,
      method: options?.method || 'GET',
      data: options?.data,
      timeout: 10000,  // 10s 超时
      header: {
        'X-Token': token,
        'Content-Type': 'application/json'
      }
    })

    if (res.statusCode === 403) {
      Taro.showToast({ title: 'Token 无效，请在设置中检查', icon: 'none', duration: 3000 })
      throw new Error('Unauthorized')
    }

    if (res.statusCode >= 500) {
      Taro.showToast({ title: '服务器错误，请稍后重试', icon: 'none' })
      throw new Error(`Server Error: ${res.statusCode}`)
    }

    if (res.statusCode >= 400) {
      Taro.showToast({ title: '请求失败', icon: 'error' })
      throw new Error(`HTTP ${res.statusCode}`)
    }

    return res.data as T
  } catch (err) {
    // 网络超时 / 无网络
    if (err.errMsg?.includes('timeout') || err.errMsg?.includes('fail')) {
      // 尝试返回缓存数据
      const cached = Taro.getStorageSync(`cache:${path}`)
      if (cached) {
        Taro.showToast({ title: '网络异常，显示缓存数据', icon: 'none' })
        return cached as T
      }
      Taro.showToast({ title: '网络连接失败', icon: 'error' })
    }
    throw err
  }
}

// 写入缓存（GET 请求成功后调用）
function cacheGet<T>(path: string): Promise<T> {
  return request<T>(path).then(data => {
    Taro.setStorageSync(`cache:${path}`, data)
    return data
  })
}

// ===== API 函数 =====

export function getSnapshotSummary(date?: string) {
  const query = date ? `?date=${date}` : ''
  return cacheGet<any>(`/snapshots/summary${query}`)
}

export function getCampaigns(params: {
  status?: 'all' | 'active' | 'paused'
  sort?: 'cost' | 'cpa'
  limit?: number
  offset?: number
}) {
  const query = new URLSearchParams(params as any).toString()
  return cacheGet<{ campaigns: any[], total: number }>(`/campaigns?${query}`)
}

export function getCampaignDetail(id: string) {
  return cacheGet<{ campaign: any, snapshots_24h: any[], alerts: any[] }>(`/campaigns/${id}`)
}

export function getAlerts(params: {
  severity?: 'high' | 'medium' | 'low'
  date?: string
  limit?: number
}) {
  const query = new URLSearchParams(params as any).toString()
  return cacheGet<{ alerts: any[] }>(`/alerts?${query}`)
}

export function submitAction(data: {
  campaign_id: string
  action_type: 'pause' | 'enable' | 'update_budget'
  params?: Record<string, any>
}) {
  return request<{ action_id: string, status: string, result: any }>('/actions', {
    method: 'POST',
    data
  })
}

export function getActionLogs(params: {
  campaign_id?: string
  date?: string
  limit?: number
}) {
  const query = new URLSearchParams(params as any).toString()
  return cacheGet<{ actions: any[] }>(`/actions?${query}`)
}

export function getHealth() {
  return request<{ chrome: string, cdp: string, last_snapshot: string }>('/health')
}
```

---

## 八、操作确认组件示例

```tsx
// src/components/ConfirmDialog/index.tsx
import Taro from '@tarojs/taro'
import { View, Text, Input } from '@tarojs/components'
import { useState } from 'react'
import styles from './index.module.css'

interface ConfirmDialogProps {
  visible: boolean
  level: 'L1' | 'L2' | 'L3'
  actionType: 'pause' | 'enable' | 'update_budget'
  campaignName: string
  detail: string            // 如 "当前消耗: ¥3,200"
  impact: string            // 如 "暂停后该计划将停止消耗"
  onConfirm: () => void
  onCancel: () => void
}

const ACTION_LABELS: Record<string, string> = {
  pause: '暂停计划',
  enable: '启用计划',
  update_budget: '调整预算'
}

export function ConfirmDialog({
  visible, level, actionType, campaignName, detail, impact, onConfirm, onCancel
}: ConfirmDialogProps) {
  const [inputValue, setInputValue] = useState('')

  if (!visible) return null

  const actionLabel = ACTION_LABELS[actionType] || actionType
  const isHighRisk = level === 'L3'

  const handleConfirm = () => {
    if (isHighRisk && inputValue !== '确认') {
      Taro.showToast({ title: '请输入"确认"', icon: 'none' })
      return
    }
    onConfirm()
    setInputValue('')
  }

  return (
    <View className={styles.overlay} onClick={onCancel}>
      <View className={styles.dialog} onClick={e => e.stopPropagation()}>
        <Text className={styles.title}>
          {isHighRisk ? '⚠️ 高风险操作确认' : '确认操作'}
        </Text>

        <View className={styles.info}>
          <Text className={styles.infoRow}>操作类型: {actionLabel}</Text>
          <Text className={styles.infoRow}>目标计划: {campaignName}</Text>
          <Text className={styles.infoRow}>{detail}</Text>
        </View>

        <View className={styles.warning}>
          <Text>⚠️ {impact}</Text>
        </View>

        {isHighRisk && (
          <View className={styles.inputArea}>
            <Text className={styles.inputLabel}>请输入 "确认" 以继续:</Text>
            <Input
              className={styles.input}
              value={inputValue}
              onInput={e => setInputValue(e.detail.value)}
              placeholder='输入"确认"'
              focus={visible}
            />
          </View>
        )}

        <View className={styles.buttons}>
          <View className={styles.btnCancel} onClick={onCancel}>
            <Text>取消</Text>
          </View>
          <View
            className={`${styles.btnConfirm} ${isHighRisk ? styles.btnDanger : ''}`}
            onClick={handleConfirm}
          >
            <Text className={styles.btnText}>
              {isHighRisk ? '确认执行' : `确认${actionLabel}`}
            </Text>
          </View>
        </View>
      </View>
    </View>
  )
}
```

---

## 九、数据流与操作闭环

### 9.1 数据读取流

```
微信小程序页面
    │ 30s 定时轮询（setInterval）
    │ Taro.request GET /api/v1/snapshots/summary
    │ X-Token: <stored_token>
    ▼
Nginx :443 (HTTPS)
    │ proxy_pass http://100.x.x.x:8899  (Tailscale 内网 IP)
    ▼
本机 feedback-server :8899
    │ 查 SQLite oceanengine.db
    │ 返回 JSON
    ▼
小程序渲染数据
    │ 写入 wx.setStorageSync('cache:...', data)  // 离线缓存
```

### 9.2 操作执行流

```
用户在小程序点击"暂停计划"
    │ 前端弹出 ConfirmDialog
    │ 用户确认
    │ POST /api/v1/actions { campaign_id, action_type: 'pause' }
    ▼
本机 feedback-server :8899
    ├── 校验参数（campaign_id 存在？action_type 合法？）
    ├── 判定确认等级（pause → L1）
    ├── 写入 operation_log 表 (status=pending)
    │
    ├── 立即执行（L1/L2，前端已确认）:
    │   ├── import cdp-action.mjs
    │   ├── togglePlanStatus(planName, 'pause')
    │   │   └── raw CDP Input.dispatchMouseEvent（真实事件）
    │   ├── 执行前截图 + 执行后截图
    │   └── 更新 operation_log (status=executed, result={ok:true})
    │
    └── 返回响应: { action_id, status: 'executed', result: {ok:true} }
        ▼
小程序显示结果: "✅ 已暂停 xxx计划"
```

> **注意**：因为采用 Tailscale 隧道方案（§4.3），操作指令直达本机 feedback-server，
> **不需要"云端入队 → 本机轮询"的异步机制**。操作是同步执行的（HTTP 请求等待 CDP 完成后返回）。
>
> **⚠️ CDP 操作耗时 5-15 秒**，HTTP 请求可能超时。解决方案：
> - 小程序端 timeout 设为 30s
> - 如果超时，小程序显示"操作已提交，请刷新查看结果"
> - feedback-server 后台继续执行，结果写入 operation_log
> - 小程序 30s 后自动刷新操作日志，展示最终结果

### 9.3 异常处理

| 异常 | 处理 |
|---|---|
| Chrome 离线 | POST /actions 返回 `{ status: 'queued' }`，操作入 SQLite 队列，chrome-guard 恢复后自动续执行 |
| CDP 操作超时（>30s） | HTTP 先返回 `{ status: 'queued' }`，后台继续执行，小程序轮询结果 |
| CDP 操作失败 | 自动重试 1 次，仍失败则标记 `status=failed` + 截图留证 |
| 网络断开 | 小程序显示缓存数据 + "网络异常"提示 |
| Token 过期/错误 | 403 → 跳转设置页重新输入 Token |

---

## 十、成本估算

| 项目 | 费用 | 备注 |
|---|---|---|
| 云服务器（轻量 ECS 2C2G） | ¥40-80/月 | 阿里云/腾讯云 |
| 域名（.com） | **¥55-70/年** | 标准价格，非溢价域名 |
| ICP 备案 | ¥0 | 免费（企业主体） |
| HTTPS 证书 | ¥0 | Let's Encrypt 免费 |
| 微信小程序 | ¥0 | 免费（企业开发者） |
| Tailscale | ¥0 | 免费版足够（最多 100 设备） |
| **月均总计** | **¥50-100** | 首年含域名约 ¥600-1030 |

---

## 十一、风险与缓解

| # | 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|---|
| R1 | **ICP 备案周期超预期** | 高 | 高 | P0 立即启动备案；开发期不校验域名 + Tailscale 调试，不阻塞开发 |
| R2 | 小程序审核驳回 | 中 | 高 | 类目选"工具>信息查询"；准备隐私协议页；体验版先行验证功能 |
| R3 | **首包超 2MB** | 中 | 高 | 用 uCharts（100KB）替代 ECharts（1MB）；详情页放分包 |
| R4 | Chrome 离线 → 操作不可用 | 中 | 高 | 操作入队持久化（SQLite），chrome-guard 恢复后续执行 |
| R5 | Cookie 过期 → 数据中断 | 中 | 高 | oec-auto-login 自动续期 + 过期告警推送 |
| R6 | CDP 操作耗时长（5-15s） | 中 | 中 | HTTP 超时 → 返回 queued 状态 → 小程序轮询结果 |
| R7 | Tailscale 断连 | 低 | 高 | Tailscale 自动重连 + 小程序显示"连接中"状态 |
| R8 | 云服务器宕机 | 低 | 中 | ECS 自动重启 + Tailscale 重连 + 数据在本机有完整备份 |
| R9 | 订阅消息授权率低 | 中 | 低 | 自用工具自己授权即可；飞书推送作为补充通知渠道 |

---

## 十二、关键里程碑

| 里程碑 | 目标 | 交付 | 备注 |
|---|---|---|---|
| **M0** 备案提交 | 第 1 天 | ICP 备案申请已提交 | **立即启动，不可压缩** |
| **M1** 开发环境就绪 | 第 2 周 | Taro 工程 + 本机 API 联调通 | 不依赖备案 |
| **M2** 基建就绪 | 第 3-5 周 | 备案通过 + HTTPS + Tailscale | 备案是最大变量 |
| **M3** 数据就绪 | M2 + 1 周 | SQLite + 历史迁移 + API 全通 | — |
| **M4** 开发版可体验 | M3 + 2-3 周 | 小程序开发版真机可用 | — |
| **M5** 正式上线 | M4 + 1 周 | 小程序正式版发布 | 审核 1-3 天 |

---

## 十三、微信小程序特殊配置清单

> 这些配置容易遗漏，提前准备好：

| 配置项 | 位置 | 内容 |
|---|---|---|
| **request 合法域名** | 微信后台 → 开发管理 → 开发设置 → 服务器域名 | `https://your-domain.com` |
| **业务域名** | 同上 | web-view 内嵌网页域名（如果有） |
| **隐私协议** | 微信后台 → 设置 → 基本设置 → 服务内容声明 | 必须填写，否则审核不过 |
| **类目** | 微信后台 → 设置 → 基本设置 → 服务类目 | "工具 > 信息查询" 或 "商业服务 > 企业管理" |
| **小程序名称** | 同上 | 不能与已有小程序重名，提前占位 |
| **订阅消息模板** | 微信后台 → 功能 → 订阅消息 | 提前创建模板（告警通知/操作结果） |
| **Tab 图标** | 项目代码 | 81×81px PNG，选中/未选中各一套 |

---

## 十四、待其他 Agent Review 的重点

1. **Taro 3.x 实际编译产物体积**：Taro + React + NutUI 编译后的真实首包大小？是否需要切换到原生小程序？
2. **uCharts vs echarts-for-weixin**：uCharts 是否支持双 Y 轴、面积图等看板常用图表类型？
3. **Tailscale 稳定性**：在中国大陆网络环境下 Tailscale 的连通率和延迟？是否需要考虑 frp 备选？
4. **CDP 操作同步等待**：HTTP 请求等待 CDP 完成（5-15s）是否会导致连接池耗尽？是否需要异步模式？
5. **小程序审核类目**：广告监控工具选哪个类目通过率最高？是否需要特殊资质？
6. **SQLite WAL 模式 + 并发读写**：feedback-server 同时被采集脚本写入和被 API 读取时是否安全？
7. **数据清理**：SQLite 数据增长后的清理策略是否合理？Parquet 归档是否需要？
8. **首包体积实测**：完成开发后实测首包大小，如果超 2MB 需要额外的分包策略

---

*本文档为完整的小程序实施指南（v1.1 审查修正版）。当前因企业资质约束暂不可行，但所有技术细节、API 设计、页面规划、代码示例、架构方案均已就绪。一旦前置条件满足，可直接按 P0→P5 顺序执行。*
