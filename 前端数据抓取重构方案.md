# 前端数据抓取重构方案

> 日期：2026-08-01
> 状态：方案设计（待评审，含 2026-08-01 决策更新）
>
> ⚠️ **决策更新（2026-08-01）**：取消「CDP DOM 抓取 / CDP 抓包」两条生产降级路径。
> - 生产抓取仅保留 **HTTP API 直连** 唯一路径；
> - 失败处理改为 **每分钟重试（窗口内最多 5 次）→ 飞书群告警（含失败原因/受影响账户）→ 持续重试 + 恢复通知**；401/403 先自动刷新 Cookie 再重试；
> - CDP 仅保留三处非抓取用途：**Cookie 提取/刷新、写操作执行、逆向工具（发现新端点）**；
> - 本文档中所有「DOM 降级」「抓包兜底」「直播分析页 DOM 备选源」内容均作废，以本节为准。
> 范围：巨量引擎前端数据抓取链路 —— HTTP API 直连 / CDP DOM 抓取 / CDP 抓包逆向 三路径收敛

---

## 一、背景与目标

### 1.1 现状

巨量引擎没有开放官方数据 API，本项目通过三种方式从 Web 前端获取数据：

| 路径 | 原理 | 代表文件 | 状态 |
|------|------|---------|------|
| A. HTTP API 直连 | 浏览器提取 Cookie → Node 直调内部 JSON API | `oceanengine-api-client.mjs`、`ai-regions-http.mjs` | 主链路（15min/5min 监控、日汇总、直播监听） |
| B. CDP DOM 抓取 | Chrome CDP 连接 → 页面校准 → 解析表格 DOM | `oceanengine-monitor-v3.mjs`(scrapeOnePage)、`calibrate-page.mjs`、`fetch-ai-regions.mjs`(proxy 3456) | 历史主链路，现为降级/一次性任务 |
| C. CDP 抓包逆向 | Network 域监听 → 捕获请求/响应 → 反推新端点 | `oec-capture-status-api.mjs`、`capture-ai-regions-api.mjs`、`oec-reverse-*.mjs` | 逆向工具，未产品化 |

关键前提（已在《按需加载方案分析与优化》确认）：**数据采集全程可走 HTTP API，Chrome/CDP 仅在 Cookie 刷新时被需要（占用 0.2%），写操作（暂停/调预算）仍需 CDP。**

### 1.2 重构目标

1. **收敛**：生产抓取收敛为「HTTP API 直连唯一路径」；CDP 抓包仅作逆向工具；DOM 抓取废弃（相关代码归档）。
2. **去重**：消除 Cookie 提取、HTTP 请求、CDP WebSocket 三处以上重复实现。
3. **抗变更**：消灭 DOM 列索引硬编码与字段解析散落，页面改版不静默出错。
4. **可运维**：抓取失败有统一重试/熔断/降级策略与结构化日志。
5. **可扩展**：新端点 = 在注册表加一条配置，不再复制粘贴脚本。

---

## 二、现状盘点与痛点诊断

### 2.1 重复实现清单（实测）

| 重复点 | 出现文件 | 危害 |
|--------|---------|------|
| `Network.getCookies` 提取 | `oceanengine-api-client.mjs`、`oec-auto-login.mjs`、`oec-internal-api-discovery.mjs` | Cookie 策略改动需多处同步 |
| `new WebSocket` + wsSend 封装 | `cdp-client.mjs`(统一版) + `capture-ai-regions-api.mjs`、`oceanengine-api-client.mjs`、`oec-capture-status-api.mjs`、`oec-reverse-*.mjs`(3个) 共 7 处 | 超时/心跳/重连策略各写各的 |
| `https.request` httpPost 封装 | `ai-regions-http.mjs`、`fetch-video-account-cost.mjs`、`fetch-short-video-cost.mjs`、`fetch-ai-regions-time-range.mjs`、`oceanengine-ai-regions.mjs`、`oceanengine-daily-summary.mjs` 等 8 处 | 修复（如 setEncoding utf8、千分位）需逐文件打补丁 |
| DOM 列索引硬编码 | `scrapeOnePage`（data[7]~data[21]）、`fetch-ai-regions.mjs`（cells[0]~cells[3]） | 页面加列/换列即静默错位 |
| 数值解析 | 各脚本各自 `parseFloat(x.replace(/,/g,''))` | 已发生重大事故：5 区直播消耗 ¥9459 被解析成 ¥7（`ai-regions-http.mjs`） |
| 账户/端点配置 | aadvid 硬编码：`1842681352509635`、`1852666142648332`、`AI_REGIONS` 5 账户 | 新账户接入成本高 |
| 失败处理 | 各脚本独立 try/catch + 空返回 | 无统一熔断与告警口径 |

### 2.2 结构性痛点

- **无路由策略**：同一数据（计划列表）存在 API 与 DOM 两套抓法，但没有"API 优先、失败降级 DOM"的编排层。
- **逆向成果未沉淀**：`oec-capture-*` 抓到的端点（如 `statistics_sophonx/statQuery`）靠人工拷贝到业务脚本，无版本管理。
- **40102 未突破**：`projects/get`、`adgroups/list`、`live_room/list` 三个详情端点需要页面 HTML 内注入的 CSRFToken（与 Cookie 中同名不同值），HTTP 直连必被拒（详见 `40102端点逆向思路.md`）。
- **一次性脚本游离体系外**：`fetch-*.mjs` 不在 PM2 编排内，无健康检查、无日志归口。

---

## 三、目标架构

```
┌─────────────────────────────────────────────────────┐
│                 编排层 oec-fetch.mjs                  │
│   HTTP API 直连 · 失败每分钟重试 → 飞书群告警        │
└─────────────────────────────────────────────────────┘
        │                     │                    │
┌───────▼───────┐   ┌─────────▼─────────┐   ┌──────▼───────┐
│  会话层        │   │  传输层            │   │  数据层       │
│ oec-session   │   │ oec-http /        │   │ oec-normalize│
│  Cookie统一    │   │ oec-cdp /        │   │ 字段映射      │
│  提取/缓存/刷新 │   │ oec-capture      │   │ 数值解析/校验 │
└───────┬───────┘   └─────────┬─────────┘   └──────┬───────┘
        │                     │                    │
        └──────────┬──────────┴────────────────────┘
                   │
        ┌──────────▼───────────┐
        │ 端点注册表 oec-endpoints│
        │ url / method / body   │
        │ fields / 重试参数 / 校验  │
        └──────────────────────┘
```

### 3.1 分层职责

| 层 | 模块（新建） | 职责 | 消费方 |
|----|-------------|------|--------|
| 会话层 | `oec-session.mjs` | Cookie 单点提取（CDP Network.getCookies）、2h 缓存、过期检测、自动登录兜底、401 失效清理 | 所有 HTTP 抓取 |
| 传输层 | `oec-http.mjs` | 统一 `httpPost/httpGet`：utf8 解码、超时、重试、限频日志、非 ASCII Cookie 清洗 | 所有 HTTP 抓取 |
| 传输层 | `oec-cdp.mjs` | 薄封装 `cdp-client.mjs`：Tab 发现、evaluate、真实鼠标事件 | 写操作（Cookie 刷新/执行，非抓取） |
| 传输层 | `oec-capture.mjs` | 通用抓包：新开 Tab → Network.enable → requestWillBeSent/loadingFinished → getResponseBody | 逆向、40102 突破 |
| 接口层 | `oec-endpoints.mjs` | 端点注册表 + body 构造器 + 字段定义 | 所有业务脚本 |
| 数据层 | `oec-normalize.mjs` | 统一数字解析（千分位）、字段名归一、schema 校验、列头映射 | DOM 与 API 共用 |
| 编排层 | `oec-fetch.mjs` | HTTP API 直连、每分钟重试（窗口内 N 次）、飞书群告警、恢复通知 | 监控主链路 |

### 3.2 端点注册表设计（核心）

```js
// oec-endpoints.mjs
export const ENDPOINTS = {
  projectsList: {
    method: 'POST',
    url: (cfg) => `${cfg.base}/ad/api/promotion/projects/list?aadvid=${cfg.aadvid}`,
    body: (ctx) => ({ st: ctx.date, et: ctx.date, ..., isSophonx: 1 }),
    fields: ['project_id', 'project_name', 'stat_cost', 'convert_cnt', ...],  // 对照 API字段全量档案.md
    normalize: 'projectRow',          // 复用 oec-normalize 的映射
    retry: { intervalMs: 60000, maxAttempts: 5, alertAfter: 5 },   // 失败重试 + 告警
  },
  statQuery: { ... },                 // 报表类，AI区域/短视频共用
  dashboardStats: { ... },
  liveRoomList: {                     // 40102：标记 specialAuth，缺失时记录缺失标记 + 告警（无 DOM 备选源）
    auth: 'pageToken',
    fallback: 'liveAnalyzePage',
  },
};
```

收益：新增账户只改配置（`monitor-utils.mjs` 的账户注册表）；新增端点只加一条定义；字段变更只改 normalize 映射。

---

## 四、具体改造项

### 4.1 会话层收敛（P0，先行）

- 从 `oceanengine-api-client.mjs` 抽出 `extractCookiesFromBrowser` + Cookie 缓存逻辑 → `oec-session.mjs`，导出 `getCookieData()` / `invalidateCookie()` / `refreshIfExpired()`。
- `oec-auto-login.mjs`、`oec-internal-api-discovery.mjs` 改为调用统一实现。
- 新增行为：HTTP 返回 401/403 时自动作废缓存并触发一次刷新（当前只按 2h TTL 过期，会话提前失效会静默空数据）。

### 4.2 传输层收敛（P1）

- `oec-http.mjs`：合并 8 处 `httpPost`，固定 `res.setEncoding('utf8')`、15s 超时、3 次重试、`failLogOnce` 限频。
- `oec-capture.mjs`：把 `capture-ai-regions-api.mjs` / `oec-capture-status-api.mjs` 的监听骨架通用化，参数化为 `{ urlFilter, onRequest, onResponse, maxWaitMs }`。
- 迁移顺序：`ai-regions-http.mjs` → `fetch-video-account-cost.mjs` → `fetch-short-video-cost.mjs` → `oceanengine-daily-summary.mjs`。

### 4.3 数据层收敛（P2）

- `oec-normalize.mjs` 提供 `parseNum(str)`（去千分位+空值+NaN 防护）、`mapRow(columns, schema)`（按列头名而非索引取值）、`validate(record, schema)`。
- 从 `API字段全量档案.md` 反推字段映射表作为 schema 基线，仅 HTTP API 单一路径输出，下游 `analyzeData` 无感知。
- **学习期字段映射**：API `learning_status` → `learningStatus`（learning/learned/learning_failed/not_learning）+ `learningDone`（是否完成，learned/learning_failed/not_learning=true，learning=false）；API 未提供时由 `oec-normalize` 按投放时长/转化数规则推断并标注 `inferred=true`。
- DOM 抓取改为"先读表头列名 → 建立列索引映射 → 按列名取值"，列头变化时打印结构化告警而非静默错位。

### 4.4 DOM 抓取废弃与归档（P2）

> 决策更新：DOM 不再作为生产抓取路径。`scrapeOnePage` / `calibrate-page.mjs` / `fetch-ai-regions.mjs` 相关代码移入 `archive/`，保留「列头映射 + 结构化告警」思路仅作 API 字段校验参考。

- 从 `oceanengine-monitor-v3.mjs` 抽出 `oec-dom-scraper.mjs`：`scrapeProjectTable(client)` / `scrapeSummaryRow(client)` / `scrapeToolbarStats(client)`。
- `monitor-v3` 保留编排，调用模块；行为不变，接口不变（向后兼容）。

### 4.5 编排层（P3）

- `oec-fetch.mjs` 提供 `fetchWithRetry(endpoint, ctx)`：HTTP API 失败 → 每分钟重试（窗口内最多 5 次）→ 连续失败触发飞书群告警（失败原因/受影响账户）→ 告警后维持每分钟重试，恢复后发送恢复通知。
- 主链路（15min/5min）先接入，验证后再推广到日汇总。

### 4.6 逆向能力沉淀 + 40102 突破（P4，可选）

- 将 `oec-capture-*` 捕获的端点（statQuery 等）登记进注册表，替代手工拷贝。
- 40102 三端点按《40102端点逆向思路.md》方案 4.1「CDP 主动触发 + 抓包」落地：程序化点击详情入口 → 拦截携带页面 CSRFToken 的请求 → 沉淀为注册表中的 `auth:'pageToken'` 端点。
- 直播分析页（数据页 → 直播分析）`live_room/list` 不再作为 DOM 降级源接入；端点缺失时记录缺失标记并告警。

---

## 五、分阶段实施计划

| 阶段 | 内容 | 涉及文件 | 验收标准 |
|------|------|---------|---------|
| P0 | 会话层收敛 | 新建 `oec-session.mjs`；改 `oceanengine-api-client.mjs`、`oec-auto-login.mjs` | 15min/5min 监控 24h 无回归；Cookie 失效可自动刷新 |
| P1 | 传输层收敛 | 新建 `oec-http.mjs`、`oec-capture.mjs`；迁移 4 个 fetch 脚本 | 8 处 httpPost 归零；ai-regions 21:30 推送数据与重构前一致（对照历史快照） |
| P2 | 数据层 + DOM 归档 | 新建 `oec-normalize.mjs`、`oec-fetch.mjs`；DOM 代码移入 `archive/` | 抓取结果与历史快照 diff 一致；失败告警生效 |
| P3 | 编排层 | 新建 `oec-retry.mjs`；接入主链路 | API 失败每分钟重试 + 飞书群告警 + 恢复通知正常 |
| P4 | 逆向沉淀 + 40102 | 新建端点注册条目；`oec-capture` 主动触发 | statQuery 端点登记；直播分析页数据源接入 |

每个阶段**先并行灰度（新模块与旧路径同时跑、比对结果），再切换默认路径**，避免一次性大重构风险。

---

## 六、风险与决策

| 风险 | 等级 | 缓解 |
|------|:---:|------|
| 主链路回归（15min/5min 卡片断更） | 高 | P0~P2 保持旧路径并行，diff 一致后再切换；PM2 逐进程回滚 |
| API 字段变更导致解析错位 | 高 | 统一 parseNum + schema 校验 + 字段变更结构化告警 |
| Cookie 频繁刷新触发验证码/风控 | 中 | 2h TTL 保持；401 主动刷新加冷却（30 分钟内最多 1 次）；CAPTCHA_REQUIRED 告警人工介入 |
| 40102 端点突破失败 | 低 | 仅 HTTP API 抓取；缺失端点记录缺失标记 + 飞书告警，不阻塞主线 |
| 重构期间一次性脚本行为漂移 | 中 | 迁移前后各留一份输出快照比对；旧脚本保留至下个版本再归档 |

### 决策记录

| 日期 | 决策 | 理由 |
|------|------|------|
| 2026-08-01 | 采用「API 为主 + 注册表 + 分层」架构 | 主链路已验证纯 HTTP 可行，Chrome 仅需 0.2% 时间 |
| 2026-08-01 | 取消 DOM/CDP 抓取降级，改飞书告警 + 每分钟重试 | 无固定公网环境 + 简化链路，CDP 仅保留 Cookie/写操作/逆向工具 |
| 2026-08-01 | 分 5 阶段、逐阶段灰度 | 监控系统实时性要求高，禁止一次性大重构 |

---

## 七、相关文档

- `API字段全量档案.md` — 已逆向 6 端点 130+ 字段，作 schema 基线
- `40102端点逆向思路.md` — 3 个详情端点根因与突破路径
- `docs/archive/按需加载方案分析与优化.md` — Chrome 按需加载与 Cookie 刷新占比分析（已归档）
- `工程化优化方案_20260725.md` — 工程化基线
