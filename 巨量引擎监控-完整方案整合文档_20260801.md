# 巨量引擎监控 · 完整方案整合文档 v1.3

> 版本：**v1.3**（2026-08-01）
> 版本规则：语义化版本 —— 主版本=架构级重构；次版本=新增章节/决策变更；补丁=勘误修订
> 版本历史：
> - **v1.3**（2026-08-01）：统一 CPA/CPS/CPL 与深层/浅层转化口径；补齐 `snapshots`/`shifts`/`trigger_rules` 数据模型；AI 短期仅学习与建议、所有调整由人工下达；梳理 6 进程归属；下播后 4min 补采；收紧远程安全边界；操作队列直接迁移 SQLite `operations`；DOM 仅归档/离线研究；新增现状快照。
> - **v1.2**（2026-08-01）：新增第二十一章 飞书群分工与推送模板规范（监控群/汇报群双群架构、两群开启对话、推送归属总表、全部消息固定模板含取数规则与计算逻辑、配置迁移）
> - **v1.1**（2026-08-01 定稿）：整合修复 —— 第一部分 5.x 章节去重重排、风险章节合并（6.1 S 系列 / 6.2 D 系列）、补入重构方案总览/文档地图/附录 A/B/衔接章节、路线图依赖说明
> - **v1.0**（2026-08-01 初稿）：三份方案文档 + 前端数据抓取重构方案 + 六项专项内容首次整合
> 日期：2026-08-01
> 定位：全项目方案的一体化整合文档 —— 覆盖项目全景、目标架构、完整重构（S 系列）、数据中枢与 AI 建议（D 系列，当前人工执行）、抓取链路（P 系列）、运行逻辑、远程访问、日志规范、排班校对、学习期规则、路线图与治理。
> 整合来源：《完整重构方案_20260801.md》《数据中枢与AI调优架构设计.md》《巨量引擎监控-项目完整文档_20260801.md》《前端数据抓取重构方案.md》《巨量引擎广告投放知识库_Agent学习版_20260801.md》及后续评审补充。
> 关联决策：抓取仅 HTTP API 直连（失败每分钟重试 + 飞书告警）；不采用固化地址远程网关；排班驱动活跃期；统一日志强制规范；AI 短期仅学习与建议，所有调整由人工下达。

---

# 第一部分 项目全景


## 一、项目概述

**巨量引擎监控**（oceanengine-monitor）是一套面向"线索直播广告"场景的实时监控与调优系统：

- 每 5/15 分钟自动采集巨量引擎（字节跳动广告平台）各广告计划数据
- 通过飞书交互卡片推送 5min 速报、15min 完整监控、主播换班数据、日汇总、日报
- 支持在飞书群内以自然语言指令执行广告操作（暂停/恢复/调预算/调出价），形成「指令 → 审计 → 执行 → 回读 → 报告」闭环
- 提供本地 Dashboard（Alpine.js + Chart.js）可视化、操作队列与审计、一键回滚、AI 建议
- 配套 AI 区域号汇总、周报 PPT 生成、多 Agent 协作研发流水线

**核心链路（现状）**：

```
采集（HTTP API 主 / CDP 降级）
  → 快照落盘（JSON 双写 + SQLite）
  → 分析（delta/告警/生命周期）
  → 推送（飞书卡片）
  → 群指令（暂停/预算/出价）→ 队列 → 执行 → 审计回读 → 效果追踪
  → Dashboard 可视化 + AI 建议
```

**目标链路（重构后，详见第八章）**：

```
采集独立（oec-collector 只写库，HTTP API 唯一路径；失败每分钟重试 → 飞书群告警 → 恢复通知）→ SQLite 数据中枢（唯一事实源）
  → 汇报层（卡片/日报/Dashboard 全部读库）
  → 规则引擎（trigger_rules 条件触发）→ AI 决策 → 操作入库 → 效果追踪回写
```

---


## 二、环境与运行依赖

### 2.1 运行时

| 项 | 值 |
|---|---|
| Node.js | **22.22.2（ABI 127）**，路径 `C:\Users\HTF2026\.workbuddy\binaries\node\versions\22.22.2\node.exe` |
| 模块规范 | ESM（`.mjs`），`package.json` 声明 `"type": "module"` |
| 引擎约束 | `"engines": { "node": ">=22 <23" }`，`.nvmrc` = 22 |
| 警告 | 本机另有 Node 24（ABI 137），**PM2 必须绑定 Node 22**；原生模块安装/重建用 Node 22 的 npm |

### 2.2 依赖（package.json）

| 包 | 版本 | 用途 |
|---|---|---|
| better-sqlite3 | ^12.11.1 | 唯一原生依赖，SQLite 访问（ABI 127 预编译） |
| jszip | ^3.10.1 | PPT 模板解压 |
| node-cron | ^4.5.0 | 定时任务 |
| pptxgenjs | ^4.0.1 | 周报 PPT 生成 |
| ws | ^8.21.0 | CDP WebSocket 客户端 |

### 2.3 进程管理（PM2 · 12 进程）

配置源：`ecosystem.config.cjs`（`MONITOR_DIR` / `NODE_EXE` / `LOG_DIR` 环境变量可覆盖）。

| 进程 | 脚本 | 调度 | 内存上限 |
|---|---|---|---|
| pm2-15min | `oceanengine-monitor-v3.mjs` | cron `*/15 * * * *` | — |
| pm2-5min | `oceanengine-5min-check.mjs` | cron `*/5 * * * *` | — |
| shift-pusher | `oceanengine-shift-pusher.mjs` | 常驻 60s 轮询 + 每日 6:00 重启 | 300M |
| pm2-daily-report | `oceanengine-daily-report-scheduler.mjs` | cron `5 23 * * *` | — |
| pm2-daily-summary | `oceanengine-daily-summary.mjs` | cron `35 23 * * *` | — |
| pm2-sync-tomorrow | `sync-tomorrow-shifts.mjs` | cron `0 23 * * *` | — |
| pm2-ai-regions | `ai-regions-http.mjs` | cron `30 21 * * *` | — |
| live-watcher | `live-watcher.mjs` | 常驻 60s 轮询 | 256M |
| feishu-listener | `feishu-listener.mjs` | 常驻 10s 轮询 | 200M |
| action-queue-worker | `action-queue-worker.mjs --watch` | 常驻 15s 轮询 | 200M |
| feedback-server | `feedback-server.mjs` | 常驻 HTTP（端口 8899） | 200M |
| chrome-guard | `chrome-guard.mjs` | 常驻 10s 探活 | 256M |

### 2.4 数据库

- **引擎**：SQLite 3（WAL + better-sqlite3），`PRAGMA synchronous=FULL`（写入侧）
- **文件**：`monitor-data/oceanengine.db`（唯一 DB 文件，约 7MB）
- **Schema**：v2.0，9 张基础表 + 3 张物化聚合表（详见第六章）
- **代码**：当前三套并存 —— `db/`（旧）、`巨量引擎监控数据库/`（v2 模块化）、`巨量引擎快照数据库/`（快照差计算）；重构后统一为 `src/db/`

### 2.5 浏览器（Chrome CDP）

| 项 | 值 |
|---|---|
| CDP 端口 | 9222（`chrome-guard` 守护，崩溃自动拉起） |
| 用户数据目录 | `CHROME_USER_DATA_DIR=D:\ChromeCDP\User Data` |
| Profile | `CHROME_PROFILE_DIRECTORY=Profile 4` |
| 用途 | Cookie 提取/刷新（占比 ~0.2%）、写操作（暂停/预算/出价）、逆向工具（发现新端点）；**不用于生产数据抓取降级** |

### 2.6 推送通道（飞书）

- **工具**：`lark-cli`（`findLarkCli()` 自动发现），推送封装 `feishu-push-guard.mjs`
- **群**：监控群 `oc_8deeb3061bdbd43608de252a44c97a25`、汇报群 `oc_b245ee4b255c7b25b7f8d953802c49ff`（原「主播群」更名；推送归属、模板与对话能力见第二十一章）
- **Bot**：`cli_a92d0bfc68f89cb2`（feishu-listener 使用）
- **排班表**：飞书电子表格（`SHIFT_SPREADSHEET_TOKEN` / `SHIFT_SHEET_ID`）；每日 23:00 同步次日排班 + **校对当日排班与当日已填写的主播换班数据**（缺班次/多余/主播不一致 → 飞书告警）

### 2.7 前端服务

- `feedback-server.mjs`（8899）：静态资源 + REST API（`/api/snapshots`、`/api/campaigns`、`/api/actions`、`/api/alerts`、`/api/ai/learning-data`、`/api/live-status` 等 30+ 路由）
- `dashboard-v2.html/js/css`：Alpine.js + Chart.js（vendor/ 本地化），PWA（sw.js / manifest.json）

### 2.8 配置（.env）

| 变量 | 说明 |
|---|---|
| `OEC_EMAIL` / `OEC_PASSWORD` | 巨量引擎登录凭据 |
| `OEC_ACCOUNT_ID` / `OEC_VIDEO_ACCOUNT_ID` | 广告账户 / 视频账户 |
| `LARK_MONITOR_CHAT_ID` / `LARK_REPORT_CHAT_ID` | 监控群 / 汇报群（原 `LARK_ANCHOR_CHAT_ID` 迁移更名，群 ID 不变） |
| `SHIFT_SPREADSHEET_TOKEN` / `SHIFT_SHEET_ID` | 飞书排班表 |
| `OEC_DAILY_BUDGET` | 日预算（默认 45000） |
| `CHROME_USER_DATA_DIR` / `CHROME_PROFILE_DIRECTORY` | Chrome 配置 |
| `MONITOR_DIR` / `NODE_EXE` | PM2 路径覆盖 |
| `LOG_RETENTION_DAYS` | 日志保留天数（默认 14） |
| `SIMULATE_CDP=1` | 测试模式：跳过真实 CDP 操作 |



### 2.9 远程运维通道（CodeBuddy 网关）

系统可通过 CodeBuddy 远程网关从任意位置访问（Web UI / webCLI），已投入运行：

| 项 | 现状 |
|---|---|
| 网关鉴权 | CodeBuddy `settings.json` → `gateway: { auth: "password" }`，密码保护 Web UI |
| 隧道 | cloudflared trycloudflare 快速隧道（2 个进程常驻），本地 CodeBuddy Web 服务端口（如 59687 / 61278）→ 公网 `https://<随机>.trycloudflare.com/?password=***` |
| 状态文件 | `C:\Users\HTF2026\.codebuddy\tunnel-state.json`（url / localPort / cliPid） |
| 地址通知 | `E:\炼丹炉\WorkBuddy\.codebuddy\check_gateway_hook.sh`（CodeBuddy Stop 事件）→ 从 transcript 提取最新 URL → `send_gateway_to_feishu.py` 推飞书私聊 → `.gateway_last_url.txt` 去重 |
| 关联文件 | `E:\炼丹炉\WorkBuddy\网关地址.txt`（Web UI / Tunnel / 本地三地址记录） |

> 说明：该通道属于 WorkBuddy 层运维基础设施，独立于监控系统自身；监控 Dashboard（8899）目前仅本机访问，未走隧道。重构方案将其纳入统一治理（见 6.5）。

### 2.10 目录结构（现状）

```
巨量引擎监控/
├── *.mjs（150 个核心脚本，扁平散落）     # 重构后迁入 src/
├── ecosystem.config.cjs                 # PM2 12 进程
├── monitor-data/                        # 运行数据（快照/日志/状态/DB）
├── db/ + 巨量引擎监控数据库/ + 巨量引擎快照数据库/  # 三套数据库代码（重构后统一）
├── dashboard*.html/js/css               # 前端（新旧两版，重构后只留 v2）
├── tests/（8 个） scripts/ .github/workflows/ci.yml
├── docs 体系（115 个 md：方案/评审/规范/历史）
├── prompts/ + opencode.json             # 多 Agent 研发流水线
└── archive/（cdp-probes / debug-scripts / deprecated-prototypes）
```

---


## 三、项目目标

### 3.1 业务目标

1. **实时监控**：5/15min 卡片稳定推送，异常消耗/CPL 波动即时可见
2. **降本提效**：CPL（线索成本）为核心指标，监测留资转化链路（观看 → 私信打开 → 私信留资 → 表单 → 线索 → 转化）
3. **操作闭环**：运营在飞书群直接调优，全部操作留痕、可确认、可回滚
4. **数据沉淀**：历史快照/操作/效果可查，支撑 AI 诊断与规则学习

### 3.2 重构目标（完整重构方案）

1. **目录分层**：根目录 150 文件 → `src/` 分层骨架（config/platform/domain/services/web/db/feishu/cdp/utils）
2. **模块化**：巨型文件拆分（163KB / 72KB / 33KB → <30KB）
3. **去重**：8 处 httpPost、7 处 WebSocket、3 处 Cookie 提取 → 统一实现
4. **进程合并**：12 进程 → ≤6 进程
5. **配置外置**：20+ 处硬编码（chat_id/aadvid/账户）→ 配置化
6. **测试强化**：单测 8 → ≥30，CI 双平台 + import 检查

### 3.3 数据中枢与 AI 建议目标（核心深化）

1. **采集独立**：`oec-collector` 只写库，与汇报解耦
2. **唯一事实源**：汇报/监控 100% 读库，JSON 降级为灾备回放
3. **操作闭环入库**：`operations` + `operation_effects` 替代文件队列与 audit
4. **条件触发 AI 建议**：`trigger_rules` 配置化 → 命中触发 → 生成 AI 建议 → 人工下达执行 → 效果回写规则置信度

### 3.4 量化指标（目标态）

| 指标 | 现状 | 目标 |
|---|---|---|
| 根目录生产代码文件数 | ~150 | < 20 |
| 最大单文件 | 163KB | < 30KB |
| PM2 进程数 | 12 | ≤ 6 |
| httpPost/WebSocket/Cookie 重复实现 | 8/7/3 | 0 |
| 单测文件数 | 8 | ≥ 30 |
| 硬编码配置 | 20+ 处 | 0 |
| 消费方读 JSON 文件 | 全部 | 0（仅灾备） |
| AI 自动调优 | 无 | AI 仅建议，人工执行（短期） |

---


## 四、功能模块清单

### 4.1 数据采集层

| 模块 | 文件 | 职责 |
|---|---|---|
| HTTP API 客户端 | `oceanengine-api-client.mjs` | 18 个导出函数：getProjects/getDashboardStats/getHourlyStats/collectAllData/getSessionStats/updateProjectStatus/updateProjectBudget/updateProjectBid/updateAccountBudget/createClient/getOnlineRoomList/getLiveRoomStatus 等 |
| CDP 连接 | `cdp-client.mjs` | WebSocket 封装、checkCDP、getOceanEngineTab |
| 页面校准 | `calibrate-page.mjs` | （已废弃生产抓取用途，随 DOM 抓取归档） |
| 抓包逆向 | `oec-capture-status-api.mjs`、`capture-ai-regions-api.mjs`、`oec-reverse-*.mjs`、`oec-internal-api-discovery.mjs` | Network 域监听捕获新端点 |
| 自动登录 | `oec-auto-login.mjs` | Cookie 提取/刷新 |
| 一次性抓取 | `fetch-ai-regions*.mjs`、`fetch-video-account-cost.mjs`、`fetch-short-video-cost.mjs` | 专项数据抓取（游离体系外，待收敛） |

### 4.2 监控与汇报层（PM2 业务进程）

| 模块 | 文件 | 职责 |
|---|---|---|
| 15min 完整监控 | `oceanengine-monitor-v3.mjs`（163KB） | 采集+分析+卡片+推送一体；状态变化检测（投放中→超出预算） |
| 5min 速报 | `oceanengine-5min-check.mjs`（33KB） | 轻量速报卡片（消耗/转化/CPL），HTTP API 唯一路径 |
| 换班推送 | `oceanengine-shift-pusher.mjs` | 主播班次结束推送，写飞书排班表 |
| 日汇总 | `oceanengine-daily-summary.mjs` | 23:35 全天数据核对后推送 |
| 日报 | `oceanengine-daily-report-scheduler.mjs` | 23:05 生成日报并推送 |
| 排班同步+校对 | `sync-tomorrow-shifts.mjs`（重构后 `sync-shifts`） | 23:00 同步次日排班 + 校对当日排班/当日换班数据（缺班次/主播不一致告警） |
| AI 区域号 | `ai-regions-http.mjs` / `oceanengine-ai-regions.mjs` | 21:30 区域号汇总推送（5 账户） |
| 直播状态 | `live-watcher.mjs` | 60s 轮询开播/下播，下播二次确认 |

### 4.3 操作与反馈闭环

| 模块 | 文件 | 职责 |
|---|---|---|
| 飞书监听 | `feishu-listener.mjs`（32KB） | 10s 轮询群消息，指令解析：暂停/恢复/加预算/拒绝/取消/状态/帮助；三阶段反馈（确认→执行→报告） |
| 操作队列 | `action-queue-worker.mjs` | 15s 轮询文件队列（action-queue.json），单实例锁，双路径执行（HTTP API 主 / CDP 降级），审计写入 |
| CDP 操作 | `cdp-action.mjs` | 浏览器内暂停/预算/出价操作 |
| 推送守卫 | `feishu-push-guard.mjs` | 推送熔断/重试/去重 |
| 回滚 | Dashboard + worker | beforeValue/afterValue 回读，一键回滚 |
| AI 学习 | `/api/ai/learning-data` | 读审计+快照，按 (deliveryType, actionType) 统计成功率/置信度/15min 消耗变化 |

### 4.4 服务与展示层

| 模块 | 文件 | 职责 |
|---|---|---|
| HTTP 服务 | `feedback-server.mjs`（72KB） | 静态资源 + 30+ REST 路由 |
| Dashboard | `dashboard-v2.html/js/css` | 实时看板：计划列表/操作队列/审计/回滚/AI 建议/异常高亮 |
| PWA | `sw.js`、`manifest.json` | 离线缓存 |

### 4.5 基础设施层

| 模块 | 文件 | 职责 |
|---|---|---|
| 共享工具 | `monitor-utils.mjs` | 配置常量、快照工具、推送工具、排班、锁、原子写 |
| 日志 | `logger.mjs` | 统一聚合：运行日志 + agent 变更日志（`[CHANGE]`）双写 monitor.log + ndjson，按日轮转 |
| 守护 | `monitor-daemon.mjs` | Windows 任务/日志错误/数据间隔/服务健康检查，CDP 自动拉起 |
| Chrome 守护 | `chrome-guard.mjs` | 9222 探活与拉起 |
| 动态调度 | `adaptive-scheduler.mjs` | 排班驱动：排班时段内按消耗 5min/15min 采集，**非排班时段=低活跃期** 30min 低频或暂停（待并入 scheduler） |
| 路由熔断 | `autonomous-router.mjs` | provider 选择 + CircuitBreaker + 成本 guardrail |
| 影子竞技 | `shadow-arena.mjs` | 5% 输入影子测试新路径 |
| 安全 | `csrf-utils.mjs`、`wait-utils.mjs`、`data-consistency-check.mjs` | CSRF、等待、一致性校验 |

### 4.6 数据库模块（三套现状）

| 模块 | 文件 | 职责 |
|---|---|---|
| 旧版 | `db/`（schema.sql、writer.mjs、init.mjs、backfill.mjs、refresh-materialized.mjs） | v1 双写接口 |
| v2 模块化 | `巨量引擎监控数据库/`（dal.mjs、writer.mjs、index.mjs、schema.sql、schema-views.sql、migrations/） | 统一入口 connect/createAPI、双写、物化刷新 |
| 快照差 | `巨量引擎快照数据库/`（snapshot-db.mjs） | getShiftDelta/getSnapshotAt/getDailyShiftReport，HTTP 回退 |

### 4.7 辅助与研发支撑

| 模块 | 文件 | 职责 |
|---|---|---|
| 周报 PPT | `gen-anchor-ppt*.mjs`、`gen-from-template*.mjs` | pptxgenjs/jszip 生成周报 |
| 测试 | `tests/`（8 个） | csrf、autonomous-router、feishu-listener、action-queue-lock、refresh-materialized-lock、push-guard、e2e |
| CI | `.github/workflows/ci.yml` | ubuntu：单测 + 备份验证 |
| 多 Agent | `prompts/*` + `opencode.json` | PM/架构/工程师/测试/评审/发布角色流水线 |

---



### 4.8 远程运维与访问通道

| 组件 | 文件/位置 | 职责 |
|---|---|---|
| CodeBuddy 远程网关 | `settings.json`（gateway 配置） | 密码鉴权的远程访问入口 |
| cloudflared 隧道 | 系统进程（2 个） | 本地 CodeBuddy Web 服务 → 公网 trycloudflare URL |
| Web UI / webCLI | 隧道 URL（带 password） | 远程查看/操作本机（pm2、日志、监控命令） |
| 地址变更通知 | `check_gateway_hook.sh` + `send_gateway_to_feishu.py` | 隧道地址变化 → 飞书私聊通知 |
| 监控 Dashboard（可选） | `feedback-server` 8899 | 当前仅本机；可决策是否纳入远程通道 |


## 五、数据逻辑与数据模型


### 5.1 数据流总览

```
采集（monitor-v3 / 5min-check）
  ├─ 15min 快照 JSON：monitor-data/{UTC时间}.json（完整分析结果，含 summary/delta/active/_multiDay/alerts）
  ├─ 5min 快照 JSON：monitor-data/5m-{时间}.json（轻量）
  ├─ SQLite 双写：db/writer.mjs → insertSnapshot（JSON 失败不阻塞 SQLite）
  ├─ 状态文件：live-state.json / listener-state*.json / action-queue.json
  └─ 排班：shifts-{date}.json（sync-tomorrow 23:00 同步；重构后入 shifts 表，23:00 同时做当日排班/换班校对）
消费（汇报/AI 诊断）
  ├─ 主要读 JSON 快照文件（daily-summary / shift-pusher / AI 诊断）
  └─ Dashboard 部分读 DB（/api/snapshots /api/campaigns /api/actions）
操作（feishu-listener → action-queue-worker）
  ├─ 队列：action-queue.json（文件，单实例锁）
  ├─ 待确认：monitor-data/pending-actions.json
  ├─ 审计：monitor-data/action-audit.jsonl（beforeValue/afterValue/snapshotBefore）
  └─ DB：actions 表（状态机 pending→executing→succeeded/failed）
```




### 5.2 数据文件清单（monitor-data/）

| 类型 | 命名 | 内容 |
|---|---|---|
| 15min 快照 | `{YYYY-MM-DD}T{HH-mm-ss}.json` | 完整分析：summary/delta/active/allSpending/paused/_multiDay/alerts/topNewSpenders/rampingUp/dropping/budgetExceededChanges |
| 5min 快照 | `5m-*.json` | 轻量指标 |
| 日汇总 | `daily-{date}.json`、`daily-summary-done-{date}.json`、`daily-report-done-{date}.json` | 日数据 + 完成标记 |
| 排班 | `shifts-{date}.json` | 班次/主播/时段 |
| 操作 | `action-queue.json`、`pending-actions.json`、`action-audit.jsonl` | 队列/待确认/审计 |
| 状态 | `live-state.json`、`listener-state.json`、`listener-state-anchor.json`、`last-5m-push.json`、`shift-push-lock.json`、`.fail-log-state.json` | 各进程状态与去重 |
| Cookie | `.oec-cookies.json` | 会话缓存（2h TTL） |
| 日志（统一聚合） | `monitor.log` + `logs/monitor-YYYY-MM-DD.ndjson` | 运行日志与 agent 变更日志（`[CHANGE] agent=... 原因:... 方法:... 结果:...`）统一聚合；按日轮转，`LOG_RETENTION_DAYS` 控制保留 |
| 遥测 | `telemetry.jsonl` | autonomous-router 执行记录 |
| DB | `oceanengine.db` | SQLite 唯一文件 |

**统一日志规范**：任何 agent 对项目更改后执行 `node scripts/log-change.mjs --agent <agent> --reason <原因> --method <方法> --files <文件> --result <done|partial|failed>`，记录与运行日志聚合在同一文件（`[CHANGE]` 前缀），实现「运行 + 修改」单一日志入口。


### 5.3 快照明细字段全量档案

> 来源：实测快照文件（`monitor-data/5m-*.json` 与 `monitor-data/{UTC时间}.json`）整理，作为 D1 采集器字段映射基线。

### 5.3.1 三层结构

快照明细共三层：**结构化列（DB 表）→ 计划级字段（每条计划）→ 账户级/分析级内容（JSON 列）**。

### 5.3.2 结构化列（snapshots 表）

| 分组 | 字段 |
|---|---|
| 标识 | `id`、`snapshot_time`(UTC)、`snapshot_cst`(CST)、`account_id`、`campaign_id`、`source_type`(api/cdp/dom) |
| 计划状态/配置 | `status`（投放中/暂停/超出预算）、`daily_budget`、`bid` |
| 核心指标列 | `cost`（当日累计消耗）、`leads`、`conversions`、`msg_open`、`msg_lead`、`form_submit`、`ctr`、`cpm`、`cvr`、`views`、`views_1min`、`comments` |
| 学习期 | `learning_status`（learning/learned/learning_failed/not_learning）、`learning_done`（0/1） |
| 原始留存 | `page_summary_json`（账户级+分析）、`raw_json`（完整原始快照） |

### 5.3.3 计划级完整字段（实测 active[0]）

```json
{
  "id": "7668720435709788202",
  "name": "0801-真人直播-画面直投",
  "status": "投放中",
  "rawStatus": "启用",
  "optStatus": 2,
  "spend": 7061.05,
  "conversions": 69,
  "formSubmit": 0,
  "privateMsgOpen": 98,
  "privateMsgRetain": 69,
  "attributionClue": 69,
  "leads": 69,
  "ctr": 0.0943, "cpm": 75.17, "cvr": 0.0078, "cpa": 102.33,
  "budget": 9000, "budgetMode": "按日预算",
  "liveEnter": 9455, "liveViews": 9455,
  "liveOneMin": 1507, "liveOver1Min": 1507,
  "liveComment": 212,
  "learningStatus": "learned",
  "learningDone": true,
  "_lifecycle": "active",
  "_justRevived": false
}
```

字段含义：

| 字段 | 含义 |
|---|---|
| `status` | 业务状态：投放中 / 暂停 / 超出预算 |
| `rawStatus` | 巨量引擎原始状态（启用…） |
| `optStatus` | 操作状态数字码 |
| `spend` | 当日累计消耗 |
| `conversions` | 转化数 |
| `formSubmit` | 表单提交数 |
| `privateMsgOpen` / `privateMsgRetain` | 私信打开 / 私信留资 |
| `attributionClue` | 归因线索 |
| `leads` | 线索数 = msg_lead + form_submit |
| `ctr` / `cpm` / `cvr` / `cpa` | 点击率 / 千次展示成本 / 转化率 / 转化成本 |
| `budget` / `budgetMode` | 预算 / 预算模式（按日预算等） |
| `liveEnter` / `liveViews` | 直播间进入 / 观看 |
| `liveOneMin` / `liveOver1Min` | 1 分钟+观看 |
| `liveComment` | 直播评论 |
| `learningStatus` / `learningDone` | 学习期状态 / 是否完成（详见第二十章） |
| `_lifecycle` | 生命周期标记（active…） |
| `_justRevived` | 是否刚复活 |

### 5.3.4 账户级/分析级内容（page_summary_json）

**summary（账户汇总）**：`totalActive`、`totalSpending`、`totalSpend`、`totalConversions`、`avgCPA/avgCTR/avgCVR/avgCPM`、`totalLeads`、`totalPrivateMsgOpen/Retain`、`totalFormSubmit`、`openRetainRate`、`totalLiveViews`、`totalLiveOver1Min`、`viewRetention`、`convEfficiency`、`accountSpend/accountBudget/accountBalance`、`statusLabels`。

**delta（窗口变化量）**：`spendLast15min`、`spendLastHour`、`speedCurrent/speedHour`、`prevCPA15/30`、`budgetUsed`、`convLast15min`、`cplLast15min`、`timeProgress`、`idealSpend`、`dailyBudget`。

**分析榜单**：`topNewSpenders`、`rampingUp`、`dropping`、`topSpenders`、`topPerformers`、`topCVR`（每项含 `spendDelta/changeRate/convDelta/cpa15/trend/spendPrev`）。

**基线统计**：`_multiDay`（近 N 天同时段 mean/stdev/min/max）、`_window3h`（3 小时窗口节律）。

**其他**：`alerts`（type/severity/detail）、`budgetExceededChanges`（prevStatus→curStatus）、`paused` 计数。

### 5.3.5 5min 与 15min 快照差异

| 内容 | 5min 速报 | 15min 完整 |
|---|---|---|
| 账户工具条 | accountSpend/accountBudget/accountBalance | ✅（合并进 summary） |
| 直播指标 | liveViews/liveOver1Min | ✅ + liveEnter/liveOneMin/liveComment |
| 滚动窗口 | `_rolling`（last5min/windows/convLast5min） | `_window3h` |
| 方法标记 | `_method`（http_api/cdp） | — |
| 分析榜单/基线/告警 | 无 | ✅ 全部 |

### 5.3.6 落库规则

- 结构化列：每条计划核心指标 → 独立行，支撑 SQL 聚合/趋势。
- `page_summary_json`：账户级 summary + delta + 榜单 + 基线。
- `raw_json`：原始完整快照，供回溯/灾备。



### 5.4 学习期字段
学习期字段（计划级）：
- `learningStatus`：巨量引擎原始枚举 `learning`（学习中）/ `learned`（学习完成）/ `learning_failed`（学习失败）/ `not_learning`（无需学习）/ 空
- `learningDone`（**学习期是否完成**）：布尔 —— `learned`/`learning_failed`/`not_learning` → `true`（学习期已结束）；`learning` → `false`（学习中）
- 来源：HTTP API 提供 `learning_status` 时直取；未提供时保守标记 `unknown`/`learning_done=0`，不按行业阈值推断；新建 4 天内计划只提醒赔付影响
- 用途：卡片「学习中」标记；`learning_failed` 计划告警；trigger-engine 条件支持 `learning_done=0` 时**不生成调优建议**（学习中转化不稳定）




### 5.5 数据库表结构（v2，9 表 + 3 物化视图）

| 表 | 关键字段 | 说明 |
|---|---|---|
| schema_migrations | version/applied_at | 迁移记录 |
| campaigns | campaign_id UNIQUE、name、status、daily_budget、bid、first_seen_at、platform_created_at | 计划主表；first_seen_at 为系统首次观察到时间，platform_created_at 为平台创建时间 |
| snapshots | snapshot_time/snapshot_cst、campaign_id、cost、leads、conversions、msg_open、msg_lead、form_submit、ctr、cpm、cvr、views、views_1min、comments、**learning_status / learning_done（学习期）**、external_action、conversion_depth、is_lead_source、source_type(api/cdp/dom)、page_summary_json、raw_json | 快照明细（核心事实表）；learning_done=0 时禁止/延迟调优 |
| daily_summaries | date UNIQUE、total_cost、total_leads、total_conversions、avg_cpa、avg_ctr、avg_cpm、active_count、peak_hour、peak_cost、source | 账户级日汇总 |
| shift_metrics（迁移后 `shifts`） | date、shift_label、record_type、planned_anchor_name、actual_anchor_name、spend、leads、cpl、UNIQUE(date,shift_label,account_id,record_type) | 排班与实绩分开记录 |
| alerts | alert_time、alert_type、severity、campaign_id、message、resolved | 告警事件 |
| actions | action_time、action_type、campaign_id、before_value、after_value、source、status、executed_at | 操作记录（重构后升级为 operations） |
| feedback | feedback_time、alert_id、response、note | 告警反馈 |
| config | key/value | 键值配置 |
| telemetry | check_time、write_lag_ms、db_size_mb、snap_count、wal_size_mb、checks_ok | 数据库自监控 |
| hourly_stats（物化） | stat_date/stat_hour/campaign_id、delta_cost、delta_leads、peak_cost、snap_count | 按计划×小时聚合 |
| daily_stats（物化） | stat_date/campaign_id、total_cost、total_leads、avg_ctr/cpm/cvr、peak_hour、peak_cost | 按计划×日聚合 |
| alert_timeline（物化） | stat_date/stat_hour/campaign_id、alert_count、severity 计数 | 告警时间线 |

刷新：`refresh-views.mjs` 增量（基于 `config.last_materialized_refresh`）；`backfill.mjs` 历史回灌。




### 5.6 关键计算逻辑

**v1.3 统一业务口径（所有计算、模板、规则必须先按本口径定义指标）**

| 口径 | 定义 | 本项目当前使用 |
|---|---|---|
| CPA | 指定成果转化成本 = 消耗 / 指定转化目标转化数 | 不替代 CPL；仅用于按 `external_action` 分类分析 |
| CPS | 实际订单转化成本 = 消耗 / 实际订单数 | 当前无订单回传，不计算、不推断 |
| CPL | 销售线索成本 = 消耗 / 销售线索数 | 本项目首要转化目标 |
| 销售线索数 | 深层计划转化数 | `leads = msg_lead + form_submit` 作为长期统计口径 |
| 深层计划 | 优化目标为私信留资、表单提交、多转化目标的计划 | 计入销售线索 |
| 浅层计划 | 优化目标为展示、点击、互动、停留等 | 不计入销售线索，仅作链路观察 |

当前 `snapshots.conversions` 必须结合 `external_action`/`conversion_depth` 区分深层与浅层；卡片与规则禁止直接用 `spend / conversions` 当作 CPL。
`external_action` 属于私信留资、表单提交、多转化目标时，`conversion_depth='deep'` 且 `is_lead_source=1`；其余展示/点击/互动/停留等为浅层，不计入销售线索。

| 逻辑 | 位置 | 算法 |
|---|---|---|
| 15min delta | monitor-v3 analyze | 当前累计 - 上一窗口累计 → spendLast15min/spendLastHour/speedCurrent |
| CPL | 各卡片 | 消耗 / 销售线索数（深层转化数，长期口径 leads = msg_lead + form_submit） |
| CPM / CTR / CVR | 卡片构建 | 千次展示成本 / 点击率 / 转化率 |
| 班次消耗 | snapshot-db getShiftDelta | 班次开始/结束快照差，快照缺失回退 HTTP getHourlyStats |
| 消耗节律基线 | AI 诊断 Rule-001 | 近 15 天同时段 mean/stdev → 5 档分级（过慢/偏慢/正常/偏快/过快），仅诊断不输出操作建议 |
| 操作效果 | worker writeAudit | snapshotBefore（最新 5m 快照），before/after 15min accountSpend delta；阈值：pause 后 delta<200=high_positive、<600=positive、<1000=neutral、否则 negative |
| 数值解析 | 各脚本散落 | `parseFloat(x.replace(/,/g,''))` —— **存在 ¥9459→¥7 事故，重构后统一 parseNum** |
| 数据一致性 | data-consistency-check.mjs | JSON vs SQLite 对账 |




### 5.7 AI 诊断与学习

- **规则手册**：`AI诊断规则手册.md`（Rule-001 起），原则「仅诊断标注异常，不输出操作建议」
- **数据源**：最新快照 JSON（`_multiDay` 近 3 天）+ 历史快照文件（15/30 天基线）+ `shifts-{date}.json`
- **学习回路**：`/api/ai/learning-data` 读审计 + 快照 → 按 (deliveryType, actionType) 分组 → successRate/confidence/avgDeltaSpend15min → 规则/证据列表
- **重构目标**：规则从静态文档迁移到 `trigger_rules` 表，数据源全部改读 DB，规则效果自动回写

---




### 5.8 数据保留策略

| 表/数据 | 保留期 | 策略 |
|---|---|---|
| `snapshots` | 90 天 | 超期按日归档/清理（分批 DELETE + 每月 VACUUM） |
| `hourly_stats` / `daily_stats` / `daily_summaries` / `shifts` | 长期 | 不清理 |
| `operations` / `operation_effects` / `ai_decisions` | 永久 | 审计要求 |
| `alerts` / `feedback` | 1 年 | 归档后清理 |
| `telemetry` | 90 天 | 清理 |
| 原始报文 JSON | 30 天 | 灾备回放窗口 |

实现：`scripts/purge-data.mjs` 每日 04:00 执行，保留期存 `config` 表可配置。90 天窗口下 DB 规模控制在 ~120-150MB。



---


## 六、重构方案总览（一页速览）

### 6.1 完整重构（S 系列）

- **目录分层**：`src/` 骨架 + 分层依赖规则（platform 只依赖 utils/config；domain 纯逻辑；services 只编排；禁止跨层 import）
- **进程合并**：采集+规则建议 → `oec-collector`；定时汇报/排班/清理 → `oec-scheduler`；换班+直播 → `live-pusher`；监听+队列+执行 → `feishu-bus`；12 → 6
- **monitor-data 分区**：snapshots/daily/shifts/state/logs/push-fallback + oceanengine.db
- **文档治理**：115 个 md → 有效 <15 + `docs/archive/` + ADR

### 6.2 数据中枢与 AI 建议（D 系列，当前人工执行）

- **采集独立**：`oec-collector` 5min 粒度，只写库；规则建议在采集完成后同进程求值；live-watcher 归 `live-pusher`
- **数据模型**：`snapshots` 5min + account_id；新增 `shifts` / `operations`（状态机 proposed→pending→confirmed→executing→succeeded/failed→rolled_back）/ `operation_effects`（15/30/60/120min 窗口）/ `trigger_rules` / `ai_decisions`
- **触发引擎**：条件求值 → 学习期/样本量过滤 → 冷却/日上限/互斥检查 → 生成 AI 建议 → 人工下达后执行 → 效果追踪 → 规则置信度回写
- **安全边界**：AI 短期只建议不执行；人工指令仍走现有确认、队列、执行、审计、回滚链路

### 6.3 前端抓取重构（P 系列，已被 D 系列吸收）

- 会话层（oec-session）→ 传输层（oec-http/oec-capture）→ 数据层（oec-normalize）→ 编排层（oec-router）→ 40102 逆向沉淀；`oec-dom-scraper` 仅归档/离线研究

### 6.4 多租户（S7，独立立项）

- 运营 1:N 直播间 1:N 广告账户；accounts.json 配置化接入；数据/推送隔离；Chrome Profile 池

---



### 6.5 远程运维通道治理（新增主题 K）

**决策（2026-08-01）**：不采用固定域名/固化地址的远程网关方案（无固定公网域名等环境条件），远程通道维持 CodeBuddy 网关 + cloudflared trycloudflare 动态隧道 + 地址变更飞书通知的现状，仅做治理与安全加固。

1. **纳入健康监控**：隧道进程（cloudflared）、隧道状态文件、网关 hook 执行结果纳入 `monitor-daemon`/`watchdog.ps1` 检查项，异常告警飞书。
2. **地址通知标准化**：`check_gateway_hook.sh` + `send_gateway_to_feishu.py` 收敛到 `scripts/`，记录结构化日志；密码轮换策略写入文档。
3. **安全加固**：当前仅内部本机使用，指定人员可通过网关隧道访问；隧道密码与飞书通知凭据入库 `.env`（不写死脚本）；Dashboard 若走隧道使用密码 URL + CSRF，禁止无密钥路径；后续可迁移到登录 session/一次性 token。
4. **文档化**：远程访问手册（如何获取最新地址、如何连接 webCLI、如何重启隧道）写入 README 附录。
5. **访问边界**：Dashboard 默认仅本机访问；隧道仅向指定人员临时开放，不对外公开。

---

# 第二部分 目标架构与完整重构方案（S 系列）


## 一、现状总览

### 1.1 系统定位

单账户"线索直播广告"实时监控系统：5/15 分钟定时采集巨量引擎数据 → 规则分析 → 飞书卡片推送 → 群消息指令（暂停/恢复/调预算/调出价）→ 审计反馈闭环，另含 Dashboard 可视化、AI 区域号汇总、周报 PPT 生成等能力。

### 1.2 运行拓扑（现状）

- **运行时**：Node 22.22.2 (ABI 127) + PM2，12 个进程（见 `ecosystem.config.cjs`）
- **数据库**：SQLite（better-sqlite3，WAL），数据落在 `monitor-data/oceanengine.db`
- **浏览器**：Chrome CDP 9222（`chrome-guard` 守护），Cookie 提取/写操作依赖
- **推送**：lark-cli + 飞书交互卡片
- **前端**：`feedback-server.mjs`（8899）+ `dashboard-v2`（Alpine.js + Chart.js）

### 1.3 进程清单

| PM2 进程 | 脚本 | 规模 | 调度 |
|---|---|---|---|
| pm2-15min | `oceanengine-monitor-v3.mjs` | **163KB** | cron */15 |
| pm2-5min | `oceanengine-5min-check.mjs` | 33KB | cron */5 |
| shift-pusher | `oceanengine-shift-pusher.mjs` | 18KB | 常驻 60s |
| pm2-daily-summary | `oceanengine-daily-summary.mjs` | 11KB | cron 23:35 |
| pm2-daily-report | `oceanengine-daily-report-scheduler.mjs` | 12KB | cron 23:05 |
| pm2-sync-tomorrow | `sync-tomorrow-shifts.mjs` | 5KB | cron 23:00（重构后并入 oec-scheduler，扩展当日排班/换班校对） |
| pm2-ai-regions | `ai-regions-http.mjs` | 9KB | cron 21:30 |
| live-watcher | `live-watcher.mjs` | 5KB | 常驻 60s |
| feishu-listener | `feishu-listener.mjs` | 32KB | 常驻 10s |
| action-queue-worker | `action-queue-worker.mjs` | 18KB | 常驻 15s |
| feedback-server | `feedback-server.mjs` | **72KB** | 常驻 |
| chrome-guard | `chrome-guard.mjs` | 5KB | 常驻 |

### 1.4 代码规模（README 统计，2026-07-20）

- `.mjs` 150 个 / 39,359 行；`.md` 115 个 / 19,186 行；`.py` 34 个 / 5,852 行
- 核心代码 `.mjs + .py` = 184 个文件 / 45,211 行

### 1.5 与已有方案的关系

| 已有方案 | 状态 | 本次重构的处理 |
|---|---|---|
| 前端数据抓取重构方案（oec-session/http/capture/normalize/router） | 待实施 | **吸收为主题 B**，列为 P1 阶段 |
| 工程化优化方案_20260725 | 已完成 5/7 项 | 吸收其未完成项（容器化、代码审查流程） |
| 多租户架构设计_线索直播广告监控 | 待实施 | **吸收为主题 I**，列为 P3 阶段 |
| 四层架构（D1-D7 决策档案） | v1.1 已实施 | 保持，纳入审计/队列模块迁移范围 |
| 小程序实现方案 | 待实施 | 不纳入本次重构范围，依赖 P3 完成后的 API 层 |


> **核心深化**：《数据中枢与AI调优架构设计》（2026-08-01）定义了本次重构的首要目标形态 —— 采集独立（`oec-collector` 只写库）、数据库为唯一事实源（汇报/监控全部读库）、调优操作与短期效果追踪全入库（`operations` / `operation_effects`）、规则配置化后条件触发 AI 自动调优（`trigger_rules` / `ai_decisions` / trigger-engine）。下文目标架构与路线图均按此形态组织。


---


## 二、痛点诊断

### 2.1 P0 · 风险级（影响正确性/可恢复性）

1. **数值解析事故隐患**：`ai-regions-http.mjs` 曾把 ¥9459 解析成 ¥7。各脚本自行 `parseFloat(x.replace(/,/g,''))`，无统一解析器。
2. **DOM 列索引硬编码**：`oceanengine-monitor-v3.mjs` 的 `scrapeOnePage`（data[7]~data[21]）、`fetch-ai-regions.mjs`（cells[0]~cells[3]），页面改版即静默错位产出脏数据。
3. **Cookie 多实现**：`oceanengine-api-client.mjs` / `oec-auto-login.mjs` / `oec-internal-api-discovery.mjs` 三处独立提取；HTTP 401 时无自动失效刷新，会话过期会静默空数据。
4. **巨型文件不可维护**：`oceanengine-monitor-v3.mjs`（163KB）与 `feedback-server.mjs`（72KB）混合了采集、分析、卡片构建、HTTP 路由、静态资源、DB 访问等全部职责，改一处风险波及全链路。
5. **根目录临时文件污染**：`_q*.py`、`_query_db.*`、`_build_*.mjs`、`_fix_*.mjs`、`_patch_*.mjs`、`.bak`、`.log` 等 30+ 个调试残留文件与生产代码混放。

### 2.2 P1 · 架构级（影响演进/维护）

6. **重复实现**（已在《前端数据抓取重构方案》量化）：8 处 `httpPost`、7 处 WebSocket 封装、3 处 Cookie 提取、多份推送封装。
7. **数据库三套并存**：`db/`（旧版）+ `巨量引擎监控数据库/`（v2 模块化）+ `巨量引擎快照数据库/`，入口不统一，`db/writer.mjs` 与新 `writer.mjs` 并存。
8. **配置硬编码**：`feishu-listener.mjs` 中 `MONITOR_CHAT_ID`/`ANCHOR_CHAT_ID`/`BOT_APP_ID`、`monitor-utils.mjs` 中账户常量、`fetch-*.mjs` 中 aadvid 硬编码（`1842681352509635` 等），`.env` 仅覆盖部分。
9. **调度体系重叠**：PM2 cron + `monitor-daemon.mjs` + `adaptive-scheduler.mjs` + `autonomous-router.mjs` 并存，其中 adaptive-scheduler / autonomous-router 未真正接入 PM2 主链路，成为"半成品体系"。
10. **前端双版本**：`dashboard.html/js/css`（旧）与 `dashboard-v2.html/js/css`（新）并存，旧版仍被 `feedback-server` 路由支持。

### 2.3 P2 · 工程级（影响效率/质量）

11. **测试覆盖不足**：`tests/` 仅 8 个文件，集中在 csrf/队列锁/推送守卫；采集、分析、卡片构建、DB 读写、HTTP 路由等核心链路几乎无测试。
12. **CI 偏弱**：`.github/workflows/ci.yml` 仅跑 `scripts/ci-test.mjs`（部分逻辑测试）与 `backup-verify`，无 lint、无构建校验、无 Windows 行为验证（CI 在 ubuntu 上跑，与生产 Windows 环境有偏差）。
13. **日志/产物位置散乱**：根目录存在 `listener-output.log`、`monitor-v3.log`、`feedback-server.log`、`listener-test.log` 等；`monitor-data/` 同时混放快照 JSON、状态文件、备份、日志，无分区。
14. **一次性脚本游离体系外**：`fetch-*.mjs`、`oec-*.mjs` 不在 PM2 编排、无健康检查、无日志归口。
15. **文档治理缺失**：115 个 md 中约 30 个为历史评审/多轮选型文档（`架构规划_*` 系列 20+ 个），与现状严重脱节，新人不辨真伪。

### 2.4 P3 · 演进级（影响业务扩展）

16. **单账户绑定**：`OEC_ACCOUNT_ID` 模块级常量 + 单 Chrome Profile + 单飞书群 + 单排班表，无法支撑多运营/多直播间/多账户（详见《多租户架构设计》）。

---


## 三、目标架构

### 3.1 目标目录结构

```
巨量引擎监控/
├── package.json / ecosystem.config.cjs / .env.example
├── README.md / AGENTS.md / CHANGELOG.md
├── src/                          # 生产代码（按层分包）
│   ├── config/                   # 配置加载（env + accounts.yaml/JSON + 校验）
│   │   └── index.mjs
│   ├── platform/                 # 巨量引擎对接层（唯一入口）
│   │   ├── oec-client.mjs        # 会话+HTTP+重试+熔断（聚合 session/http/normalize）
│   │   ├── oec-session.mjs       # Cookie 提取/缓存/刷新（P0，来自前端抓取方案）
│   │   ├── oec-http.mjs          # 统一 HTTP（合并 8 处 httpPost）
│   │   ├── oec-normalize.mjs     # 数值解析/字段映射/校验（P2）
│   │   ├── oec-capture.mjs       # CDP 抓包逆向（P1，工具化）
│   │   ├── oec-fetch.mjs        # HTTP API 直连 + 重试/告警（P2）
│   │   └── oec-retry.mjs        # 失败每分钟重试 → 飞书群告警（P3）
│   │   ├── oec-cdp.mjs           # 统一 CDP 连接（合并 7 处 WebSocket）
│   │   └── oec-actions.mjs       # 写操作（暂停/预算/出价，API+CDP 双路径）
│   ├── domain/                   # 业务域（纯逻辑，可单测）
│   │   ├── analyze.mjs           # 15min/5min 分析（从 monitor-v3 拆出）
│   │   ├── card-builder.mjs      # 飞书卡片构建（CARD-FORMAT 落地）
│   │   ├── alerts.mjs            # 告警规则（AI诊断规则手册落地）
│   │   ├── shifts.mjs            # 排班/换班
│   │   ├── daily.mjs             # 日汇总/日报
│   │   └── ai-regions.mjs        # AI 区域号
│   ├── services/                 # 进程级编排（瘦入口）
│   │   ├── monitor-15min.mjs     # ← 由 monitor-v3 拆分
│   │   ├── monitor-5min.mjs      # ← 由 5min-check 拆分
│   │   ├── shift-pusher.mjs
│   │   ├── daily-summary.mjs
│   │   ├── daily-report.mjs
│   │   ├── sync-shifts.mjs      # 排班同步 + 当日校对（替代 sync-tomorrow）
│   │   ├── ai-regions.mjs
│   │   ├── live-watcher.mjs
│   │   ├── feishu-listener.mjs
│   │   ├── action-worker.mjs
│   │   └── http-server.mjs       # ← feedback-server 拆分后的服务壳
│   ├── web/                      # 前端
│   │   ├── dashboard/            # 仅保留 dashboard-v2（删除旧版）
│   │   └── assets/  vendor/
│   ├── db/                       # 统一数据库模块（合并三套）
│   │   ├── schema.sql / migrations/
│   │   ├── dal.mjs / writer.mjs / index.mjs
│   │   └── refresh-views.mjs
│   ├── feishu/                   # 飞书对接（推送/监听/卡片）
│   │   ├── push.mjs / listener.mjs / cards.mjs / guard.mjs
│   ├── cdp/                      # Chrome 运维
│   │   ├── client.mjs / action.mjs / guard.mjs / auto-login.mjs
│   ├── utils/                    # logger / wait / csrf / fs-atomic / locks
│   └── index.mjs                 # 业务入口聚合（供 pm2 与脚本共用）
├── scripts/                      # 运维脚本（备份/健康检查/数据一致性/逆向工具）
├── tests/                        # 与 src/ 同构镜像
├── docs/                         # 仅保留有效文档（见主题 J）
├── monitor-data/                 # 运行数据（已分区，见 3.4）
└── vendor/
```

### 3.2 分层与依赖规则

```
web (dashboard-v2) ──HTTP──▶ services/http-server ──▶ domain/* ──▶ db
PM2 进程 (services/*) ────────▶ domain/* ──▶ platform/oec-* ──▶ feishu/*, cdp/*
                                  │                │
                                  └──▶ db ◀─────────┘
```

依赖规则（写进 AGENTS.md）：
1. `platform/` 只依赖 `utils/` 与 `config/`，不依赖 `domain/`
2. `domain/` 为纯逻辑，不 import 任何 `platform/`（数据通过参数注入），保证可单测
3. `services/` 只做编排：定时触发、进程生命周期、日志上下文，不写业务算法
4. 禁止跨层 import（如 `domain/` 直接调 `feishu/`）；违规由 CI 的 import 检查拦截

### 3.3 目标运行拓扑（进程合并建议）

| 现状（12 进程） | 目标（6 进程） | 合并理由 |
|---|---|---|
| pm2-15min 采集部分 + pm2-5min 采集部分 + trigger-engine | `oec-collector`（采集 + 规则建议） | 采集完成后同进程求值，避免跨进程事件竞态 |
| pm2-daily-summary + pm2-daily-report + pm2-ai-regions + pm2-sync-tomorrow | `oec-scheduler`（定时汇报/排班/清理） | 合并冷启动成本，共享 DB 连接 |
| shift-pusher + live-watcher | `live-pusher` | 两者同为 60s 轮询常驻，读同一批快照 |
| feishu-listener + action-queue-worker | `feishu-bus` | 监听→入队→执行天然同进程串行，省去文件队列往返 |
| feedback-server | `http-server` | 保留独立（端口服务需稳定） |
| chrome-guard | `chrome-guard` | 独立守护 Chrome 9222 |

> 注意：队列方案已确定。D2 直接迁移到 SQLite `operations` 持久化队列，停止使用 `action-queue.json`；不改为纯内存队列，`feishu-bus` 只负责调度和执行。

### 3.4 monitor-data 分区

```
monitor-data/
├── snapshots/     # 15min/5min 快照 JSON（按日归档）
├── daily/         # 日汇总/日报
├── shifts/        # 排班缓存
├── state/         # live-state / listener-state / action-queue / fail-log
├── logs/          # monitor.log + ndjson（保留现有 logger 格式）
├── push-fallback/
└── oceanengine.db # SQLite（唯一 DB 文件）
```


### 3.5 数据中枢（核心目标形态）

```
oec-collector（采集 + 内联 trigger-engine 建议） → SQLite 数据中枢 → 汇报层 / 监控层
                           ▲                     │
                           └── 效果回写 ◀── operations + operation_effects
```

- 全部写入只经 `src/db/writer.mjs`，全部读取只经 `src/db/dal.mjs`；`domain/` 层通过 `dbApi` 注入，不再直接读文件。
- 数据模型：`snapshots` 改 5min 粒度 + `account_id` 列 + `learning_status`/`learning_done`（学习期）字段；新增 `shifts` / `operations` / `operation_effects` / `trigger_rules` / `ai_decisions` 五张业务表。
- 建议闭环：`trigger_rules`（条件配置）→ trigger-engine 命中 → 只生成 `ai_decisions(suggest)` → 人工下达后创建 `operations` → `operation_effects`（15/30/60/120min 多窗口追踪）→ 回写规则 `success_rate`。
- AI 建议安全边界：短期只生成建议；所有操作由人工下达，冷却/日上限/互斥/参数护栏/熔断/一键回滚/全量审计保留。
- 详细表结构、状态机与安全边界见《数据中枢与AI调优架构设计》。


---


## 四、重构主题分解

### 主题 A · 目录与模块重组（P0，先做）

**目标**：建立 `src/` 分层骨架，生产代码全部迁入，根目录只保留入口文件与配置。

**步骤**：
1. 新建 `src/` 骨架与各层空目录，迁移 `monitor-utils.mjs` → `src/utils/`（作为第一批，因其被 90% 模块引用）
2. 以 `utils → config → platform → db → feishu → cdp → domain → services → web` 的依赖顺序逐批迁移，每批一个 commit
3. 迁移期间保留根目录旧文件，通过 `ecosystem.config.cjs` 的 `script` 指向新路径完成切换；确认无回归后删除旧文件
4. 临时脚本（`_*.mjs`、`_q*.py` 等）统一移入 `scripts/_deprecated/`，`git rm` 后归档到 `archive/` 或直接删除（按 git 历史可恢复）

**验收**：根目录仅剩配置文件、README、`ecosystem.config.cjs`、`src/`、`scripts/`、`tests/`、`docs/`、`monitor-data/`；PM2 12 进程全部指向新路径且 24h 无回归。

### 主题 B · 采集链路收敛（P1，直接执行《前端数据抓取重构方案》）

按该方案 P0→P4 执行，产出 `oec-session / oec-http / oec-capture / oec-normalize / oec-router`，全部落在 `src/platform/`；`oec-dom-scraper` 仅归档/离线研究，不进入生产：
- 消灭 8 处 httpPost、7 处 WebSocket、3 处 Cookie 提取
- `parseNum` 统一数值解析（杜绝 ¥9459→¥7 事故）
- 抓取失败策略：HTTP API 失败 → 每分钟重试（窗口内最多 5 次）→ 飞书群告警 → 持续重试 + 恢复通知；取消 CDP DOM/抓包降级路径（CDP 仅保留 Cookie 刷新、写操作、逆向工具）
- 主链路（15min/5min）先接入，diff 一致后再切换

### 主题 C · 巨型文件拆分（P1，与 B 并行）

**C1 `oceanengine-monitor-v3.mjs`（163KB）** 按职责拆为：
- `src/services/monitor-15min.mjs`：编排（入口，~2KB）
- `src/domain/analyze.mjs`：delta 计算、告警判定、生命周期
- `src/domain/card-builder.mjs`：卡片构建
- `src/domain/shifts.mjs`：排班时段/主播映射 + 活跃期判定 + 排班/换班校对
- 数据抓取部分由 `platform/oec-*` 接管

**C2 `feedback-server.mjs`（72KB）** 拆为：
- `src/services/http-server.mjs`：路由骨架
- `src/services/http-routes/`：`api-snapshots / api-actions / api-ai / api-alerts / static` 各路由模块
- DB 访问统一走 `src/db/dal.mjs`

**C3 `oceanengine-5min-check.mjs`（33KB）** 同 C1 模式拆分。

**策略**：先"抽函数不搬逻辑"（保持行为不变），全部抽完后做行为对比测试（新旧输出 diff），再删旧文件。

### 主题 D · 服务层统一（P1）

- `feishu-push-guard.mjs` 的推送逻辑收敛到 `src/feishu/push.mjs`，卡片构建收敛到 `src/feishu/cards.mjs`
- 前端删除 `dashboard.html/js/css` 旧版与 `dashboard-v2` 的旧路由分支，只保留 v2
- 统一日志：`logger.mjs` 扩展 `writeChange()`，运行日志与 agent 变更日志（`[CHANGE]` 前缀）聚合到同一文件；配套 `scripts/log-change.mjs` CLI 与 AGENTS.md 强制规范（调试原因 / 执行方法 / 结果 done|partial|failed）。
- PWA（sw.js/manifest.json）保留，纳入 `src/web/`

### 主题 E · 数据库统一（P1）

1. 以 `巨量引擎监控数据库/`（v2，9 表 + 3 物化视图 + migrations）为唯一基线
2. 删除 `db/` 旧版（`db/writer.mjs` 等），所有引用改走新 `index.mjs`
3. `巨量引擎快照数据库/snapshot-db.mjs` 改为依赖 `src/db/dal.mjs` 的统一入口（其快照文件读取逻辑保留）
4. `monitor-data/oceanengine.db` 路径由 `src/config/` 统一定义，消除各处 `path.join(__dirname, '..', 'monitor-data', ...)`
5. schema 增加 `account_id`/`tenant_id` 预留列（为多租户做无损迁移，默认值 = 当前账户）

### 主题 F · 配置管理（P1）

1. `.env` 收敛为"运行时敏感项"（Cookie 相关除外，Cookie 已在 `monitor-data/state/`）
2. 新增 `src/config/accounts.json`（或 YAML）：账户/直播间/群/chrome profile 的多实例配置，结构与《多租户架构设计》的租户模型对齐
3. 所有硬编码（chat_id、bot app id、aadvid、账户 ID）迁移到配置；启动时校验必填项，缺失即 fail-fast
4. 保留 `.env.example`，补充新增配置项说明

### 主题 G · 调度与守护统一（P2）

1. 保留 PM2 作为进程管理器（不引入新编排工具）
2. 明确 `adaptive-scheduler.mjs` / `autonomous-router.mjs` 的归属：前者并入 scheduler 的间隔策略，并升级为「排班驱动」——按当日排班时段精确判定活跃期（非排班时段 = 低活跃期，30min 低频或暂停）；后者并入 `oec-fetch` 的重试/告警策略；两个独立文件不再作为"游离模块"存在
3. `monitor-daemon.mjs` 的守护能力（Windows 任务/日志/数据间隔检查）收敛进 `scripts/`，健康检查统一输出 `monitor-data/state/health.json`
4. 进程合并（3.3）在阶段 5 执行，先合并 cron 类进程，再合并常驻进程

### 主题 H · 测试与 CI 强化（P0 起持续）

**优先级**：P0 domain/analyze、domain/card-builder 纯逻辑单测（无外部依赖，跑 CI）；P1 platform/oec-* 集成测试（mock HTTP/CDP）；P2 端到端测试（需要真实 DB/Chrome）。

**单元测试（node:test）**，镜像 `src/` 结构：
- `platform/oec-normalize`：数值解析、字段映射、列头告警（重点回归 ¥9459 事故）
- `platform/oec-fetch`：重试/告警逻辑（复用现有 `autonomous-router.test.mjs` 的熔断思路）
- `domain/analyze`、`domain/card-builder`：纯逻辑全覆盖
- `db`：schema 迁移、writer 幂等
- `feishu`：指令解析、确认闭环、推送守卫

**集成测试**：保留 `csrf-http`（端口级）、新增 `http-server` 路由冒烟（临时端口）、`oec-session` 用 mock cookie 文件验证 TTL/401 刷新逻辑。

**CI 强化**：
- 保留 ubuntu 单测 job；新增 `windows-latest` job 跑 `npm ci && node scripts/ci-test.mjs`（覆盖 better-sqlite3 原生模块与路径语义）
- 新增 import 依赖规则检查脚本（禁止跨层 import），纳入 CI
- 新增 `npm run check`（lint + 单测 + import 检查），`npm run test`

### 主题 I · 多租户演进（P3，按《多租户架构设计》）

前置：主题 B（oec-session 支持多 Cookie 缓存）、主题 E（account_id 列）、主题 F（accounts.json）。
执行顺序：租户模型 → 采集循环改造（每账户一个 session）→ 数据隔离（account_id 过滤）→ 推送隔离（每运营自己的群）→ Chrome Profile 池与调度限额。

### 主题 J · 文档治理（P1 起）

1. `docs/` 仅保留：README、CHANGELOG、CARD-FORMAT、AI诊断规则手册、API字段全量档案、直播状态巡检协议规范、多租户设计、前端抓取重构方案、本次完整重构方案
2. 历史评审/选型文档（`架构规划_*`、`*_评审*`、`*_审查*` 等 30+ 个）`git mv` 到 `docs/archive/`，每个文件头部加「已归档，内容与当前版本可能不一致」标记。之后 `git rm --cached` 保留历史可回溯。
3. 新建 `docs/DECISIONS.md` 记录关键架构决策（ADR 风格），替代散落文档

---



### 主题 K · 远程运维通道治理（P1/P2）

**优先级**：密码外置、飞书凭据进 `.env` 为 P1（安全欠债）；健康监控纳入、README 手册为 P2。

现状：CodeBuddy 远程网关 + cloudflared trycloudflare 隧道 + Web UI/webCLI 已在运行（密码鉴权），隧道地址变更通过 `check_gateway_hook.sh` + `send_gateway_to_feishu.py` 推送飞书；该通道游离于本方案之外。

**决策（2026-08-01）**：不采用固定域名/固化地址的远程网关方案（无固定公网域名等环境条件），远程通道维持 CodeBuddy 网关 + cloudflared trycloudflare 动态隧道 + 地址变更飞书通知的现状，仅做治理与安全加固。

目标：
1. 隧道进程（cloudflared）、`tunnel-state.json`、网关 hook 执行结果纳入统一健康监控（monitor-daemon/watchdog），异常飞书告警。
2. 地址通知脚本收敛到 `scripts/`，结构化日志 + 去重状态统一管理。
3. 安全加固：密码/飞书凭据进 `.env`；webCLI 会话可配只读；Dashboard 走隧道时强制鉴权 + CSRF。
4. 远程访问手册写入 README（如何获取最新地址 / 连接 webCLI / 重启隧道）。
5. 可选：监控 Dashboard（8899）纳入远程通道，实现异地查看与运维。



---

# 第三部分 数据中枢与 AI 建议（D 系列，当前人工执行）


## 一、设计目标

1. **采集独立**：数据抓取与业务汇报解耦，采集器只负责「抓 → 写库」，不再内嵌卡片构建/推送/分析逻辑。
2. **数据库为唯一事实源**：绝大部分监控与汇报数据从 SQLite 读取；JSON 快照降级为灾备回放，不作为读取源。
3. **调优闭环入库**：调优操作（operations）与短期效果追踪（operation_effects）全部入库，替代文件队列 + audit 文件。
4. **条件触发 AI 建议**：规则配置化（trigger_rules），命中条件后生成 AI 建议，人工下达后执行，效果自动回写规则置信度，形成「规则 → 建议 → 人工执行 → 效果 → 规则」闭环。

---


## 二、目标数据流

```
┌─ 采集层（独立进程，只写）───────────────────────────────┐
│ oec-collector（5min 定时，遍历所有账户）                  │
│   └─ oec-fetch：HTTP API 直连（唯一抓取路径）              │
│       失败 → 每分钟重试 → 飞书群告警 → 恢复通知   │
│   ├─ trigger-engine 内联（采集完成后生成 AI 建议）          │
│   └─ 统一 writer（事务 + 幂等 upsert）→ src/db           │
└────────────────────────────────────────────────────────┘
        │ 写
        ▼
┌─ 数据中枢 SQLite（唯一事实源）───────────────────────────┐
│  campaigns · snapshots(5min) · hourly_stats · daily_stats│
│  shifts · operations · operation_effects · trigger_rules │
│  ai_decisions · alerts · feedback · config · telemetry   │
└────────────────────────────────────────────────────────┘
        │ 读
        ├─▶ 汇报层：15min/5min卡片、日报、日汇总、Dashboard、周报PPT
        ├─▶ 监控层：shift-pusher、monitor-daemon、健康检查
        └─▶ 决策层：trigger-engine（条件触发）→ AI 建议 → 人工下达 → operations → effects
```

**原则**
- 所有写入只经 `src/db/writer.mjs` 一个入口；所有读取只经 `src/db/dal.mjs`。
- `domain/` 层函数签名改为 `(ctx, dbApi, inputs)`，不再直接读文件。
- JSON 文件仅保留两类：采集原始报文（灾备回放）与状态锁文件（进程互斥）。

---


## 三、数据模型设计

### 3.1 通用改造
- 所有业务表增加 `account_id TEXT NOT NULL DEFAULT '1842681352509635'`（现值）与 `tenant_id TEXT DEFAULT ''`（多租户预留，见《多租户架构设计》）。
- 所有按计划查询的索引升级为 `(account_id, campaign_id, ...)` 复合前缀。

### 3.2 snapshots —— 改为 5min 粒度为主
现表以 15min 为主（`source_type` 区分 5min/15min），新模型统一 5min 采集：

```sql
CREATE TABLE IF NOT EXISTS snapshots (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  snapshot_time TEXT NOT NULL,              -- UTC ISO
  snapshot_cst  TEXT NOT NULL,              -- CST HH:MM
  account_id    TEXT NOT NULL DEFAULT '',
  campaign_id   TEXT NOT NULL,
  status        TEXT NOT NULL DEFAULT '',   -- 投放中/暂停/超出预算…
  daily_budget  REAL NOT NULL DEFAULT 0,
  bid           REAL,
  cost          REAL NOT NULL DEFAULT 0,    -- 当日累计
  leads         INTEGER NOT NULL DEFAULT 0,
  conversions   INTEGER NOT NULL DEFAULT 0,
  msg_open      INTEGER NOT NULL DEFAULT 0,
  msg_lead      INTEGER NOT NULL DEFAULT 0,
  form_submit   INTEGER NOT NULL DEFAULT 0,
  ctr           REAL NOT NULL DEFAULT 0,
  cpm           REAL NOT NULL DEFAULT 0,
  cvr           REAL NOT NULL DEFAULT 0,
  views         INTEGER NOT NULL DEFAULT 0,
  views_1min    INTEGER NOT NULL DEFAULT 0,
  comments      INTEGER NOT NULL DEFAULT 0,
  external_action TEXT NOT NULL DEFAULT '',   -- 2=表单提交/100=多转化/192=私信留资等
  conversion_depth TEXT NOT NULL DEFAULT 'unknown', -- deep/shallow/unknown
  is_lead_source INTEGER NOT NULL DEFAULT 0,   -- 深层计划转化是否计入销售线索
  learning_status TEXT NOT NULL DEFAULT '',   -- learning/learned/learning_failed/not_learning
  learning_done INTEGER NOT NULL DEFAULT 0, -- 学习期是否完成 (0/1)
  source_type   TEXT NOT NULL DEFAULT 'api', -- api/cdp/dom
  page_summary_json TEXT,
  raw_json      TEXT,
  UNIQUE(account_id, campaign_id, snapshot_time)
);
CREATE INDEX IF NOT EXISTS idx_snap_acc_time ON snapshots(account_id, snapshot_time);
CREATE INDEX IF NOT EXISTS idx_snap_camp_time ON snapshots(campaign_id, snapshot_time);
```

- 15min 卡片 = 最近 3 个 5min 窗口聚合（沿用现有 delta 逻辑，改为 SQL/内存聚合）。
- `UNIQUE` 保证幂等：同一时刻重复采集 upsert 而非插入。


学习期字段（计划级）：
- `learningStatus`：巨量引擎原始枚举 `learning`（学习中）/ `learned`（学习完成）/ `learning_failed`（学习失败）/ `not_learning`（无需学习）/ 空
- `learningDone`（**学习期是否完成**）：布尔 —— `learned`/`learning_failed`/`not_learning` → `true`（学习期已结束）；`learning` → `false`（学习中）
- 来源：HTTP API 提供 `learning_status` 时直取；未提供时保守标记 `unknown`/`learning_done=0`，不按行业阈值推断；新建 4 天内计划只提醒赔付影响
- 用途：卡片「学习中」标记；`learning_failed` 计划告警；trigger-engine 条件支持 `learning_done=0` 时**不生成调优建议**（学习中转化不稳定）

### 3.3 shifts —— 排班/班次表（替代 shifts-*.json）
```sql
CREATE TABLE IF NOT EXISTS shifts (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  date        TEXT NOT NULL,                -- YYYY-MM-DD
  shift_label TEXT NOT NULL,                -- HH:MM-HH:MM
  record_type TEXT NOT NULL DEFAULT 'schedule', -- schedule/actual
  planned_anchor_name TEXT NOT NULL DEFAULT '',
  actual_anchor_name TEXT NOT NULL DEFAULT '',
  account_id  TEXT NOT NULL DEFAULT '',
  spend       REAL NOT NULL DEFAULT 0,
  leads       INTEGER NOT NULL DEFAULT 0,
  cpl         REAL NOT NULL DEFAULT 0,
  source      TEXT NOT NULL DEFAULT 'snapshot',
  detail_json TEXT,
  created_at  TEXT NOT NULL DEFAULT (datetime('now','localtime')),
  UNIQUE(date, shift_label, account_id, record_type)
);
```

### 3.3.1 排班驱动的活跃期判定

活跃期 = 当日排班表覆盖的班次时段集合（`[{start, end, anchor}]`）；**非预定直播排期时段一律记为低活跃期**：

| 当前时间 | 采集频率 | 说明 |
|---|---|---|
| 在任一排班时段内 且 账户有消耗 | 5min 高频 | 直播进行中 |
| 在任一排班时段内 且 无消耗 | 15min 中频 | 已排班未起量 |
| **非排班时段（无预定直播）** | **30min 低频或暂停** | **低活跃期（核心变化：不再按固定 7-23 小时窗口，而按排班表精确判定）** |
| 数据陈旧 > 1h | 立即补采一次 | stale_compensation |

- 判定源：`shifts` 表当日班次；缓存缺失时回退 `getTodayShiftWindow()` 小时范围 → 再回退默认 7-23。
- 替代现有 `adaptive-scheduler.mjs` 的固定小时窗口策略（其窗口逻辑保留为回退层）。

### 3.3.2 每日排班同步与当日校对（sync-shifts，替代 sync-tomorrow-shifts）

每日 23:00 执行（重构后并入 `oec-scheduler`）：

1. **次日排班确认**（现状保留）：读飞书排班表次日班次 → 写入 `shifts` 表（date=次日）。
2. **当日排班校对**：读飞书排班表当日班次 vs `shifts` 表当日记录 —— 时段/主播变化 → 差异告警。
3. **当日换班数据校对**：`shifts` 表当日排班 vs `shift-pusher` 已生成的当日换班数据：
   - 排班时段无对应换班记录（缺班次）→ warn
   - 换班记录不在排班时段内（多余/漂移）→ warn
   - 换班主播名与排班不一致 → warn
   - 班次消耗/CPL 缺失或明显异常 → warn
4. **校对报告**：输出 `monitor-data/state/shift-audit-{date}.json` + 飞书推送（✅ 汇总 + ⚠️ 差异明细），作为日汇总前的人工核查依据。

### 3.4 operations —— 调优操作主表（替代 actions 表 + action-queue.json + action-audit.jsonl）
```sql
CREATE TABLE IF NOT EXISTS operations (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  op_id           TEXT UNIQUE,              -- traceRef/业务ID
  account_id      TEXT NOT NULL DEFAULT '',
  campaign_id     TEXT NOT NULL,
  plan_name       TEXT NOT NULL DEFAULT '',
  op_type         TEXT NOT NULL,            -- pause/resume/adjust_budget/adjust_bid
  params_json     TEXT,                     -- { value, reason, mode }
  source          TEXT NOT NULL DEFAULT 'feishu',  -- feishu/dashboard/manual；ai_suggest 仅生成建议不生成可执行操作
  status          TEXT NOT NULL DEFAULT 'proposed',-- 状态机见 3.4.1
  before_value    TEXT,                     -- 执行前回读
  after_value     TEXT,                     -- 执行后回读
  trigger_rule_id INTEGER,
  ai_decision_id  INTEGER,
  trace_ref       TEXT NOT NULL DEFAULT '',
  error           TEXT,
  rollback_status TEXT,                     -- none/succeeded/failed
  created_at      TEXT NOT NULL DEFAULT (datetime('now','localtime')),
  confirmed_at    TEXT,
  executed_at     TEXT,
  finished_at     TEXT
);
CREATE INDEX IF NOT EXISTS idx_ops_acc_status ON operations(account_id, status);
CREATE INDEX IF NOT EXISTS idx_ops_camp_time   ON operations(campaign_id, created_at);
CREATE INDEX IF NOT EXISTS idx_ops_pending     ON operations(status) WHERE status IN ('proposed','pending','executing');
```

**3.4.1 状态机**
```
proposed ──(规则命中，AI建议)──▶ pending
pending  ──(人工确认)──▶ confirmed ──▶ executing ──▶ succeeded
pending  ──(拒绝/超时)──────────▶ rejected
executing ──(失败可回滚)────────▶ failed ──▶ rolled_back
```
- `proposed`：仅 AI 建议（未入执行队列），用于 Dashboard/AI 建议 Tab 展示。
- `pending`：进入执行队列待确认（复用现有飞书确认闭环，指令语义不变）。
- 状态迁移写 `operations_status_log`（事件表，可选，P2 再上）或仅记录时间戳。

### 3.5 operation_effects —— 短期效果追踪
```sql
CREATE TABLE IF NOT EXISTS operation_effects (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  operation_id INTEGER NOT NULL,
  account_id  TEXT NOT NULL DEFAULT '',
  campaign_id TEXT NOT NULL,
  op_type     TEXT NOT NULL,
  op_time     TEXT NOT NULL,
  window_minutes INTEGER NOT NULL,          -- 15/30/60/120
  before_cost REAL NOT NULL DEFAULT 0,
  after_cost  REAL NOT NULL DEFAULT 0,
  cost_delta  REAL NOT NULL DEFAULT 0,
  before_leads INTEGER NOT NULL DEFAULT 0,
  after_leads  INTEGER NOT NULL DEFAULT 0,
  leads_delta  INTEGER NOT NULL DEFAULT 0,
  before_cpl  REAL NOT NULL DEFAULT 0,
  after_cpl   REAL NOT NULL DEFAULT 0,
  cpl_delta   REAL NOT NULL DEFAULT 0,
  cpl_delta_pct REAL NOT NULL DEFAULT 0,
  eval_label  TEXT NOT NULL DEFAULT 'neutral',  -- high_positive/positive/neutral/negative（沿用现有阈值）
  computed_at TEXT NOT NULL DEFAULT (datetime('now','localtime')),
  UNIQUE(operation_id, window_minutes)
);
```

**3.5.1 计算规则（由 effect-tracker 异步执行）**  
阈值从 `config` 表读取（`key=effect_eval_thresholds`），默认值沿用 v1.2 设定：`{"pause":{"high_positive":200,"positive":600,"neutral":1000}}`。各 op_type 可独立配置，缺失时回退硬编码默认值。
- `before` = 操作时刻前 `window` 分钟窗口的平均/累计指标（读 snapshots）
- `after` = 操作时刻后 `window` 分钟窗口的指标（读 snapshots）
- 阈值沿用现有 v1.2 设定：`pause` 后 `delta<200=high_positive, <600=positive, <1000=neutral，否则 negative`；其他 op_type 阈值按指标（cpl_delta_pct / leads_delta）配置化
- 时间不足窗口（如刚执行 10 分钟）→ 不计算，标记 `pending`，由后续轮询补齐（≤120min 后仍不足则放弃并标记 `skipped`）。

15min 卡片聚合降级：3 个 5min 窗口不足时按实有窗口数聚合，卡片注明 `partial` 标签。

### 3.6 trigger_rules —— 条件触发规则（AI诊断规则手册配置化）
```sql
CREATE TABLE IF NOT EXISTS trigger_rules (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  name          TEXT UNIQUE NOT NULL,       -- Rule-001…
  description   TEXT NOT NULL DEFAULT '',
  enabled       INTEGER NOT NULL DEFAULT 1,
  scope         TEXT NOT NULL DEFAULT 'campaign', -- account/campaign/live_room
  condition_json TEXT NOT NULL,             -- 见 3.6.1
  action_template TEXT NOT NULL,            -- 见 3.6.2
  approval_mode TEXT NOT NULL DEFAULT 'suggest',   -- suggest/confirm/disabled；auto 暂不开放
  cooldown_minutes INTEGER NOT NULL DEFAULT 60,
  max_daily_ops INTEGER NOT NULL DEFAULT 3,
  confidence_floor REAL NOT NULL DEFAULT 0.6,
  success_rate REAL NOT NULL DEFAULT 0,     -- 由效果追踪回写
  sample_count INTEGER NOT NULL DEFAULT 0,
  min_conversions INTEGER NOT NULL DEFAULT 0, -- 深层转化数下限
  min_leads       INTEGER NOT NULL DEFAULT 0, -- 销售线索数下限
  min_cost        REAL NOT NULL DEFAULT 0,    -- 消耗样本下限
  basis           TEXT NOT NULL DEFAULT 'delta', -- cumulative/delta
  baseline_scope  TEXT NOT NULL DEFAULT 'same_hour', -- same_shift/same_weekday/same_hour
  version       TEXT NOT NULL DEFAULT '1.0',
  created_at    TEXT NOT NULL DEFAULT (datetime('now','localtime')),
  updated_at    TEXT NOT NULL DEFAULT (datetime('now','localtime'))
);
```

**3.6.1 condition_json 示例**
```json
{
  "metric": "cpl_ratio",        // cpl_ratio / pacing(消耗节律) / budget_overflow / leads_speed / cost_speed
  "basis": "delta",             // cumulative/delta
  "baseline_scope": "same_shift", // same_shift/same_weekday/same_hour
  "window": "60m",              // 观察窗口
  "op": ">",                    // > / < / >= / <= / ==
  "value": 1.2,                 // 或引用基线: {"baseline": "multi_day", "offset_stdev": 2}
  "min_occurrences": 2,         // 连续/最近N次命中才触发
  "min_conversions": 20,        // 深层转化数不足时不建议
  "min_leads": 10,              // 销售线索数不足时不建议
  "min_cost": 100,              // 消耗样本不足时不建议
  "scope_filter": { "status": "投放中", "learning_done": 1 }   // learning_done=0 时禁止/延迟调优
}
```
**3.6.2 action_template 示例**
```json
{
  "op_type": "adjust_budget",
  "params": { "mode": "percent", "value": -5, "reason": "Rule-001 消耗过速，建议小幅降预算观察" },
  "guardrails": { "min_budget": 500, "max_change_pct": 10, "min_budget_multiple": 10 }
}
```

### 3.7 ai_decisions —— AI 决策日志
```sql
CREATE TABLE IF NOT EXISTS ai_decisions (
  id             INTEGER PRIMARY KEY AUTOINCREMENT,
  account_id     TEXT NOT NULL DEFAULT '',
  campaign_id    TEXT NOT NULL DEFAULT '',
  rule_id        INTEGER,
  decision_type  TEXT NOT NULL,             -- suggest/skip；当前不生成 execute
  reasoning      TEXT NOT NULL DEFAULT '',
  confidence     REAL NOT NULL DEFAULT 0,
  proposed_op_id INTEGER,
  executed_op_id INTEGER,
  context_json   TEXT,                      -- 触发时的快照上下文（供复盘）
  model          TEXT NOT NULL DEFAULT '',
  cost_estimate  REAL NOT NULL DEFAULT 0,
  created_at     TEXT NOT NULL DEFAULT (datetime('now','localtime'))
);
```

---


## 四、采集器设计（oec-collector）

### 4.1 职责
- 每 5min 遍历所有账户（S7 前为单账户），经 `oec-router` 抓取：计划列表、账户级统计、直播状态、预算/出价。
- 写入：`campaigns` upsert + `snapshots` upsert + `shifts` 增量 + 直播状态（`live_state` 表或 state JSON）。
- 采集原始报文 JSON 落 `monitor-data/snapshots/`（灾备回放，可 `backfill.mjs` 从文件回灌 DB）。

### 4.2 健壮性
- 单账户失败不阻塞其他账户；整体失败重试 3 次后熔断并告警（复用 `oec-router` 熔断）。
- 数据新鲜度检查：`monitor-data/state/health.json` 记录 `lastSnapshotTime`，超过 15min 未更新 → 飞书告警。
- 采集与汇报进程解耦后，汇报进程即使短暂失败也不影响数据完整性。

### 4.3 与现有模块的关系
| 现脚本 | 去向 |
|---|---|
| `oceanengine-monitor-v3.mjs`（采集部分） | 拆入 `oec-collector` |
| `oceanengine-5min-check.mjs`（采集部分） | 拆入 `oec-collector` |
| `live-watcher.mjs` | 并入 `live-pusher`（60s 直播状态轮询，不并入 5min 采集器） |
| `fetch-*.mjs` | 收敛为 `oec-collector` 的可选任务或归档 |
| 卡片构建/分析/推送 | 留在 `domain/` + 各汇报进程，不再进采集器 |

---


## 五、汇报层改造（全部改读库）

| 汇报 | 现数据源 | 目标数据源 |
|---|---|---|
| 15min 卡片 | 采集进程内实时计算 | `snapshots`（3×5min 聚合）+ `campaigns` |
| 5min 速报卡 | 采集进程内实时计算 | `snapshots`（最新窗口） |
| 日汇总 23:35 | `daily-*.json` + `shifts-*.json` | `daily_stats` / `hourly_stats` / `shifts` |
| 日报 23:05 | `daily-*.json` | `daily_stats` + `hourly_stats` |
| Dashboard | DB（部分）+ JSON | 全部 DAL |
| AI 诊断 | 快照 JSON 文件 + `_multiDay` | `snapshots` + `hourly_stats`，基线 SQL 聚合 |
| 周报 PPT | 临时抓取 | `daily_stats`（保留专用抓取为补充） |

---


## 六、条件触发引擎（trigger-engine）

### 6.1 执行流程
```
采集完成事件驱动（每5min，采集完成 → 触发，避免与采集器竞态；若采集失败则跳过本次触发）
  → trigger-engine.run(ctx, dbApi)
    1. SELECT * FROM trigger_rules WHERE enabled=1
    2. 按 scope 加载候选对象（账户/计划/直播间）
     3. 逐规则求值 condition（读 DB 最近 N 窗口）
     4. 命中且通过冷却/日上限/互斥检查：
       - 只写 ai_decisions(decision_type='suggest', confidence)
       - 不写 operations；operations 仅由人工指令创建
    5. 按 approval_mode 分派：
       - suggest → 推飞书建议卡片并同步 Dashboard，人工可选择处理或忽略
    6. 执行完成后 effect-tracker 计算 operation_effects
    7. 每日/每周回写 trigger_rules.success_rate / sample_count
```

### 6.2 安全边界（防失控）
- **短期边界（v1.3）**：`approval_mode` 仅允许 `suggest`/`disabled`；AI 只写 `ai_decisions(decision_type='suggest')`，不创建可执行 `operations`；所有调整必须由人工在飞书/Dashboard 下达。
- **学习期保护**：`learning_done=0` 或推断状态未人工确认时，不生成调优建议。
- **样本量保护**：`min_conversions/min_leads/min_cost` 不足时不建议，只记录“样本不足”。
- **新计划提醒**：不新增赔付状态字段，不以赔付为调优方向；新建 4 天内的计划在生成建议或人工确认卡片时提示“调整可能影响赔付保障”。
- **冷却**：同 `(account, campaign, rule)` 在 `cooldown_minutes` 内不重复触发。
- **日上限**：`max_daily_ops` 每规则 + 全局 `MAX_DAILY_OPS_TOTAL`（config 表，默认 10）。
- **互斥**：同 campaign 存在 `pending/executing` 未决操作时跳过新触发。
- **参数护栏**：`action_template.guardrails` 硬校验（min_budget / max_change_pct），超限拒绝并告警。
- **熔断**：同规则连续 3 次 `failed` 或效果 `negative` 占比 > 60% → 自动 `enabled=0` + 飞书告警。
- **全量审计**：AI 建议与人工执行分别留痕；人工操作仍复用现有 rollback。

### 6.3 规则效果回写
- 只统计人工最终执行成功、afterValue 回读完整、且 15/30/60/120min 窗口数据完整的操作；拒绝、超时、执行失败或窗口不完整不计入。
- 每操作执行满 120min 后，按 `operation_effects` 的 `eval_label` 汇总：
  `success_rate = (high_positive + positive) / total`
- 低于 `confidence_floor` 的规则降权（`enabled` 保留但排序靠后），由人工复核。

### 6.4 短期 AI 执行链路（人工闭环）

1. **AI 建议**：采集完成后，trigger-engine 只生成 `ai_decisions(decision_type='suggest')`，不创建 `operations`。
2. **人工下达**：用户在飞书群或 Dashboard 手动输入调整指令。
3. **预检查**：`feishu-listener` 校验计划状态、当日重复；重复指令进入 3 分钟 pending 二次确认。
4. **入队**：D2 起直接写入 SQLite `operations` 持久化队列，停止使用 `action-queue.json`，保持串行语义。
5. **执行**：`action-queue-worker` 单实例锁，先回读 `beforeValue`；HTTP API 重试 3 次，失败后 CDP 降级 2 次。
6. **审计**：回读 `afterValue`，写 `action-audit.jsonl` + `actions` 表（重构后写 `operations` + `operation_effects`）。
7. **学习**：`effect-tracker` 只对有效完整样本按 15/30/60/120min 计算效果并回写规则置信度，仅用于后续建议校准。

---


## 七、分阶段实施（并入主重构路线图）

| 阶段 | 交付 | 验收标准 |
|---|---|---|
| D0 数据模型（1-2 天） | schema 迁移：新表 + account_id 列；snapshots 增加 external_action/conversion_depth/is_lead_source；operations 兼容 actions（视图或双写） | 迁移脚本幂等可回滚；`npm run check` 通过 |
| D1 采集器独立（3-5 天，依赖 S3 工具链 + S4 拆分结果，将拆分后的采集职责组合为 oec-collector 独立进程） | `oec-collector` 上线 5min 粒度；下播后 4min 补采；卡片/汇总改读库（新旧并行 diff） | 卡片与 DB 数据一致；JSON 不再被消费方读取 |
| D2 操作闭环入库（2-3 天） | `operations`/`operation_effects` 替换文件队列与 audit；effect-tracker 上线 | 队列/审计文件停写；Dashboard 审计 Tab 读库 |
| D3 规则引擎（3-4 天） | `trigger_rules` + trigger-engine，先 `suggest` 模式（Rule-001 试点） | 条件命中生成 AI 建议；人工下达后按现有链路执行 |
| D4 AI 学习与建议校准（1-2 周） | 基于人工执行结果回写规则置信度；暂不开放 `auto` | AI 不直接执行；所有 operations 的 source 均来自人工 |

依赖关系：D1 依赖抓取重构 P0-P2；D2 依赖 S2（数据库统一）；D3 依赖 D1+D2；D4 依赖 D3 + 多租户可选。

---


## 八、数据生命周期与保留策略

> 5min 高频化后数据量约为 15min 时代的 3 倍（实测 ~4000 行/天），必须配套保留策略，防止 DB 无限膨胀。

### 8.1 保留策略（按表）

| 表 | 保留期 | 策略 | 理由 |
|---|---|---|---|
| `snapshots`（快照明细） | **90 天** | 超期按日归档/清理：先导出归档（可选 `VACUUM INTO` 或 JSON 备份）→ 分批 DELETE；每月一次 `VACUUM` 回收空间 | 明细数据量大，90 天覆盖规则基线与效果追踪需要 |
| `hourly_stats` / `daily_stats`（物化聚合） | **长期保留** | 不清理 | 体积小（~240+15 行/天），是历史分析/基线的数据源 |
| `daily_summaries` / `shifts` | 长期保留 | 不清理 | 汇报与排班历史 |
| `operations` / `operation_effects` | **永久保留** | 不清理 | 审计要求（操作留痕、效果可回溯） |
| `ai_decisions` | 永久保留 | 不清理 | 审计与规则复盘 |
| `alerts` / `feedback` | 1 年 | 超期归档后清理 | 告警复盘窗口 |
| `campaigns` | 长期保留 | 保留当前值 | 计划主数据 |
| `telemetry` | 90 天 | 超期清理 | 运维诊断 |
| 原始报文 JSON（`monitor-data/snapshots/`） | 30 天 | 超期清理 | 灾备回放窗口 |

### 8.2 实现机制

- 新增 `scripts/purge-data.mjs`：按日 cron（每日 04:00 低峰）执行，保留期从 `config` 表读取（`key=data_retention_*`），可配置覆盖。
- 删除策略：分批 `DELETE ... WHERE snapshot_time < ?`（每次 5000 行 + 事务），避免长事务锁库与 WAL 膨胀；每月一次 `VACUUM`。
- 归档（可选）：超期 `snapshots` 先导出为 `monitor-data/archive/snapshots-YYYY-MM.parquet|jsonl`，确认归档成功后再删除。
- 保留期变更：改 `config` 表即可，无需改代码。

### 8.3 量级估算（90 天窗口）

- `snapshots` ≈ 4000 行/天 × 90 ≈ 36 万行；DB 规模控制在 ~120-150MB（当前 41MB / 3.2 万行）。
- 多租户后按账户数线性增长，保留期可按账户独立配置（`account_id` 维度）。

---



---


## 九、与《完整重构方案》的衔接

- 本设计是主重构方案的**核心深化**：目标架构中的 `domain/`、`db/`、`services/` 均按本设计落地。
- 主重构方案路线图更新：S2 后插入 D0-D2；S4 后插入 D3；D4 与 S7（多租户）并行或其后。
- 现有四层架构（审计/确认/熔断/回滚）全部保留，语义映射到 `operations` 状态机与 trigger-engine 安全边界。


# 第四部分 抓取链路重构（P 系列）


# 前端数据抓取重构方案

> 日期：2026-08-01
> 状态：方案设计（待评审，含 2026-08-01 决策更新）
>
> ⚠️ **决策更新（2026-08-01）**：取消「CDP DOM 抓取 / CDP 抓包」两条生产降级路径。
> - 生产抓取仅保留 **HTTP API 直连** 唯一路径；
> - 失败处理改为 **每分钟重试（窗口内最多 5 次）→ 飞书群告警（含失败原因/受影响账户）→ 持续重试 + 恢复通知**；401/403 先自动刷新 Cookie 再重试；
> - CDP 仅保留三处非抓取用途：**Cookie 提取/刷新、写操作执行、逆向工具（发现新端点）**；
> - 本文档中所有「DOM 降级」「抓包兜底」「直播分析页 DOM 备选源」内容均作废，以本节为准。
> 范围：巨量引擎前端数据抓取链路 —— HTTP API 直连 / CDP DOM 抓取 / CDP 抓包逆向 三路径收敛

---

## 一、背景与目标

### 1.1 现状

巨量引擎没有开放官方数据 API，本项目通过三种方式从 Web 前端获取数据：

| 路径 | 原理 | 代表文件 | 状态 |
|------|------|---------|------|
| A. HTTP API 直连 | 浏览器提取 Cookie → Node 直调内部 JSON API | `oceanengine-api-client.mjs`、`ai-regions-http.mjs` | 主链路（15min/5min 监控、日汇总、直播监听） |
| B. CDP DOM 抓取 | Chrome CDP 连接 → 页面校准 → 解析表格 DOM | `oceanengine-monitor-v3.mjs`(scrapeOnePage)、`calibrate-page.mjs`、`fetch-ai-regions.mjs`(proxy 3456) | 历史主链路，现为降级/一次性任务 |
| C. CDP 抓包逆向 | Network 域监听 → 捕获请求/响应 → 反推新端点 | `oec-capture-status-api.mjs`、`capture-ai-regions-api.mjs`、`oec-reverse-*.mjs` | 逆向工具，未产品化 |

关键前提（已在《按需加载方案分析与优化》确认）：**数据采集全程可走 HTTP API，Chrome/CDP 仅在 Cookie 刷新时被需要（占用 0.2%），写操作（暂停/调预算）仍需 CDP。**

### 1.2 重构目标

1. **收敛**：生产抓取收敛为「HTTP API 直连唯一路径」；CDP 抓包仅作逆向工具；DOM 抓取废弃（相关代码归档）。
2. **去重**：消除 Cookie 提取、HTTP 请求、CDP WebSocket 三处以上重复实现。
3. **抗变更**：消灭 DOM 列索引硬编码与字段解析散落，页面改版不静默出错。
4. **可运维**：抓取失败有统一重试/熔断/降级策略与结构化日志。
5. **可扩展**：新端点 = 在注册表加一条配置，不再复制粘贴脚本。

---

## 二、现状盘点与痛点诊断

### 2.1 重复实现清单（实测）

| 重复点 | 出现文件 | 危害 |
|--------|---------|------|
| `Network.getCookies` 提取 | `oceanengine-api-client.mjs`、`oec-auto-login.mjs`、`oec-internal-api-discovery.mjs` | Cookie 策略改动需多处同步 |
| `new WebSocket` + wsSend 封装 | `cdp-client.mjs`(统一版) + `capture-ai-regions-api.mjs`、`oceanengine-api-client.mjs`、`oec-capture-status-api.mjs`、`oec-reverse-*.mjs`(3个) 共 7 处 | 超时/心跳/重连策略各写各的 |
| `https.request` httpPost 封装 | `ai-regions-http.mjs`、`fetch-video-account-cost.mjs`、`fetch-short-video-cost.mjs`、`fetch-ai-regions-time-range.mjs`、`oceanengine-ai-regions.mjs`、`oceanengine-daily-summary.mjs` 等 8 处 | 修复（如 setEncoding utf8、千分位）需逐文件打补丁 |
| DOM 列索引硬编码 | `scrapeOnePage`（data[7]~data[21]）、`fetch-ai-regions.mjs`（cells[0]~cells[3]） | 页面加列/换列即静默错位 |
| 数值解析 | 各脚本各自 `parseFloat(x.replace(/,/g,''))` | 已发生重大事故：5 区直播消耗 ¥9459 被解析成 ¥7（`ai-regions-http.mjs`） |
| 账户/端点配置 | aadvid 硬编码：`1842681352509635`、`1852666142648332`、`AI_REGIONS` 5 账户 | 新账户接入成本高 |
| 失败处理 | 各脚本独立 try/catch + 空返回 | 无统一熔断与告警口径 |

### 2.2 结构性痛点

- **无路由策略**：同一数据（计划列表）存在 API 与 DOM 两套抓法，但没有"API 优先、失败降级 DOM"的编排层。
- **逆向成果未沉淀**：`oec-capture-*` 抓到的端点（如 `statistics_sophonx/statQuery`）靠人工拷贝到业务脚本，无版本管理。
- **40102 未突破**：`projects/get`、`adgroups/list`、`live_room/list` 三个详情端点需要页面 HTML 内注入的 CSRFToken（与 Cookie 中同名不同值），HTTP 直连必被拒（详见 `40102端点逆向思路.md`）。
- **一次性脚本游离体系外**：`fetch-*.mjs` 不在 PM2 编排内，无健康检查、无日志归口。

---

## 三、目标架构

```
┌─────────────────────────────────────────────────────┐
│                 编排层 oec-fetch.mjs                  │
│   HTTP API 直连 · 失败每分钟重试 → 飞书群告警        │
└─────────────────────────────────────────────────────┘
        │                     │                    │
┌───────▼───────┐   ┌─────────▼─────────┐   ┌──────▼───────┐
│  会话层        │   │  传输层            │   │  数据层       │
│ oec-session   │   │ oec-http /        │   │ oec-normalize│
│  Cookie统一    │   │ oec-cdp /        │   │ 字段映射      │
│  提取/缓存/刷新 │   │ oec-capture      │   │ 数值解析/校验 │
└───────┬───────┘   └─────────┬─────────┘   └──────┬───────┘
        │                     │                    │
        └──────────┬──────────┴────────────────────┘
                   │
        ┌──────────▼───────────┐
        │ 端点注册表 oec-endpoints│
        │ url / method / body   │
        │ fields / 重试参数 / 校验  │
        └──────────────────────┘
```

### 3.1 分层职责

| 层 | 模块（新建） | 职责 | 消费方 |
|----|-------------|------|--------|
| 会话层 | `oec-session.mjs` | Cookie 单点提取（CDP Network.getCookies）、2h 缓存、过期检测、自动登录兜底、401 失效清理 | 所有 HTTP 抓取 |
| 传输层 | `oec-http.mjs` | 统一 `httpPost/httpGet`：utf8 解码、超时、重试、限频日志、非 ASCII Cookie 清洗 | 所有 HTTP 抓取 |
| 传输层 | `oec-cdp.mjs` | 薄封装 `cdp-client.mjs`：Tab 发现、evaluate、真实鼠标事件 | 写操作（Cookie 刷新/执行，非抓取） |
| 传输层 | `oec-capture.mjs` | 通用抓包：新开 Tab → Network.enable → requestWillBeSent/loadingFinished → getResponseBody | 逆向、40102 突破 |
| 接口层 | `oec-endpoints.mjs` | 端点注册表 + body 构造器 + 字段定义 | 所有业务脚本 |
| 数据层 | `oec-normalize.mjs` | 统一数字解析（千分位）、字段名归一、schema 校验、列头映射 | DOM 与 API 共用 |
| 编排层 | `oec-fetch.mjs` | HTTP API 直连、每分钟重试（窗口内 N 次）、飞书群告警、恢复通知 | 监控主链路 |

### 3.2 端点注册表设计（核心）

```js
// oec-endpoints.mjs
export const ENDPOINTS = {
  projectsList: {
    method: 'POST',
    url: (cfg) => `${cfg.base}/ad/api/promotion/projects/list?aadvid=${cfg.aadvid}`,
    body: (ctx) => ({ st: ctx.date, et: ctx.date, ..., isSophonx: 1 }),
    fields: ['project_id', 'project_name', 'stat_cost', 'convert_cnt', ...],  // 对照 API字段全量档案.md
    normalize: 'projectRow',          // 复用 oec-normalize 的映射
    retry: { intervalMs: 60000, maxAttempts: 5, alertAfter: 5 },   // 失败重试 + 告警
  },
  statQuery: { ... },                 // 报表类，AI区域/短视频共用
  dashboardStats: { ... },
  liveRoomList: {                     // 40102：标记 specialAuth，缺失时记录缺失标记 + 告警（无 DOM 备选源）
    auth: 'pageToken',
    fallback: 'liveAnalyzePage',
  },
};
```

收益：新增账户只改配置（`monitor-utils.mjs` 的账户注册表）；新增端点只加一条定义；字段变更只改 normalize 映射。

---

## 四、具体改造项

### 4.1 会话层收敛（P0，先行）

- 从 `oceanengine-api-client.mjs` 抽出 `extractCookiesFromBrowser` + Cookie 缓存逻辑 → `oec-session.mjs`，导出 `getCookieData()` / `invalidateCookie()` / `refreshIfExpired()`。
- `oec-auto-login.mjs`、`oec-internal-api-discovery.mjs` 改为调用统一实现。
- 新增行为：HTTP 返回 401/403 时自动作废缓存并触发一次刷新（当前只按 2h TTL 过期，会话提前失效会静默空数据）。

### 4.2 传输层收敛（P1）

- `oec-http.mjs`：合并 8 处 `httpPost`，固定 `res.setEncoding('utf8')`、15s 超时、3 次重试、`failLogOnce` 限频。
- `oec-capture.mjs`：把 `capture-ai-regions-api.mjs` / `oec-capture-status-api.mjs` 的监听骨架通用化，参数化为 `{ urlFilter, onRequest, onResponse, maxWaitMs }`。
- 迁移顺序：`ai-regions-http.mjs` → `fetch-video-account-cost.mjs` → `fetch-short-video-cost.mjs` → `oceanengine-daily-summary.mjs`。

### 4.3 数据层收敛（P2）

- `oec-normalize.mjs` 提供 `parseNum(str)`（去千分位+空值+NaN 防护）、`mapRow(columns, schema)`（按列头名而非索引取值）、`validate(record, schema)`。
- 从 `API字段全量档案.md` 反推字段映射表作为 schema 基线，仅 HTTP API 单一路径输出，下游 `analyzeData` 无感知。
- **学习期字段映射**：API `learning_status` → `learningStatus`（learning/learned/learning_failed/not_learning）+ `learningDone`（是否完成，learned/learning_failed/not_learning=true，learning=false）；API 未提供时由 `oec-normalize` 标记 `unknown`/`inferred=true`，需人工确认，不按行业阈值推断。
- DOM 列头映射思路仅作为 API 字段校验参考；生产抓取不启用 DOM 列索引路径。

### 4.4 DOM 抓取废弃与归档（P2）

> 决策更新：DOM 不再作为生产抓取路径。`scrapeOnePage` / `calibrate-page.mjs` / `fetch-ai-regions.mjs` 相关代码移入 `archive/`，保留「列头映射 + 结构化告警」思路仅作 API 字段校验参考。

- 从 `oceanengine-monitor-v3.mjs` 抽出的 `oec-dom-scraper.mjs` 仅作为归档/离线研究工具，不注册为生产模块。
- `monitor-v3` 不调用 DOM 模块；该决策已在 v1.3 确认，DOM 不进入生产链路，也不再作为灰度对比路径。

### 4.5 编排层（P3）

- `oec-fetch.mjs` 提供 `fetchWithRetry(endpoint, ctx)`：HTTP API 失败 → 每分钟重试（窗口内最多 5 次）→ 连续失败触发飞书群告警（失败原因/受影响账户）→ 告警后维持每分钟重试，恢复后发送恢复通知。
- 主链路（15min/5min）先接入，验证后再推广到日汇总。

### 4.6 逆向能力沉淀 + 40102 突破（P4，可选）

- 将 `oec-capture-*` 捕获的端点（statQuery 等）登记进注册表，替代手工拷贝。
- 40102 三端点按《40102端点逆向思路.md》方案 4.1「CDP 主动触发 + 抓包」落地：程序化点击详情入口 → 拦截携带页面 CSRFToken 的请求 → 沉淀为注册表中的 `auth:'pageToken'` 端点。
- 直播分析页（数据页 → 直播分析）`live_room/list` 不再作为 DOM 降级源接入；端点缺失时记录缺失标记并告警。

---

## 五、分阶段实施计划

| 阶段 | 内容 | 涉及文件 | 验收标准 |
|------|------|---------|---------|
| P0 | 会话层收敛 | 新建 `oec-session.mjs`；改 `oceanengine-api-client.mjs`、`oec-auto-login.mjs` | 15min/5min 监控 24h 无回归；Cookie 失效可自动刷新 |
| P1 | 传输层收敛 | 新建 `oec-http.mjs`、`oec-capture.mjs`；迁移 4 个 fetch 脚本 | 8 处 httpPost 归零；ai-regions 21:30 推送数据与重构前一致（对照历史快照） |
| P2 | 数据层 + DOM 归档 | 新建 `oec-normalize.mjs`、`oec-fetch.mjs`；DOM 代码移入 `archive/` | 抓取结果与历史快照 diff 一致；失败告警生效 |
| P3 | 编排层 | 新建 `oec-retry.mjs`；接入主链路 | API 失败每分钟重试 + 飞书群告警 + 恢复通知正常 |
| P4 | 逆向沉淀 + 40102 | 新建端点注册条目；`oec-capture` 主动触发 | statQuery 端点登记；直播分析页数据源接入 |

每个阶段**先并行灰度（新模块与旧路径同时跑、比对结果），再切换默认路径**，避免一次性大重构风险。

---

## 六、风险与决策

| 风险 | 等级 | 缓解 |
|------|:---:|------|
| 主链路回归（15min/5min 卡片断更） | 高 | P0~P2 保持旧路径并行，diff 一致后再切换；PM2 逐进程回滚 |
| API 字段变更导致解析错位 | 高 | 统一 parseNum + schema 校验 + 字段变更结构化告警 |
| Cookie 频繁刷新触发验证码/风控 | 中 | 2h TTL 保持；401 主动刷新加冷却（30 分钟内最多 1 次）；CAPTCHA_REQUIRED 告警人工介入 |
| 40102 端点突破失败 | 低 | 仅 HTTP API 抓取；缺失端点记录缺失标记 + 飞书告警，不阻塞主线 |
| 重构期间一次性脚本行为漂移 | 中 | 迁移前后各留一份输出快照比对；旧脚本保留至下个版本再归档 |

### 决策记录

| 日期 | 决策 | 理由 |
|------|------|------|
| 2026-08-01 | 采用「API 为主 + 注册表 + 分层」架构 | 主链路已验证纯 HTTP 可行，Chrome 仅需 0.2% 时间 |
| 2026-08-01 | 取消 DOM/CDP 抓取降级，改飞书告警 + 每分钟重试 | 无固定公网环境 + 简化链路，CDP 仅保留 Cookie/写操作/逆向工具 |
| 2026-08-01 | 分 5 阶段、逐阶段灰度 | 监控系统实时性要求高，禁止一次性大重构 |

---

## 七、相关文档

- `API字段全量档案.md` — 已逆向 6 端点 130+ 字段，作 schema 基线
- `40102端点逆向思路.md` — 3 个详情端点根因与突破路径
- `docs/archive/按需加载方案分析与优化.md` — Chrome 按需加载与 Cookie 刷新占比分析（已归档）
- `工程化优化方案_20260725.md` — 工程化基线


---

# 第五部分 运行逻辑与运维手册


## 第十六章 重构后运行逻辑说明

### 16.1 一句话总结

**采集与消费彻底解耦：一个 `oec-collector` 只负责抓数据写库，所有汇报/监控全部从 SQLite 数据中枢读数据，AI 只生成建议，人工下达后执行，形成「规则 → 建议 → 人工执行 → 效果 → 规则」闭环。**

### 16.2 运行架构总览

```
采集层（只写）           数据中枢（唯一事实源）          消费层（只读）
oec-collector (5min)  →  SQLite                   →  oec-scheduler（卡片/日报/日汇总）
  └─ oec-fetch           campaigns/snapshots        →  live-pusher（换班/直播）
      HTTP API 直连       hourly_stats/daily_stats   →  feishu-bus（群监听+执行）
      失败→每分钟重试      shifts/operations          →  http-server（Dashboard 8899）
      飞书群告警          operation_effects          →  oec-collector 内联 trigger-engine（建议）
                          trigger_rules/ai_decisions
```

### 16.3 四个核心链路

1. **采集链路**：`oec-collector` 每 5min 醒来 → `oec-fetch` HTTP API 直连（唯一路径）→ 失败每分钟重试（窗口内 5 次）→ 飞书群告警 → 恢复通知；成功则 upsert 写库（幂等）；采集完成事件驱动 trigger-engine。
2. **汇报链路**：5min 速报卡（最新窗口）、15min 完整卡（3×5min 聚合，不足标 partial）、日汇总/日报（物化视图）、Dashboard（全 DAL 读库）——汇报进程完全不感知数据来源。
3. **操作链路**：飞书指令/Dashboard → 预检查/二次确认 → `operations` 状态机（pending→confirmed→executing→succeeded/failed→rolled_back）→ 执行（HTTP API 主/CDP 备）→ 回读 before/after → `operation_effects` 多窗口追踪。
4. **AI 建议链路**：`trigger_rules` 条件求值（采集完成后）→ 学习期/样本量过滤 → 冷却/互斥/日上限检查 → 只生成 `ai_decisions(suggest)` → 人工下达后按操作链路执行 → 效果回写 `success_rate`。

### 16.4 进程视角（12 → 6）

| 进程 | 职责 |
|---|---|
| `oec-collector` | 采集 + 写库 + trigger-engine 规则建议 |
| `oec-scheduler` | 定时汇报/排班同步/数据清理 |
| `live-pusher` | 换班推送 + 直播状态（合并 shift-pusher/live-watcher） |
| `feishu-bus` | 群监听 + 操作执行（合并 listener/worker） |
| `http-server` | Dashboard + REST API（8899） |
| `chrome-guard` | Chrome 9222 守护 |

### 16.5 一天的时间线

```
00:00-开播前   非排班时段 = 低活跃期：30min 低频或暂停（排班驱动判定）
排班时段内     每 5min 采集（平台存在 3-5min 延迟，仅作实时参考）→ 写库 → 规则求值
               5min 速报 + 15min 完整卡 → 监控群；AI 建议/告警 → 飞书卡片 + Dashboard
               直播开播/下播由 live-pusher 实时感知；下播后等待 4min 补采账户数据，再触发监控日报 → 监控群
21:30          AI 区域号汇总（读库 → 汇报群，5 大区）
23:00          排班同步 + 当日排班/换班校对（sync-shifts，飞书报告 → 汇报群）
23:05          日报兜底（下播未触发时生成 → 监控群）
23:35          日汇总（主号直播间日汇总 → 汇报群）
任意时刻       群指令（监控群 / 汇报群均可对话） / Dashboard / AI 建议 → 统一任务队列按序处理 → operations 闭环
04:00          数据清理（purge-data：snapshots 90 天等）
```

### 16.6 与现状的关键差异

| 维度 | 现状 | 重构后 |
|---|---|---|
| 数据源 | JSON 文件 | SQLite 唯一事实源 |
| 进程 | 12 个 | ≤6 个 |
| 操作存储 | 文件队列+audit+actions | operations + effects 单表闭环 |
| 规则 | 静态 md | DB 配置化 + 效果回写 |
| AI 调优 | 无 | AI 仅建议，人工执行（短期） |
| 抓取降级 | API→CDP→DOM | 仅 HTTP API + 重试/告警 |

## 第十七章 远程访问手册

> 决策（2026-08-01）：不采用固定域名/固化地址方案（无固定公网域名环境条件），远程通道维持 CodeBuddy 网关 + cloudflared trycloudflare 动态隧道 + 地址变更飞书通知。

### 17.1 通道构成

| 组件 | 说明 |
|---|---|
| CodeBuddy 远程网关 | `settings.json` → `gateway: { auth: "password" }`，密码保护 Web UI |
| cloudflared 隧道 | trycloudflare 快速隧道，本地 CodeBuddy Web 服务端口（如 59687/61278）→ 公网 `https://<随机>.trycloudflare.com/?password=***` |
| 状态文件 | `C:\Users\HTF2026\.codebuddy\tunnel-state.json`（url / localPort / cliPid） |
| 地址通知 | `E:\炼丹炉\WorkBuddy\.codebuddy\check_gateway_hook.sh`（CodeBuddy Stop 事件）→ `send_gateway_to_feishu.py` 推飞书私聊 → `.gateway_last_url.txt` 去重 |
| 地址记录 | `E:\炼丹炉\WorkBuddy\网关地址.txt`（Web UI / Tunnel / 本地三地址） |

### 17.2 获取最新访问地址

1. **飞书私聊**：CodeBuddy 隧道地址每次变化会自动推送（消息含 Web UI / 本地地址）。
2. **状态文件**：`C:\Users\HTF2026\.codebuddy\tunnel-state.json` 读取 `tunnels.<port>.url`。
3. **本地记录**：`E:\炼丹炉\WorkBuddy\网关地址.txt`。

### 17.3 访问 Web UI / webCLI

- 浏览器打开 `https://<随机>.trycloudflare.com/?password=<密码>` 即可远程访问 CodeBuddy Web 界面。
- webCLI 用于远程执行运维命令：`pm2 status`、查看日志、重启进程等（须在有授权的会话内执行）。

### 17.4 访问监控 Dashboard

- 带通行证：`https://<隧道域名>/dashboard-v2?password=<URL编码密码>`（`feedback-server` 8899 的 `/dashboard-v2`）。
- 无密钥：不开放。Dashboard 走隧道时必须带授权密码，且仅向指定人员提供。
- 本地：`http://192.168.1.5:<端口>/dashboard-v2`。

### 17.5 运维操作

| 操作 | 命令/位置 |
|---|---|
| 查看进程 | `pm2 status`（远程 webCLI 或本机） |
| 主日志 | `tail -f monitor-data/monitor.log` |
| 变更日志 | `Select-String "[CHANGE]" monitor-data/monitor.log` |
| 重启核心监控 | `pm2 restart pm2-15min pm2-5min` |
| 隧道进程检查 | `Get-Process cloudflared` / `tunnel-state.json` |
| 重建原生模块 | `C:/Users/HTF2026/.workbuddy/binaries/node/versions/22.22.2/npm.cmd rebuild better-sqlite3` |

### 17.6 故障排查

| 现象 | 排查 |
|---|---|
| 隧道 URL 失效 | cloudflared 进程是否存活；重新触发 CodeBuddy 网关获取新地址 |
| 地址未推送 | `.gateway_hook.log` / `.gateway_last_url.txt` 是否更新；`send_gateway_to_feishu.py` 是否报错 |
| Dashboard 打不开 | `feedback-server`（8899）是否在线；隧道是否指向正确本地端口 |

### 17.7 安全注意

- 密码定期轮换：`settings.json` → `gateway.password`，并同步更新飞书通知与文档记录。
- 监控 Dashboard 走隧道时强制密码 + CSRF（`csrf-utils.mjs` 已具备）。
- 密码 URL 为临时方案，仅向指定人员提供；不开放无密钥路径；`网关地址.txt` 注意访问权限，后续迁移到 session/一次性 token。

## 第十八章 统一日志规范

> 目标：项目运行日志与 agent 修改日志**统一聚合到一个日志文件**，任何 agent 对项目内容的更改都留痕（原因 / 方法 / 结果）。

### 18.1 日志文件

| 文件 | 说明 |
|---|---|
| `monitor-data/monitor.log` | 纯文本聚合入口（运行 + 变更，按日轮转） |
| `monitor-data/logs/monitor-YYYY-MM-DD.ndjson` | 结构化 JSON 行（按日轮转，`LOG_RETENTION_DAYS` 控制保留，默认 14 天） |

运行日志由 `logger.mjs`（`createLogger` / `installConsoleInterceptor`）自动写入，无需人工干预；变更日志由 agent 手动追加。

### 18.2 变更日志记录规范（强制）

任何 agent（Codex / CodeBuddy / WorkBuddy 等）对项目内容（代码 / 文档 / 配置 / 数据库 / 脚本）更改后，**必须**执行：

```
node scripts/log-change.mjs --agent <agent名> --reason "<调试原因>" --method "<执行方法>" --files "<变更文件，逗号分隔>" --result <done|partial|failed> [--tag <标签>]
```

- 必填：`--reason`（为什么改）、`--method`（怎么改的）、`--result`（done 完成 / partial 部分完成 / failed 失败）
- 建议：`--files` 列出变更文件；`--agent` 标注执行 agent（默认 unknown）
- 校验：缺失必填参数时报错并提示用法

### 18.3 记录格式

```
[CHANGE] agent=codebuddy tag=v1.1 原因:修复XXX 方法:修改YYY 文件:a.mjs,b.mjs 结果:partial
```

- `result` 三态：`done`（完成）/ `partial`（部分完成）/ `failed`（失败未完成）→ 对应日志级别 info / warn / error。
- 结构化 ndjson：`{"ts":"...","level":"warn","module":"change","msg":"[CHANGE] ..."}`。

### 18.4 查询方法

```
Select-String "[CHANGE]" monitor-data/monitor.log                    # 全部变更记录
Select-String "[CHANGE]" monitor-data/monitor.log | Select-String "failed"  # 失败变更
# 按 agent：Select-String "agent=codex" monitor-data/monitor.log
```

### 18.5 落地与演进

- 已实现：`logger.mjs`（`writeChange`）、`scripts/log-change.mjs`（CLI）、AGENTS.md 强制规范。
- 后续（P2）：`scripts/query-log.mjs` 按 agent/结果/时间过滤查询；可选将变更日志同步入 DB（`config`/`telemetry` 扩展）供 Dashboard 展示。

## 第十九章 当日排班同步与换班校对

### 19.1 职责（sync-shifts，每日 23:00，重构后并入 oec-scheduler）

1. **次日排班确认**：读飞书排班表（人工填写为准）次日班次 → 写入 `shifts` 表 `record_type='schedule'`。
2. **当日排班校对**：读飞书排班表当日班次 vs `shifts` 表当日 `record_type='schedule'` 记录 —— 时段/主播变化 → 差异告警。
3. **当日换班数据校对**：`shifts` 表当日 `record_type='schedule'` vs `shift-pusher` 生成的 `record_type='actual'` 实绩记录。
4. **校对报告**：输出 `monitor-data/state/shift-audit-{date}.json` + 飞书推送。

> 排班源表由人工填写为准；源表字段映射待用户提供后补充，并据此填写到日常核对直播排班表。

### 19.2 换班数据校对检查规则

| 检查项 | 规则 | 级别 |
|---|---|---|
| 缺班次 | 排班时段在 `record_type='actual'` 中无对应实绩记录 | warn |
| 多余/漂移 | 实绩记录不在排班时段内 | warn |
| 主播不一致 | 实绩 `actual_anchor_name` ≠ 排班 `planned_anchor_name` | warn |
| 指标异常 | 班次消耗缺失 / CPL 明显偏离（如 > 基线 × 3） | warn |
| 时长异常 | 班次实际时长与排班偏差 > 30min | warn |

### 19.3 校对报告示例

```
📋 排班校对 2026-08-01 23:00
✅ 次日排班已同步：3 个班次（07:00-12:00 张三 / 12:00-19:00 李四 / 19:00-23:00 王五）
📊 当日换班校对：
  ✅ 已完成班次：2/3
  ❌ 12:00-19:00 李四：缺换班记录（排班有，未生成）
  ⚠️ 19:00-23:00 王五：排班主播为"王五"，实际记录为"王五2"（不一致）
```

### 19.4 排班驱动的活跃期判定

活跃期 = 当日排班表覆盖的班次时段集合；**非预定直播排期时段一律记为低活跃期**：

| 当前时间 | 采集频率 |
|---|---|
| 在任一排班时段内 且 有消耗 | 5min 高频 |
| 在任一排班时段内 且 无消耗 | 15min 中频 |
| 非排班时段（无预定直播） | 30min 低频或暂停 |
| 数据陈旧 > 1h | 立即补采一次 |

判定源：`shifts` 表当日班次 → 缓存缺失回退 `getTodayShiftWindow()` 小时范围 → 再回退默认 7-23。

## 第二十章 学习期推断规则

### 20.1 概念与字段

巨量引擎广告计划存在「学习期」：新计划投放后模型需要积累转化样本，学习期结束（成功或失败）后计划才进入稳定投放。

| 字段 | 取值 | 含义 |
|---|---|---|
| `learningStatus` | `learning` | 学习中（转化不稳定） |
| | `learned` | 学习完成（稳定投放） |
| | `learning_failed` | 学习失败（学习期结束但未达标，需关注） |
| | `not_learning` | 无需学习（如老计划复投，视为可正常投放） |
| `learningDone` | `true` | 学习期是否完成：learned / learning_failed / not_learning → true |
| | `false` | learning → false（学习中） |

### 20.2 推断规则（L-001 起）

**L-001 直取**：HTTP API 提供 `learning_status` 字段时直接映射，`inferred=false`。

**L-002 保守处理**（API 未提供时由 `oec-normalize` 执行，`inferred=true`）：

| 信号 | 条件 | 判定 |
|---|---|---|
| API 无状态 | 默认 | `learning_status='unknown'`，`learning_done=0` |
| 新建计划 | `platform_created_at`/`first_seen_at` 距今 < 4 天 | 卡片提示“新建 4 天内，调整可能影响赔付保障”，不据此推断学习完成 |
| 人工确认 | Dashboard/飞书手动标记 | 按人工标记写入 `learning_status`，`inferred=false` |

不将行业经验阈值（如 24h/72h/20/50 转化）作为学习期判定默认值；学习状态以 API 直取和人工确认为准。

### 20.3 推断信号来源

- `first_seen_at` / `platform_created_at`：用于新建计划提醒，不用于自动判定学习完成。
- `conversions`：`snapshots.conversions` 最新值，仅统计深层转化（`conversion_depth='deep'`）。
- 人工标记：Dashboard/飞书可手动确认 `learning_status`，写 `learning_status` 覆盖。

### 20.4 业务用途

| 场景 | 规则 |
|---|---|
| 卡片展示 | `learningStatus=learning` 时卡片加「学习中」标记 |
| 告警 | `learning_failed` 计划触发告警（重点关注，可能需优化素材/出价） |
| **建议边界** | trigger-engine `scope_filter.learning_done=0` 时**不生成调优建议**（学习中转化不稳定，避免误调）；仅诊断告警 |
| 分析 | 按学习状态分组统计（learned 计划的规则效果置信度更高） |

### 20.5 回写与校准

- `operation_effects` 计算时排除 `learning_done=0` 计划（效果不可信）。
- 每周人工抽查推断结果 vs 平台实际学习状态，偏差 > 10% 时调整阈值。


---

## 第二十一章 飞书群分工与推送模板规范

> 新增于 v1.2（2026-08-01）。定义推送双群架构（监控群 / 汇报群）、双群对话能力、以及全部固定推送消息的模板、取数规则与计算逻辑。所有模板输出前统一 `parseNum`（修复 ¥9459→¥7 事故），字段缺失显示 `--` 不推断，推送去重/重试由 `feishu-push-guard` 统一兜底。

### 21.1 群角色与对话能力

| 群 | 群 ID | 角色 | 推送内容 | 对话能力 |
|---|---|---|---|---|
| 监控群 | `oc_8deeb3061bdbd43608de252a44c97a25`（`LARK_MONITOR_CHAT_ID`） | 实时监控与盯盘 | 5min 速报、15min 完整监控卡、下播后监控日报 | ✅ 开启（@机器人下达指令） |
| 汇报群 | `oc_b245ee4b255c7b25b7f8d953802c49ff`（`LARK_REPORT_CHAT_ID`，原「主播群」更名） | 数据汇报与经营复盘 | 主播换班数据、主号直播间日汇总、5 大区日汇总 | ✅ 开启（@机器人下达指令） |

- **对话开启要求**（两个群均需满足）：
  1. 应用可用性：飞书开放平台给机器人授予群消息权限（`im:message` / `im:message.group_at_msg`），机器人加入两个群；
  2. 群设置：允许群成员 @机器人、允许机器人收发消息（开启「群机器人」功能）；
  3. 监听端：`feishu-listener`（重构后并入 `feishu-bus`）同时监听两个群，指令解析共用同一套解析器（暂停/恢复/加预算/调出价/状态/帮助），不按群区分指令语法。
- **指令范围收敛**（可选安全策略）：敏感写操作（调预算/出价）可按群收敛——默认两群均可执行，如需收敛在 `config` 表配置 `cmd_permission.{chat_id}` 白名单；读取类指令（状态/数据查询）两群始终开放。
- **对话上下文**：`buildSystemPrompt` 注入实时账户数据（今日消耗/预算/CPL/线索/余额/告警），两群共用同一数据上下文，确保指令回复有据可查（见《飞书消息模板优化_20260727.md》）。
- **AI 建议与告警**：飞书卡片和 Dashboard 同步展示，人工可从任一处处理或回复；同一计划存在多个任务时按任务队列顺序正常处理。

### 21.2 推送归属总表

| 消息 | 群 | 触发时机 | 数据来源（重构后） | 对应模块 |
|---|---|---|---|---|
| 5min 速报卡 | 监控群 | 活跃期每 5min | `snapshots` 最新窗口 | oec-scheduler（原 oceanengine-5min-check） |
| AI 建议 / 告警 | 监控群 + Dashboard | 规则命中/异常发生时 | `ai_decisions` / `alerts` | oec-collector（trigger-engine） / monitor |
| 15min 完整监控卡 | 监控群 | 活跃期每 15min | `snapshots` 3×5min 窗口聚合 | oec-scheduler（原 oceanengine-monitor-v3） |
| 下播后监控日报 | 监控群 | live-pusher 感知下播后等待 4min 补采账户数据再触发；当日未下播则 23:05 兜底 | `daily_stats` / `hourly_stats` | oec-scheduler（原 daily-report） |
| 主播换班数据 | 汇报群 | 班次结束即时推送 | `shifts` + `snapshots` 班次窗口 | live-pusher（原 oceanengine-shift-pusher） |
| 主号直播间日汇总 | 汇报群 | 23:35（核对后） | `daily_stats` + 直播指标聚合 | oec-scheduler（原 oceanengine-daily-summary） |
| 5 大区日汇总 | 汇报群 | 21:30 | `daily_stats` 按 `account_id` 分组 | oec-scheduler（原 ai-regions-http） |

### 21.3 监控群模板

#### 21.3.1 5min 速报卡（固定模板）

```
⚡ 5min 速报 {HH:mm}
💰 消耗 ¥{spend}（+¥{deltaSpend}）· 线索 {leads}（+{deltaLeads}）
🎯 CPL ¥{cpl} · CPM ¥{cpm} · 预算使用 {budgetPct}%
🚀 速度 ¥{speed}/min · 投放中 {active} 条 · 有消耗 {spending} 条
{高优先级告警 1-2 条，无则省略}
🕐 {timestamp}
```

**取数规则**：读 `snapshots` 表最新一个 5min 窗口（`source_type='api'`，账户级 `page_summary_json` + 计划级结构化列）；delta 为本窗口减上一窗口；`budget` 取当日 `daily_budget`。

**计算逻辑**：

| 变量 | 公式/口径 |
|---|---|
| `spend` | `SUM(snapshots.cost)`（当日累计，账户级） |
| `deltaSpend` | 本窗口 `cost` − 上一窗口 `cost`（跨天归零修复） |
| `leads` | `SUM(msg_lead + form_submit)`（线索口径，见 5.6） |
| `cpl` | `spend / leads` |
| `cpm` | 快照明细 `cpm` 直取（累计，快照无展示数差值） |
| `budgetPct` | `spend / daily_budget × 100` |
| `speed` | `deltaSpend / 5min` |
| `active` / `spending` | 计划级 `status` 统计（投放中 / 有消耗） |

#### 21.3.2 15min 完整监控卡（固定模板）

```
📊 极狐直播 · 消耗 ¥{spend} ({pct}%) · {slot}
█████░░░░░ {timePct}%  (已过{elapsed}h/{window}h)
███░░░░░░░ {budgetPct}%  (¥{spend} / ¥{budget})
📊 {pacing} | {slot}
🎯 预估今日 ¥{projected} | 剩余 {remaining}h
💳 账户余额: ¥{balance} (约{days}天)
──
━ 累计 ━
💰 消耗: ¥{spend} | CPL ¥{cpl}
🎯 转化: {conv}条
📨 开口成本: ¥{openCost} | 开口留资率: {retainRate}%
━ 近{N}分差值 ━
📊 新增消耗: +¥{deltaSpend} | 新增线索: +{deltaLeads}条
📈 CPL: ¥{deltaCpl} | CPM: ¥{cpm} | 停留率: {retain}%
⚡ 速度: ¥{speed}/min | 有消耗 {spending}条 · 投放中 {active}条
──
🔵 节奏提醒
ℹ {info 告警，按 severity 过滤}
──
📊 计划状态: 🔥 活跃 {n} · 💀 疑似死亡 {n}
🔥 起量: {names}
📊 近{N}分钟新增消耗 TOP5
1. ➡ {name} — ¥{spend} (+{rate}%) · {N}mCPL ¥{cpl}
──
📅 同比昨天同时段 ({date})
消耗: ¥{ySpend} → ¥{spend} ({pct}) | CPL: ¥{yCpl} → ¥{cpl} ({pct})
📊 近{N}天同时段
消耗: ¥{avgSpend} → ¥{spend} ({pct}) | CPL: ¥{avgCpl} → ¥{cpl} ({pct})
──
💡 盯盘建议: {advice}
🕐 {timestamp} · {slot}
```

**取数规则**：读 `snapshots` 最近 3 个 5min 窗口聚合（不足 3 窗标 `partial`）；同环比基线读 `hourly_stats` 昨日同时段与近 N 天同时段 mean/stdev；直播间指标（停留率）取计划级 `liveOver1Min / liveViews`；对比基线近 15 天同时段（AI 诊断 Rule-001 节律基线，仅诊断不输出操作建议）。

**计算逻辑**（沿用《CARD-FORMAT.md》口径）：

| 变量 | 公式/口径 |
|---|---|
| `timePct` | `(now - 06:30) / (23:30 - 06:30) × 100` |
| `budgetPct` | `spend / budget × 100` |
| `pacing` | 按预算进度判定（过快/过慢/正常 5 档） |
| `projected` | `spend / timePct`（线性外推） |
| `cpl`（累计） | `spend / leads` |
| `openCost` | `spend / privateMsgOpen` |
| `retainRate` | `privateMsgRetain / privateMsgOpen × 100` |
| `deltaSpend` | `spend − prevWindowSpend`（15min 窗口差） |
| `deltaLeads` | 计划级深层转化/`leads` 差值求和 |
| `deltaCpl` | `deltaSpend / deltaLeads` |
| `cpm` / `retain` | 累计直取（快照无展示/观看差值） |
| `speed` | `deltaSpend / 窗口分钟数` |
| 同比 | 当前窗口 vs `hourly_stats` 昨日/近 N 天同时段（`(cur − base) / base`） |

#### 21.3.3 下播后监控日报（固定模板）

```
📋 监控日报 {date}（下播后）
💰 总消耗 ¥{cost} · 预算使用 {budgetPct}% · 线索 {leads} · 转化 {conversions}
🎯 CPL ¥{cpl} · CPM ¥{cpm} · CTR {ctr}% · CVR {cvr}%
💬 开口 {msgOpen} · 留资 {msgRetain}（留资率 {retainRate}%）
📺 直播：进入 {liveEnter} · 观看 {liveViews} · 1min+ {liveOver1Min} · 评论 {liveComment}
📊 计划表现 TOP5（按消耗；含状态/CPL/转化）
⚠️ 告警汇总：{高/中告警数} 条（类型分布）
🕐 生成时间 {timestamp}
```

**取数规则**：下播事件由 `live-pusher` 感知（60s 轮询 + 下播二次确认）后，等待约 4min 触发一次账户数据补采，再读 `daily_stats`（消耗/线索/转化/CPL/CTR/CPM/CVR）+ `hourly_stats` 汇总直播指标（`liveEnter/liveViews/liveOver1Min/liveComment` 当日累计）；计划表现 TOP5 读 `snapshots` 当日最新窗口按 `cost` 排序；告警读 `alerts` 表当日聚合。若当日未下播，23:05 由 oec-scheduler 兜底生成（模板相同，标题注明「日报」）。

**计算逻辑**：

| 变量 | 公式/口径 |
|---|---|
| `budgetPct` | `cost / daily_budget × 100` |
| `cpl` | `cost / leads`（leads = msg_lead + form_submit） |
| `ctr` / `cvr` / `cpm` | 快照明细直取（当日累计口径） |
| `retainRate` | `msgRetain / msgOpen × 100` |
| 直播停留率 | `liveOver1Min / liveViews × 100` |
| 计划表现 TOP5 | `snapshots` 当日最新窗口 `cost` 降序前 5，附 `status`/`cpl`/`conversions` |

### 21.4 汇报群模板

#### 21.4.1 主播换班数据卡（固定模板）

```
🔄 换班汇报 {date} {时段}
👤 主播：{anchor}（{shiftName} 班次）
💰 班次消耗 ¥{shiftCost} · 线索 {shiftLeads} · CPL ¥{shiftCpl}
📊 消耗速度 ¥{speed}/min · 相对昨日同时段 {pct}%
✅ 排班一致（或 ⚠️ 与排班差异：{diffDetail}）
🕐 {timestamp}
```

**取数规则**：读 `shifts` 表班次记录（主播/时段）+ `snapshots` 班次起止快照；差异项来自 23:00 `sync-shifts` 当日校对结果（`shift-audit-{date}.json`：缺班次/多余/主播不一致/指标异常/时长异常，见第十九章）。

**计算逻辑**：

| 变量 | 公式/口径 |
|---|---|
| `shiftCost` | 班次结束快照 `cost` − 班次开始快照 `cost`（快照缺失回退 `getHourlyStats`） |
| `shiftLeads` | 班次结束快照 `leads` − 开始快照 `leads` |
| `shiftCpl` | `shiftCost / shiftLeads` |
| `speed` | `shiftCost / 班次时长(min)` |
| 同比 | `shifts` 昨日同班次 `shiftCost` 对比 |

#### 21.4.2 主号直播间日汇总卡（固定模板）

```
📅 主号直播间日汇总 {date}
💰 消耗 ¥{cost} · 线索 {leads} · CPL ¥{cpl} · 转化 {conversions}
📺 进入 {liveEnter} · 观看 {liveViews} · 1min+ {liveOver1Min} · 停留率 {retention}%
💬 评论 {liveComment} · 开口 {msgOpen} · 留资率 {retainRate}%
📊 分时段消耗 TOP3 班次：{shift1} ¥{cost1} / {shift2} ¥{cost2} / {shift3} ¥{cost3}
📈 同比昨日：消耗 {pct}% · CPL {cplPct}% · 线索 {convPct}%
🕐 23:35 生成
```

**取数规则**：23:35 全天数据核对后推送；读 `daily_stats`（消耗/线索/CPL/转化）+ 直播指标当日聚合（`liveEnter/liveViews/liveOver1Min/liveComment`）；分时段消耗按 `shifts` 班次切分 `snapshots`；同比读 `daily_stats` 昨日记录。

**计算逻辑**：`cpl = cost / leads`；`retention = liveOver1Min / liveViews × 100`；`retainRate = msgRetain / msgOpen × 100`；同比 `(今日 − 昨日) / 昨日 × 100`；班次消耗算法同 21.4.1。

#### 21.4.3 5 大区日汇总卡（固定模板）

```
🌏 5 大区日汇总 {date}
| 区域 | 消耗 | 线索 | CPL | 转化 | 状态 |
|---|---|---|---|---|---|
| 华东 | ¥{c1} | {l1} | ¥{cpl1} | {cv1} | ✅ |
| 华南 | ¥{c2} | {l2} | ¥{cpl2} | {cv2} | ✅ |
| 华北 | ¥{c3} | {l3} | ¥{cpl3} | {cv3} | ✅ |
| 西南 | ¥{c4} | {l4} | ¥{cpl4} | {cv4} | ✅ |
| 华中 | ¥{c5} | {l5} | ¥{cpl5} | {cv5} | ✅ |
📊 合计：消耗 ¥{total} · 线索 {totalLeads} · CPL ¥{totalCpl}
⚠️ 异常区域：{region 异常告警，无则省略}
🕐 21:30 生成
```

**取数规则**：21:30 读 `daily_stats` 按 `account_id` 分组（5 个区域账户），区域映射存 `config` 表（`region_map.{account_id}=区域名`）；异常判定：CPL 超近 7 天均值 1.5 倍 或 消耗为 0 且排班有直播。

**计算逻辑**：`区域 CPL = 区域消耗 / 区域线索`；`total = Σ各区域`；`totalCpl = totalCost / totalLeads`；状态 `✅`=正常，`⚠️`=异常（触发告警规则）。

### 21.5 模板统一约定

- 所有数值输出前统一 `parseNum`（去千分位逗号），杜绝 ¥9459→¥7 类解析事故；
- 字段缺失显示 `--`，不推断不补零；`_method`/`source_type` 非 `api` 时卡片标注数据来源；
- 推送失败自动重试（`feishu-push-guard`），同内容去重（防重复推送）；
- 学习期内计划（`learning_done=0`）卡片标注「学习中」，不参与调优建议（见第二十章）；
- 模板或取数口径变更必须同步更新本文档，并走 ADR 决策记录。

### 21.6 配置与迁移

- `.env`：`LARK_MONITOR_CHAT_ID`（不变）、`LARK_ANCHOR_CHAT_ID` → **`LARK_REPORT_CHAT_ID`**（群 ID 不变，仅语义更名）；`REGION_MAP` 存 5 大区账户映射；
- 对话开启：飞书开放平台应用权限加 `im:message` / `im:message.group_at_msg`，两个群均添加机器人并允许 @；
- 迁移步骤：① 群内确认机器人可用 → ② 改名 `LARK_ANCHOR_CHAT_ID` 为 `LARK_REPORT_CHAT_ID` 并同步 `.env`/PM2 → ③ 双群监听灰度（先读后写）→ ④ 旧「主播群」文案全部替换为「汇报群」；
- 历史数据兼容：`shifts` 表、`shift-audit-{date}.json` 等文件与群名无关，无需迁移。

## 第二十二章 现状快照（S0 简单盘点）

> 2026-08-01 简单盘点，作为后续执行基线参考；正式 `refactor-baseline` 仍建议用脚本固化。

| 项 | 实测值 |
|---|---|
| Node | 22.22.2；`package.json` engines `>=22 <23` |
| PM2 配置 | 12 个应用；6 个常驻 online（action-queue-worker、chrome-guard、feedback-server、feishu-listener、live-watcher、shift-pusher），6 个 cron 类当前 stopped 等待调度 |
| DB 文件 | 46,280,704 bytes（约 44.1MiB） |
| DB 对象 | 14 张表，0 个 SQL view |
| `snapshots` | 37,707 行；当日 9,386 行；最新 `2026-08-01T11:25:01Z` |
| `campaigns` / `actions` / `shift_metrics` | 148 / 8 / 10 |
| 代码规模 | 全项目 `.mjs` 158（根目录 71）；`.md` 139（根目录 53）；`.py` 40（根目录 11） |
| 测试 | 8 个测试文件 |
| CI | 仅 ubuntu：`ci-test` + `backup-verify` |
| `package.json` | 暂无 `scripts` |
| 队列/审计 | `action-queue.json` 19 bytes；`action-audit.jsonl` 24,540 bytes |
| git | 38 个变更项，其中 13 个未跟踪 |
| 风险信号 | `feedback-server` 当前重启计数 7333，后续需重点排查稳定性 |

# 第六部分 实施与治理


## 五、分阶段实施路线图

> 每阶段独立验收、可回滚；主链路（15min/5min 卡片）禁止中断。

| 阶段 | 主题 | 关键交付 | 验收标准 |
|---|---|---|---|
| S0 冻结基线（0.5 天） | — | 以第二十二章现状快照为基础，补齐卡片输出样例；创建 git 标签 `refactor-baseline` | 所有进程状态快照存档 |
| S1 安全清理（0.5 天） | A | 删除/归档临时脚本与 .bak/.log；根目录瘦身 | 根目录文件数 < 40；PM2 无异常 |
| S1.5 远程通道治理（1 天） | K | 隧道/网关/地址通知纳入健康监控；密码进 .env；手册入 README | 隧道异常可告警；地址变更通知日志可查；无明文密码残留 |
| S2 基础设施（2 天） | E/F/G 部分 | 数据库统一、配置外置、monitor-data 分区 | 12 进程迁移后 24h 无回归；DB 仍为单一文件 |
| S3 采集收敛（3-5 天） | B | oec-session/http/normalize 上线；迁移 fetch-* 4 个脚本（→ D1 前置） | 8 处 httpPost 归零；ai-regions 21:30 数据与历史快照一致 |
| S4 业务拆分（4-6 天） | C/D | monitor-v3、5min-check、feedback-server 拆分；删除旧前端 | 拆分后输出 diff 一致；http-server 路由冒烟通过 |

| D0 数据模型（1-2 天） | 数据中枢 | schema 迁移：新表 + account_id 列；snapshots 增加 external_action/conversion_depth/is_lead_source；operations 视图兼容 actions（双写灰度） | 迁移幂等可回滚；`npm run check` 通过 |
| D1 采集器独立（3-5 天） | 数据中枢 | `oec-collector` 上线 5min 粒度；下播后 4min 补采；卡片/汇总改读库（并行 diff）；`scripts/purge-data.mjs` 保留策略上线（snapshots 90 天，operations/effects 永久） | 卡片与 DB 一致；JSON 不再被消费方读取；DB 规模受控 |
| D2 操作闭环入库（2-3 天） | 数据中枢 | operations/operation_effects 替换文件队列与 audit；effect-tracker 上线 | 队列/审计文件停写；Dashboard 审计 Tab 读库 |
| D3 规则引擎（3-4 天） | 数据中枢 | trigger_rules + trigger-engine，先 suggest 模式（Rule-001 试点） | 条件命中生成 AI 建议卡片；人工下达后执行 |

| S5 进程合并（2-3 天） | G | 采集/规则建议合并为 oec-collector；定时汇报合并为 oec-scheduler；常驻进程合并 | PM2 进程数 12→6；24h 无回归 |
| S6 测试强化（2-3 天） | H | 单测补齐 + windows CI + scripts/check-imports.mjs（import 正则检查拦截跨层引用） | `npm run check` 全绿；CI 双平台通过 |

| D4 AI 学习与建议校准（1-2 周） | 数据中枢 | 基于人工执行结果回写规则置信度；暂不开放 auto | AI 不直接执行；所有 operations 的 source 均来自人工 |

| S7 多租户（1-2 周，独立立项） | I | accounts.json + 账户循环 + 数据/推送隔离 | 第二个账户接入仅改配置 |

依赖：D1 依赖 S3；D2 依赖 S2；D3 依赖 D1+D2；D4 依赖 D3；S7 可与 D4 并行。

---


## 六、风险与回滚

### 6.1 重构链路风险（S 系列）

| 风险 | 等级 | 缓解 |
|---|---|---|
| 主链路断更（15min/5min 卡片） | 高 | S0 基线 + 每阶段新旧并行 diff；PM2 逐进程回滚；保留旧文件至下阶段 |
| 拆分引入行为漂移 | 高 | "抽函数不搬逻辑"；拆分后 diff 测试（卡片输出、DB 行数、推送日志） |
| 数值/字段解析事故（¥9459→¥7） | 高 | 统一 parseNum + 列头映射 + 结构化告警 |
| 数据库迁移损坏历史数据 | 高 | 迁移前 `VACUUM INTO` 备份；schema 变更走 migrations，不直接改表 |
| 进程合并改变队列语义（文件→内存） | 中 | action-queue 合并放阶段 5 最后，先行双写比对 |
| 40102 端点突破失败 | 低 | 仅 HTTP API 抓取；缺失端点记录缺失标记 + 飞书告警，不阻塞主线 |
| 多租户改造范围蔓延 | 中 | 单独立项，S7 前不动主链路 |

### 6.2 数据中枢风险（D 系列）

| 风险 | 等级 | 缓解 |
|---|---|---|
| 汇报读库出现延迟/缺数据 | 高 | 采集器健康检查 + 陈旧数据告警；JSON 灾备回放兜底 |
| 迁移历史操作数据丢失 | 中 | actions→operations 双写；`backfill.mjs` 从 audit jsonl 回灌 |
| AI 建议误导或人工误执行 | 高 | AI 仅建议；建议卡片展示上下文与风险；人工执行仍走确认、护栏、审计、回滚 |
| 规则误判（如 40102 数据缺失） | 中 | `min_occurrences` 防抖；条件求值失败即跳过并告警，不默认触发；40102 端点缺失时记录缺失标记并告警，不接 DOM 备选源 |
| 效果窗口数据不完整 | 低 | 延迟补齐机制；窗口超时放弃并标记 |

---

## 七、量化验收指标

| 指标 | 现状 | 目标 |
|---|---|---|
| 根目录生产代码文件数 | ~150 | < 20（余进 src/） |
| 最大单文件 | 163KB | < 30KB |
| PM2 进程数 | 12 | ≤ 6 |
| httpPost/WebSocket/Cookie 重复实现 | 8/7/3 | 0 |
| 单测文件数 | 8 | ≥ 30，`npm run check` 全绿 |
| 硬编码配置（chat_id/aadvid/账户） | 20+ 处 | 0（全部进配置） |

| 消费方读 JSON 文件 | 全部 | 0（仅灾备回放） |
| 调优操作存储 | 文件队列 + audit + actions 表 | operations + effects 单表闭环 |
| 规则配置 | 静态 md 文档 | DB 配置化 + 效果回写 |
| AI 自动调优 | 无 | AI 仅建议，人工执行（短期） |
| 数据保留 | 无策略（DB 无限增长） | snapshots 90 天 / 操作审计永久 / 物化聚合长期 |

| CI | ubuntu 单测 | ubuntu + windows + import 检查 |
| 新增账户接入成本 | 改代码 | 只改配置 |
| docs/ 有效文档 | 115 个混放 | 有效 < 15，归档可查 |

---



## 八、文档地图


### 8.1 有效文档（维护中）

| 文档 | 内容 |
|---|---|
| `完整重构方案_20260801.md` | 全项目重构方案（S 系列） |
| `数据中枢与AI调优架构设计.md` | 采集独立 + 唯一事实源 + AI 建议闭环（当前人工执行，D 系列） |
| `前端数据抓取重构方案.md` | 采集链路收敛（P 系列） |
| `多租户架构设计_线索直播广告监控.md` | 多租户演进 |
| `AI诊断规则手册.md` | 诊断规则基线（将配置化） |
| `巨量引擎广告投放知识库_Agent学习版_20260801.md` | 平台层级、转化口径、规则建议参考（行业阈值不作默认） |
| `API字段全量档案.md` | 已逆向端点字段 |
| `CARD-FORMAT.md` | 卡片格式规范 |
| `直播状态巡检协议规范.md` | 直播巡检协议 |
| `工程化优化方案_20260725.md` | 工程化基线 |
| `CHANGELOG.md` | 版本演进 v0.1~v1.3 |
| `README.md` / `CODEBUDDY.md` | 快速开始 / 项目规范 |

### 8.2 归档建议（docs/archive/）

`架构规划_*` 系列（20+）、`*_评审*`、`*_审查*`、`*_替代方案选型*`、`*_决策档案*`、`40102端点逆向思路.md`（已并入 API 档案）、各临时 `_*.mjs` 脚本说明等历史文档。


---

## 九、附录 A · 常用命令


```bash
pm2 start ecosystem.config.cjs          # 启动全部
pm2 status                              # 查看状态
pm2 restart pm2-15min pm2-5min          # 重启核心监控
tail -f monitor-data/monitor.log        # 主日志
# 重建原生模块（Node 升级后）
C:/Users/HTF2026/.workbuddy/binaries/node/versions/22.22.2/npm.cmd rebuild better-sqlite3
# 代码量统计
bash scripts/count-lines.sh --markdown
```

## 十、附录 B · 测试入口


```bash
node scripts/ci-test.mjs                # CI 逻辑测试（csrf/router/push-guard）
node scripts/backup-verify.mjs          # 备份完整性/灾备演练
node tests/action-queue-lock.test.mjs   # 队列锁测试（单测）
```

## 十一、附录 C · 关键文件迁移映射表（首轮）

| 现文件 | 目标位置 | 处理 |
|---|---|---|
| `monitor-utils.mjs` | `src/utils/` | 迁移+拆分（config 部分进 `src/config/`） |
| `logger.mjs` | `src/utils/logger.mjs` | 迁移 |
| `oceanengine-api-client.mjs` | `src/platform/oec-client.mjs` | 按主题 B 重构 |
| `oceanengine-monitor-v3.mjs` | `src/services/monitor-15min.mjs` + `src/domain/*` | 拆分 |
| `oceanengine-5min-check.mjs` | `src/services/monitor-5min.mjs` + `src/domain/*` | 拆分 |
| `feedback-server.mjs` | `src/services/http-server.mjs` + `src/services/http-routes/*` | 拆分 |
| `feishu-listener.mjs` | `src/services/feishu-listener.mjs` + `src/feishu/*` | 迁移+拆分 |
| `action-queue-worker.mjs` | `src/services/action-worker.mjs` | 迁移 |
| `cdp-client.mjs` / `cdp-action.mjs` | `src/cdp/` | 迁移 |
| `feishu-push-guard.mjs` | `src/feishu/guard.mjs` | 迁移 |
| `db/` | 删除 | 由 `src/db/`（v2 基线）接管 |
| `巨量引擎监控数据库/` / `巨量引擎快照数据库/` | `src/db/` | 迁移 |
| `dashboard.html/js/css` | 删除 | 仅保留 v2 |
| `fetch-*.mjs`、`oec-*.mjs` | `scripts/` 或 `src/platform/oec-capture.mjs` | 按主题 B 收敛 |
| `_*.mjs`、`_q*.py`、`*.bak*`、`*.log` | `scripts/_deprecated/` 或删除 | 归档 |
| 历史方案文档 | `docs/archive/` | 归档 |
