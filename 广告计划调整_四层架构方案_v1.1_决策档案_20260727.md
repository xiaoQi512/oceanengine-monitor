# 巨量引擎广告计划调整 · v1.1 方案 · 决策档案

> 文档类型：决策档案 (Decision Record) + 优化过程记录
> 落档日期：2026-07-27（周一 22:02 GMT+8）
> 评审对象：`广告计划调整_四层架构方案_v1.1_20260727.md`
> 决策人：主人
> 评审人：爆量君（WorkBuddy Agent）

---

## 0. 文档元信息

| 关联文档 | 路径 | 角色 |
|---------|------|------|
| v1.0 原始方案 | `广告计划调整_四层架构方案_20260727.md` | 上一版本 |
| **v1.1 原始方案（评审对象）** | `广告计划调整_四层架构方案_v1.1_20260727.md` | 当前方案 |
| **v1.1 评审意见** | `广告计划调整_四层架构方案_v1.1_评审意见_20260727.md` | 详细评审分析 |
| **v1.1 决策档案（本文）** | `广告计划调整_四层架构方案_v1.1_决策档案_20260727.md` | 最终决策脉络 |

---

## 1. 原始方案摘要（一句话）

v1.1 在 v1.0 基础上把"自动幂等去重"换成"重复指令二次确认"机制，**保留四层架构**：接入层（飞书 + Dashboard）→ 命令层（parseCommand + enqueue + acknowledgeStart）→ 执行层（worker + cdp-action + api-client + 自检）→ 审计层（action-audit.jsonl 单文件）。traceRef 贯通全链路。

---

## 2. 决策脉络（按主题）

### 决策 D1：审计归一（P0 采纳）

| 字段 | 内容 |
|------|------|
| **背景事实** | `cdp-action.mjs:420/535/657` 3 处 `writeAuditLog` + `action-queue-worker.mjs:78` 1 处 `writeAudit`，同一 action 生命周期被劈到两个审计写入点 |
| **选项** | A. 归一到 worker / B. 各写各的 |
| **决策** | A. 归一到 worker |
| **理由** | 避免双写造成的字段不一致、缺失、回查困难 |
| **落地步骤** | ① 备份 `monitor-data/action-audit.jsonl` 到 `.archive/`；② 注释 cdp-action 三处调用；③ worker 的 `writeAudit` 扩展字段 traceRef/attempts/method；④ 跑 3 天对比归档 |
| **关联代码** | `cdp-action.mjs:83/420/535/657`、`action-queue-worker.mjs:19/78` |

### 决策 D2：重复指令二次确认机制（P0 采纳）

| 字段 | 内容 |
|------|------|
| **背景** | v1.0 自动去重会丢弃"分次调整预算"这种有意操作 |
| **决策** | 保留 v1.1 设计：当日重复指令触发"二次确认"（飞书文本回复"执行/拒绝"） |
| **理由** | 兼顾安全与灵活，避免误操作丢失用户真实意图 |
| **关联** | v1.1 3.1 节 |

### 决策 D3：API 优先 + CDP 降级（P1 增强）

| 字段 | 内容 |
|------|------|
| **决策** | 保留 v1.1 设计 |
| **增强点** | CDP 挂时第三层 fail-safe 缺失 → 加 P1 项：worker 共享 chrome-guard 的 9222 探活，HTTP API 失败 + Chrome 不健康 → 整体拒绝 |
| **理由** | 巨量广告 API 偶发 503 + Chrome 挂 = 整条执行链断 |
| **关联代码** | `chrome-guard.mjs`、`action-queue-worker.mjs`、`monitor-utils.mjs` 共享 ping 函数 |

### 决策 D4：pending-actions.json 路径约定（P0 采纳）

| 字段 | 内容 |
|------|------|
| **背景** | v1.1 引入 `pending-actions.json` 但路径未约定；现有 audit 在 `monitor-data/`，queue/lock 在根目录 |
| **决策** | pending-actions.json 放在 `monitor-data/`，走 `monitor-utils.mjs` 集中导出 `PENDING_FILE / AUDIT_FILE / QUEUE_FILE / LOCK_FILE` |
| **理由** | 与现有 audit 同目录，避免散落 |
| **关联** | `monitor-utils.mjs` v3.2 |

### 决策 D5：Dashboard 操作面板（P1 采纳）

| 字段 | 内容 |
|------|------|
| **背景** | dashboard 现在是只读视图，缺操作手臂——用户只能在飞书端操作 |
| **决策** | 补 P1 项：Dashboard "操作执行"面板，包含三页 tab —— 当前队列 / pending 待确认 / 最近审计 |
| **理由** | 解锁 dashboard 主闭环；traceRef 可点击 → `/api/audit/:traceRef` |
| **关联代码** | `dashboard.html`、`dashboard.js`、`feedback-server.mjs` |

### 决策 D6：ACK 卡片化（P1 采纳）

| 字段 | 内容 |
|------|------|
| **背景** | 当前飞书二次确认是文本回复"执行/拒绝"，群聊文本极易误识别 |
| **决策** | 改成飞书卡片 [✅ 确认] / [❌ 取消] 按钮 + token 校验，与 v5.0 建议反馈同构 |
| **理由** | 防误识别；按钮回调直接调 `/api/actions/confirm` |
| **关联** | `CARD-FORMAT.md` 增章；`feishu-listener.mjs` |

### 决策 D7：audit schema 扩展（P1 采纳）

| 字段 | 内容 |
|------|------|
| **决策** | audit 字段统一扩展：① `source: auto/manual/dashboard/feishu` 枚举；② `method: http_api/cdp/none` 记录执行路径；③ `attempts: 1..3`；④ `traceRef` 全程贯通；⑤ `afterValue` 从 API 回读（pause→status、budget→budget） |
| **理由** | 区分人机；可观测执行路径；afterValue 为未来回滚 / ML 反事实分析铺路 |

### 决策 D8：Dashboard 一键回滚按钮（P2 暂留，本次不实施）

| 字段 | 内容 |
|------|------|
| **背景** | 评审中提出 Dashboard "一键回滚"按钮；用户追问意义 |
| **澄清** | **一键回滚 ≠ 自动回滚**——是把"手工翻日志 → 命令行敲反向指令"图形化为一个按钮，仍要人点确认，但省去 3 步最容易出错的动作：① 重输计划名；② 重输金额；③ 反向语义手写 |
| **边界** | pause ↔ resume ✅；adjust_budget/bid 反向 ✅；stop（关停）❌ 需业务判断（按钮直接禁用） |
| **决策** | **本次 v1.1 不实施**，纳入 P2 |
| **理由** | v1.1 sprint 收口更紧；先把 P0 跑顺；P2 阶段实施 |
| **落地预留** | audit 需保留 `beforeValue`（已 D7 覆盖），`rollbackOf` 字段标记父子关联 |

### 决策 D9：live-watcher 接入 / 命令拦截（暂缓，留到"智能提醒"项目）

| 字段 | 内容 |
|------|------|
| **背景** | 评审中提出"直播中暂停高风险，建议加 precheckAction hook" |
| **用户澄清（关键）** | **直播中暂停高成本计划是常规救火动作**（流量贵 + 计划成本高 = 必须立刻停），不是误操作——"直播中"不能作为拦截条件 |
| **决策** | **本次 v1.1 不做** precheckAction 接入 live-watcher，不写 `risk-classifier.mjs`，不写 riskTag 字段 |
| **理由** | 业务澄清：拦截条件应是"学习期 / 主力消耗 / CPA 突降"等多维分类，"直播中"维度与之正交 |
| **后续（"智能提醒"项目）** | 再讨论：① 主力计划定义（top 5 vs top 10）；② 学习期边界（7 天 vs 14 天）；③ 与 v3.0 建议系统的耦合 |
| **本次边界** | **只做同一计划单日多次调整的人工确认**——v1.1 已有的"重复指令二次确认"机制已覆盖 |

### 决策 D10：暂缓保留（与 v1.1 一致）

| 字段 | 内容 |
|------|------|
| **决策** | 学习期保护、接口风险管控、多账户风险、多维表格入口 —— v1.1 已明确暂缓，本次继续保留 |
| **理由** | 与 v1.1 "代码已走得通保留，不硬拆不新增空概念"原则一致 |

---

## 3. 最终优先级清单（v1.1 精简后）

| 序号 | 任务 | 优先级 | 涉及文件 | 来源决策 |
|:---:|------|:---:|------|:---:|
| 1 | 备份 audit.jsonl → 注释 cdp-action 三处 writeAuditLog | P0 | `cdp-action.mjs` | D1 |
| 2 | worker 审计写入字段扩展（traceRef/attempts/method/source） | P0 | `action-queue-worker.mjs` | D1 / D7 |
| 3 | 引入 pending-actions.json 放 monitor-data/，走 monitor-utils 统一 | P0 | 新增 + `monitor-utils.mjs` | D4 |
| 4 | acknowledgeStart 实现重复检测 + 二次确认 | P0 | `feishu-listener.mjs` | D2 |
| 5 | 30s pending 扫描 + 启动扫描补跑（setTimeout 自循环） | P0 | `feishu-listener.mjs` | D2 |
| 6 | ACK 超时取消飞书回执（含 traceRef + cancelled_by_timeout） | P0 | `feishu-listener.mjs` | D2 |
| 7 | ECOSYSTEM 注释：worker instances=1 单实例约束 | P1 | `ecosystem.config.cjs` | 本机适配 |
| 8 | CDP 熔断：HTTP API 失败 + Chrome 不健康 → 整体拒绝 | P1 | `action-queue-worker.mjs` + `monitor-utils.mjs` | D3 |
| 9 | ~~precheckAction 接入 live-watcher~~ | **暂缓** | — | D9 |
| 10 | Dashboard "操作执行"面板（队列 + pending + 最近审计） | P1 | `dashboard.html` + `dashboard.js` + `feedback-server.mjs` | D5 |
| 11 | `/api/audit/:traceRef` 接口 | P1 | `feedback-server.mjs` | D5 |
| 12 | CARD-FORMAT.md 增加"操作回执卡 v3.x"章节 | P1 | `CARD-FORMAT.md` | D6 / D7 |
| 13 | ACK 卡片化（按钮替代文本匹配） | P1 | `feishu-listener.mjs` | D6 |
| 14 | audit schema source 枚举统一 | P1 | `action-queue-worker.mjs` | D7 |
| 15 | afterValue 从 API 回读 | P2 | `action-queue-worker.mjs` + `oceanengine-api-client.mjs` | D7 / D8 |
| 16 | Dashboard "一键回滚"按钮（手动但工具化） | P2 | `dashboard.html` + `feedback-server.mjs` | D8 |
| 17 | pending 搬 SQLite + 物化视图统计 ACK 接受率 | P2 | `db/` + `monitor-utils.mjs` | 评审 |
| 18 | 学习期保护 + 多账户接口风险 + 多维表格入口 | 按需 | — | D10 |

---

## 4. 关键代码锚点（后续 PR 定位用）

| 文件 | 行 | 内容 |
|------|---|------|
| `cdp-action.mjs` | 83 | `function writeAuditLog(entry)` —— 归一时需删 |
| `cdp-action.mjs` | 420 / 535 / 657 | 3 处 `writeAuditLog` 调用 —— 归一时需注释 |
| `action-queue-worker.mjs` | 17 | `const QUEUE_FILE = ...` 根目录 queue 文件 |
| `action-queue-worker.mjs` | 18 | `const LOCK_FILE = ...` 根目录 lock 文件 |
| `action-queue-worker.mjs` | 19 | `const AUDIT_LOG_FILE = ...` monitor-data 下的 audit |
| `action-queue-worker.mjs` | 21-22 | `MAX_RETRIES = 3`、`RETRY_INTERVAL_MS = 5000` —— 已合规 |
| `action-queue-worker.mjs` | 36-56 | `acquireLock()` `wx` 标志 + 10 分钟僵死回收 —— 已合规 |
| `action-queue-worker.mjs` | 78-87 | `writeAudit()` 当前实现 —— D1 落地时需扩展字段 |
| `action-queue-worker.mjs` | 141-203 | `processHead()` 3 次重试 + 失败移队尾 —— 已合规 |
| `ecosystem.config.cjs` | 167-186 | `action-queue-worker` PM2 配置，instances=1 需加注释 |
| `feishu-listener.mjs` | 51 | `function enqueue(action)` —— D2 改造点 |
| `feishu-listener.mjs` | 220 | `async function acknowledgeStart(...)` —— D2 改造点 |
| `feishu-listener.mjs` | 244 | `function parseCommand(msg)` —— 已合规 |
| `feedback-server.mjs` | 9-12 | 从 `monitor-utils.mjs` 导入（已合规，可加 PENDING_FILE/AUDIT_FILE） |
| `feedback-server.mjs` | 19 | `ACTION_QUEUE_FILE` —— 路径与 worker 一致 |
| `feedback-server.mjs` | 73-75 | `sanitize()` / `escHtml()` —— CSRF 双验证 |
| `chrome-guard.mjs` | — | 60s 探活 9222 —— D3 共享 ping 函数目标 |
| `cdp-client.mjs` | — | 统一 CDP 客户端 —— worker 落地 `tryCdpAction` 必须走这个 |

---

## 5. 优化过程回溯（按时间倒序）

### 阶段 5：21:59 主人决策
- **决策内容**：暂时只做同一计划单日多次调整的人工确认，不做命令拦截；命令拦截留到"智能提醒"项目
- **影响**：评审文档 P1-2 整段重写为"暂缓"，任务清单第 9 项划线

### 阶段 4：21:56 用户业务澄清
- **反馈**：直播中暂停高成本计划是**常规救火动作**，不是误操作
- **影响**：评审 P1-2 原建议（precheckAction 接入 live-watcher 硬拦截）被推翻

### 阶段 3：21:30 我给出三条路径
- 路径 A：先观察不拦截（写 audit 字段）
- 路径 B：MVP 软警告上线（半骨架）
- 路径 C：完整方案一刀切
- **结果**：主人没有立刻选，而是进一步澄清业务边界

### 阶段 2：21:20 一键回滚追问
- **用户反馈**：不理解"Dashboard 不带一键回滚"的实际意义
- **澄清**：
  - 一键回滚 ≠ 自动回滚
  - 是图形化"日志查阅 + 命令行反向指令"按钮，仍要人点确认
  - 核心价值：不重输计划名 / 不重输金额 / 反向语义自动生成 / traceRef 父子关联
  - 边界：pause↔resume ✅、adjust_budget/bid 反向 ✅、stop ❌
- **影响**：D8 决策 —— 暂留 P2

### 阶段 1：21:06 评审原始方案
- **评审对象**：v1.1 四层架构方案
- **产出**：评审意见文档（11 节 / 18 项行动清单）
- **核心结论**：v1.1 架构走向正确；3 处落地风险；本机适配 9 点；前端展示优缺点

---

## 6. 未来 follow-up（不阻塞 v1.1）

### 6.1 智能提醒项目（含命令拦截规则）
- **触发**：v1.1 上线后跑 1-2 周，积累真实"直播中暂停"分布数据
- **议题**：① 主力计划定义（top 5 vs top 10）；② 学习期边界（7 天 vs 14 天）；③ 与 v3.0 建议系统的耦合（自动建议 + 人工审核）；④ 多账户接口风险（rate limiter / 熔断 / token 轮换）
- **关联**：D9

### 6.2 一键回滚 UI（D8 落地）
- **前置条件**：audit 字段扩展（D7）已完成
- **工作量**：worker ~30 行、dashboard ~50 行、`feedback-server` 加 1 个 `/api/rollback` 接口
- **关联**：D8

### 6.3 pending 搬 SQLite + 物化视图
- **当前**：JSON 足够（pending 通常 0-3 条）
- **未来收益**：查询"近 7 天所有用户的 ACK 接受率"等指标
- **关联**：评审 P2-1

### 6.4 多维表格入口（飞书）
- **当前**：仅飞书群 + Dashboard
- **未来**：监听多维表格 Webhook 事件，入队同样经 `pending` 二次确认
- **关联**：v1.1 P2-2

---

## 7. 评审签字栏

| 角色 | 签字 | 日期 |
|------|------|------|
| 评审人 | 爆量君 | 2026-07-27 |
| 决策人 | 主人 | 2026-07-27 |

---

## 附：评审期间发现的本机适配要点（不构成决策，但供参考）

1. **`monitor-daemon.mjs` 不在 `ecosystem.config.cjs` 13 个进程里** —— 守护程序位置需确认（系统级 cron / Windows 任务计划？）
2. **`_check_daily.mjs` / `_check_lines.mjs` 等 `_*.mjs` 调试脚本残留** —— 建议归档到 `archive/debug-scripts/`
3. **`feishu-listener.mjs.bak-20260727` 备份文件** —— 应纳入 `.gitignore` 或定期清理
4. **CDP 客户端统一基础设施已具备** —— worker 落地 `tryCdpAction` 必须走 `cdp-client.mjs`，不要 worker 自实现（v3.1 教训）
5. **SQLite 物化视图收益没在审计维度复用** —— v5.0 已把 snapshots/alerts/feedback 物化，audit 仍 jsonl，未来若要查"近 30 天各计划暂停次数"等指标，建议同步搬 SQLite
6. **`feishu-listener` 应加 `OEC_SILENT=1`** —— pending 扫描是 30s tick + ack 回调高频消息，单独打 `monitor.log` 比塞 `pm2-feishu-listener-out.log` 排障更方便
7. **飞书回推一律走 `feishu-push-guard.mjs` 的 `pushText`** —— 带熔断，避免测试时反复发送把 lark-cli 限流