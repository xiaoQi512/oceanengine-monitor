# 按需加载方案分析与优化

> 日期：2026-07-26
> 状态：技术分析
> 用途：对多租户架构的 Chrome Profile 按需加载方案进行瓶颈分析、优化设计和容量重算

---

## 一、按需加载的核心前提

### 1.1 关键发现

通过代码审查确认，Chrome 进程只在 Cookie 刷新时被需要，数据采集全程不需要 Chrome：

```
collectAllData(client)  标注："HTTP API 数据采集 (无CDP/无浏览器)"
  -> getProjects(client)       纯 HTTP 请求，用缓存 Cookie
  -> getDashboardStats(client) 纯 HTTP 请求，用缓存 Cookie
  -> 返回 campaigns 数据
```

时间占比：

```
Cookie 有效期:  2 小时 = 7200 秒
Cookie 刷新耗时: ~15 秒（CDP 提取 + WebSocket）
采集周期:       每 5 分钟一次，每次 ~1.8 秒

Chrome 需要运行的时间占比: 15s / 7200s = 0.2%
Chrome 空闲浪费:           99.8%
```

常驻模式下 99.8% 的 Chrome 内存是浪费的。按需加载正是消除这部分浪费。

### 1.2 collectAllData 无状态可并行

```
collectAllData(client) 签名:
  输入: client（含该账户的 cookieData）
  输出: { campaigns, accountSpend, accountBudget, ... }
  副作用: 无（纯数据函数，不修改全局状态）
```

不同账户的 collectAllData 调用互相独立，可以并行执行。当前串行只是因为只有一个账户，不是技术限制。

### 1.3 apiRequest 无限流处理

```
apiRequest 现状:
  - 超时 15s，无重试
  - 无 429 / rate-limit 识别
  - Cookie 过期时内联触发 extractCookiesFromBrowser()（阻塞式）
```

多账户并行采集时，必须补限流和重试逻辑，否则高并发可能触发 Ocean Engine 内部 API 的频率限制。

---

## 二、瓶颈识别

### 2.1 瓶颈一：Cookie 刷新串行（主要约束）

Cookie 刷新必须串行（同一时刻只有一个 Chrome Profile 在用 CDP），耗时构成：

```
Chrome 冷启动到调试端口就绪:  ~2s（估算，chrome-guard 注释"启动慢"）
CDP 连接 + Network.enable:    ~1s
Network.getCookies:           ~2s
Runtime.evaluate (UA):        ~1s
WebSocket 关闭 + 清理:         ~1s
Chrome 进程退出:               ~2s
合计:                         ~9-15s/账户
```

按 15s/账户计算，不同场景的刷新吞吐：

| 场景 | 可刷新账户数 | 说明 |
|------|------------|------|
| 2 小时内全部刷新一遍（错峰） | 7200/15 = 480 | 理论上限，每 15s 刷一个 |
| 15 分钟预刷新窗口内刷完 | 900/15 = 60 | 刷新在过期前 15min 启动 |
| 系统重启后全部重刷（灾难恢复） | 600/15 = 40 | 10 分钟内恢复 40 个账户 |

### 2.2 瓶颈二：采集吞吐（可并行缓解）

采集用 HTTP API，不依赖 Chrome，可并行。但 Ocean Engine 内部 API 的并发限制未知。

| 采集模式 | 单账户耗时 | 5min 窗口(300s)容量 | 说明 |
|---------|-----------|--------------------|----|
| 串行 | 4s（1.8s 采集 + 2s 间隔） | 75 个 | 当前模式 |
| 并发=5 | 1.8s/5 批次 | 208 个 | 推荐 |
| 并发=10 | 1.8s/10 批次 | 416 个 | 激进，可能触发限流 |

### 2.3 瓶颈三：API 限流（未知风险）

Ocean Engine 内部 API（`ad.oceanengine.com/nbs/api/...`）的限流策略未知。当前单账户每 5 分钟调 2-3 个 API，不会触发限流。多账户并行后调用量倍增：

```
单账户: 2-3 次/5min = 0.4-0.6 次/min
50 账户并发=5: 50*3/5min = 30 次/min（6 个并发请求）
100 账户并发=5: 60 次/min
```

无 429 处理是当前代码的缺口，多账户必须补上。

### 2.4 瓶颈四：内存（按需后不再是约束）

按需加载后，Chrome 只在刷新时短暂运行：

```
固定基座:
  Node 常驻进程 (6个): 332 MB
  Chrome 刷新时峰值:   ~500 MB（1 个 Profile + 渲染器）
  合计峰值:           ~832 MB

每账户增量:
  Cookie 缓存文件:    ~5 KB
  5min 快照:          ~1.2 KB/次
  15min 快照:         ~45 KB/次
  SQLite 行:          ~2 KB/天
  合计:              可忽略

24GB 系统，监控占用 <1GB，内存不再是约束。
约束转移到 Cookie 刷新吞吐和 API 限流。
```
---

## 三、优化措施

### 3.1 优化一：Cookie 预刷新（消除过期阻塞）

**问题：** 现有 apiRequest 在 Cookie 过期时内联触发 `extractCookiesFromBrowser()`，阻塞当前请求 15s。多账户场景下，如果多个账户同时过期，会形成刷新风暴。

**方案：** 独立 Cookie 刷新守护进程，提前刷新：

```
cookie-refresh-daemon.mjs:
  每 60s 扫描所有账户的 cookie 缓存文件
  对每个 cookieData.expireAt - now < 15min 的账户：
    加入刷新队列（按过期时间排序，先过期先刷）
  串行处理队列：launch Chrome -> extract -> kill Chrome -> 下一个
```

```
刷新时间线（N=50 账户，错峰 2.4min/个）：
  账户1: 刷新于 00:00  过期于 02:00
  账户2: 刷新于 00:02  过期于 02:02
  账户3: 刷新于 00:05  过期于 02:05
  ...
  账户50: 刷新于 01:58 过期于 03:58
  -> 永远不会出现多个账户同时过期
```

**效果：** 消除采集期间的 Cookie 刷新阻塞，采集永远命中缓存（<1ms），不需要等待 Chrome。

### 3.2 优化二：采集并发池（突破串行瓶颈）

**问题：** 当前 scheduler-engine 设计为串行采集（账户间 3-5s 间隔），50 个账户要 200s，接近 5min 窗口上限。

**方案：** 采集不依赖 Chrome，可安全并行。引入并发池：

```js
// concurrency-pool.mjs
async function runWithConcurrency(tasks, limit = 5) {
  const results = [];
  const executing = new Set();
  for (const task of tasks) {
    const p = task().then(r => { executing.delete(p); return r; });
    executing.add(p);
    results.push(p);
    if (executing.size >= limit) {
      await Promise.race(executing);
    }
  }
  return Promise.all(results);
}

// 调度引擎中使用
const accounts = loadActiveAccounts();
await runWithConcurrency(
  accounts.map(ctx => () => collectAndAnalyze(ctx)),
  5  // 并发数，可配置
);
```

**并发数选择依据：**
- 并发=5：50 账户耗时 18s，100 账户 36s，安全余量大
- 并发=10：吞吐翻倍但 API 限流风险增加
- 起步推荐 5，根据 429 出现频率逐步调高

### 3.3 优化三：API 限流防护（补 429 处理）

**问题：** 现有 apiRequest 无限流识别，多账户并发会放大风险。

**方案：** 在 apiRequest 层增加 429 识别和指数退避：

```js
async function apiRequest(url, options = {}) {
  for (let attempt = 0; attempt < 3; attempt++) {
    const resp = await rawRequest(url, options);
    
    if (resp.status === 429) {
      // 限流：读取 Retry-After 头，无则指数退避
      const retryAfter = resp.headers?.['retry-after'];
      const delay = retryAfter ? parseInt(retryAfter) * 1000 : Math.pow(2, attempt) * 1000;
      console.log(`  ⏸ API 限流，${delay/1000}s 后重试 (${attempt+1}/3)`);
      await sleep(Math.min(delay, 10000));
      continue;
    }
    
    if (resp.status >= 500) {
      // 服务器错误：短延迟重试
      await sleep(1000 * (attempt + 1));
      continue;
    }
    
    return resp;  // 2xx 或不可恢复的错误
  }
  return { ok: false, status: 429, error: 'rate_limited' };
}
```

**全局并发节流：** 除单请求重试外，加全局令牌桶限制每秒最大请求数：

```
令牌桶: 10 请求/秒（初始值，根据实测调整）
漏出: 1 令牌/100ms
并发请求超过令牌数时排队等待
```

### 3.4 优化四：Chrome 暖池（减少冷启动开销）

**问题：** 每次 Cookie 刷新都冷启动 Chrome（~2s），频繁刷新时累计开销显著。

**方案：** 保持一个 Chrome 实例常驻作为"刷新工作机"，通过命令行参数切换 Profile：

```
方案 A（推荐）：单 Chrome 实例 + Profile 命令行切换
  chrome-guard 启动 Chrome on :9222 with default profile
  刷新账户 N 时:
    1. 关闭当前所有标签页
    2. spawn: chrome --profile-directory="Profile N" (复用已运行实例)
    3. 等待 ad.oceanengine.com 标签就绪
    4. CDP 提取 Cookie
    5. 关闭标签页（不关闭 Chrome）

  优势: Chrome 主进程不重启，省去 2s 冷启动
  劣势: Profile 切换需要 ~3-5s（Chrome 加载新 Profile 的渲染器）
```

```
方案 B（备选）：Chrome 实例池
  维护 2-3 个 Chrome 实例常驻（不同端口）
  每个实例轮流服务一个账户的刷新
  优势: 更高吞吐（2-3 账户可并行刷新）
  劣势: 内存增加 500MB-1GB
```

**推荐方案 A**，常驻 1 个 Chrome 实例（~300MB，无标签页时），刷新时加载对应 Profile 渲染器（~170MB），刷完关闭标签页。峰值内存 ~470MB，均值 ~300MB。
---

## 四、容量重算

### 4.1 优化后的资源模型

```
常驻基座（不随账户数增长）:
  Node 进程 (6个):         332 MB
  Chrome 暖池 (1实例):     300 MB（无标签页）/ 470 MB（刷新中）
  合计:                    632-802 MB

每账户增量（极小）:
  Cookie 缓存:             5 KB
  5min 快照:               1.2 KB * 288次/天 = 346 KB/天
  15min 快照:              45 KB * 96次/天 = 4.3 MB/天
  SQLite:                  ~2 KB/天
  合计:                    ~5 MB/天/账户

30天数据量: 150 MB/账户（可忽略，磁盘不是约束）
```

### 4.2 瓶颈转移后的容量

优化后，内存不再是约束，三个时间维度的约束取最小值：

| 约束维度 | 公式 | 50 账户 | 100 账户 | 200 账户 |
|---------|------|---------|---------|---------|
| Cookie 错峰刷新 | 7200s / 15s | 480 上限 | 480 上限 | 480 上限 |
| 采集并发=5 | 300s / (1.8s/5) | 18s 完成 | 36s 完成 | 72s 完成 |
| API 限流(10/s) | 300s * 10 | 3000 上限 | 3000 上限 | 3000 上限 |
| 灾难恢复(10min) | 600s / 15s | 750s=12.5min | 1500s=25min | 3000s=50min |

### 4.3 容量结论

| 优化等级 | 推荐账户数 | 瓶颈 | 说明 |
|---------|-----------|------|------|
| 保守（推荐起步） | 50-80 | API 限流(未知) | 并发=5，API 调用 30-48 次/min |
| 标准 | 80-120 | 灾难恢复时间 | 并发=5-8，重启后 15-20min 恢复 |
| 激进 | 120-200 | 灾难恢复 + API 限流 | 并发=10，需实测 API 限流阈值 |
| 理论极限 | 480 | Cookie 刷新吞吐 | 每分钟刷 4 个，刚好 2 小时循环 |

**推荐起步 50-80 个账户**。原因：
1. API 限流阈值未知，保守起步避免触发封禁
2. 灾难恢复（系统重启后全部重刷）在 10-13 分钟内完成
3. 内存占用 <1GB，24GB 系统绰绰有余
4. 采集 18-29s 完成，5min 窗口余量充足

### 4.4 与常驻模式对比

| 维度 | 常驻模式 | 按需加载(优化后) | 提升 |
|------|---------|----------------|------|
| 内存瓶颈 | 15-20 账户 | 不再是约束 | 消除 |
| 推荐账户数 | 15-20 | 50-80 | 3-4 倍 |
| 峰值内存(50账户) | ~23GB(危险) | ~0.8GB | 96% 降幅 |
| Chrome 进程数(50账户) | 150+(不稳定) | 1-2 | 99% 降幅 |
| Cookie 刷新阻塞 | 采集期间可能阻塞 | 预刷新，零阻塞 | 消除 |
| 采集并行 | 串行 | 并发=5 | 5 倍 |
| 灾难恢复 | N/A(常驻) | 10-13min | 可控 |

---

## 五、失败模式与恢复

### 5.1 Cookie 刷新失败

| 失败原因 | 检测方式 | 恢复策略 |
|---------|---------|---------|
| Chrome 进程崩溃 | CDP 连接超时 | chrome-guard 重启 Chrome，5min 后重试 |
| 登录会话过期 | 提取的 Cookie 为空 | 触发自动登录，CAPTCHA 则推告警 |
| Profile 文件损坏 | Chrome 启动报错 | 从备份恢复 Profile，或重建 |
| 网络中断 | apiRequest 超时 | 15s 超时后标记该账户本次跳过 |

**Cookie 过期容错：** 即使刷新失败，旧 Cookie 在 expireAt 之前仍可用。预刷新在过期前 15min 启动，有 3 次重试机会（每次间隔 5min），最后一次失败时 Cookie 仍有 0-5min 有效期用于告警。

### 5.2 采集失败

| 失败原因 | 影响 | 恢复策略 |
|---------|------|---------|
| API 限流(429) | 部分账户本轮跳过 | 令牌桶限流 + 指数退避，下一轮恢复 |
| Cookie 过期(401/403) | 该账户数据为空 | 标记需刷新，下一轮预刷新队列优先 |
| 网络波动 | 部分请求超时 | 单请求重试 3 次，仍失败跳过 |
| Chrome 全部不可用 | 无法刷新 Cookie | 续用缓存 Cookie 采集，推告警提醒人工介入 |

### 5.3 系统重启恢复

```
重启后的恢复序列:
  1. PM2 拉起常驻进程 (scheduler/feishu-listener/chrome-guard)
  2. chrome-guard 启动 Chrome 暖池实例 (~3s)
  3. cookie-refresh-daemon 启动，扫描所有 cookie 缓存
     -> 全部过期（进程重启后 expireAt 可能仍有效但 session 已断）
     -> 按直播间优先级排序（开播中的优先）
     -> 串行刷新（15s/账户）
  4. scheduler-engine 启动采集
     -> 已刷新的账户立即采集
     -> 未刷新的标记 skip，下一轮补采
  5. 10-13 分钟内全部 50-80 个账户恢复

  优先级: 开播中的直播间账户 > 有消耗的账户 > 其他
```

### 5.4 Cookie 健康监控

```
Dashboard 新增 Cookie 健康看板:
  账户名 | Cookie 状态 | 剩余有效期 | 上次刷新 | 刷新失败次数
  账户A  | 有效        | 1h 23min  | 36min前  | 0
  账户B  | 即将过期    | 8min      | 1h 52min | 0  <- 预刷新队列中
  账户C  | 过期        | -         | 2h 15min | 3  <- 需人工介入（登录过期）
```

连续 3 次刷新失败推飞书告警：`账户C Cookie 刷新连续失败 3 次，可能登录已过期，需人工检查`

---

## 六、实施优先级

| 优先级 | 优化项 | 依赖 | 预计工时 | 容量影响 |
|-------|-------|------|---------|---------|
| P0 | Cookie 预刷新守护 | 多租户数据库 | 4h | 消除采集阻塞 |
| P0 | 采集并发池 | scheduler-engine | 2h | 5 倍采集吞吐 |
| P1 | API 限流防护 | apiRequest 改造 | 3h | 防止封禁 |
| P1 | Chrome 暖池 | chrome-guard 改造 | 4h | 减少 2s 冷启动 |
| P2 | Cookie 健康看板 | feedback-server | 3h | 可观测性 |
| P2 | 灾难恢复优先级 | 刷新队列排序 | 1h | 恢复速度 |

P0 两项完成后，容量从常驻模式的 15-20 提升到 50-80。P1 防止规模化后的限流风险。P2 提升可运维性。

---

## 七、验收清单

- [ ] cookie-refresh-daemon 在过期前 15min 预刷新，采集期间零阻塞
- [ ] 采集并发池支持可配置并发数（默认 5）
- [ ] apiRequest 识别 429 并指数退避重试
- [ ] 全局令牌桶限流（10 请求/秒起步）
- [ ] Chrome 暖池常驻 1 实例，刷新时加载 Profile 渲染器
- [ ] 50 账户场景下峰值内存 <1GB
- [ ] 50 账户 5min 采集周期内 <30s 完成
- [ ] 系统重启后 50 账户 13 分钟内全部恢复
- [ ] Cookie 连续 3 次刷新失败推飞书告警
- [ ] Dashboard 展示 Cookie 健康状态
