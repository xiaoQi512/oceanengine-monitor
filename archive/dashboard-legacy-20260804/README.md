# 仪表盘旧版留档 2026-08-04

此目录保存旧版仪表盘文件，作为回退和设计参考。

包含内容：

- `dashboard.html/css/js`：v1 旧仪表盘
- `dashboard-v2.html/css/js`：当前生产使用的 v2 仪表盘
- `dashboard-v3.html/css/js`：v3 兼容副本
- `dashboard-preview.html`：离线设计预览版

新版仪表盘入口为 `/dashboard` 和 `/dashboard-v4`，旧版仍可通过 `/dashboard-v2` 访问。
