# 巨量引擎广告计划调整 — 四层架构设计方案（v1.1）

> 日期：2026-07-27
> 范围：`feishu-listener` → `action-queue-worker` → `cdp-action` / `oceanengine-api-client`
> 原则：代码已走得通的保留，不硬拆不新增空概念

---

## 一、架构总览

```
用户 → 接入层 → 命令层 → 执行层 → 审计层
                      ↘ (traceRef 贯通全链路)
```

| 层 | 现有载体 | 改动量 |
|----|---------|--------|
| 接入层 | `feishu-listener.mjs` + `feedback-server:8899` | 不改 |
| 命令层 | `feishu-listener` 的 `parseCommand` + `enqueue` | 重点改 |
| 执行层 | `action-queue-worker.mjs` + `cdp-action.mjs` + `oceanengine-api-client.mjs` | 中量改 |
| 审计层 | `action-audit.jsonl`（归一到 worker） | 小量改 |

### 为什么不是六层

| 候选层 | 归属 | 理由 |
|--------|------|------|
| 原子执行层 | 合并到执行层 | `cdp-action.mjs` + HTTP API 写函数 |
| 结果校验层 | 执行层自检 | `verifyPlanStateWithRetry` 已在 finally 内，强行拆会产生两份校验代码互相打架 |
| 命令审核层 | 保留在命令层 | 仅 `acknowledgeStart` 语义确认，补 pending 表 |
| 状态层 | 合并到审计层 | 双审计文件分散写入，归一即可 |
| 调度层 | 合并到执行层 | `action-queue-worker.mjs` 仅 200 行，与执行强耦合 |
| 接入层 | 保留 | `feishu-listener` + `feedback-server` |

---

## 二、第 1 层：接入层（不改）

### 职责

接收来自飞书群和 Dashboard 的指令，做 @检测、命令解析、Get 表情反馈。

### 入口

| 来源 | 模块 | 方式 |
|------|------|------|
| 飞书群 | `feishu-listener.mjs` | 10s 轮询 `chat-messages-list` |
| Dashboard | `feedback-server:8899` | HTTP POST `/api/actions` |

### 已实现功能（本章不做改动）

- @小七 检测 + `addReaction(Get)` 表情反馈
- 命令前缀匹配：暂停/关停/恢复/加预算/拒绝/执行
- Dashboard CSRF 双验证入队

---

## 三、第 2 层：命令层（重点改）

### 3.1 重复指令确认机制（P0-1，v1.1 调整）

**v1.0 → v1.1 变更**：去掉自动幂等去重，改为每次指令都执行，但对当日同一计划的重复指令增加确认环节。

**问题**：同一 `planName` 相同操作类型被多次入队，Worker 重试 3 次全失败。v1.0 方案采用自动去重跳过，但实际业务中重复指令可能是用户有意为之（如分次调整预算），直接跳过会导致用户指令丢失。

**方案**：每次指令都入队执行，不做自动去重。但在入队前检测当日是否已对同一计划执行过相同操作，若已执行则触发确认流程，要求用户二次确认。

```javascript
// 检查当日是否已有相同计划的相同操作（基于审计日志）
async function checkDuplicateToday(action) {
  const auditLog = loadAuditLog();
  const today = todayDate();
  const duplicates = auditLog.filter(a =>
    a.timestamp?.startsWith(today) &&
    a.planName === action.planName &&
    a.actionType === action.type &&
    a.result?.ok === true
  );
  return duplicates.length > 0 ? duplicates : null;
}

function enqueue(action) {
  // 不再生成幂等键做自动去重，每次都入队
  const traceRef = crypto.randomUUID();
  const q = loadQueue();
  q.actions.push({ ...action, traceRef });
  saveQueue(q);
  return { traceRef, position: q.actions.length };
}
```

**确认流程**：

```javascript
async function acknowledgeStart(action, chatId) {
  // 1. 预检查（见 3.2）
  const precheck = await precheckAction(action);
  if (!precheck.ok) {
    await sendMsg(chatId, `⚠️ ${precheck.reason}`);
    return null;
  }

  // 2. 检测当日重复指令
  const duplicates = await checkDuplicateToday(action);

  if (duplicates) {
    // 重复指令 → 需要二次确认
    const count = duplicates.length;
    const lastTime = duplicates[duplicates.length - 1].timestamp?.slice(11, 19) || '未知';
    await sendMsg(chatId,
      `🟡 当日已对「${action.planName}」执行过 ${count} 次${actionText(action.type)}操作\n` +
      `   最近一次：${lastTime}\n` +
      `   确认要再次执行吗？\n` +
      `   回复"执行"确认 · 回复"拒绝"取消`
    );
    // 标记为 pending 状态，等待用户确认后才入队
    return { status: 'pending_confirm', traceRef: null, isDuplicate: true };
  }

  // 3. 非重复指令 → 正常入队
  const result = await enqueue(action);
  const planInfo = precheck.target;
  const detail = `${planInfo.name} · 当前${planInfo.status} · 预算¥${planInfo.budget}`;
  await sendMsg(chatId,
    `🔵 ${actionText(action.type)}「${action.planName}」\n` +
    `   ${detail}\n` +
    `   已入队 #${result.position} · traceRef: ${result.traceRef}\n` +
    `   3 分钟内回复"执行"确认 · 回复"拒绝"取消`
  );

  return result.traceRef;
}
```

**设计要点**：

| 场景 | 行为 |
|------|------|
| 当日首次对某计划执行某操作 | 正常入队，回复确认信息 |
| 当日已对某计划执行过相同操作 | 不直接入队，提示重复次数和最近时间，要求二次确认 |
| 用户确认"执行" | 入队并分配 traceRef |
| 用户回复"拒绝"或超时 | 取消，不入队 |
| 跨天相同操作 | 视为新指令，正常入队 |

**补充说明**：广告计划在信息流投放中，频繁对同一计划进行暂停/恢复/调价操作可能干扰系统模型学习，导致成本波动或计划进入重新学习期。重复指令确认机制在操作层面提供缓冲，让用户有机会 reconsider 是否真的需要重复操作。

---

### 3.2 命令审核与预检查（P1-3）

**问题**：当前 `acknowledgeStart` 只做语义确认，无计划状态预检查、无用户确认机制、无超时处理。

**方案**：入队前预检查 + `traceRef` 待确认追踪。

```javascript
// 预检查：验证计划存在且状态合理
async function precheckAction(action) {
  const camps = await getCampaignList();
  const target = camps.find(c => c.name.includes(action.planName));
  if (!target) return { ok: false, reason: '未找到计划 ' + action.planName };

  // 暂停已在暂停态的计划无意义
  if (action.type === 'pause' && target.status !== '启用')
    return { ok: false, reason: `计划已处于「${target.status}」状态` };

  // 恢复已在投放中的计划无意义
  if (action.type === 'resume' && target.status === '启用')
    return { ok: false, reason: '计划已在投放中' };

  return { ok: true, target };
}
```

**pending 表设计**：

```javascript
// pending-actions.json — 待确认操作暂存
{
  "pending": [
    {
      "tempId": "uuid",
      "action": { "planName": "...", "type": "pause", "amount": 0 },
      "chatId": "...",
      "createdAt": "2026-07-27T10:00:00Z",
      "expiresAt": "2026-07-27T10:03:00Z",  // 3 分钟超时
      "isDuplicate": true
    }
  ]
}
```

**超时处理**：每 30s 扫描 pending 表，清理过期记录并通知飞书群"操作已超时取消"。

---

### 3.3 学习期保护（P1-4，v1.1 明确暂不考虑）

**v1.1 决策**：学习期保护暂不考虑。

**背景**：巨量引擎的新计划通常需要经历 7 天左右的学习期，在此期间系统需要积累 20-50 个转化事件来建立转化模型。频繁调整出价、定向或预算可能导致模型重置，延长学习期甚至导致"学习期失败"。oCPM 模式下，单计划日预算低于 50 倍 CPA 易学习失败，跑量时大幅修改定向或素材（波动超 30%）会触发重新学习。

**暂不考虑原因**：
- 当前系统定位为执行层工具，学习期保护属于策略层判断
- 学习期状态判断依赖平台 API 返回的计划阶段标识，接口尚未逆向
- 后续如需支持，可在 `precheckAction` 中增加学习期检测逻辑

---

## 四、第 3 层：执行层（整合原子 + 调度 + 校验）

### 4.1 API 优先 + CDP 降级（P2-1）

**问题**：原子层仍是 CDP 点击为主 + API 验证兜底的过渡形态。已逆向出 `updateProjectStatus` HTTP API，但未集成进执行流程。

**方案**：统一封装 `executeAction`，默认走 HTTP API；API 不可用时降级到 CDP。

```javascript
async function executeAction(queuedAction, client) {
  // 统一 entry point，内部按方法优先级选择路径
  const result = await tryHttpApi(queuedAction, client);
  if (result.httpOk) return result;

  // API 不可用 → CDP 降级
  return await tryCdpAction(queuedAction);
}

async function tryHttpApi(action, client) {
  switch (action.type) {
    case 'pause':         return updateProjectStatus(client, { projectId: action.projectId, status: 'pause' });
    case 'resume':        return updateProjectStatus(client, { projectId: action.projectId, status: 'enable' });
    case 'adjust_budget': return updateProjectBudget(client, { projectId: action.projectId, budget: action.amount });
    case 'adjust_bid':    return updateProjectBid(client, { projectId: action.projectId, bid: action.amount });
    default: throw new Error(`未知操作类型: ${action.type}`);
  }
}
```

**优势对比**：

| 维度 | HTTP API | CDP |
|------|----------|-----|
| 幂等性 | 天然幂等（同一参数多次调用结果一致） | 非幂等，依赖状态检查 |
| 响应时间 | < 1s | 5-15s |
| 依赖 | 无浏览器 | 需要 Chrome |
| 错误处理 | 结构化响应 | DOM 解析，易出错 |

---

### 4.2 执行结果自检（替代独立的"结果校验层"）（P1-1）

**问题**：`cdp-action.mjs` 已经在 finally 里调用了 `verifyPlanStateWithRetry` 做 after 校验（resume→检查开关是否启用、budget→检查预算值是否匹配、bid→检查出价值是否更新），还带"CDP 失败但 API 通过则以 API 为准"的降级策略。强行独立一层会导致两份校验并存。

**方案**：保留已有逻辑，声明为"执行层内部自检"，不对外宣称独立层。

```javascript
async function executeWithRetry(action, maxAttempts = 3) {
  for (let attempt = 1; attempt <= maxAttempts; attempt++) {
    const client = await createClient({ useCache: true });
    const result = await executeAction(action, client);

    // 自检：操作是否生效
    const verified = await selfCheck(action, result);
    if (verified.ok) return { ...result, attempts: attempt };

    if (attempt < maxAttempts) await sleep(5000);
  }
  return { ok: false, error: `${maxAttempts}次重试均失败`, attempts: maxAttempts };
}

// 自检 = 原 verifyPlanStateWithRetry 的 wrapper
async function selfCheck(action, result) {
  // pause/resume: 检查 status 是否到达目标态
  // adjust_budget: 检查 campaign_budget 是否匹配目标值
  // adjust_bid: 检查 bid 是否匹配目标值
  // 策略: CDP 失败但 API 返回 ok → 以 API 为准
  // 策略: CDP 成功但 API 验证超时 → 以 CDP 为准（最坏情况已操作成功）
}
```

---

### 4.3 单审计写入点（P0-2）

**问题**：`action-queue-worker.mjs` 的 `writeAudit` 写 `action-audit.jsonl`；`cdp-action.mjs` 的 `writeAuditLog` 又写一份审计。同一条 action 的生命周期被劈到两个文件。

**方案**：cdp-action 不再写审计，只返回结构化 result。worker 作为唯一审计写入点。

```javascript
// action-queue-worker.mjs（唯一审计写入点）
async function processQueue() {
  const q = loadQueue();
  if (!q.actions.length) return;

  const action = q.actions[0];
  const client = await createClient({ useCache: true });

  // 执行
  const result = await executeWithRetry(action);

  // 审计（唯一写入）
  writeAudit({
    traceRef: action.traceRef,
    actionType: action.type,
    planName: action.planName,
    projectId: action.projectId,
    beforeValue: action.amount || action.bid || null,
    afterValue: null, // 执行后可从 API 回读
    result,
    workerPid: process.pid,
    timestamp: new Date().toISOString(),
  });

  // 出队
  q.actions.shift();
  saveQueue(q);

  // 飞书回推
  await reportResult(action);
}
```

---

### 4.4 接口风险管控（P2-9，v1.1 明确暂不考虑）

**v1.1 决策**：目前单机单账户运行，不需要考虑接口风险。

**暂不考虑原因**：
- 当前部署为单机 PM2（`instances: 1`），单账户操作
- 不存在多账户并发调用同一 API 的竞态风险
- 接口调用频率受限于人工指令触发节奏，远低于平台限流阈值

后续如扩展到多账户并行操作，需补充：
- API 调用频率限制（rate limiter）
- 接口异常熔断机制
- 多账户 token 独立管理与轮换

---

## 五、第 4 层：审计层

### 5.1 唯一审计文件

| 文件 | 格式 | 写入者 |
|------|------|--------|
| `action-audit.jsonl` | JSON Lines | 仅 `action-queue-worker.mjs` |

### 5.2 traceRef 传递链路

```
入队时生成     → action.traceRef（命令层）
worker 执行    → result.traceRef（执行层）
审计写入       → audit.traceRef（审计层）
飞书回推       → 回复中包含 traceRef
```

`traceRef` 采用 `crypto.randomUUID()` 生成，全链路贯通，可按 traceRef 查询单次操作的完整生命周期：

```bash
grep "traceRef" action-audit.jsonl | jq 'select(.traceRef == "xxx")'
```

---

### 5.3 聚合日志与回滚策略（P2-8，v1.1 调整）

**v1.1 决策**：只需要有聚合日志即可，不需要自动回滚执行。如果需要回滚，直接读取执行日志进行手动回滚。

**聚合日志设计**：

```javascript
// action-audit.jsonl 每行记录格式
{
  "traceRef": "a1b2c3d4-...",
  "actionType": "adjust_budget",
  "planName": "女装-春装上新",
  "projectId": "123456",
  "beforeValue": 500,
  "afterValue": 800,
  "result": {
    "ok": true,
    "method": "http_api",   // http_api | cdp
    "responseTime": 320,     // ms
    "attempts": 1
  },
  "workerPid": 12345,
  "timestamp": "2026-07-27T14:30:00.000Z"
}
```

**聚合查询示例**：

```bash
# 查看某计划当日所有操作
cat action-audit.jsonl | jq 'select(.planName | contains("女装")) | select(.timestamp | startswith("2026-07-27"))'

# 查看当日所有失败操作
cat action-audit.jsonl | jq 'select(.result.ok == false) | select(.timestamp | startswith("2026-07-27"))'

# 统计当日各操作类型次数
cat action-audit.jsonl | jq -r 'select(.timestamp | startswith("2026-07-27")) | .actionType' | sort | uniq -c
```

**手动回滚流程**：

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 查询日志 | 按 traceRef 或 planName 查询历史操作记录 |
| 2 | 确认 beforeValue | 从审计记录中获取操作前的值 |
| 3 | 下发反向指令 | 通过飞书群或 Dashboard 下发恢复指令 |
| 4 | 验证 | 通过 API 回读确认值已恢复 |

**不做自动回滚的原因**：
- 广告操作回滚需要业务判断（如预算从 800→500 是否合理取决于当前 ROI），不适合自动化
- 自动回滚可能在系统异常时产生连锁错误操作
- 手动回滚流程简单（读日志 → 下指令），耗时 < 1 分钟

---

## 六、双路径选择矩阵

| 操作类型 | HTTP API | CDP | 默认路径 | 备注 |
|---------|:---:|:---:|:---:|------|
| 暂停/启用 | ✅ | ✅ | API 优先 | 已实现 `updateProjectStatus`，<1s 完成 |
| 调整日预算 | ✅ | ✅ | API 优先 | 已实现 `updateProjectBudget` |
| 调整出价 | ✅ | ✅ | API 优先 | 已实现 `updateProjectBid` |
| 新计划创建 | ❌ | ✅ | CDP only | 无 API，需 CDP 模拟 |
| 状态只读 | ✅ | ❌ | API only | `getProjects()` 已成熟 |

---

## 七、文件锁边界标注（P2-3）

当前 `action-queue.json.lock` 使用 `fs.writeFileSync(flag: 'wx')` 文件锁，单机 PM2 多进程适用。

**容器化适配**：如后续启用 Docker/K8s 多实例部署，文件锁将失效。迁移路径：

| 方案 | 改动量 | 适用场景 |
|------|--------|---------|
| 单 Worker 模式 | 低，修改 PM2 配置 `instances: 1` | 容器单实例 |
| Redis 分布式锁 | 中 | 多实例高可用 |
| 数据库锁 | 高 | 有现成 DB 基础设施 |

**当前标注**：单机 PM2 适用（`instances: 1`），容器化时需升级为 Redis 锁或确保单 worker 实例。

---

## 八、多维表格入口（P2-2）

**当前状态**：接入层描述中"多维表格入口"无代码实现。

**如要实现**：飞书多维表格可通过 `lark-cli base` 操作，或监听多维表格 Webhook 事件。

```javascript
// 伪代码：多维表格 → 操作队列
async function listenBaseWebhook() {
  // 监听多维表格中"建议执行"列的变更
  // 提取 planName + actionType + targetValue
  // 入队到 action-queue.json（同样经过重复指令确认机制）
}
```

**优先级**：低。已有飞书群 + Dashboard 双入口覆盖核心场景。多维表格入口按需补充，不阻塞主线。

---

## 九、v1.0 → v1.1 变更摘要

| 编号 | v1.0 方案 | v1.1 调整 | 原因 |
|------|----------|----------|------|
| P0-1 | 幂等键自动去重，相同操作直接跳过 | 去掉自动去重，每次都执行；当日重复指令增加确认环节 | 重复指令可能是有意操作，不能直接丢弃；确认机制兼顾安全与灵活 |
| P1-4 | （未在 v1.0 明确） | 明确暂不考虑学习期保护 | 当前定位为执行层工具，学习期保护属策略层；接口尚未逆向 |
| P2-8 | （审计层基础设计） | 明确只做聚合日志，不做自动回滚 | 回滚需业务判断，手动回滚流程简单且安全 |
| P2-9 | （未在 v1.0 明确） | 明确暂不考虑接口风险 | 单机单账户运行，无并发竞态风险 |

---

## 十、实施顺序

| 序号 | 任务 | 涉及文件 | 优先级 | 备注 |
|:---:|------|---------|:---:|------|
| 1 | 重复指令确认机制 | `feishu-listener.mjs` | P0 | 替换原幂等去重逻辑，增加 pending 表 |
| 2 | 统一审计写入 | `action-queue-worker.mjs` + `cdp-action.mjs` | P0 | cdp-action 移除 writeAuditLog |
| 3 | 预检查 + traceRef | `feishu-listener.mjs` | P1 | 入队前状态校验 |
| 4 | 执行层自检重试 | `action-queue-worker.mjs` | P1 | 替代独立校验层 |
| 5 | API 优先执行策略 | `action-queue-worker.mjs` | P2 | CDP 作为降级路径 |
| 6 | 容器化锁升级 | `action-queue-worker.mjs` | 按需 | 迁移到 Redis 锁 |
| 7 | 多维表格入口 | 新增模块 | 低 | 按需补充 |

---

## 十一、版本记录

| 日期 | 版本 | 变更内容 |
|------|------|---------|
| 2026-07-27 | v1.0 | 初始四层方案，基于代码验证结论设计 |
| 2026-07-27 | v1.1 | P0-1 改为重复指令确认机制（去自动去重）；P1-4 学习期保护暂不考虑；P2-8 明确仅聚合日志不做自动回滚；P2-9 接口风险暂不考虑；新增变更摘要章节 |
