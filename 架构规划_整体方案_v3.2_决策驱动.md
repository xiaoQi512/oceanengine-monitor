# 巨量引擎监控 · 架构升级整体方案 v3.2

> **版本**: v3.2（决策驱动版）| 日期: 2026-06-28
> **基线**: v3.0 控制面板与执行层 + WorkBuddy 审查报告 v2.0
> **核心变化**: 每个决策点写清条件→选项→分析→结论，所有事实经源码核验
> **本文档可直接转发给其他模型做 review**

---

## 0. 决策前置条件（事实环境）

### 0.1 产品定位与使用场景

| 项目 | 事实 | 来源 |
|---|---|---|
| **产品定位** | **个人桌面工具**，非产品/平台/服务 | **用户明确声明** |
| 使用人数 | **仅用户本人** | — |
| 广告账户 | **1 个**巨量引擎账户 (`ACCOUNT_ID = '1842681352509635'`) | api-client.mjs L22 |
| 登录方式 | Cookie 鉴权（邮箱+密码登录，2h 过期） | oec-auto-login.mjs |
| 部署环境 | **Windows 本机单机**，PM2 进程管理 | ecosystem.config.cjs |
| 使用场景 | 本地盯盘 + 飞书群接收推送 + 偶尔远程查看 | — |

> **⚠️ 本方案的刚性约束（所有设计必须遵守）**：
> 1. **单用户**：无用户系统、无登录鉴权、无多租户
> 2. **单账户**：固定 1 个广告账户，`ACCOUNT_ID` 硬编码即可
> 3. **自用**：不上架、不分发、不需要企业资质
> 4. **桌面级**：运行在用户本机，不搞云端部署、不搞 HTTPS 备案
>
> **以下设计全部基于此定位展开。任何多用户/多账户/平台化的设计都属于过度设计，本期不做。**

### 0.2 小程序方案排除说明

> v3.0 主推"飞书小程序 (Taro)"方案，但：
> - 飞书小程序正式版 → 需**企业主体**才能上架
> - 微信小程序 → 需**企业主体 + 备案域名 + HTTPS**
> - 飞书小程序测试版 → 仅开发者本人可用，无分发意义
>
> **结论：小程序方案全部不可行，彻底排除。**
> v3.2 不再讨论小程序/Taro/跨端方案。

### 0.3 当前代码库现状（经源码核验，2026-06-28）

#### 读数据层

| 文件 | 行数 | 函数/功能 | 接入方式 | 状态 |
|---|---|---|---|---|
| `oceanengine-api-client.mjs` | 546 | `fetchSpendingCampaigns` / `fetchAccountReport` / `fetchCampaignSummary` / `fetchPromotionHourly` | HTTP fetch + Cookie 鉴权 | **生产主力** |
| `cdp-client.mjs` | 338 | `quickConnectWithRetry` / `evalJs` / `screenshot` / `navigate` | raw CDP WebSocket (ws 包) | **生产主力** |
| `oec-oauth.mjs` | 208 | `getAccessToken` / `getOAuthLoginUrl` / OAuth token 刷新 | HTTP + 巨量引擎 OAuth | **已完成但仅被 api-client 内部使用** |
| `calibrate-page.mjs` | 341 | 页面校准 v2 (3 次重试 + 多策略回退) | raw CDP `Runtime.evaluate` | **生产主力** |

#### 写操作层

| 文件 | 行数 | 函数/功能 | 接入方式 | 状态 |
|---|---|---|---|---|
| `cdp-action.mjs` | 416 | `togglePlanStatus` / `adjustBudget` / `searchPlanInTable` / `confirmPopupIfAny` / `screenshotOnError` | raw CDP `Input.dispatchMouseEvent` + `Runtime.evaluate` | **✅ 生产级，已在 feishu-listener 中使用** |
| `action-executor.mjs` | 251 | 轮询飞书群消息 → 解析命令 → CDP 执行暂停/放量 | raw CDP WebSocket + lark-cli | **⚠️ 原型级，仅支持暂停和放量** |
| `adjust-budget.mjs` | — | 单独预算调整脚本 | raw CDP | **辅助工具** |
| `autonomous-router.mjs` | — | 智能路由：分析结果 → 生成操作建议 → 分发 | 调用 action-executor | **自治系统** |
| `shadow-arena.mjs` | — | 模拟沙盒：操作前预演 | — | **自治系统** |

#### 推送/交互层

| 文件 | 行数 | 功能 | 状态 |
|---|---|---|---|
| `feedback-server.mjs` | 678 | HTTP 服务 :8899，含建议反馈 + 报表服务 + API 端点 | **生产主力** |
| `feishu-listener.mjs` | — | 飞书群消息监听 → 命令分发 → cdp-action 执行 | **✅ 已集成 cdp-action.mjs** |
| `feishu-push-guard.mjs` | — | 推送频率控制 + 去重 | **生产** |
| `oec-auto-login.mjs` | — | 巨量引擎自动登录（邮箱+密码） | **生产** |

#### 进程管理

| 文件 | 功能 | 状态 |
|---|---|---|
| `ecosystem.config.cjs` | PM2 全家桶配置（8 个进程） | **生产主力** |
| `chrome-guard.mjs` | Chrome 60s 探活 + CDP proxy | **PM2 常驻** |

#### 已有方案文档

| 文件 | 日期 | 内容 | 对 v3.2 的意义 |
|---|---|---|---|
| `巨量引擎监控-小程序上架方案.md` | 6.25 | 数据库设计 + 小程序上架卡点分析 | 已识别企业资质约束，提出 "H5 + 飞书内嵌" 替代路线 |
| `oec-data-strategy-analysis.md` | — | 数据策略分析 | — |
| `oec-architecture-overview.md` | — | 当前架构概览 | — |
| `oec-oauth/` 目录 | — | Python OAuth 原型 (`api_client.py` + `oauth_helper.py`) | 可作为 Marketing API Node.js 实现的参考 |

### 0.4 已有执行能力详表（cdp-action.mjs 实际接口）

```javascript
// cdp-action.mjs 导出函数（416行，生产级）
export async function searchPlanInTable(planName)     // 搜索定位计划行
export async function togglePlanStatus(planName, action)  // action: 'pause' | 'enable' | 'stop'
export async function adjustBudget(planName, amount)   // 修改日预算
export async function confirmPopupIfAny(client)        // 处理 OVUI 二次确认弹窗
export async function screenshotOnError(client, prefix) // 失败截图留证

// 设计约束（已验证）：
// - 全部使用 CDP Input.dispatchMouseEvent / Input.dispatchKeyEvent 真实事件
// - OVUI Vue 组件不响应合成事件（Playwright locator.click 会失效）
// - 失败返回 { ok: false, err: string }，不抛异常
// - 默认 30s 超时 + 重试 + 截图辅助
```

### 0.5 技术约束（不可变）

| 约束 | 事实依据 | 影响 |
|---|---|---|
| **Cookie 2h 过期** | `COOKIE_CACHE_TTL = 2 * 60 * 60 * 1000` (api-client L20) | 采集层必须本机，且需自动续期 |
| **OVUI 只响应真实事件** | MEMORY.md + cdp-action.mjs L10 | 回退方案必须是 raw CDP `Input.dispatch*`，**Playwright 高级 API 不行** |
| **Chrome 必须在线** | 采集层依赖 `--remote-debugging-port=9222` | 关机 = 数据断 + 操作不可用 |
| **单账户** | `ACCOUNT_ID = '1842681352509635'` (api-client L22) | 固定 1 个账户，无需多账户管理 |
| **CDP 数据目录** | `D:\ChromeCDP\User Data\` (非默认目录) | Chrome 149 要求独立 profile |
| **Windows 本机** | 部署在用户 Windows 桌面 | 不搞 Docker/云端/Linux 服务 |

---

## 1. 决策树 D1：前端方案选择

### 决策条件
```
用户场景    → 单用户自用，本地盯盘为主
操作复杂度  → 只读看板 + 简单操作（暂停/启用/调预算）
部署环境    → Windows 本机 localhost
资质条件    → 无企业资质，小程序全部不可行
```

### 选项对比

| 选项 | 开发量 | 维护成本 | 用户体验 | 离线 | 适合自用 |
|---|---|---|---|---|---|
| **A: 单文件 HTML 看板** | **低** | **零** | 中 | ✅ | ✅✅ |
| **B: React + Vite SPA** | 中 | 中 | 高 | 无 | ⚠️ 过重 |
| **C: Electron 桌面应用** | 高 | 高 | 高 | ✅ | ⚠️ 过重 |
| **D: 保持现状（飞书多维表格 + HTML 报表）** | 无 | 零 | 低 | — | ✅ |

### 决策逻辑

```
自用 + 单用户 + 无资质 → 排除所有小程序/跨端方案

if (需要比多维表格更好的交互) {
    if (追求最简实现)  → 选 A（单文件 HTML，feedback-server 直接托管）
    if (追求交互丰富)  → 选 B（React SPA，但对个人工具偏重）
} else {
    → 选 D（保持现状，仅增强后台）
}

⚠️ 不推荐 C（Electron）：
   - 对个人桌面工具引入完整桌面框架 = 过度设计
   - 打包/签名/更新维护成本高
   - feedback-server :8899 已经是本地服务，浏览器访问即可
```

### 🎯 结论

> **推荐 A: 单文件 HTML 看板**
>
> **理由**：
> - **零框架依赖**：纯 HTML + CSS + JS，feedback-server 已有 `/report` 端点，加个 `/dashboard` 即可
> - **零构建步骤**：不需要 Vite/Webpack/npm build，直接写文件
> - **复用现有资产**：oceanengine-report.html 已有 ECharts 图表，看板复用同样技术栈
> - **移动端也能用**：本机局域网内手机浏览器直接访问 `http://<本机IP>:8899/dashboard`
> - **未来可升级**：如果功能变复杂了，再考虑 React SPA 不迟
>
> **页面规划**（精简为 3 个核心视图，单页面 Tab 切换）：
> 1. **实时看板**：消耗/CPA/告警数/今日节奏（复用 report.html 的 ECharts）
> 2. **计划列表**：全量计划 + 暂停/启用/调预算按钮
> 3. **操作日志**：近期操作记录 + 执行结果

---

## 2. 决策树 D2：广告操作执行通道选择

### 决策条件
```
操作类型   → 读/写/高危
API 权限  → 是否已获取 Marketing API 开发者资质
稳定性要求 → 允许 CDP 回退 vs 必须 100% 成功
```

### 已有执行能力盘点

| 能力 | 来源 | 接口数 | 生产验证 |
|---|---|---|---|
| **读数据（HTTP）** | `oceanengine-api-client.mjs` | 4 函数 | ✅ 生产 3+ 个月 |
| **读数据（CDP 回退）** | `cdp-client.mjs` + `calibrate-page.mjs` | 多函数 | ✅ 生产 |
| **写操作（CDP）** | `cdp-action.mjs` | 3 核心函数 | ✅ 已在 feishu-listener 生产使用 |
| **写操作（API）** | `oec-oauth.mjs` | OAuth 已完成，**0 个写函数** | ⚠️ 无写 API 实现 |
| **写操作（Python 原型）** | `oec-oauth/api_client.py` | 有参考代码 | ⚠️ 非生产 |

### 选项对比

| 选项 | 延迟 | 成功率 | 审计能力 | 反检测风险 | 开发量 |
|---|---|---|---|---|---|
| **A: Marketing API 主通道** | 低 (<1s) | 高 | ✅ 完整 | 无 | **高**（需申请资质 + 实现 4+ 写函数） |
| **B: raw CDP 主通道** | 高 (5-15s) | 中 | ⚠️ 需自建 | **有** | **低**（cdp-action.mjs 已就绪） |
| **C: A + B 混合** | 低/中 | 高 | ✅ | 低 | 中 |

### 决策逻辑

```
自用工具 → 不急着搞 Marketing API 资质（申请周期长、对个人工具意义不大）

if (已获得 Marketing API 开发者资质 && 写函数已实现) {
    → 选 A（纯 API）
} else if (未申请资质 || 资质审核中) {
    → 选 B（纯 CDP，cdp-action.mjs 已有）
    → 是否申请 Marketing API 资质 → 看后续需求，不阻塞
} else {
    → 选 C（混合模式）
}
```

### 🎯 结论

> **推荐 B: raw CDP 主通道（Phase 1 即用）**
>
> **理由**：
> - cdp-action.mjs 已有 3 个核心写函数，已在 feishu-listener 生产验证
> - 自用工具不需要为 Marketing API 资质投入时间
> - 如果后续确实需要 API 通道（比如 Chrome 不稳定），再异步推进 C 方案
>
> **⚠️ Playwright 不适合作为执行通道**：
> - OVUI Vue 组件要求 `Input.dispatchMouseEvent` 真实事件
> - Playwright 的 `locator.click()` 内部用 `Element.dispatchEvent`，OVUI 不响应
> - 如果一定要用 Playwright，需调用其底层 CDP session API，但这等于回到了 raw CDP

---

## 3. 决策树 D3：数据存储方案选择

### 决策条件
```
数据量级     → 当前 ~640 行/天 × 365 天 ≈ 23 万行/年
查询模式     → 时序查询 + 聚合分析 + 实时看板
部署环境     → Windows 本机（无 Docker / 无 Linux 服务）
运维复杂度   → 越低越好
并发需求     → 无（单用户，不存在并发写）
```

### 现状

```
当前存储：monitor-data/*.json 散文件
├── daily-YYYY-MM-DD.json         (账户级日快照，~36 字段/条)
├── YYYY-MM-DDTHH-MM-SS.json      (采集明细，含 active/allSpending 数组)
├── suggestion-history.json        (建议反馈)
└── daemon-health.json             (守护状态)
```

### 选项对比

| 选项 | 性能 | 运维 | 分析能力 | 迁移成本 | 适配 Windows |
|---|---|---|---|---|---|
| **A: SQLite** | 优 | 零 | 中（无 OLAP） | 低 | ✅ 原生 |
| **B: SQLite + DuckDB** | 优 | 低 | **优（OLAP）** | 中 | ✅ 原生 |
| **C: PostgreSQL** | 优 | 高 | 优 | 高 | ⚠️ 需 Docker |
| **D: 保持 JSON** | 差 | 零 | 差 | 无 | ✅ |

### 决策逻辑

```
单用户 + 本机部署 → 排除 PostgreSQL（不需要服务端数据库）

if (数据量 < 100 万行 && 不需要复杂 OLAP) {
    → 选 A（SQLite 足够）
} else if (需要时序分析 + 聚合查询 + 低运维) {
    → 选 B（SQLite 主存 + DuckDB 分析）
    → DuckDB 的 ATTACH 'oceanengine.db' (TYPE SQLITE) 可直读 SQLite
}
```

### 🎯 结论

> **推荐 B: SQLite + DuckDB**（v2.0 已确定，v3.2 沿用并补充细节）
>
> **理由**：
> - SQLite 单文件、零运维、Windows 原生，**对个人工具最友好**
> - DuckDB 列式存储做 OLAP 分析，可直接 ATTACH SQLite 文件
> - 23 万行/年，SQLite 单表性能无忧（10 亿行以下都没问题）
> - Parquet 归档压缩历史数据（>90 天的数据导出为 .parquet）
> - **不需要并发控制**（单用户无并发写），简化设计

---

## 4. 决策树 D4：推送/实时通信方案选择

### 决策条件
```
推送触发方  → 监控脚本主动推送 vs 前端主动拉取
实时性要求  → 秒级 vs 分钟级 vs 手动刷新
使用场景    → 本地盯盘（浏览器）+ 飞书群接收推送
```

### 现状

```
当前推送链路：
monitor-v3 → lark-cli → 飞书群（5min/15min 定时 + 告警即时）
feedback-server :8899 → 飞书卡片按钮回调（建议反馈）
```

### 选项对比

| 选项 | 实时性 | 复杂度 | 适合自用 |
|---|---|---|---|
| **A: 手动刷新** | 无 | 零 | ✅（最简单） |
| **B: 定时轮询 (setInterval)** | 分钟级 | 低 | ✅✅ |
| **C: SSE (Server-Sent Events)** | 秒级 | 中 | ⚠️ 对自用偏重 |
| **D: WebSocket (自建 Server)** | 秒级 | 高 | ❌ 过度设计 |

### 事实澄清

> **v3.0 提到的 "飞书 WebSocket 实时推送" 不存在。** 飞书开放平台没有通用 WebSocket 推送能力。

### 🎯 结论

> **推荐 B: 定时轮询（30s 间隔）**
>
> **理由**：
> - 个人工具不需要秒级实时性，30s 轮询足够
> - `setInterval(() => fetch('/api/snapshots?since=...'), 30000)` — 5 行代码搞定
> - 飞书推送保持不变（5min/15min 定时 + 告警即时），这是用户盯盘外的补充通道
> - **不需要 SSE/WebSocket**：对单用户工具来说复杂度过高、收益过低

---

## 5. 决策树 D5：操作确认机制设计

### 决策条件
```
操作风险等级 → 对广告消耗的影响程度
使用场景    → 单用户自用，自己操作自己确认
误操作代价   → 恢复难度 + 业务损失
```

### 操作分类

| 等级 | 操作类型 | 影响 | 确认方式 |
|---|---|---|---|
| **L0 无风险** | 查看数据 / 导出报表 | 无 | 无确认 |
| **L1 低风险** | 暂停单个计划 / 启用已暂停计划 | 单计划消耗停止/恢复 | **前端弹窗确认**（"确认暂停 xxx？"） |
| **L2 中风险** | 调整预算 / 调整出价 | 消耗速率变化 | **前端弹窗 + 显示变化量**（"预算 5000→8000，确认？"） |
| **L3 高风险** | 批量暂停 / 删除计划 | 多计划或不可逆变动 | **前端弹窗 + 二次输入确认**（输入计划名确认） |

### 与 v3.0 的差异

> **v3.0 的 L2 飞书卡片审批 + L3 飞书正式审批流 → 全部删除。**
> 理由：单用户自用工具，自己操作自己确认，不需要推给别人审批。
> 飞书推送仅用于**通知操作结果**（"✅ 已暂停 xxx"），不用于审批。

### 操作记录

```javascript
// optimization_actions 表 — 不是审批流，是操作日志
// 目的：记录"做了什么"，方便回溯和复盘
{
  action_id: 'uuid',
  action_type: 'pause',           // pause/enable/update_budget
  campaign_id: '7452xxx',
  campaign_name: 'xxx计划',
  params: { budget: 8000 },       // 操作参数
  execution_result: { ok: true }, // 执行结果
  channel: 'cdp',                 // cdp/api
  created_at: '2026-06-28T...'
}
```

### 🎯 结论

> **前端弹窗确认 + 操作日志记录（无审批流）**
> - L1: 确认弹窗
> - L2: 确认弹窗 + 显示变化量
> - L3: 确认弹窗 + 二次输入确认（防误触）
> - 所有操作记录入 optimization_actions 表（操作日志，非审批流）

---

## 6. 决策树 D6：后端架构演进

### 决策条件
```
当前服务 → feedback-server.mjs (678行, :8899, Express-like)
新增需求 → 看板页面 + 操作执行 + 操作日志 + 计划列表 API
约束     → 单机 Windows, PM2 管理, 单用户
```

### 选项对比

| 选项 | 架构 | 优点 | 缺点 |
|---|---|---|---|
| **A: 扩展 feedback-server** | 单进程，加路由 + 看板 | **最快，零部署** | feedback-server 变臃肿 |
| **B: 新建 operation-executor** | 独立进程 :8900 | 职责分离 | 多一层通信，对个人工具偏重 |
| **C: A + B 组合** | feedback-server 做 API + executor 做执行 | 职责清晰 | 对个人工具过度设计 |

### 决策逻辑

```
单用户自用 + 单机部署 → 进程越少越好

if (需要高可用/分布式) {
    → 选 B 或 C（独立进程）
    → 但自用工具不需要
} else {
    → 选 A（扩展 feedback-server）
    → 一个进程搞定一切
}
```

### 🎯 结论

> **推荐 A: 扩展 feedback-server（单进程）**
>
> **理由**：
> - 个人工具不需要微服务化，一个进程足够
> - feedback-server 已有 678 行，加几个路由不会质变
> - 减少 PM2 进程数 = 减少资源占用 = 减少维护
>
> **新增路由设计**：
> ```
> feedback-server :8899
> ├── GET  /dashboard          → HTML 看板页面（单文件）
> ├── GET  /api/campaigns      → 计划列表（调 api-client）
> ├── GET  /api/snapshots      → 快照查询（SQLite）
> ├── GET  /api/actions        → 操作日志查询
> ├── POST /api/actions        → 提交操作 → 调 cdp-action → 记录日志
> ├── GET  /report             → HTML 报表（已有）
> └── POST /feedback           → 建议反馈（已有）
> ```
>
> **已有模块映射**：

| 现有模块 | v3.2 中的角色 | 改造方式 |
|---|---|---|
| `feedback-server.mjs` (678行) | **唯一的后端服务** | 新增看板 + API 路由 |
| `cdp-action.mjs` (416行) | **操作执行模块** | 被 feedback-server import 调用 |
| `action-executor.mjs` (251行) | **废弃** | 功能合并到 feedback-server 路由 |
| `feishu-listener.mjs` | **飞书指令入口** | 保留，改为调用 cdp-action |
| `oceanengine-api-client.mjs` (546行) | **数据采集核心** | 保持不变 |
| `autonomous-router.mjs` | **自治决策引擎** | 保留 |
| `shadow-arena.mjs` | **操作预演沙盒** | 保留 |
| `chrome-guard.mjs` | **Chrome 健康守护** | 保持不变 |
| `calibrate-page.mjs` (341行) | **页面校准** | 保持不变 |

---

## 7. 决策树 D7：数据库 Schema 设计

### 设计原则

1. **极简**：单用户单账户，去掉 platform / account_id 等扩展字段
2. 复合主键（日期+业务ID，v2.0 共识）
3. 与已有 JSON 结构对齐（减少迁移映射工作）
4. 不做过度预留（未来需要时再加字段，SQLite ALTER TABLE 很方便）

### 核心表

```sql
-- 计划快照表（核心时序表，替代 active/allSpending JSON 数组）
CREATE TABLE campaign_snapshots (
    snapshot_date   TEXT NOT NULL,     -- YYYY-MM-DD
    snapshot_time   TEXT NOT NULL,     -- HH:MM:SS
    campaign_id     TEXT NOT NULL,
    campaign_name   TEXT,
    status          TEXT,              -- 投放中/已暂停/超出预算
    cost            REAL,              -- 消耗（元）
    leads           INTEGER,           -- 线索数
    conversions     INTEGER,           -- 转化数
    private_msg_open INTEGER,          -- 私信开口
    private_msg_lead INTEGER,          -- 私信留资
    form_submit     INTEGER,           -- 表单提交
    ctr             REAL,              -- 点击率
    cpm             REAL,              -- 千次展示费用
    cvr             REAL,              -- 转化率
    views           INTEGER,           -- 观看数
    view_gt_1min    INTEGER,           -- >1min 观看
    comments        INTEGER,           -- 评论数
    budget          REAL,              -- 日预算
    cpa             REAL,              -- 计算字段
    PRIMARY KEY (snapshot_date, snapshot_time, campaign_id)
);
CREATE INDEX idx_cs_date ON campaign_snapshots(snapshot_date);
CREATE INDEX idx_cs_campaign ON campaign_snapshots(campaign_id, snapshot_date);

-- 账户级日快照（替代 daily-YYYY-MM-DD.json）
CREATE TABLE daily_snapshots (
    snapshot_date   TEXT NOT NULL,
    snapshot_time   TEXT NOT NULL,
    total_cost      REAL,
    total_leads     INTEGER,
    total_conversions INTEGER,
    active_campaigns INTEGER,
    paused_campaigns INTEGER,
    alerts_count    INTEGER,
    -- 其余汇总字段按需添加
    PRIMARY KEY (snapshot_date, snapshot_time)
);

-- 告警表
CREATE TABLE alerts (
    alert_date      TEXT NOT NULL,     -- YYYY-MM-DD
    alert_id        TEXT NOT NULL,
    alert_type      TEXT NOT NULL,     -- speed/cpa_rise/budget/zero_conv/...
    severity        TEXT NOT NULL,     -- high/medium/low
    campaign_id     TEXT,
    campaign_name   TEXT,
    message         TEXT,
    created_at      TEXT DEFAULT (datetime('now')),
    PRIMARY KEY (alert_date, alert_id)
);

-- 操作日志表（注意：是日志，不是审批流）
CREATE TABLE operation_log (
    log_date        TEXT NOT NULL,     -- YYYY-MM-DD
    log_id          TEXT NOT NULL,     -- UUID
    campaign_id     TEXT NOT NULL,
    campaign_name   TEXT,
    action_type     TEXT NOT NULL,     -- pause/enable/update_budget/update_bid
    params_json     TEXT,              -- JSON: { "budget": 10000 }
    confirm_level   TEXT NOT NULL,     -- L1/L2/L3（确认等级）
    execution_result TEXT,             -- JSON: { "ok": true }
    channel         TEXT DEFAULT 'cdp',-- cdp/api（执行通道）
    created_at      TEXT DEFAULT (datetime('now')),
    PRIMARY KEY (log_date, log_id)
);
CREATE INDEX idx_ol_campaign ON operation_log(campaign_id, log_date);

-- 推送日志表
CREATE TABLE push_logs (
    push_date       TEXT NOT NULL,
    push_id         TEXT NOT NULL,
    push_type       TEXT NOT NULL,     -- 5min/15min/daily/alert
    channel         TEXT NOT NULL DEFAULT 'feishu',
    content_hash    TEXT,
    sent_at         TEXT DEFAULT (datetime('now')),
    PRIMARY KEY (push_date, push_id)
);

-- Schema 版本管理
CREATE TABLE schema_migrations (
    version         INTEGER PRIMARY KEY,
    description     TEXT,
    applied_at      TEXT DEFAULT (datetime('now'))
);
```

### 与 v3.0 Schema 的关键差异

| 差异 | v3.0 | v3.2 | 理由 |
|---|---|---|---|
| **platform 列** | 每表都有 `platform TEXT NOT NULL DEFAULT 'oceanengine'` | **删除** | 单平台，不需要 |
| **account_id 列** | 每表都有 `account_id TEXT NOT NULL` | **删除** | 单账户，不需要 |
| **advertisers 表** | 独立的账户表 | **删除** | 单账户硬编码即可 |
| **optimization_actions** | 含 approval_level/approval_status/approved_by/lock_token | 改为 **operation_log**，仅记录操作+结果 | 无审批流、无并发 |
| **主键** | `(date, platform, account_id, id)` | `(date, id)` 或 `(date, time, campaign_id)` | 简化 |
| **表数量** | 7 张 | **6 张** | 去掉 advertisers |

### 🎯 设计决策

| 决策点 | 选择 | 理由 |
|---|---|---|
| 主键 | 复合主键（日期+业务ID） | v2.0 共识，但去掉 platform/account_id |
| 时间戳 | ISO 8601 文本（`datetime('now')`） | SQLite 原生，可读性好 |
| JSON 字段 | `params_json` / `execution_result` | 灵活参数，避免宽表 |
| WAL 模式 | 开启 | 提升读写性能，虽然单用户但推荐 |

---

## 8. 整体架构图（v3.2）

```
┌──────────────────────────────────────────────────────────────────┐
│                     用户交互                                      │
│                                                                  │
│  ┌────────────────────┐   ┌────────────────────────────────┐    │
│  │ 浏览器（本机/手机）  │   │ 飞书群（推送 + 指令）           │    │
│  │ http://127.0.0.1   │   │ 5min/15min 定时 + 告警即时      │    │
│  │   :8899/dashboard  │   │ 飞书卡片按钮 → 建议反馈          │    │
│  │   :8899/report     │   │ 群消息 → 暂停/放量 命令          │    │
│  └────────┬───────────┘   └───────────────┬────────────────┘    │
│           │ HTTP                           │ lark-cli            │
└───────────┼────────────────────────────────┼────────────────────┘
            │                                │
            ▼                                ▼
┌──────────────────────────────────────────────────────────────────┐
│               feedback-server :8899（唯一后端）                   │
│                                                                  │
│  看板 + API 路由：                                                │
│  ├── GET  /dashboard         → 单文件 HTML 看板                  │
│  ├── GET  /api/campaigns     → 计划列表（调 api-client）         │
│  ├── GET  /api/snapshots     → 快照查询（SQLite）               │
│  ├── GET  /api/actions       → 操作日志查询                     │
│  ├── POST /api/actions       → 提交操作 → 前端确认 → 执行       │
│  ├── GET  /report            → HTML 报表（已有）                │
│  └── POST /feedback          → 建议反馈（已有）                 │
│                                                                  │
│  操作执行（import cdp-action.mjs）：                              │
│  ├── togglePlanStatus()      → 暂停/启用                        │
│  ├── adjustBudget()          → 调预算                           │
│  └── screenshotOnError()     → 执行截图留证                     │
│                                                                  │
│  数据采集（import oceanengine-api-client.mjs）：                  │
│  ├── fetchSpendingCampaigns()                                    │
│  ├── fetchAccountReport()                                        │
│  └── fetchCampaignSummary()                                      │
└──────────────────────────────────────────────────────────────────┘
            │
            ▼
┌──────────────────────────────────────────────────────────────────┐
│               存储层 (SQLite + DuckDB)                            │
│                                                                  │
│  SQLite oceanengine.db                                           │
│  ├── campaign_snapshots   (计划级时序，~640 行/天)               │
│  ├── daily_snapshots      (账户级日快照)                         │
│  ├── alerts               (告警)                                 │
│  ├── operation_log        (操作日志)                              │
│  ├── push_logs            (推送日志)                              │
│  └── schema_migrations    (版本管理)                              │
│                                                                  │
│  DuckDB (分析层，按需启动)                                        │
│  └── ATTACH 'oceanengine.db' (TYPE SQLITE)                       │
│      + Parquet 归档 (>90 天数据)                                  │
│                                                                  │
│  JSON (兼容层，双写 14 天后退役)                                   │
│  └── monitor-data/*.json                                         │
└──────────────────────────────────────────────────────────────────┘
            │
            ▼
┌──────────────────────────────────────────────────────────────────┐
│               采集层（不变）                                      │
│                                                                  │
│  oceanengine-api-client.mjs  ← HTTP 主采集（Cookie 鉴权）        │
│  ├── cdp-client.mjs          ← Cookie 提取 + 回退采集            │
│  ├── calibrate-page.mjs      ← 页面校准                         │
│  └── oec-auto-login.mjs      ← Cookie 过期自动登录              │
│                                                                  │
│  PM2 进程：                                                      │
│  ├── pm2-5min    (5 分钟速报)                                    │
│  ├── pm2-15min   (15 分钟监控)                                   │
│  ├── pm2-daily   (日报)                                          │
│  ├── feedback-server (常驻)                                      │
│  └── chrome-guard  (常驻)                                        │
└──────────────────────────────────────────────────────────────────┘
```

---

## 9. 实施路线（3 阶段 · 3-4 周）

> **对比 v3.0 的 5-6 周 → v3.2 缩减为 3-4 周**
> 原因：去掉小程序开发、去掉审批流、去掉多账户支持、前端简化为单文件 HTML

### 阶段总览

| 阶段 | 工期 | 内容 | 交付物 |
|---|---|---|---|
| **S1 数据基建** | 1 周 | SQLite 建表 + JSON 迁移 + 双写 | `oceanengine.db` + 迁移脚本 |
| **S2 执行层 + API** | 1 周 | feedback-server 扩展路由 + cdp-action 集成 + 操作日志 | API 端点 + 操作能力 |
| **S3 前端看板** | 1-2 周 | 单文件 HTML 看板 + 计划列表 + 操作面板 | `/dashboard` 页面 |

### S1 数据基建（第 1 周）

| 任务 | 输入 | 输出 | 验证标准 |
|---|---|---|---|
| 1.1 建 SQLite 表 | §7 DDL | `oceanengine.db` | 6 张表 + 索引创建成功 |
| 1.2 JSON 历史导入 | `monitor-data/*.json` | 全量历史数据入库 | 行数一致 + 抽样校验 |
| 1.3 双写集成 | `oceanengine-api-client.mjs` | 采集同时写 SQLite + JSON | JSON vs DB 数据一致 |
| 1.4 迁移脚本 | — | `db-migrate.mjs` | 可重复执行，幂等 |

### S2 执行层 + API（第 2 周）

| 任务 | 输入 | 输出 | 验证标准 |
|---|---|---|---|
| 2.1 feedback-server 新增路由 | §6 路由设计 | 5 个新 API 端点 | curl 验证 |
| 2.2 集成 cdp-action | `cdp-action.mjs` | POST /api/actions 可执行操作 | 暂停/启用/调预算跑通 |
| 2.3 操作日志记录 | operation_log 表 | 每次操作自动记录 | 查询日志正确 |
| 2.4 前端确认弹窗交互 | — | POST 前弹出确认 | L1/L2/L3 各跑通 |

### S3 前端看板（第 3-4 周）

| 任务 | 输入 | 输出 | 验证标准 |
|---|---|---|---|
| 3.1 看板 HTML | ECharts + 原生 JS | `/dashboard` 页面 | 消耗/CPA/告警数实时展示 |
| 3.2 计划列表视图 | `/api/campaigns` | 全量计划 + 筛选排序 | 与多维表格数据一致 |
| 3.3 操作面板 | `/api/actions` | 暂停/启用/调预算按钮 | 弹窗确认 → 执行 → 日志 |
| 3.4 定时刷新 | setInterval 30s | 数据自动更新 | 30s 后数据变化 |
| 3.5 移动端适配 | CSS 响应式 | 手机浏览器可用 | 局域网内手机访问正常 |

### S4 增强（按需，不阻塞上线）

| 任务 | 触发条件 | 内容 |
|---|---|---|
| 4.1 Marketing API 写函数 | CDP 不稳定 / Chrome 频繁掉线 | 参考 `oec-oauth/api_client.py` 移植 |
| 4.2 DuckDB 分析 | 需要复杂聚合查询 | ATTACH SQLite + Parquet 归档 |
| 4.3 远程访问 | 出门时也需要看数据 | 内网穿透 (frp/ngrok) 或 Tailscale |

---

## 10. 风险矩阵

| # | 风险 | 概率 | 影响 | 缓解措施 |
|---|---|---|---|---|
| R1 | Chrome 离线 → CDP 操作不可用 | 中 | 高 | chrome-guard 守护 + 操作失败自动重试 |
| R2 | Cookie 过期 → 采集中断 | 中 | 高 | oec-auto-login + Cookie TTL 监控 + 提前 5min 刷新 |
| R3 | OVUI 页面改版 → CDP 选择器失效 | 中 | 高 | calibrate-page v2 多策略回退 + 选择器版本化 |
| R4 | 操作误触（误暂停高 ROI 计划） | 中 | 高 | 前端确认弹窗 + 操作日志回溯 |
| R5 | access_token 过期 | 低 | 中 | oec-oauth 自动刷新 + 重试 |
| R6 | SQLite 数据文件损坏 | 低 | 高 | WAL 模式 + 每日自动备份（cron 复制 .db 文件） |
| R7 | Marketing API 限流 | 中 | 中 | Phase 1 不用 API；Phase 2 指数退避 + 429 检测 |
| R8 | 本机 IP 变化 → 手机访问失败 | 低 | 低 | 看板绑定 `0.0.0.0:8899`，手机重连时刷新 |

---

## 11. 给后续 Agent 的实施指引

> **⚠️ 以下 7 条是必须遵守的硬性约束。**

### 11.1 这是个人工具，不是产品

```
不要做：
- 用户系统 / 登录鉴权 / JWT / session
- 多租户 / 多账户 / 多平台
- 审批流 / 权限管理 / 角色系统
- Docker / 云端部署 / HTTPS / 备案
- 小程序 / Taro / 跨端框架
- SSE / WebSocket / 实时推送（30s 轮询足够）
- React / Vue / Angular 前端框架（单文件 HTML 足够）

要做：
- 极简、够用、快速交付
- 所有代码跑在 Windows 本机
- 浏览器访问 localhost:8899
- 飞书群作为通知通道
```

### 11.2 CDP 操作必须用真实事件

```javascript
// ✅ 正确 — cdp-action.mjs 已有实现
await client.send('Input.dispatchMouseEvent', {
  type: 'mousePressed', x, y, button: 'left', clickCount: 1
});
await client.send('Input.dispatchMouseEvent', {
  type: 'mouseReleased', x, y, button: 'left', clickCount: 1
});

// ❌ 错误 — Playwright locator.click() / Element.click() / dispatchEvent
// OVUI Vue 组件不响应合成事件，这些都会静默失败
```

### 11.3 复用已有模块，不要重写

| 需要的能力 | 已有模块 | 行数 | 做法 |
|---|---|---|---|
| 暂停/启用计划 | `cdp-action.mjs` | 416 | import 调用 |
| 调整预算 | `cdp-action.mjs` | 416 | import 调用 |
| 搜索计划 | `cdp-action.mjs` | 416 | import 调用 |
| Cookie 提取 | `oceanengine-api-client.mjs` | 546 | import 调用 |
| 页面校准 | `calibrate-page.mjs` | 341 | import 调用 |
| CDP 连接 | `cdp-client.mjs` | 338 | import 调用 |
| 飞书推送 | `feedback-server.mjs` | 678 | 新增路由 |

### 11.4 不要退役 cdp-client.mjs

```
cdp-client.mjs 是以下模块的基础依赖：
├── oceanengine-api-client.mjs (Cookie 提取)
├── cdp-action.mjs (所有写操作)
├── calibrate-page.mjs (页面校准)
├── chrome-guard.mjs (健康检查)
└── 5min/15min 监控脚本 (数据采集)

退役它 = 整个系统瘫痪。
```

### 11.5 SQLite 操作规范

```javascript
// ✅ 正确 — 使用 better-sqlite3（同步 API，性能最优）
const Database = require('better-sqlite3');
const db = new Database('oceanengine.db', { wal: true });

// ✅ 正确 — 使用参数化查询
db.prepare('INSERT INTO campaign_snapshots VALUES (?,?,?,?,?)').run(date, time, ...);

// ❌ 错误 — 字符串拼接 SQL（注入风险，虽然自用工具但也要规范）
db.exec(`INSERT INTO ... VALUES ('${userInput}')`);
```

### 11.6 前端实现规范

```
看板 = 单文件 HTML（dashboard.html）
├── ECharts 5 CDN 引入（与 report.html 一致）
├── 原生 JS fetch() 调 API
├── 原生 CSS（响应式，兼容手机浏览器）
├── setInterval 30s 刷新
└── 无构建步骤，feedback-server 直接托管静态文件

不需要：
- React / Vue / 任何前端框架
- npm / webpack / vite / 任何构建工具
- TypeScript / JSX / 任何编译步骤
```

### 11.7 双写与迁移策略

```
Week 1: JSON 为主，SQLite 为辅（双写）
  - 采集脚本同时写 JSON + SQLite
  - 前端仍然读 JSON

Week 2: SQLite 为主，JSON 为辅
  - 看板/API 切到 SQLite 查询
  - JSON 继续写但降为备份

Week 3+: 退役 JSON 写入
  - 仅保留 SQLite
  - JSON 文件归档
```

---

## 12. 与 v3.0 的关键差异

| 维度 | v3.0 | v3.2 |
|---|---|---|
| **产品定位** | 产品/平台 | **个人桌面工具** |
| **用户模型** | 多用户/多账户/多平台 | **单用户/单账户/单平台** |
| **前端** | 飞书小程序 (Taro) + H5 兜底 | **单文件 HTML 看板**，feedback-server 直接托管 |
| **前端框架** | React + Taro + ECharts | **无框架**，原生 JS + ECharts CDN |
| **执行通道** | Marketing API 主 + Playwright 回退 | **raw CDP 主**（cdp-action.mjs 已有） |
| **审批流** | L0-L3 四级审批 + 飞书审批卡片 + 正式审批实例 | **前端弹窗确认** + 操作日志记录 |
| **后端架构** | feedback-server + operation-executor 双进程 | **feedback-server 单进程**（扩展路由） |
| **实时通信** | WebSocket / SSE | **30s 定时轮询** |
| **Schema** | 7 张表，每表含 platform + account_id | **6 张表**，无 platform/account_id |
| **advertisers 表** | 独立账户表 | **删除**（单账户硬编码） |
| **optimization_actions** | 含审批字段 + 并发锁 | 改为 **operation_log**（纯日志） |
| **小程序** | 核心方案 | **彻底排除**（无企业资质） |
| **Taro** | 核心框架 | **彻底排除** |
| **Playwright** | 退役 cdp-client 换 playwright | **明确不兼容 OVUI**，保留 cdp-client |
| **已有模块** | 未提及 cdp-action / feishu-listener 等 | 完整映射到 v3.2 角色 |
| **工期** | 5-6 周 | **3-4 周**（减少 ~40%） |
| **阶段数** | 4 阶段 | **3 阶段** |
| **SSE/WS** | 需要 | **不需要** |
| **企业资质** | 未提及 | **明确无资质，排除所有小程序方案** |

---

## 13. 附录

### 13.1 完整文件清单

| 文件 | 行数 | 角色 | v3.2 状态 |
|---|---|---|---|
| `oceanengine-api-client.mjs` | 546 | 数据采集 | **保持不变** |
| `cdp-client.mjs` | 338 | CDP 连接 | **保持不变** |
| `cdp-action.mjs` | 416 | 写操作 | **被 feedback-server import** |
| `calibrate-page.mjs` | 341 | 页面校准 | **保持不变** |
| `monitor-utils.mjs` | — | 共享工具 | **保持不变** |
| `wait-utils.mjs` | — | 等待工具 | **保持不变** |
| `feedback-server.mjs` | 678 | HTTP 服务 | **扩展 API + 看板路由** |
| `feishu-listener.mjs` | — | 飞书监听 | **改为调用 cdp-action** |
| `action-executor.mjs` | 251 | 原型执行器 | **废弃，功能合并** |
| `autonomous-router.mjs` | — | 自治路由 | **保持不变** |
| `shadow-arena.mjs` | — | 模拟沙盒 | **保持不变** |
| `chrome-guard.mjs` | — | Chrome 守护 | **保持不变** |
| `oec-auto-login.mjs` | — | 自动登录 | **保持不变** |
| `feishu-push-guard.mjs` | — | 推送控制 | **保持不变** |
| `adaptive-scheduler.mjs` | — | 自适应调度 | **保持不变** |
| `ecosystem.config.cjs` | — | PM2 配置 | **保持不变** |

### 13.2 术语表

| 术语 | 解释 |
|---|---|
| CDP | Chrome DevTools Protocol，浏览器调试协议 |
| OVUI | 巨量引擎前端 UI 框架（基于 Vue） |
| raw CDP | 直接使用 WebSocket 发送 CDP 命令，不经过 Playwright 等封装 |
| WAL | SQLite Write-Ahead Logging，提升读写性能 |

### 13.3 待其他模型 Review 的重点

1. **D1 前端**：单文件 HTML 看板是否够用？是否应该在第一版就上 React SPA？
2. **D2 执行通道**：纯 CDP 对自用工具是否足够可靠？Chrome 频繁掉线时的降级策略？
3. **D7 Schema**：去掉 platform/account_id 后是否影响未来扩展（如果确实需要多账户时）？
4. **S2 执行层**：feedback-server 单进程承载 API + 操作执行是否会阻塞？
5. **操作确认**：前端弹窗确认（无审批流）对高风险操作是否足够安全？
6. **双写策略**：JSON→SQLite 迁移的回滚方案是否充分？
7. **工期评估**：3-4 周对 3 阶段是否合理？

---

*本文档所有事实均经源码核验。引用模块的行数、函数签名、依赖关系均来自 2026-06-28 实际代码扫描。未经验证的假设已明确标注。*
