# 线索直播广告监控 - 多租户架构设计

> 日期：2026-07-26
> 状态：设计阶段，待评审
> 目标：将现有单账户监控系统改造为多运营、多直播间、多广告账户的通用监控平台，适配巨量引擎线索直播广告场景

---

## 一、设计目标与核心问题

### 1.1 现状

当前系统是一个**单租户单账户**的巨量引擎监控工具，绑定一个广告账户（`OEC_ACCOUNT_ID`）、一个 Chrome Profile（Profile 4）、一个飞书群、一组排班表。所有 PM2 进程、数据库表、配置常量都围绕这一个账户运转。

### 1.2 核心问题

当不同运营人员面对不同直播间和不同广告账户时，现有架构在六个层面存在硬限制：

| 层面 | 现状 | 限制 |
|------|------|------|
| 认证 | `ACCOUNT_ID` 是模块级常量，Cookie 缓存单文件 | 无法按账户切换，多账户无法并存 |
| 采集 | `collectAllData(client)` 隐式绑定全局 `ACCOUNT_ID` | 一个请求只能采一个账户 |
| 数据库 | campaigns/snapshots 等表无 `account_id` 列 | 多账户数据混在一起，无法区分 |
| 配置 | `.env` 单组配置 + `monitor-utils.mjs` 硬编码常量 | 加一个账户要改代码 |
| 调度 | PM2 cron `*/15` 跑单账户脚本 | N 个账户就要 N 套 cron，无法动态扩展 |
| 推送 | `FEISHU_CHAT_ID` 硬编码单群 | 所有告警涌进一个群，运营无法只看自己的 |

### 1.3 设计目标

1. **运营隔离**：每个运营只看到自己负责的账户和直播间数据，告警只推到自己的群
2. **账户可插拔**：新增/停用一个广告账户只需改配置（数据库记录），不改代码
3. **线索特化**：监控指标和告警逻辑围绕线索直播漏斗设计（留资、表单、CPL），而非通用电商指标
4. **平滑迁移**：现有单账户数据不丢失，改造后原账户作为第一个租户自然接入
5. **资源可控**：单机 Chrome 实例数量有上限，通过 Profile 池和调度队列避免资源耗尽

---

## 二、线索直播广告业务模型

### 2.1 三层租户结构

```
运营 (Operator)          直播间 (LiveRoom)          广告账户 (AdAccount)
┌──────────┐            ┌──────────────┐           ┌─────────────────┐
│ 张三      │──负责──▶ │ 极狐区域福利号  │──关联──▶ │ aadvid=184268...  │
│ 飞书ID    │            │ 排班表Token    │           │ Chrome Profile 5  │
│ 飞书群A   │            │ 直播状态轮询    │           │ Cookie缓存独立    │
└──────────┘            └──────────────┘           ├─────────────────┤
                              │                    │ aadvid=184268...(东区)│
                              ├──关联──▶           │ Chrome Profile 6  │
                              │                    └─────────────────┘
                        ┌──────────────┐           ┌─────────────────┐
                        │ 另一个直播间   │──关联──▶ │ aadvid=185266...  │
                        └──────────────┘           └─────────────────┘
┌──────────┐
│ 李四      │──负责──▶ ...
└──────────┘
```

**关系约束：**
- 运营 1:N 直播间（一个运营负责多个直播间）
- 直播间 1:N 广告账户（一个直播间可挂多个投放账户，如分区域号）
- 广告账户 N:1 直播间（一个账户只属于一个直播间）
- 运营 N:M 直播间（可选：支持多人协作同一直播间）

### 2.2 线索直播漏斗（从现有 analyzeData 代码确认）

线索直播广告的核心转化链路不同于电商直播，关注的是**留资**而非直接成交：

```
观看 (views) → 观看1分钟+ (views_1min) → 评论 (comments)
    → 私信打开 (msg_open) → 私信留资 (msg_lead) → 表单提交 (form_submit)
        → 线索 (leads = msg_lead + form_submit) → 转化 (conversions)
```

**核心监控指标：**
- **CPL（线索成本）**：消耗 / 线索数，线索直播的第一成本指标（比 CPA 更前置）
- **留资率**：私信留资 / 私信打开，衡量直播间转化效率
- **表单转化率**：表单提交 / 观看1分钟+
- **线索速度**：近15分钟新增线索数，直播中的实时产能指标
- **零线索告警**：有消耗但无线索，线索直播最严重的异常

### 2.3 直播间状态联动

线索直播的监控节奏由直播间状态驱动（现有 `live-watcher.mjs` 已实现单账户版本）：

- **未开播**：低频巡检（30min），只看预算和余额，不推速报
- **开播中**：高频监控（5min 速报 + 15min 详细），CPL/零线索告警加密
- **刚下播**：等待数据延迟（5min），触发日汇总
- **已下播**：日汇总推送，归档当日数据

多租户改造后，每个直播间独立维护状态机，互不干扰。
---

## 三、目标架构总览

### 3.1 分层架构

```
┌─────────────────────────────────────────────────────────┐
│                    推送路由层                             │
│   按直播间/运营分发到对应飞书群，支持 @ 指定运营           │
├─────────────────────────────────────────────────────────┤
│                    监控分析层                             │
│   线索漏斗分析 / CPL告警 / 生命周期判定 / 趋势检测        │
│   ← 每次分析传入 accountCtx，不再依赖全局 CONFIG          │
├─────────────────────────────────────────────────────────┤
│                    调度引擎层                             │
│   任务队列驱动：遍历活跃账户 -> 采集 -> 分析 -> 推送       │
│   替代 PM2 cron 单账户触发                               │
├──────────────────┬──────────────────────────────────────┤
│   多账户采集层    │              配置中心                  │
│ Profile池+Cookie池│   DB驱动的账户注册表/阈值/推送规则     │
│ createClient(ctx) │   替代 .env 单组配置                  │
├──────────────────┴──────────────────────────────────────┤
│                    数据持久层                             │
│   多租户 SQLite（account_id 维度隔离）                    │
├─────────────────────────────────────────────────────────┤
│                    认证层                                │
│   Chrome 多 Profile / Cookie 独立缓存 / CDP 复用          │
└─────────────────────────────────────────────────────────┘
```

### 3.2 与现有架构的对应关系

| 现有模块 | 现有职责 | 改造方向 |
|----------|----------|----------|
| `oceanengine-api-client.mjs` | 单账户 Cookie + API | `createClient(accountCtx)` 参数化，Cookie 按账户隔离 |
| `monitor-utils.mjs` | 全局配置常量 | 拆分为「全局基础设施配置」+「DB 驱动的账户配置」 |
| `oceanengine-monitor-v3.mjs` | 单账户采集+分析+推送 | 拆为 `collector` + `analyzer` + `pusher`，均接收 accountCtx |
| `ecosystem.config.cjs` | PM2 13 进程单账户 | 调度引擎进程 + 共享基础设施进程 |
| `db/schema.sql` | 6 表无租户维度 | 加 account_id + 新增 4 张租户管理表 |
| `live-watcher.mjs` | 单账户直播状态 | 改为多直播间状态机管理器 |

---

## 四、数据模型设计

### 4.1 新增租户管理表

```sql
-- ============================================================
-- 租户管理表（v2.0 新增）
-- ============================================================

-- 1. operators - 运营人员
CREATE TABLE IF NOT EXISTS operators (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  name          TEXT NOT NULL,                    -- 运营姓名
  feishu_open_id TEXT,                             -- 飞书 open_id（用于 @ 通知）
  feishu_chat_id TEXT,                             -- 专属飞书群 chat_id（可空，空则用直播间群）
  phone         TEXT,
  status        TEXT NOT NULL DEFAULT 'active',   -- active/inactive
  created_at    TEXT NOT NULL DEFAULT (datetime('now'))
);

-- 2. live_rooms - 直播间
CREATE TABLE IF NOT EXISTS live_rooms (
  id                 INTEGER PRIMARY KEY AUTOINCREMENT,
  name               TEXT NOT NULL,               -- 直播间名称（如"极狐区域福利号"）
  shift_spreadsheet_token TEXT,                    -- 排班表 spreadsheet token
  shift_sheet_id     TEXT,                          -- 排班表 sheet id
  shift_base_date    TEXT,                          -- 排班表基准日期
  shift_base_row     INTEGER,                       -- 排班表基准行号
  default_feishu_chat_id TEXT,                     -- 默认推送飞书群
  status             TEXT NOT NULL DEFAULT 'active',
  created_at         TEXT NOT NULL DEFAULT (datetime('now'))
);

-- 3. operator_rooms - 运营-直播间关联（多对多）
CREATE TABLE IF NOT EXISTS operator_rooms (
  operator_id  INTEGER NOT NULL,
  room_id      INTEGER NOT NULL,
  role         TEXT NOT NULL DEFAULT 'primary',    -- primary/backup
  PRIMARY KEY (operator_id, room_id),
  FOREIGN KEY (operator_id) REFERENCES operators(id),
  FOREIGN KEY (room_id) REFERENCES live_rooms(id)
);

-- 4. ad_accounts - 广告账户（核心注册表）
CREATE TABLE IF NOT EXISTS ad_accounts (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  aadvid            TEXT NOT NULL UNIQUE,          -- 巨量引擎账户ID
  name              TEXT NOT NULL,                 -- 账户名称
  room_id           INTEGER NOT NULL,              -- 所属直播间
  oec_email         TEXT,                          -- 登录邮箱
  oec_password      TEXT,                          -- 登录密码（加密存储）
  chrome_profile    TEXT NOT NULL,                 -- Chrome Profile 目录名（如 Profile 5）
  video_account_id  TEXT,                          -- 关联的视频号ID
  daily_budget      REAL NOT NULL DEFAULT 45000,   -- 日预算
  monitor_enabled   INTEGER NOT NULL DEFAULT 1,    -- 0=暂停监控 1=启用
  monitor_interval  INTEGER NOT NULL DEFAULT 15,   -- 监控间隔(分钟)
  status            TEXT NOT NULL DEFAULT 'active',
  created_at        TEXT NOT NULL DEFAULT (datetime('now')),
  FOREIGN KEY (room_id) REFERENCES live_rooms(id)
);

-- 5. notify_rules - 推送规则（按账户/直播间可定制）
CREATE TABLE IF NOT EXISTS notify_rules (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  scope         TEXT NOT NULL DEFAULT 'account',   -- account/room/operator
  scope_id      INTEGER NOT NULL,                   -- 对应 ad_accounts.id / live_rooms.id / operators.id
  alert_type    TEXT NOT NULL DEFAULT '*',          -- 告警类型通配
  min_severity  TEXT NOT NULL DEFAULT 'medium',     -- low/medium/high/critical
  feishu_chat_id TEXT,                               -- 推送目标群（空=用 scope 默认）
  push_5min     INTEGER NOT NULL DEFAULT 1,         -- 是否推5min速报
  push_15min    INTEGER NOT NULL DEFAULT 1,         -- 是否推15min详细
  push_daily    INTEGER NOT NULL DEFAULT 1,         -- 是否推日汇总
  quiet_hours   TEXT,                                 -- 免打扰时段（如 "23:00-07:00"）
  enabled       INTEGER NOT NULL DEFAULT 1,
  created_at    TEXT NOT NULL DEFAULT (datetime('now'))
);
```

### 4.2 现有表改造（加 account_id 维度）

```sql
-- ============================================================
-- 现有表增加 account_id 列（向后兼容：允许 NULL 表示历史数据）
-- ============================================================

-- campaigns: 加 account_id
ALTER TABLE campaigns ADD COLUMN account_id TEXT;
CREATE INDEX IF NOT EXISTS idx_campaigns_account ON campaigns(account_id);

-- snapshots: 加 account_id
ALTER TABLE snapshots ADD COLUMN account_id TEXT;
CREATE INDEX IF NOT EXISTS idx_snapshots_account_time ON snapshots(account_id, snapshot_time);

-- alerts: 加 account_id + room_id
ALTER TABLE alerts ADD COLUMN account_id TEXT;
ALTER TABLE alerts ADD COLUMN room_id INTEGER;
CREATE INDEX IF NOT EXISTS idx_alerts_account ON alerts(account_id);
CREATE INDEX IF NOT EXISTS idx_alerts_room ON alerts(room_id);

-- actions: 加 account_id
ALTER TABLE actions ADD COLUMN account_id TEXT;

-- feedback: 加 operator_id
ALTER TABLE feedback ADD COLUMN operator_id INTEGER;
```

**迁移策略：** 现有数据全部填充为第一个账户的 `account_id`（从 `.env` 的 `OEC_ACCOUNT_ID` 读取），作为租户化的基线数据，零数据丢失。

### 4.3 数据隔离查询模式

所有业务查询都带 `account_id` 过滤，确保租户隔离：

```sql
-- 查某账户最新快照
SELECT * FROM snapshots WHERE account_id = ? ORDER BY snapshot_time DESC LIMIT 1;

-- 查某直播间所有账户的告警
SELECT a.* FROM alerts a
  JOIN ad_accounts aa ON a.account_id = aa.aadvid
  WHERE aa.room_id = ? AND a.resolved = 0;

-- 查某运营负责的所有账户
SELECT aa.* FROM ad_accounts aa
  JOIN operator_rooms or ON aa.room_id = or.room_id
  WHERE or.operator_id = ? AND aa.monitor_enabled = 1;
```
---

## 五、多账户认证与采集层

### 5.1 问题根因

现有 `oceanengine-api-client.mjs` 的认证绑定在三个全局单例上：

```js
const ACCOUNT_ID = process.env.OEC_ACCOUNT_ID || '1842681352509635';  // 模块级常量
const COOKIE_CACHE_FILE = path.join(DATA_DIR, '.oec-cookies.json');    // 单文件缓存
// extractCookiesFromBrowser() 隐式使用全局 CHROME_PROFILE_DIRECTORY     // 单 Profile
```

`createClient()`、`getOnlineRoomList()`、`collectAllData()` 全部直接引用 `ACCOUNT_ID`，导致无法按账户切换。

### 5.2 改造方案：accountCtx 参数化

引入 `accountCtx` 上下文对象，贯穿认证 -> 采集 -> 分析全链路：

```js
// accountCtx 结构（从 ad_accounts 表加载）
const accountCtx = {
  aadvid: '1842681352509635',
  name: '极狐-区域福利号',
  roomId: 1,
  chromeProfile: 'Profile 4',
  oecEmail: 'xxx@example.com',
  oecPassword: 'xxx',
  videoAccountId: '1852666142648332',
  dailyBudget: 45000,
  monitorInterval: 15,
  cookieCacheFile: path.join(DATA_DIR, '.oec-cookies-1842681352509635.json'),
};
```

### 5.3 Cookie 池设计

每个广告账户独立 Cookie 缓存文件，按 aadvid 命名：

```
monitor-data/
  .oec-cookies-1842681352509635.json    # 账户A
  .oec-cookies-1842681994872135.json    # 账户B（东区）
  .oec-cookies-1842681830951944.json    # 账户C（西区）
```

`createClient` 改造为接收 `accountCtx`：

```js
export async function createClient(accountCtx, options = {}) {
  const { forceRefresh = false, useCache = true } = options;
  let cookieData = null;

  // Cookie 缓存按账户隔离
  if (!forceRefresh && useCache) {
    try {
      if (fs.existsSync(accountCtx.cookieCacheFile)) {
        const cached = JSON.parse(fs.readFileSync(accountCtx.cookieCacheFile, 'utf-8'));
        if (cached.expireAt && Date.now() < cached.expireAt) {
          cookieData = cached;
        }
      }
    } catch {}
  }

  if (!cookieData) {
    cookieData = await extractCookiesFromBrowser(accountCtx);
  }

  return {
    cookieData,
    accountCtx,
    refreshCookies: () => extractCookiesFromBrowser(accountCtx),
    request: (url, opts) => apiRequest(url, { ...opts, cookieData }),
  };
}
```

`extractCookiesFromBrowser` 改造为按 `accountCtx.chromeProfile` 连接对应 Chrome Profile 的 CDP：

```js
async function extractCookiesFromBrowser(accountCtx) {
  // 按账户的 chromeProfile 获取标签页
  const tab = await getOceanEngineTab(
    ['投放管理', '巨量引擎工作台'],
    accountCtx.chromeProfile  // 新增参数：指定 Profile
  );
  // ... 后续 Cookie 提取逻辑不变，缓存写入 accountCtx.cookieCacheFile
}
```

### 5.4 Chrome Profile 池

**单机资源约束：** Chrome 每个 Profile 是独立进程，单机同时开 5-8 个 Profile 是安全上限。通过 `ad_accounts.chrome_profile` 列管理分配：

| Profile | 账户 | 状态 |
|---------|------|------|
| Profile 4 | 极狐区域福利号（主账户） | 在用 |
| Profile 5 | 东区 | 在用 |
| Profile 6 | 西区 | 在用 |
| Profile 7 | 中区 | 在用 |
| Profile 8 | 南区 | 在用 |
| Profile 9 | 北区 | 在用 |

**Profile 生命周期管理（`chrome-guard.mjs` 改造）：**
- 启动时按 `ad_accounts` 表拉起所有 `monitor_enabled=1` 的账户对应 Profile
- 每个 Profile 独立 `--remote-debugging-port`（9222 + offset，避免冲突）
- Cookie 过期时按账户触发对应 Profile 的自动登录，互不干扰

### 5.5 采集层改造

`collectAllData` 和 `getOnlineRoomList` 不再引用全局 `ACCOUNT_ID`，改为从 `client.accountCtx` 获取：

```js
export async function collectAllData(client) {
  const aadvid = client.accountCtx.aadvid;  // 替代全局 ACCOUNT_ID
  // ... 后续逻辑不变
}

export async function getOnlineRoomList(client) {
  const aadvid = client.accountCtx.aadvid;
  const resp = await apiRequest(
    `${BASE_URL}/nbs/api/statistics/live_show/online_room/list?aadvid=${aadvid}`,
    { method: "POST", cookieData: client.cookieData }
  );
  // ...
}
```

---

## 六、配置中心

### 6.1 配置分层

将现有 `monitor-utils.mjs` 的配置拆为两层：

**第一层：全局基础设施配置（保留 .env）**

这些是与具体账户无关的基础设施参数，继续用 `.env`：

```env
# 基础设施（全局）
CHROME_USER_DATA_DIR=D:\ChromeCDP\User Data
CDP_PORT=9222
FEEDBACK_PORT=8899
LOG_RETENTION_DAYS=14
NODE_EXE=...
MONITOR_DIR=...

# 默认飞书群（fallback，账户无专属群时用）
LARK_MONITOR_CHAT_ID=oc_default
LARK_ANCHOR_CHAT_ID=oc_anchor

# lark-cli 路径自动探测，无需配置
```

**第二层：账户级配置（DB 驱动）**

所有与具体广告账户相关的配置，从 `ad_accounts` / `live_rooms` / `operators` 表读取：

```js
// 新建 config-center.mjs
import Database from 'better-sqlite3';

const db = new Database(path.join(DATA_DIR, 'oceanengine.db'));

// 加载所有启用监控的账户上下文
export function loadActiveAccounts() {
  const rows = db.prepare(`
    SELECT aa.*, lr.name as room_name, lr.shift_spreadsheet_token,
           lr.shift_sheet_id, lr.shift_base_date, lr.shift_base_row,
           lr.default_feishu_chat_id
    FROM ad_accounts aa
    JOIN live_rooms lr ON aa.room_id = lr.id
    WHERE aa.monitor_enabled = 1 AND aa.status = 'active'
  `).all();
  return rows.map(rowToAccountCtx);
}

// 加载某直播间所有账户
export function loadAccountsByRoom(roomId) { ... }

// 加载某运营负责的所有账户
export function loadAccountsByOperator(operatorId) { ... }

// 加载推送规则
export function loadNotifyRules(scope, scopeId) { ... }
```

### 6.2 阈值配置可定制化

现有 `CONFIG.thresholds` 是全局统一的。多租户后，不同直播间/账户可能有不同阈值（例如新账户容忍更高 CPL）：

```sql
-- 扩展 ad_accounts 表（或单独 thresholds 表）
ALTER TABLE ad_accounts ADD COLUMN thresholds_json TEXT;
-- 存储如 {"cplWarn": 80, "cplSevere": 150, "zeroLeadSpend": 50}
```

`analyzeData` 改造为接收阈值参数：

```js
function analyzeData(campaigns, accountSpend, accountBudget, accountBalance,
                     pageSummary, thresholds) {  // 新增 thresholds 参数
  // 用 thresholds.cplWarn 替代 CONFIG.thresholds.cpaRise
}
```

未配置自定义阈值的账户，fallback 到全局默认阈值（现有 CONFIG.thresholds）。
---

## 七、调度引擎

### 7.1 现状问题

现有 PM2 配置中，监控靠 cron 触发单账户脚本：

```
pm2-15min:  cron */15 * * * *  ->  oceanengine-monitor-v3.mjs  (单账户)
pm2-5min:   cron */5 * * * *   ->  oceanengine-5min-check.mjs  (单账户)
```

N 个账户就要 N 套 cron、N 个进程名，无法动态扩展，且所有账户同时触发会造成 Chrome CDP 并发冲突。

### 7.2 调度引擎设计

新建 `scheduler-engine.mjs`，替代 PM2 cron 直接触发脚本的模式：

```
PM2 常驻进程
  └── scheduler-engine.mjs
        ├── 每 5min:  遍历活跃账户 -> 5min速报任务（错峰执行）
        ├── 每 15min: 遍历活跃账户 -> 15min详细任务（错峰执行）
        ├── 每 60s:   遍历直播间   -> 直播状态轮询
        └── 每日定点:  日汇总/日报/排班同步
```

**核心调度逻辑：**

```js
// scheduler-engine.mjs
import { loadActiveAccounts, loadLiveRooms } from './config-center.mjs';

const TASK_INTERVALS = {
  '5min':  5 * 60 * 1000,
  '15min': 15 * 60 * 1000,
  'live':  60 * 1000,
};

async function runMonitorCycle(taskType) {
  const accounts = loadActiveAccounts();

  // 错峰执行：避免所有账户同时抢 Chrome CDP
  for (const accountCtx of accounts) {
    try {
      if (taskType === '5min')  await run5minCheck(accountCtx);
      if (taskType === '15min') await run15minMonitor(accountCtx);
    } catch (e) {
      logError(accountCtx, taskType, e);
    }
    // 账户间间隔 3-5s，避免 CDP 并发冲突
    await sleep(3000);
  }
}

// 主循环
setInterval(() => runMonitorCycle('5min'),  TASK_INTERVALS['5min']);
setInterval(() => runMonitorCycle('15min'), TASK_INTERVALS['15min']);
```

### 7.3 错峰与并发控制

**问题：** 多账户同时采集时，Chrome CDP 的 WebSocket 连接和 Cookie 提取会互相干扰。

**方案：**
1. **串行采集**：账户间间隔 3-5s，同一时刻只有一个账户在用 CDP
2. **直播优先**：正在开播的直播间账户优先采集，未开播的低频巡检
3. **失败隔离**：单个账户采集失败不影响其他账户，记录到 alerts 表

```js
// 按直播状态排序：开播中的优先
const sorted = accounts.sort((a, b) => {
  const aLive = liveStatus.get(a.roomId)?.isLive ? 1 : 0;
  const bLive = liveStatus.get(b.roomId)?.isLive ? 1 : 0;
  return bLive - aLive;
});
```

### 7.4 PM2 进程精简

改造后 PM2 只保留基础设施进程 + 调度引擎：

| 进程 | 职责 | 变化 |
|------|------|------|
| `scheduler-engine` | 多账户调度引擎（5min/15min/live轮询） | 新增，替代 pm2-5min/pm2-15min/live-watcher |
| `feishu-listener` | 飞书群消息监听 | 保留，增加 accountCtx 路由 |
| `feedback-server` | Dashboard HTTP 服务 | 保留，增加多租户视图 |
| `action-queue-worker` | 操作队列 worker | 保留，操作带 account_id |
| `chrome-guard` | Chrome 多 Profile 守护 | 改造：管理 Profile 池 |
| `shift-pusher` | 换班推送 | 改造：按直播间路由 |
| `pm2-daily-report` | 日报 | 改造：按账户/直播间生成 |
| `pm2-daily-summary` | 日汇总 | 改造：按账户/直播间汇总 |
| `pm2-sync-tomorrow` | 次日排班同步 | 改造：按直播间同步 |

---

## 八、线索直播监控特化

### 8.1 线索成本（CPL）告警体系

线索直播的第一成本指标是 CPL（Cost Per Lead），而非 CPA。现有代码已有 `cplLast15min` 计算，但告警逻辑仍以 CPA 为主。新增 CPL 告警层：

```js
// 线索直播专用告警类型
const LEAD_ALERT_TYPES = {
  zero_lead:        { severity: 'critical', desc: '有消耗无线索' },
  cpl_high:         { severity: 'high',     desc: 'CPL超出阈值' },
  cpl_severe:       { severity: 'critical', desc: 'CPL严重超标' },
  lead_drop:        { severity: 'medium',   desc: '线索量骤降' },
  retain_rate_low:  { severity: 'medium',   desc: '留资率偏低' },
  form_conv_low:    { severity: 'low',      desc: '表单转化率低' },
};

// CPL 告警判定（在 analyzeData 中新增）
function checkLeadAlerts(campaign, thresholds, ctx) {
  const alerts = [];
  const cpl = campaign.spend > 0 && campaign.leads > 0
    ? campaign.spend / campaign.leads : Infinity;

  // 零线索：有消耗但无线索（线索直播最严重异常）
  if (campaign.spend > thresholds.zeroLeadSpend && campaign.leads === 0) {
    alerts.push({ type: 'zero_lead', severity: 'critical',
      message: `${campaign.name} 消耗${campaign.spend}元无线索` });
  }

  // CPL 超标
  if (cpl > thresholds.cplSevere && campaign.spend > thresholds.cplHighSpend) {
    alerts.push({ type: 'cpl_severe', severity: 'critical',
      message: `${campaign.name} CPL=${cpl.toFixed(0)}元，严重超标` });
  } else if (cpl > thresholds.cplWarn && campaign.spend > thresholds.cplMinSpend) {
    alerts.push({ type: 'cpl_high', severity: 'high',
      message: `${campaign.name} CPL=${cpl.toFixed(0)}元，超出阈值` });
  }

  // 留资率偏低
  if (campaign.privateMsgOpen > 10) {
    const retainRate = campaign.privateMsgRetain / campaign.privateMsgOpen;
    if (retainRate < thresholds.retainRateLow) {
      alerts.push({ type: 'retain_rate_low', severity: 'medium',
        message: `${campaign.name} 留资率${(retainRate*100).toFixed(0)}%偏低` });
    }
  }

  return alerts;
}
```

### 8.2 线索漏斗看板

15min 详细卡片和日汇总中，增加线索漏斗可视化（现有 `buildFeishuCard` 已有部分指标，需补全漏斗）：

```
线索转化漏斗（今日累计）
━━━━━━━━━━━━━━━━━━━━━━━━━━━
观看      ████████████████ 12,450
1分钟+    ████████████     8,230  (66%)
私信打开  █████             3,120  (25%)
私信留资  ██                  680  (22%)
表单提交  █                   240  ( 8%)
线索合计  ███                920  (7.4%)
转化      ▎                   45  (4.9%)
━━━━━━━━━━━━━━━━━━━━━━━━━━━
CPL: 49元  留资率: 22%  表单率: 8%
```

### 8.3 直播间状态联动调度

每个直播间独立维护状态机（现有 `live-watcher.mjs` 单账户版扩展为多直播间管理器）：

```js
// live-state-manager.mjs
const roomStates = new Map();  // roomId -> { isLive, lastChange, accountIds }

async function pollAllRooms() {
  const rooms = loadLiveRooms();
  for (const room of rooms) {
    // 用该直播间任一活跃账户的 client 查直播状态
    const accounts = loadAccountsByRoom(room.id);
    if (accounts.length === 0) continue;

    const client = await createClient(accounts[0]);
    const onlineRooms = await getOnlineRoomList(client);
    const isLive = onlineRooms.length > 0;

    const prev = roomStates.get(room.id);
    if (prev?.isLive !== isLive) {
      handleRoomStateChange(room.id, isLive, accounts);
    }
    roomStates.set(room.id, { isLive, accountIds: accounts.map(a => a.aadvid) });
  }
}

function handleRoomStateChange(roomId, isLive, accounts) {
  if (isLive) {
    // 开播：该直播间所有账户切换到高频监控
    accounts.forEach(a => monitorMode.set(a.aadvid, 'live'));
  } else {
    // 下播：等数据延迟 -> 触发日汇总 -> 切回低频
    accounts.forEach(a => monitorMode.set(a.aadvid, 'offline'));
    setTimeout(() => triggerDailySummary(roomId), 5 * 60 * 1000);
  }
}
```

**监控频率按直播状态动态调整：**

| 直播状态 | 5min速报 | 15min详细 | CPL告警 | 零线索告警 |
|----------|----------|-----------|---------|------------|
| 开播中 | 启用 | 启用 | 实时 | 实时 |
| 未开播 | 禁用 | 30min巡检 | 禁用 | 禁用 |
| 刚下播 | 禁用 | 触发日汇总 | 禁用 | 禁用 |
---

## 九、推送路由

### 9.1 现状问题

现有推送硬编码单飞书群：

```js
export const FEISHU_CHAT_ID = process.env.LARK_MONITOR_CHAT_ID || 'oc_8deeb3061bdbd43608de252a44c97a25';
```

所有账户的告警涌进同一个群，运营无法只看自己负责的账户。

### 9.2 推送路由设计

推送目标通过 `notify_rules` 表三级匹配，优先级从高到低：

```
1. account 级规则（精确到单个广告账户）
2. room 级规则（直播间所有账户）
3. operator 级规则（运营默认群）
4. 全局 fallback（.env 的 LARK_MONITOR_CHAT_ID）
```

```js
// push-router.mjs
export function resolvePushTarget(accountCtx, alertType, severity) {
  // 1. 账户级规则
  let rule = loadNotifyRules('account', accountCtx.accountDbId, alertType, severity);
  if (!rule) {
    // 2. 直播间级规则
    rule = loadNotifyRules('room', accountCtx.roomId, alertType, severity);
  }
  if (!rule) {
    // 3. 运营级规则
    const operators = loadOperatorsByRoom(accountCtx.roomId);
    if (operators.length > 0) {
      rule = loadNotifyRules('operator', operators[0].id, alertType, severity);
    }
  }

  // 4. fallback
  const chatId = rule?.feishu_chat_id
    || accountCtx.defaultFeishuChatId
    || process.env.LARK_MONITOR_CHAT_ID;

  return {
    chatId,
    push5min: rule?.push_5min ?? 1,
    push15min: rule?.push_15min ?? 1,
    pushDaily: rule?.push_daily ?? 1,
    quietHours: rule?.quiet_hours,
    mentionOperators: loadOperatorsByRoom(accountCtx.roomId),
  };
}
```

### 9.3 免打扰与告警抑制

- **免打扰时段**：`notify_rules.quiet_hours`（如 `"23:00-07:00"`），非 critical 告警在此时段不推送
- **告警去重**：现有 `shouldSuggest` 逻辑（8h 过期）保留，按 `account_id + campaign_id + alert_type` 去重
- **跨账户聚合**：同一直播间多账户的同类告警，可聚合为一张卡片（可选，后续优化）

### 9.4 飞书消息卡片改造

`buildFeishuCard` 接收 `accountCtx`，卡片标题增加账户/直播间标识：

```
极狐区域福利号 | 东区  15min监控  14:30
消耗 ¥3,250 / ¥45,000 (7.2%)  CPL ¥49  线索 67
```

而非现有的单账户标题。运营一眼能区分是哪个直播间的哪个账户。

---

## 十、迁移路径

### 10.1 迁移原则

- **零数据丢失**：现有 oceanengine.db 的历史数据全部保留
- **渐进式**：分 4 个阶段，每个阶段可独立验证，不破坏现有监控
- **回退安全**：每个阶段失败可回退到上一阶段

### 10.2 阶段一：数据库租户化（1天，低风险）

1. 新建 `db/schema-v2.sql`，包含 5 张租户管理表 + 现有表的 ALTER 语句
2. 执行迁移脚本：现有数据填充 `account_id = OEC_ACCOUNT_ID`
3. 插入第一条租户记录：当前账户作为 `ad_accounts` 表的第一行
4. 验证：现有查询加 `WHERE account_id = ?` 后结果不变

### 10.2 阶段二：采集层参数化（2天，中风险）

1. 改造 `createClient(accountCtx)`，Cookie 按账户隔离
2. 改造 `collectAllData` / `getOnlineRoomList`，从 `client.accountCtx` 取 aadvid
3. 新建 `config-center.mjs`，从 DB 加载账户上下文
4. 现有单账户作为 `loadActiveAccounts()` 的唯一返回值
5. 验证：单账户监控行为完全不变

### 10.3 阶段三：调度引擎（2天，中风险）

1. 新建 `scheduler-engine.mjs`，遍历活跃账户错峰执行
2. 改造 `live-watcher` 为多直播间状态机
3. PM2 配置切换：停用 `pm2-5min`/`pm2-15min`，启用 `scheduler-engine`
4. 验证：单账户调度结果与原 cron 一致

### 10.4 阶段四：多账户接入 + 推送路由（1天，低风险）

1. 在 `ad_accounts` 表新增第二个账户记录
2. 分配 Chrome Profile，登录该账户
3. 配置 `notify_rules` 推送规则
4. 调度引擎自动纳入新账户，无需改代码
5. 验证：两个账户独立监控、独立推送

---

## 十一、部署架构

### 11.1 单机部署（当前阶段）

```
本机 Windows
├── Chrome（多 Profile，9222+ 端口池）
│   ├── Profile 4 -> 账户A
│   ├── Profile 5 -> 账户B
│   └── ...
├── PM2（Node 22）
│   ├── scheduler-engine（调度引擎，遍历多账户）
│   ├── feishu-listener（多租户消息路由）
│   ├── feedback-server（多租户 Dashboard）
│   ├── action-queue-worker（操作带 account_id）
│   ├── chrome-guard（Profile 池守护）
│   └── 定时任务（日报/汇总/排班，按直播间）
├── SQLite（oceanengine.db，多租户隔离）
└── monitor-data/（按账户隔离的 Cookie/快照/日志）
```

### 11.2 容量规划

| 资源 | 单账户占用 | 上限估算 |
|------|-----------|----------|
| Chrome Profile | ~200MB 内存 | 单机 8 Profile = 1.6GB |
| Cookie 缓存 | ~5KB/账户 | 可忽略 |
| 15min 快照 | ~50KB/次 | 100 账户/天 = ~5MB |
| CDP 并发 | 串行采集 | 同时 1 个账户在采集 |
| 采集耗时 | ~30s/账户 | 10 账户错峰 = 5min 内完成 |

**单机建议上限：8-10 个活跃账户**（受 Chrome 内存和串行采集耗时约束）。超出需多机部署或容器化。

### 11.3 多机扩展（未来）

若账户数超过单机上限，按直播间分片到不同机器：

- 每台机器跑独立的 `scheduler-engine`，只加载分配给自己的直播间
- 共享一个中心数据库（PostgreSQL 替代 SQLite）或通过 API 同步
- 飞书推送统一出口，避免多机重复推送

---

## 十二、验收清单

### 数据模型
- [ ] `db/schema-v2.sql` 包含 5 张租户管理表
- [ ] 现有 5 张业务表均已加 `account_id` 列
- [ ] 历史数据迁移后 `account_id` 正确填充
- [ ] 数据隔离查询验证通过（跨账户无数据泄漏）

### 认证与采集
- [ ] `createClient(accountCtx)` 按账户隔离 Cookie
- [ ] Cookie 缓存文件按 aadvid 命名
- [ ] Chrome 多 Profile 可并行登录不同账户
- [ ] `collectAllData` 从 `client.accountCtx` 取 aadvid

### 配置中心
- [ ] `config-center.mjs` 从 DB 加载活跃账户
- [ ] `.env` 仅保留全局基础设施配置
- [ ] 账户级阈值可定制（thresholds_json）

### 调度引擎
- [ ] `scheduler-engine.mjs` 遍历活跃账户错峰执行
- [ ] 单账户采集失败不影响其他账户
- [ ] 直播状态联动：开播高频、下播低频
- [ ] PM2 进程精简，无单账户 cron 残留

### 线索监控特化
- [ ] CPL 告警（zero_lead / cpl_high / cpl_severe）
- [ ] 留资率告警（retain_rate_low）
- [ ] 线索漏斗看板在 15min 卡片和日汇总中展示
- [ ] 直播间状态驱动的监控频率切换

### 推送路由
- [ ] 三级推送规则匹配（account > room > operator > fallback）
- [ ] 免打扰时段生效
- [ ] 卡片标题含账户/直播间标识
- [ ] 不同运营只收到自己负责的账户告警

### 迁移
- [ ] 阶段一：数据库租户化，单账户行为不变
- [ ] 阶段二：采集参数化，单账户行为不变
- [ ] 阶段三：调度引擎，单账户行为不变
- [ ] 阶段四：第二个账户成功接入，独立监控独立推送
