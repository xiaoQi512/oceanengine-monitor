# 飞书机器人推送配置指南

## 为什么用飞书机器人？

飞书群聊里添加一个自定义机器人，就能通过 Webhook URL 直接推送消息。不需要 OAuth、不需要 CLI 配置、不依赖 WorkBuddy 沙箱——就是一个简单的 HTTP POST。而且飞书支持**交互式卡片消息**，比纯文本好看得多。

## 3 步搞定（比 PushPlus 更简单）

### 第一步：创建飞书机器人
1. 打开飞书（PC 或手机端都可以），进入你希望接收监控消息的**群聊**
2. 点击群设置（右上角 `···`）→ **群机器人** → **添加机器人** → **自定义机器人**
3. 给机器人起个名字，比如「巨量引擎监控」，头像随便选
4. 点击完成 → 飞书会给你一个 **Webhook URL**

> Webhook URL 格式：`https://open.feishu.cn/open-apis/bot/hook/xxxxxxxxxxxxxxxxx`

### 第二步：填入 Webhook
打开本项目目录下的 **`.feishu-webhook`** 文件，把第一行替换成上一步复制的 Webhook URL：

```
https://open.feishu.cn/open-apis/bot/hook/你的webhook地址
```

### 第三步：验证
等下次监控脚本运行，飞书群聊里就会收到第一条卡片消息。

也可以手动测试：
```bash
curl -X POST "你复制的webhook地址" \
  -H "Content-Type: application/json" \
  -d '{"msg_type":"text","content":{"text":"测试推送成功 ✅"}}'
```

## 推送策略

| 场景 | 推送频率 |
|------|---------|
| 🔴 严重告警（CPA超标2x / 零转化） | **立即推送**，不限频率 |
| 🟡 中等告警（CTR/CVR偏低） | 每 30 分钟 |
| ✅ 一切正常 | 每 60 分钟汇总 |

## 消息长这样

飞书推送的是**交互式卡片**，包含：

```
┌─────────────────────────────────┐
│ ⚠️ 极狐直播监控 · 3条告警       │  ← 红色/橙色/绿色头部
├─────────────────────────────────┤
│ 投放中  今日消耗   总转化  平均CPA│
│  13条   ¥18,762    215    ¥87.27│  ← 指标栏
├─────────────────────────────────┤
│ 💸 TOP5 消耗                    │
│ 1. 0524-真人直播 — ¥4,059 ...  │
├─────────────────────────────────┤
│ 🔴 0604-XXX-画面直投             │
│ CPA偏高: ¥156 (均值 ¥87)        │  ← 告警详情
├─────────────────────────────────┤
│ [📊 查看投放管理页]              │  ← 可点击按钮直达巨量引擎
├─────────────────────────────────┤
│ 🕐 19:15:00 · 每15分钟更新       │
└─────────────────────────────────┘
```

## 安全提醒

飞书 Webhook URL 是公开的（知道 URL 就能发消息），**不要泄露给别人**。如果泄露了，在飞书群设置里删除这个机器人重新创建即可。

## 费用

免费。飞书自定义机器人没有任何费用。
