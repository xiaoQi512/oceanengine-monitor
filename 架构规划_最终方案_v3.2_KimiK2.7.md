# 巨量引擎监控 · 架构规划最终方案 v3.2

> **审核模型**: Kimi K2.7 Code（最终版生成）
> **版本**: v3.2 最终版
> **日期**: 2026-06-28
> **基线文档**: `架构规划_整体方案_v3.2_决策驱动.md`
> **补充文档**: `架构规划_替代方案选型_v3.2-补.md`
> **代码现状核验日期**: 2026-06-28

---

## 0. 事实环境（经源码核验）

### 0.1 产品定位

| 项目 | 事实 | 来源 |
|---|---|---|
| 产品定位 | 个人桌面工具，非平台/产品/服务 | 用户明确声明 |
| 使用人数 | 仅用户本人 | — |
| 广告账户 | 1 个巨量引擎账户（`1842681352509635`） | `oceanengine-api-client.mjs` |
| 登录方式 | Cookie 鉴权（邮箱+密码，2h 过期） | `oec-auto-login.mjs` |
| 部署环境 | Windows 本机单机，PM2 进程管理 | `ecosystem.config.cjs` |
| 使用场景 | 本地盯盘 + 飞书群接收推送 + 偶尔远程查看 | — |

### 0.2 硬性约束

1. **单用户**：无用户系统、无登录鉴权、无多租户。
2. **单账户**：固定 1 个广告账户，`ACCOUNT_ID` 硬编码即可。
3. **自用**：不上架、不分发、不需要企业资质。
4. **桌面级**：运行在 Windows 本机，不云端部署、不 HTTPS 备案。

### 0.3 小程序已彻底排除

| # | 事实 | 依据 |
|---|---|---|
| F1 | 飞书小程序正式版需企业主体 | 飞书开放平台审核规则 |
| F2 | 微信小程序需企业主体 + 备案域名 + HTTPS | 微信官方文档 |
| F3 | 个人主体在两个平台均不能提交审核 | 平台规则 |
| F4 | 飞书小程序体验版仅开发者本人可用 | 飞书开发文档 |
| F5 | 飞书自建应用 → H5 内嵌仍需企业应用 | 北汽集团有企业飞书，跨账号调用受限 |

**结论**：飞书小程序 ❌ | 微信小程序 ❌ | Taro 跨端 ❌ | H5 内嵌到企业飞书 ⚠️（需借企业资质）。

---

## 1. 四大方向最终选型

### 1.1 前端展示

| 场景 | 最终方案 | 理由 |
|---|---|---|
| 桌面主看盘 | **A1 单文件 HTML 看板** | 已有 `oceanengine-report.html` 可复用，零开发量，零构建步骤 |
| 手机随手看 | **A1 响应式 + 局域网访问** | 同一看板，手机浏览器访问 `http://<本机IP>:8899/dashboard` |
| 副屏常驻 | **A1 + 全屏模式** | 原生 JS 全屏 API 即可 |
| 进阶交互（未来） | React SPA | 仅当 A1 不够用时再升级，本期不做 |

**淘汰项**：Electron/Tauri/PWA/浏览器扩展/桌面小组件 —— 对个人工具过度设计。

### 1.2 移动端访问

| 场景 | 最终方案 | 理由 |
|---|---|---|
| 在家/同 WiFi | **M1 局域网 IP 直连** | 零成本、零配置、秒开 |
| 出门/远程 | **M2 Tailscale** | 免费、稳定、安全；本机 Chrome 探活可共存 |

**淘汰项**：frp/Cloudflare Tunnel/ngrok/公网 IP/域名 DDNS —— 要么需公网服务器，要么有安全风险，要么需备案资质。

> **中国大陆环境修正**：Tailscale 在中国大陆直连连通率不稳定，需配置 `DERP` 中继或备 **M8 ZeroTier / Nebula**。

### 1.3 数据/告警通道

| 场景 | 最终方案 | 理由 |
|---|---|---|
| 定时/告警推送 | **F1 飞书群（已有）** | 0 开发量，已跑 3+ 月 |
| 命令交互 | **F2 飞书机器人（已有）** | `feishu-listener.mjs` + `cdp-action.mjs` 链路已跑通 |
| PC 前工作时 | F9 Windows Toast（S4 可选） | PowerShell 一行弹窗 |
| 看板常开 | F10 浏览器通知（S4 可选） | `Notification.requestPermission()` 即可 |
| iOS 用户 | F7 Bark（S4 可选，需自建服务） | 自建 Bark 服务，零成本 |

**本期必须项**：F1 + F2。
**本期不做**：钉钉/企微/邮件/短信（用户不用）。

### 1.4 操作入口

| 场景 | 最终方案 | 理由 |
|---|---|---|
| 紧急操作 | **O1 飞书群命令** | 5-10s 响应，CDP 链路已跑通 |
| 精细操作 | **O2 看板按钮** | 选中计划 → 看预算/出价/状态 → 操作 |
| 调试 | O4 lark-cli 命令行 | 手动调用，方便排查 |
| 高频自动化 | O3 Marketing API | 仅当 CDP 不稳定或需秒级时再上，本期不做 |

**操作确认机制**：

| 等级 | 操作 | 确认方式 |
|---|---|---|
| L0 | 查看/导出 | 无需确认 |
| L1 | 暂停/启用单个计划 | 前端弹窗确认 |
| L2 | 调整预算/出价 | 前端弹窗 + 显示变化量 |
| L3 | 批量暂停/删除 | 前端弹窗 + 二次输入确认 |

> **审批流删除**：单用户自用，自己操作自己确认，不需要推给别人审批。飞书仅用于通知操作结果。

---

## 2. 后端架构最终版

### 2.1 选型结论

**扩展 `feedback-server.mjs`（单进程 :8899）**，承担：
- HTTP 看板页面
- REST API
- 操作执行（import `cdp-action.mjs`）
- 操作日志记录
- 飞书卡片反馈（已有）

**淘汰项**：新建 `operation-executor.mjs` 独立进程 / 微服务拆分 —— 单用户不需要。

### 2.2 最终路由设计

```
feedback-server :8899
├── GET  /                  → 日报/报表入口
├── GET  /dashboard         → 单文件 HTML 看板
├── GET  /report            → HTML 报表（已有）
├── GET  /daily             → 日报（已有）
├── GET  /history           → 建议历史 JSON（已有）
├── POST /feedback          → 建议反馈（已有）
├── GET  /api/campaigns     → 计划列表（调 api-client / DB）
├── GET  /api/snapshots     → 快照查询（SQLite）
├── GET  /api/actions       → 操作日志查询
├── POST /api/actions       → 提交操作 → cdp-action → 记录日志
└── GET  /api/health        → 服务健康
```

### 2.3 实时通信

**30 秒定时轮询**。
- 前端：`setInterval(() => fetch('/api/snapshots?since=...'), 30000)`
- 个人工具不需要 SSE/WebSocket。

---

## 3. 数据存储最终版

### 3.1 选型结论

**SQLite 主存 + DuckDB 分析**。
- SQLite：单文件、零运维、Windows 原生。
- DuckDB：列式分析，可直接 `ATTACH 'oceanengine.db' (TYPE SQLITE)`。
- 23 万行/年，SQLite 单表无忧。
- Parquet 归档 >90 天历史数据。

### 3.2 最终 Schema（6 张表）

> 与数据库方案设计报告的对齐说明：
> - 删除 `campaigns` 元数据表 —— 单账户，计划元数据可直接从 snapshot 最新一条获取。
> - `optimization_actions` 改为 `operation_log`，仅记录操作与结果，无审批字段。
> - 删除所有 `platform` / `account_id` 字段 —— 单平台单账户。

```sql
-- 计划级时序快照（替代 monitor-data/*.json 中的 active/allSpending 数组）
CREATE TABLE campaign_snapshots (
    snapshot_date   TEXT NOT NULL,     -- YYYY-MM-DD
    snapshot_time   TEXT NOT NULL,     -- HH:MM:SS
    campaign_id     TEXT NOT NULL,
    campaign_name   TEXT,
    status          TEXT,              -- 投放中/已暂停/超出预算
    cost            REAL,              -- 消耗（元）
    leads           INTEGER,
    conversions     INTEGER,
    private_msg_open  INTEGER,
    private_msg_lead  INTEGER,
    form_submit     INTEGER,
    ctr             REAL,
    cpm             REAL,
    cvr             REAL,
    views           INTEGER,
    view_gt_1min    INTEGER,
    comments        INTEGER,
    budget          REAL,              -- 日预算
    cpa             REAL,              -- 计算字段
    PRIMARY KEY (snapshot_date, snapshot_time, campaign_id)
);
CREATE INDEX idx_cs_date ON campaign_snapshots(snapshot_date);
CREATE INDEX idx_cs_campaign ON campaign_snapshots(campaign_id, snapshot_date);

-- 账户级日快照（替代 daily-YYYY-MM-DD.json）
CREATE TABLE daily_snapshots (
    snapshot_date   TEXT NOT NULL,
    snapshot_time   TEXT NOT NULL,
    total_cost      REAL,
    total_leads     INTEGER,
    total_conversions INTEGER,
    active_campaigns INTEGER,
    paused_campaigns INTEGER,
    alerts_count    INTEGER,
    PRIMARY KEY (snapshot_date, snapshot_time)
);

-- 告警记录
CREATE TABLE alerts (
    alert_date      TEXT NOT NULL,
    alert_id        TEXT NOT NULL,
    alert_type      TEXT NOT NULL,     -- speed/cpa_rise/budget/zero_conv/...
    severity        TEXT NOT NULL,     -- high/medium/low
    campaign_id     TEXT,
    campaign_name   TEXT,
    message         TEXT,
    metric_context  TEXT,              -- JSON: 触发时指标快照
    created_at      TEXT DEFAULT (datetime('now')),
    PRIMARY KEY (alert_date, alert_id)
);

-- 反馈记录（替代 suggestion-history.json）
CREATE TABLE alert_feedbacks (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    alert_id        TEXT NOT NULL,
    response        TEXT NOT NULL,     -- accept/reject/ignored
    response_time   TEXT,
    created_at      TEXT DEFAULT (datetime('now')),
    FOREIGN KEY (alert_id) REFERENCES alerts(alert_id)
);

-- 操作日志（注意：是日志，不是审批流）
CREATE TABLE operation_log (
    log_date        TEXT NOT NULL,
    log_id          TEXT NOT NULL,
    campaign_id     TEXT NOT NULL,
    campaign_name   TEXT,
    action_type     TEXT NOT NULL,     -- pause/enable/update_budget/update_bid
    params_json     TEXT,              -- JSON: { "budget": 10000 }
    confirm_level   TEXT NOT NULL,     -- L1/L2/L3
    execution_result TEXT,             -- JSON: { "ok": true }
    channel         TEXT DEFAULT 'cdp',-- cdp/api
    created_at      TEXT DEFAULT (datetime('now')),
    PRIMARY KEY (log_date, log_id)
);
CREATE INDEX idx_ol_campaign ON operation_log(campaign_id, log_date);

-- Schema 版本管理
CREATE TABLE schema_migrations (
    version         INTEGER PRIMARY KEY,
    description     TEXT,
    applied_at      TEXT DEFAULT (datetime('now'))
);
```

### 3.3 双写与迁移策略

| 阶段 | 策略 |
|---|---|
| Week 1 | JSON 为主，SQLite 为辅；采集脚本同时写 JSON + SQLite；前端仍读 JSON |
| Week 2 | SQLite 为主，JSON 为辅；看板/API 切到 SQLite；JSON 继续写但降为备份 |
| Week 3+ | 仅保留 SQLite；JSON 文件归档 |

---

## 4. 执行通道最终版

### 4.1 选型结论

**raw CDP 主通道（Phase 1 即用）**。
- `cdp-action.mjs` 已有 3 个核心写函数，已在 `feishu-listener.mjs` 生产验证。
- 自用工具不需要为 Marketing API 资质投入时间。
- 若后续 Chrome 不稳定，再异步推进 Marketing API 作为备用通道。

### 4.2 为什么不是 Playwright

> **关键事实**：OVUI Vue 组件只响应 `Input.dispatchMouseEvent` 真实事件。
> - Playwright `locator.click()` 内部用 `Element.dispatchEvent`，OVUI 不响应。
> - 若强用 Playwright，需调用其底层 CDP session，等于回到 raw CDP。

**结论**：保留 `cdp-client.mjs`，不退役。

### 4.3 CDP 操作并发控制

- 同一计划串行执行：在 `POST /api/actions` 中使用内存锁（`Map<campaign_id, Promise>`）或 SQLite `operation_log` 状态字段防止并发。
- 批量操作：队列化，逐条执行，每条 5-15s。

---

## 5. 实施路线（3 阶段 · 3-4 周）

### 5.1 阶段总览

| 阶段 | 工期 | 内容 | 交付物 |
|---|---|---|---|
| **S1 数据基建** | 第 1 周 | SQLite 建表 + JSON 迁移 + 双写 + 反馈服务器绑 0.0.0.0 | `oceanengine.db` + 迁移脚本 + 局域网可访问 |
| **S2 执行层 + API** | 第 2 周 | feedback-server 扩展路由 + cdp-action 集成 + 操作日志 | API 端点 + 操作能力 |
| **S3 前端看板** | 第 3-4 周 | 单文件 HTML 看板 + 计划列表 + 操作面板 + 移动端适配 | `/dashboard` 页面 |

### 5.2 S1 数据基建

| 任务 | 输出 | 验证标准 |
|---|---|---|
| 1.1 建 SQLite 表（最终版 Schema） | `oceanengine.db` | 6 张表 + 索引创建成功 |
| 1.2 JSON 历史导入 | 全量历史数据入库 | 行数一致 + 抽样校验 |
| 1.3 双写集成 | 采集同时写 SQLite + JSON | JSON vs DB 数据一致 |
| 1.4 迁移脚本 | `db-migrate.mjs` | 可重复执行，幂等 |
| 1.5 feedback-server 绑 0.0.0.0 | 局域网访问启用 | 手机同 WiFi 可访问 `http://<本机IP>:8899` |

### 5.3 S2 执行层 + API

| 任务 | 输出 | 验证标准 |
|---|---|---|
| 2.1 feedback-server 新增路由 | 5 个新 API 端点 | curl 验证 |
| 2.2 集成 cdp-action | `POST /api/actions` 可执行操作 | 暂停/启用/调预算跑通 |
| 2.3 操作日志记录 | 每次操作自动记录 | 查询日志正确 |
| 2.4 前端确认弹窗交互 | POST 前弹出确认 | L1/L2/L3 各跑通 |
| 2.5 同一计划并发锁 | 内存锁或 DB 状态锁 | 重复请求排队或拒绝 |

### 5.4 S3 前端看板

| 任务 | 输出 | 验证标准 |
|---|---|---|
| 3.1 dashboard.html | 单文件 HTML 看板 | 消耗/CPA/告警数实时展示 |
| 3.2 计划列表视图 | 全量计划 + 筛选排序 | 与多维表格数据一致 |
| 3.3 操作面板 | 暂停/启用/调预算按钮 | 弹窗确认 → 执行 → 日志 |
| 3.4 定时刷新 | 30s 自动更新 | 数据变化可见 |
| 3.5 移动端响应式 CSS | 手机浏览器可用 | 局域网内手机访问正常 |
| 3.6 Tailscale 验证（可选） | 远程访问可用 | 手机 4G/外部 WiFi 可访问 |

### 5.5 S4 增强（按需，不阻塞上线）

| 任务 | 触发条件 |
|---|---|
| Marketing API 写函数 | CDP 不稳定 / 需秒级操作 |
| DuckDB 分析层 | 需要复杂聚合查询 |
| Windows Toast / 浏览器通知 / Bark | 需要更多通知渠道 |
| ZeroTier 备用 | Tailscale 中国大陆不稳定 |

---

## 6. 风险矩阵（最终版）

| # | 风险 | 概率 | 影响 | 缓解措施 |
|---|---|---|---|---|
| R1 | Chrome 离线 → CDP 操作不可用 | 中 | 高 | `chrome-guard.mjs` 守护 + 操作失败自动重试 |
| R2 | Cookie 过期 → 采集中断 | 中 | 高 | `oec-auto-login.mjs` + TTL 监控 + 提前 5min 刷新 |
| R3 | OVUI 页面改版 → CDP 选择器失效 | 中 | 高 | `calibrate-page.mjs` v2 多策略回退 + 选择器版本化 |
| R4 | 操作误触（误暂停高 ROI 计划） | 中 | 高 | L1/L2/L3 分级确认 + 操作日志回溯 |
| R5 | SQLite 数据文件损坏 | 低 | 高 | WAL 模式 + 每日自动备份 |
| R6 | Tailscale 中国大陆连通不稳定 | 中 | 中 | 配置 DERP 中继 + 备 ZeroTier |
| R7 | 本机 IP 变化 → 手机访问失败 | 低 | 低 | 看板绑定 `0.0.0.0:8899`，手机重连时刷新 |
| R8 | 看板响应式不充分 | 中 | 中 | S3 阶段用真机测试手机/平板 |

---

## 7. 不推荐的"看上去很美"方案

| 方案 | 不推荐理由 |
|---|---|
| Electron / Tauri 桌面应用 | 打包体积大 / 自动更新复杂 / 杀鸡用牛刀 |
| PWA | 全功能需 HTTPS，又卡企业资质 |
| 浏览器扩展 | Chrome 商店审核 + 移动端无法装 |
| 飞书自建 H5 内嵌 | 需企业飞书应用，跨账号调用受限 |
| 公网 IP + 端口映射 | 巨量引擎 Cookie 暴露 = 账户被盗风险 |
| 域名 + HTTPS 备案 | 需企业主体，回到原点 |
| 钉钉 / 企微机器人 | 用户不用 |
| React Native / Flutter | 需上架审核，个人开发者难 |

---

## 8. 与 v3.0 的关键差异

| 维度 | v3.0 | v3.2 最终版 |
|---|---|---|
| 产品定位 | 产品/平台 | 个人桌面工具 |
| 用户模型 | 多用户/多账户/多平台 | 单用户/单账户/单平台 |
| 前端 | 飞书小程序 (Taro) + H5 兜底 | **单文件 HTML 看板** |
| 前端框架 | React + Taro + ECharts | 无框架，原生 JS + ECharts CDN |
| 执行通道 | Marketing API 主 + Playwright 回退 | **raw CDP 主**（`cdp-action.mjs`） |
| 审批流 | L0-L3 四级审批 + 飞书审批流 | **前端弹窗确认 + 操作日志** |
| 后端架构 | feedback-server + operation-executor 双进程 | **feedback-server 单进程** |
| 实时通信 | WebSocket / SSE | **30s 定时轮询** |
| 移动端访问 | 小程序 | **局域网 IP + Tailscale** |
| 小程序 | 核心方案 | **彻底排除**（无企业资质） |
| Playwright | 替换 cdp-client | **明确不兼容 OVUI，保留 cdp-client** |
| 工期 | 5-6 周 | **3-4 周** |

---

## 9. 已有模块映射（v3.2 最终版）

| 文件 | 角色 | v3.2 状态 |
|---|---|---|
| `oceanengine-api-client.mjs` | 数据采集 | 保持不变 |
| `cdp-client.mjs` | CDP 连接 | 保持不变 |
| `cdp-action.mjs` | 写操作 | 被 feedback-server import |
| `calibrate-page.mjs` | 页面校准 | 保持不变 |
| `feedback-server.mjs` | 唯一后端服务 | 扩展 API + 看板路由 |
| `feishu-listener.mjs` | 飞书指令入口 | 保留，调用 cdp-action |
| `chrome-guard.mjs` | Chrome 守护 | 保持不变 |
| `oec-auto-login.mjs` | 自动登录 | 保持不变 |
| `action-executor.mjs` | 原型执行器 | 废弃，功能合并到 feedback-server |

---

## 10. 给实施 Agent 的硬性约束

1. **个人工具，不是产品**：不做用户系统、JWT、多租户、审批流、Docker、云端部署、HTTPS 备案。
2. **前端单文件 HTML**：无 React/Vue/构建工具。
3. **CDP 操作必须真实事件**：使用 `Input.dispatchMouseEvent`，不用 Playwright `locator.click()`。
4. **复用已有模块**：不要重写 `cdp-action.mjs`、`cdp-client.mjs`、`calibrate-page.mjs`。
5. **SQLite 参数化查询**：禁止字符串拼接 SQL。
6. **单进程优先**：不新建独立执行进程。
7. **30s 轮询足够**：不做 SSE/WebSocket。

---

## 11. 待后续 Review 的重点

> 由最终版文档提交给其他模型交叉验证：

1. **Marketing API 写函数**：若未来推进 API 通道，端点 `/v3.0/campaign/update/status/` 与 `/v3.0/campaign/update/budget/` 的参数是否准确。
2. **Tailscale 中国大陆稳定性**：DERP 中继配置与 ZeroTier 备用方案。
3. **CDP 操作并发锁**：内存锁在 feedback-server 单进程内的实现是否合理。
4. **移动端响应式**：360px-1920px 范围是否需要分两套 CSS。
5. **双写回滚**：Week 2 切 SQLite 后，JSON 备份保留 14 天是否充分。

---

*本文档由 Kimi K2.7 Code 基于本机源码核验生成。所有推荐方案均基于“单用户/单账户/Windows 本机/无企业资质”的事实环境。*
